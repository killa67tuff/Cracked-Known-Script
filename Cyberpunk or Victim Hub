-- Fake Desync Cheat V102 (Priority Render Fix + Absolute Accuracy)
if not game:IsLoaded() then game.Loaded:Wait() end
pcall(function() setfpscap(999) end) -- Unlock FPS

-- === SERVICES ===
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local VirtualUser = game:GetService("VirtualUser")
local VirtualInputManager = game:GetService("VirtualInputManager")
local TweenService = game:GetService("TweenService")
local GuiService = game:GetService("GuiService")
local HttpService = game:GetService("HttpService")

local player = Players.LocalPlayer
local mouse = player:GetMouse()
local camera = Workspace.CurrentCamera
local DISCORD_LINK = "https://discord.gg/9kaxZHKZkj"
local AUTH_URL = "http://5.178.107.231:2026" -- Server URL
local troll = "https://youtube.com/@silver-y2r?si=W5efcznrW44CtVfd"

-- Fonction sÃ©curisÃ©e pour copier dans le presse-papiers
local function copyToClipboard(str)
    if setclipboard then
        setclipboard(str)
        return true
    elseif toclipboard then
        toclipboard(str)
        return true
    end
    return false
end

local function SendNotification(text, duration)
    task.spawn(function()
        local NotifGui = Instance.new("ScreenGui")
        NotifGui.Name = "CyberpunksNotification"
        pcall(function() NotifGui.Parent = CoreGui end)
        if not NotifGui.Parent then NotifGui.Parent = player:WaitForChild("PlayerGui") end
        
        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(0, 130, 0, 40)
        Frame.Position = UDim2.new(1, 270, 1, -60)
        Frame.BackgroundColor3 = Color3.fromRGB(128, 10, 50)
        Frame.BackgroundTransparency = 0.25
        Frame.BorderSizePixel = 0
        Frame.Parent = NotifGui
        
        local Corner = Instance.new("UICorner") Corner.CornerRadius = UDim.new(0, 4) Corner.Parent = Frame
        local Stroke = Instance.new("UIStroke") Stroke.Color = Color3.fromRGB(255, 255, 255) Stroke.Thickness = 1 Stroke.Transparency = 0.8 Stroke.Parent = Frame
        
        local Accent = Instance.new("Frame")
        Accent.Size = UDim2.new(0, 2, 0.6, 0)
        Accent.Position = UDim2.new(0, 6, 0.2, 0)
        Accent.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        Accent.BorderSizePixel = 0
        Accent.Parent = Frame
        local ACorner = Instance.new("UICorner") ACorner.CornerRadius = UDim.new(1, 0) ACorner.Parent = Accent
        
        local Label = Instance.new("TextLabel")
        Label.Size = UDim2.new(1, -25, 1, 0)
        Label.Position = UDim2.new(0, 18, 0, 0)
        Label.BackgroundTransparency = 1
        Label.Text = text
        Label.TextColor3 = Color3.fromRGB(240, 240, 240)
        Label.Font = Enum.Font.GothamMedium
        Label.TextSize = 10
        Label.TextWrapped = true
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.Parent = Frame
        
        TweenService:Create(Frame, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Position = UDim2.new(1, -280, 1, -60)}):Play()
        task.wait(duration)
        local tween = TweenService:Create(Frame, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {Position = UDim2.new(1, 270, 1, -60)})
        tween:Play()
        tween.Completed:Wait()
        NotifGui:Destroy()
    end)
end

local PartViewerEnabled = false

local function StartPartViewer()
    PartViewerEnabled = true
    SendNotification("Part Viewer started! Check F9 console for object names", 5)
    
    task.spawn(function()
        while PartViewerEnabled do
            for _, v in pairs(workspace:GetDescendants()) do
                if v:IsA("Tool") then
                    print("[PART VIEWER] Tool: " .. v.Name .. " | Parent: " .. v.Parent.Name .. " | ToolTip: " .. (v.ToolTip or "None"))
                elseif v:IsA("Model") and v:FindFirstChild("Handle") then
                    print("[PART VIEWER] Model with Handle: " .. v.Name .. " | Parent: " .. v.Parent.Name)
                end
            end
            task.wait(5) -- Print every 5 seconds
        end
    end)
end

local function StopPartViewer()
    PartViewerEnabled = false
    SendNotification("Part Viewer stopped", 3)
end

local function CreateConfirmationPrompt(text, jobId, placeId, onYes, onNo)
    task.spawn(function()
        local ConfirmGui = Instance.new("ScreenGui")
        ConfirmGui.Name = "CyberpunksConfirm"
        pcall(function() ConfirmGui.Parent = CoreGui end)
        if not ConfirmGui.Parent then ConfirmGui.Parent = player:WaitForChild("PlayerGui") end
        
        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(0, 300, 0, 150)
        Frame.Position = UDim2.new(0.5, -150, 0.5, -60)
        Frame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
        Frame.BorderSizePixel = 0
        Frame.Parent = ConfirmGui
        
        local Corner = Instance.new("UICorner") Corner.CornerRadius = UDim.new(0, 6) Corner.Parent = Frame
        local Stroke = Instance.new("UIStroke") Stroke.Color = Color3.fromRGB(40, 40, 50) Stroke.Thickness = 1 Stroke.Parent = Frame
        
        local Label = Instance.new("TextLabel")
        Label.Size = UDim2.new(1, -20, 0, 60)
        Label.Position = UDim2.new(0, 10, 0, 10)
        Label.BackgroundTransparency = 1
        Label.Text = text
        Label.TextColor3 = Color3.fromRGB(255, 255, 255)
        Label.Font = Enum.Font.GothamBold
        Label.TextSize = 12
        Label.TextWrapped = true
        Label.Parent = Frame
        
        local YesBtn = Instance.new("TextButton")
        YesBtn.Size = UDim2.new(0.4, 0, 0, 30)
        YesBtn.Position = UDim2.new(0.05, 0, 0.75, 0)
        YesBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        YesBtn.Text = "YES"
        YesBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        YesBtn.Font = Enum.Font.GothamBold
        YesBtn.TextSize = 10
        YesBtn.Parent = Frame
        local YCorner = Instance.new("UICorner") YCorner.CornerRadius = UDim.new(0, 4) YCorner.Parent = YesBtn
        
        local NoBtn = Instance.new("TextButton")
        NoBtn.Size = UDim2.new(0.4, 0, 0, 30)
        NoBtn.Position = UDim2.new(0.55, 0, 0.75, 0)
        NoBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
        NoBtn.Text = "NO"
        NoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        NoBtn.Font = Enum.Font.GothamBold
        NoBtn.TextSize = 10
        NoBtn.Parent = Frame
        local NCorner = Instance.new("UICorner") NCorner.CornerRadius = UDim.new(0, 4) NCorner.Parent = NoBtn
        
        local CopyIdBtn = Instance.new("TextButton")
        CopyIdBtn.Size = UDim2.new(0.25, 0, 0, 20)
        CopyIdBtn.Position = UDim2.new(0.2, 0, 0.55, 0)
        CopyIdBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        CopyIdBtn.Text = "COPY ID"
        CopyIdBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
        CopyIdBtn.Font = Enum.Font.GothamBold
        CopyIdBtn.TextSize = 8
        CopyIdBtn.Parent = Frame
        local CICorner = Instance.new("UICorner") CICorner.CornerRadius = UDim.new(0, 4) CICorner.Parent = CopyIdBtn
        
        CopyIdBtn.MouseButton1Click:Connect(function()
            if setclipboard then setclipboard(jobId) end
            CopyIdBtn.Text = "COPIED!"
            task.wait(1)
            CopyIdBtn.Text = "COPY ID"
        end)

        local CopyScriptBtn = Instance.new("TextButton")
        CopyScriptBtn.Size = UDim2.new(0.25, 0, 0, 20)
        CopyScriptBtn.Position = UDim2.new(0.55, 0, 0.55, 0)
        CopyScriptBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        CopyScriptBtn.Text = "COPY SCRIPT"
        CopyScriptBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
        CopyScriptBtn.Font = Enum.Font.GothamBold
        CopyScriptBtn.TextSize = 8
        CopyScriptBtn.Parent = Frame
        local CSCorner = Instance.new("UICorner") CSCorner.CornerRadius = UDim.new(0, 4) CSCorner.Parent = CopyScriptBtn
        
        CopyScriptBtn.MouseButton1Click:Connect(function()
            local scriptTxt = string.format('game:GetService("TeleportService"):TeleportToPlaceInstance(%s, "%s", game.Players.LocalPlayer)', tostring(placeId), tostring(jobId))
            if setclipboard then setclipboard(scriptTxt) end
            CopyScriptBtn.Text = "COPIED!"
            task.wait(1)
            CopyScriptBtn.Text = "COPY SCRIPT"
        end)

        YesBtn.MouseButton1Click:Connect(function()
            if onYes then onYes() end
            ConfirmGui:Destroy()
        end)
        
        NoBtn.MouseButton1Click:Connect(function()
            if onNo then onNo() end
            ConfirmGui:Destroy()
        end)
    end)
end
local ShowKeySystem -- Forward declaration
-- === EXTERNAL HELPER FUNCTIONS (To reduce StartCheat locals) ===
local function RandomNumberRange(a)
    return math.random(-a * 100, a * 100) / 100
end

local function ApplyDescriptionCustom(humanoid, description)
    local character = humanoid.Parent
    if not character then return end
    
    -- 1. Body Colors
    local bodyColors = character:FindFirstChildOfClass("BodyColors")
    if not bodyColors then
        bodyColors = Instance.new("BodyColors")
        bodyColors.Name = "Body Colors"
        bodyColors.Parent = character
    end
    bodyColors.HeadColor3 = description.HeadColor
    bodyColors.TorsoColor3 = description.TorsoColor
    bodyColors.LeftArmColor3 = description.LeftArmColor
    bodyColors.RightArmColor3 = description.RightArmColor
    bodyColors.LeftLegColor3 = description.LeftLegColor
    bodyColors.RightLegColor3 = description.RightLegColor
    
    -- 2. Clothing
    local function updateClothing(className, propName, id)
        if id and id > 0 then
            task.spawn(function()
                local success, result = pcall(function() return game:GetObjects("rbxassetid://" .. id) end)
                if success and result then
                    for _, obj in pairs(result) do
                        if obj:IsA(className) then
                            local item = character:FindFirstChildOfClass(className)
                            if not item then
                                item = Instance.new(className)
                                item.Parent = character
                            end
                            item[propName] = obj[propName]
                            break
                        end
                    end
                end
            end)
        else
            local item = character:FindFirstChildOfClass(className)
            if item then item:Destroy() end
        end
    end
    
    updateClothing("Shirt", "ShirtTemplate", description.Shirt)
    updateClothing("Pants", "PantsTemplate", description.Pants)
    updateClothing("ShirtGraphic", "Graphic", description.GraphicTShirt)
    
    -- 3. Face
    local head = character:FindFirstChild("Head")
    if head then
        local face = head:FindFirstChildOfClass("Decal")
        if description.Face and description.Face > 0 then
            if not face then
                face = Instance.new("Decal")
                face.Name = "face"
                face.Parent = head
            end
            face.Texture = "rbxassetid://" .. description.Face
        else
            if face then face:Destroy() end
        end
    end
    
    -- 4. Accessories
    for _, v in pairs(character:GetChildren()) do
        if v:IsA("Accessory") then v:Destroy() end
    end
    
    local function loadAccessories(ids)
        if not ids or ids == "" then return end
        for id in string.gmatch(ids, "([^,]+)") do
            local assetId = tonumber(id)
            if assetId then
                task.spawn(function()
                    local success, result = pcall(function() return game:GetObjects("rbxassetid://" .. assetId) end)
                    if success and result then
                        for _, obj in pairs(result) do
                            if obj:IsA("Accessory") then obj.Parent = character end
                        end
                    end
                end)
            end
        end
    end
    
    loadAccessories(description.HatAccessory)
    loadAccessories(description.HairAccessory)
    loadAccessories(description.FaceAccessory)
    loadAccessories(description.NeckAccessory)
    loadAccessories(description.ShouldersAccessory)
    loadAccessories(description.FrontAccessory)
    loadAccessories(description.BackAccessory)
    loadAccessories(description.WaistAccessory)
    
    -- 5. Body Parts (R6 & R15)
    for _, v in pairs(character:GetChildren()) do
        if v:IsA("CharacterMesh") then v:Destroy() end
    end
    
    local function loadBodyParts(id)
        if not id or id == 0 then return end
        task.spawn(function()
            local success, result = pcall(function() return game:GetObjects("rbxassetid://" .. id) end)
            if success and result then
                for _, obj in pairs(result) do
                    local function recurse(item)
                        if item:IsA("CharacterMesh") then
                            item.Parent = character
                        elseif item:IsA("MeshPart") and humanoid.RigType == Enum.HumanoidRigType.R15 then
                            local charPart = character:FindFirstChild(item.Name)
                            if charPart and charPart:IsA("MeshPart") then
                                pcall(function() charPart.MeshId = item.MeshId end)
                                pcall(function() charPart.TextureID = item.TextureID end)
                                pcall(function() charPart.Size = item.Size end)
                            end
                        end
                        for _, child in pairs(item:GetChildren()) do
                            recurse(child)
                        end
                    end
                    recurse(obj)
                end
            end
        end)
    end
    
    loadBodyParts(description.Head)
    loadBodyParts(description.Torso)
    loadBodyParts(description.LeftArm)
    loadBodyParts(description.RightArm)
    loadBodyParts(description.LeftLeg)
    loadBodyParts(description.RightLeg)
    
    -- 6. Scales
    local function updateScale(name, val)
        if not val then return end
        local valObj = humanoid:FindFirstChild(name)
        if not valObj then
            valObj = Instance.new("NumberValue")
            valObj.Name = name
            valObj.Parent = humanoid
        end
        valObj.Value = val
    end
    
    updateScale("BodyHeightScale", description.HeightScale)
    updateScale("BodyWidthScale", description.WidthScale)
    updateScale("BodyDepthScale", description.DepthScale)
    updateScale("HeadScale", description.HeadScale)
    updateScale("BodyProportionScale", description.ProportionScale)
    updateScale("BodyTypeScale", description.BodyTypeScale)
end

local function disableDamageBlock(part)
    if part:IsA("BasePart") then
        local name = part.Name:lower()
        local parentName = part.Parent and part.Parent.Name:lower() or ""
        local shouldDisable = false
        
        -- Detect Damage Zones (Expanded Keywords + Parent Check)
        local damageKeywords = {"lava", "magma", "acid", "water", "damage", "burn", "kill", "death", "hurt", "void", "spike", "fire", "flame", "trap", "laser"}
        for _, kw in pairs(damageKeywords) do
            if (string.find(name, kw) or string.find(parentName, kw)) and not string.find(name, "safe") and not string.find(name, "spawn") then
                shouldDisable = true
                break
            end
        end
        
        if part.Material == Enum.Material.CrackedLava or part.Material == Enum.Material.Water then shouldDisable = true end
        
        -- Check Children (Textures/Decals) as requested by user
        if not shouldDisable then
            for _, child in pairs(part:GetChildren()) do
                if child:IsA("Texture") or child:IsA("Decal") then
                    local texName = child.Name:lower()
                    for _, kw in pairs(damageKeywords) do
                        if string.find(texName, kw) then shouldDisable = true break end
                    end
                end
                if shouldDisable then break end
            end
        end
        
        if shouldDisable then
            pcall(function()
                part.CanTouch = false
                part.Material = Enum.Material.Plastic -- Bypass FloorMaterial checks
                part.Name = "SafeZone_Bypassed" -- Bypass Name checks
                if part:FindFirstChildOfClass("TouchInterest") then
                    part:FindFirstChildOfClass("TouchInterest"):Destroy()
                end
            end)
        end
    end
end

-- ==================================================================
--                          MAIN CHEAT FUNCTION
-- ==================================================================

local function StartCheat(keyDuration, keyString, isWhitelisted)
    -- === CONFIGURATION ===
    
    -- Forward declaration pour Ã©viter les erreurs de scope dans les hooks
    local getClosestPlayerToMouse

    local Config = {
        IsRunning = true,
        IsWhitelisted = isWhitelisted,
        IsAdmin = true,
        -- Anti Buddha / Desync
        MasterSwitch = true,
        
        -- Settings / Mods
        SpeedKey = Enum.KeyCode.Unknown, -- Attention: V est aussi une macro key
        StandardFlyKey = Enum.KeyCode.Unknown,
        
        SpeedHack = false,
        WalkSpeedValue = 0,
        JumpPowerValue = 0,
        
        StandardFly = true,
        FlySpeedValue = 80, -- Max 75
        
        InfJump = true,
        
        -- Players Tab (Aim Lock & Kill Aura & NoClip)
        LockKey = Enum.KeyCode.Unknown,
        EasyLock = false,
        FollowDistance = 0,
        TargetName = "None",
        
        KillAura = false,
        AuraRange = 15000,
        TeamCheck = false,
        
        NoClip = true,
        AntiLava = true,
        WalkWater = true,
        AntiStun = true,
        
        InstantRespawn = false,
        AirFlashStep = false,
        AirFlashStepRange = 200,
        
        InfiniteFlashStep = false,
        
        -- Ice Water (WaterWalking attribute)
        IceWater = false,
        
        -- Unbreakable All
        UnbreakableAll = true,
        
        -- Orbit Player
        OrbitPlayer = false,
        OrbitPlayerKey = Enum.KeyCode.Unknown,
        OrbitPlayerTarget = "",
        
        FakeLag = false,
        FakeLagKey = Enum.KeyCode.Unknown,
        
        -- Visuals (ESP)
        ESPBox = false,
        ESPName = true,
        ESPLvl = false,
        ESPHP = true,
        ESPChams = false,
        ESPDot = false,
        ESPNPC = false,
        ESPTracers = false,
        ESPFruit = false,
        PotatoGraphics = true,
        FOVValue = 120,
        RemoveFog = true,
        NoAnim = true,
        ESPSize = 10,
        ESPColorR = 255,
        ESPColorG = 255,
        ESPColorB = 255,
        CustomCursorId = "",
        CursorName = "",
        InspectorName = "",
        ShowMoney = false,
        ShowFragments = false,
        ShowBounty = false,
        
        -- Server Stats
        ShowFPS = false,
        ShowPing = false,
        ShowUptime = false,
        ShowServer = false,
        
        -- Hitbox Preset
        HitboxPreset = false,
        TorsoSize = 0,
        
        -- Trigger Bot & Silent Aim
        TriggerBot = false,
        TriggerDistance = 0,
        
        SilentAim = false,
        SilentFOV = 0,
        SilentRange = 0,
        ShowFOV = false,
        SilentAimKey = Enum.KeyCode.Unknown, -- Touche de Lock
        
        -- Hitbox Extender
        HitboxExpander = false,
        HitboxSize = 2,
        HitboxTransparency = 0.7,
        HitboxTransparency = 0,
        HitboxModeAll = true,
        HitboxTargetUser = "",
        
        -- Part Extender
        PartExtender = false,
        PartExtenderHead = false,
        PartExtenderTorso = false,
        PartExtenderLeftArm = false,
        PartExtenderRightArm = false,
        PartExtenderLeftLeg = false,
        PartExtenderRightLeg = false,
        PartExtenderSize = 2,
        
        -- Self Reach
        Reach = true,
        ReachSize = 2,
        ReachTransparency = 1,
        
        -- Weapon Extender
        WeaponExtender = false,
        WeaponLength = 4,
        WeaponWidth = 5,
        WeaponTransparency = 1,
        SizeMultiplierEnabled = false,
        SizeMultiplier = 1,
        
        -- Handle Extender
        HandleExtender = false,
        HandleSize = 5,
        
        -- Cooldowns
        NoCooldown = false,
        InfiniteEnergy = false,
        MaxEnergyValue = 100,
        
        CombatKeys = {
            Enum.KeyCode.Z,
            Enum.KeyCode.X,
            Enum.KeyCode.C,
            Enum.KeyCode.R
        },
        
        -- Aimlock
        Aimlock = false,
        AimlockKey = Enum.KeyCode.Unknown,
        AimlockPixel = false,
        AimlockDuration = 0,
        MouseClickLock = false,
        MouseClickFOV = 100,
        PixelX = 1025,
        PixelY = 965,
        PixelColor = Color3.fromRGB(255, 255, 255),
        
        -- Trigger FlashStep
        TriggerFlashStep = false,
        TriggerFlashStepUser = "",
        
        -- Aim Filter
        AimFilter = false,
        AimFilterMode = "Whitelist", -- Whitelist / Blacklist
        AimFilterWhitelist = {},
        AimFilterBlacklist = {},
        AimFilterList = {},
        
        -- Cursor Lock
        CursorLock = false,
        CursorLockKey = Enum.KeyCode.Unknown,
        CursorLockDisableKey = Enum.KeyCode.Unknown,
        CursorLockUser = "",
        
        -- Zone Aimlock
        ZoneLock = false,
        ZoneKey = Enum.KeyCode.Unknown,
        ZoneDistance = 0,
        ZoneDuration = 0,
        ZoneCooldown = 0,
        
        -- Auto Race
        AutoV3 = true,
        AutoAwakening = true,
        
        -- Dash
        DashLengthEnabled = false,
        DashLengthValue = 50,
        
        -- Raid
        AutoRaid = false,
        AutoRaidKey = Enum.KeyCode.Unknown,
        SelectedRaid = "Flame",
        
        -- Auto Load
        AutoLoadConfig = false,
        AutoLoadConfigName = "",
        
        -- Shop / Devil Fruit
        AutoGacha = false,
        AutoStoreFruit = false,
        AutoGrabFruit = false,
        
        -- Sea Event
        AutoFindLeviathan = false,
        AutoBuyBoat = false,
        SelectedBoat = "Speedboat",
        JoinPlayerName = "",
        JoinJobID = "",
        BotApiUrl = AUTH_URL,
        FastAttackRange = 10000,
        
        -- Farm
        FarmNearest = false,
        FarmKey = Enum.KeyCode.Unknown,
        BringRange = 250,
        BringMob = false,
        KillDungeon = true,
        DungeonRange = 0,
        
        -- TP Player
        TPPlayerKey = Enum.KeyCode.Unknown,
        TPPlayerRange = 1000,
        
        -- One Shot
        OneShotKey = Enum.KeyCode.Unknown,
        
        -- Panic Mode
        PanicMode = false,
        PanicModeKey = Enum.KeyCode.Unknown,
        PanicModeHealthEnable = false,
        PanicModeMinHealth = 4000,
        PanicModeMaxHealth = 9000,
        
        -- Tools
        TeleportTool = false,
        
        -- Skin Changer
        SkinChangerID = "",
        
        -- Tool Color Changer
        ToolColorChanger = false,
        ToolColor = Color3.fromRGB(255, 255, 255),
        
        -- Emote Player
        EmoteID = "",
        EmoteLoop = false,
        EmoteSpeed = 1,
        EmotePlaying = false,
        EmoteFrozen = false,
        
        -- Emote Player Variables
        EmoteAnimation = nil,
        EmoteTrack = nil,
        EmoteConnection = nil,
        
        -- Snow
        DisableSnow = false,
        
        -- Mastery Farm
        MasteryFarm = false,
        FastM1 = true,
        CustomFarmNearest = false,
        SelectWeaponType = "Sword",
        SelectedWeapon = nil,
        AlwaysDay = true,
        AutoElitehunter = false,

        -- Internal
        PanicSpeed = 0.1,
        SkyHeight = 1000000,
        TornadoRadius = 150,
        TornadoSpeed = 15,
        HideGuiKey = Enum.KeyCode.LeftShift
    }
    
    local originalWeaponSizes = {}
    local originalHandSizes = {}

    local function SetupAutoLoad()
        -- Feature removed
    end
    
    local function UpdateLoaderSettings()
        if writefile then
            local data = {
                Enabled = Config.AutoLoadConfig,
                ConfigName = Config.AutoLoadConfigName,
                DisableSnow = Config.DisableSnow
            }
            pcall(function() writefile("Cyberpunks_Loader.json", HttpService:JSONEncode(data)) end)
        end
    end
    
    -- === BROADCAST SYSTEM ===
    local BroadcastGui = Instance.new("ScreenGui")
    BroadcastGui.Name = "CyberpunksBroadcast"
    BroadcastGui.IgnoreGuiInset = true
    pcall(function() BroadcastGui.Parent = CoreGui end)
    if not BroadcastGui.Parent then BroadcastGui.Parent = player:WaitForChild("PlayerGui") end
    
    local BroadcastLabel = Instance.new("TextLabel")
    BroadcastLabel.Size = UDim2.new(1, 0, 0, 60)
    BroadcastLabel.Position = UDim2.new(0, 0, 0.1, 0)
    BroadcastLabel.BackgroundTransparency = 1
    BroadcastLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
    BroadcastLabel.TextStrokeTransparency = 0
    BroadcastLabel.TextStrokeColor3 = Color3.new(0,0,0)
    BroadcastLabel.Font = Enum.Font.GothamBlack
    BroadcastLabel.TextSize = 35
    BroadcastLabel.Text = ""
    BroadcastLabel.Visible = false
    BroadcastLabel.Parent = BroadcastGui

    -- === SERVER SECURITY CHECK ===
    task.spawn(function()
        while Config.IsRunning do
            task.wait(3) -- Check status every 3s
        end
    end)

    -- === WEAPON EXTENDER / SKIN GLITCH FUNCTIONS (Moved Up) ===
    local function SetWeaponSize(multiplier)
        local character = player.Character
        if not character then return end
        
        for _, tool in pairs(character:GetChildren()) do
            if tool:IsA("Tool") then
                local handle = tool:FindFirstChild("Handle")
                if handle and handle:IsA("BasePart") then
                    local compensationMultiplier = multiplier
                    if Config.SizeMultiplierEnabled then
                        compensationMultiplier = multiplier * Config.SizeMultiplier
                    end
                    
                    if not originalWeaponSizes[tool.Name] then
                        originalWeaponSizes[tool.Name] = {}
                        originalWeaponSizes[tool.Name].HandleSize = handle.Size
                    end
                    
                    handle.Size = originalWeaponSizes[tool.Name].HandleSize * compensationMultiplier
                    handle.CanCollide = false
                    handle.Massless = true
                    handle.Transparency = Config.WeaponTransparency
                end
                
                for _, part in pairs(tool:GetDescendants()) do
                    pcall(function()
                        if part:IsA("BasePart") and part ~= handle then
                            local compensationMultiplier = multiplier
                            if Config.SizeMultiplierEnabled then
                                compensationMultiplier = multiplier * Config.SizeMultiplier
                            end
                            
                            if not originalWeaponSizes[tool.Name][part.Name] then
                                originalWeaponSizes[tool.Name][part.Name] = part.Size
                            end
                            part.Size = originalWeaponSizes[tool.Name][part.Name] * compensationMultiplier
                            part.CanCollide = false
                            part.Massless = true
                            part.Transparency = Config.WeaponTransparency
                            
                        elseif part:IsA("Attachment") then
                            local compensationMultiplier = multiplier
                            if Config.SizeMultiplierEnabled then
                                compensationMultiplier = multiplier * Config.SizeMultiplier
                            end
                            
                            if not originalWeaponSizes[tool.Name][part.Name .. "_Pos"] then
                                originalWeaponSizes[tool.Name][part.Name .. "_Pos"] = part.Position
                            end
                            part.Position = originalWeaponSizes[tool.Name][part.Name .. "_Pos"] * compensationMultiplier
                            
                        elseif part:IsA("SpecialMesh") then
                            local compensationMultiplier = multiplier
                            if Config.SizeMultiplierEnabled then
                                compensationMultiplier = multiplier * Config.SizeMultiplier
                            end
                            
                            if not originalWeaponSizes[tool.Name][part.Name .. "_Scale"] then
                                originalWeaponSizes[tool.Name][part.Name .. "_Scale"] = part.Scale
                            end
                            part.Scale = originalWeaponSizes[tool.Name][part.Name .. "_Scale"] * compensationMultiplier
                        end
                    end)
                end
            end
        end

        -- Enlarge hands
        local leftHand = character:FindFirstChild("LeftHand") or character:FindFirstChild("Left Arm") or character:FindFirstChild("LeftArm")
        local rightHand = character:FindFirstChild("RightHand") or character:FindFirstChild("Right Arm") or character:FindFirstChild("RightArm")
        if leftHand and leftHand:IsA("BasePart") then
            if not originalHandSizes["Left"] then originalHandSizes["Left"] = leftHand.Size end
            leftHand.Size = Vector3.new(2, 2, 2) * multiplier -- Enlarge hands
            leftHand.Transparency = Config.WeaponTransparency
        end
        if rightHand and rightHand:IsA("BasePart") then
            if not originalHandSizes["Right"] then originalHandSizes["Right"] = rightHand.Size end
            rightHand.Size = Vector3.new(2, 2, 2) * multiplier -- Enlarge hands
            rightHand.Transparency = Config.WeaponTransparency
        end
    end

    local function RestoreWeaponSize()
        local character = player.Character
        if not character then return end
        
        -- Restore Tools
        for toolName, data in pairs(originalWeaponSizes) do
            local tool = character:FindFirstChild(toolName) or player.Backpack:FindFirstChild(toolName)
            if tool then
                local handle = tool:FindFirstChild("Handle")
                if handle and data.HandleSize then
                    handle.Size = data.HandleSize
                    handle.Transparency = 0
                end
                for partName, size in pairs(data) do
                    if type(size) == "Vector3" and partName ~= "HandleSize" then
                         for _, p in pairs(tool:GetDescendants()) do
                             if p.Name == partName and p:IsA("BasePart") then
                                 p.Size = size
                                 p.Transparency = 0
                             end
                         end
                    end
                end
            end
        end
        
        -- Restore Hands
        local leftHand = character:FindFirstChild("LeftHand") or character:FindFirstChild("Left Arm") or character:FindFirstChild("LeftArm")
        local rightHand = character:FindFirstChild("RightHand") or character:FindFirstChild("Right Arm") or character:FindFirstChild("RightArm")
        if leftHand and leftHand:IsA("BasePart") and originalHandSizes["Left"] then
            leftHand.Size = originalHandSizes["Left"]
            leftHand.Transparency = 0
        end
        if rightHand and rightHand:IsA("BasePart") and originalHandSizes["Right"] then
            rightHand.Size = originalHandSizes["Right"]
            rightHand.Transparency = 0
        end
    end

    local function WatchForNewTools()
        local character = player.Character
        if not character then return end
        
        table.insert(Connections, character.ChildAdded:Connect(function(child)
            if child:IsA("Tool") and Config.WeaponExtender then
                task.wait(0.2)
                SetWeaponSize(Config.WeaponLength)
            end
        end))
        
        table.insert(Connections, player.Backpack.ChildAdded:Connect(function(child)
            if child:IsA("Tool") and Config.WeaponExtender then
                task.wait(0.2)
                SetWeaponSize(Config.WeaponLength)
            end
        end))
    end

    -- === STATE ===
    local character = player.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChild("Humanoid")
    
    local cachedRegion = "Loading..."
    task.spawn(function()
        if game.HttpGet then
            pcall(function()
                local data = HttpService:JSONDecode(game:HttpGet("http://ip-api.com/json/"))
                cachedRegion = data.country .. ", " .. data.regionName
            end)
        else
            cachedRegion = "Unknown"
        end
    end)
    
    local hakiAttempted = false

    -- === AUTO HAKI ON LOAD ===
    task.spawn(function()
        while Config.IsRunning do
            if character and character.Parent and not hakiAttempted then
                VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.J, false, game)
                task.wait(0.1)
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.J, false, game)
                hakiAttempted = true
            end
            task.wait(1)
        end
    end)

    local lastToolUse = 0
    local isGuiVisible = true
    
    local combatTarget = nil
    local cursorLockTarget = nil
    local reachConnection = nil
    local aimlockTarget = nil
    local mouseClickTarget = nil
    local aimlockEndTime = 0
    local aimlockCooldown = 0
    local antiLavaConnection = nil
    local potatoConnection = nil
    local wasHitboxExpanded = false
    local tpReachDebounce = {} -- Anti-Spam pour TP Hit
    
    local isOneShotActive = false
    local oneShotThread = nil
    local instantRespawnConn = nil
    
    -- Trigger FlashStep State
    local lastFlashStepTarget = nil
    local lastFlashStepTime = 0
    
    -- Table pour stocker les boutons et les mettre Ã  jour
    local ToggleButtons = {} 
    local KeybindButtons = {}
    local InputBoxes = {}
    local Connections = {}
    
    -- === DRAWING API (FOV CIRCLE) ===
    local FOVCircle = Drawing.new("Circle")
    FOVCircle.Thickness = 2
    FOVCircle.NumSides = 100
    FOVCircle.Radius = Config.SilentFOV
    FOVCircle.Filled = false
    FOVCircle.Visible = false
    FOVCircle.Color = Color3.fromRGB(255, 255, 255)
    FOVCircle.Transparency = 0.5
    
    -- === ESP SETUP ===
    local ESPFolder = Instance.new("Folder")
    ESPFolder.Name = "Cyberpunks_ESP"
    pcall(function() ESPFolder.Parent = CoreGui end)
    if not ESPFolder.Parent then ESPFolder.Parent = player:WaitForChild("PlayerGui") end

    -- === HELPER FUNCTIONS ===
    
    getClosestPlayerToMouse = function(fovRadius, useFilter)
        local closestPlayer = nil
        
        -- Fix Hitbox Extender: Augmente le FOV pour dÃ©tecter les clics sur les bords des hitboxes gÃ©antes
        local effectiveFOV = fovRadius
        if Config.HitboxExpander then effectiveFOV = 2000 end 
        
        local shortestDistance = effectiveFOV or math.huge
        local mousePos = UserInputService:GetMouseLocation()

        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                local targetPart = p.Character:FindFirstChild("Head") or p.Character:FindFirstChild("HumanoidRootPart")
                
                -- Aim Filter Check
                local allow = true
                if Config.TeamCheck and p.Team and player.Team and p.Team == player.Team then allow = false end
                if useFilter and Config.AimFilter then
                    local status = Config.AimFilterList[p.Name]
                    if Config.AimFilterMode == "Whitelist" then
                        if not Config.AimFilterWhitelist[p.Name] then allow = false end
                        if status ~= "Whitelist" then allow = false end
                    elseif Config.AimFilterMode == "Blacklist" then
                        if Config.AimFilterBlacklist[p.Name] then allow = false end
                        if status == "Blacklist" then allow = false end
                    end
                end
                
                if targetPart and allow then
                    local isInRange = true
                    if rootPart and Config.SilentRange > 0 then
                        local dist = (rootPart.Position - targetPart.Position).Magnitude
                        if dist > Config.SilentRange then isInRange = false end
                    end
                    
                    if isInRange then
                        local pos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                        if onScreen then
                            local distance = (Vector2.new(mousePos.X, mousePos.Y) - Vector2.new(pos.X, pos.Y)).Magnitude
                            if distance < shortestDistance then
                                closestPlayer = p
                                shortestDistance = distance
                            end
                        end
                    end
                end
            end
        end
        return closestPlayer
    end
    
    -- Cible TOUT (NPC + Joueurs)
    local function getClosestEnemy(range)
        local closest = nil
        local dist = range
        
        -- Scan Workspace Children (NPCs)
        for _, obj in pairs(Workspace:GetChildren()) do
            if obj ~= character and obj:IsA("Model") then
                local hum = obj:FindFirstChild("Humanoid")
                local root = obj:FindFirstChild("HumanoidRootPart")
                
                if hum and root and hum.Health > 0 then
                    local mag = (rootPart.Position - root.Position).Magnitude
                    if mag < dist then
                        closest = obj
                        dist = mag
                    end
                end
            end
        end
        
        -- Scan Players
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                local mag = (rootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
                if mag < dist then
                    closest = p.Character
                    dist = mag
                end
            end
        end
        
        return closest
    end
    
    local function getClosestTargetToMouse(fovRadius)
        local closest = nil
        local minDist = fovRadius or math.huge
        local mousePos = UserInputService:GetMouseLocation()
        
        -- Check Players
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                local root = p.Character.HumanoidRootPart
                local pos, onScreen = camera:WorldToViewportPoint(root.Position)
                if onScreen then
                    local dist = (Vector2.new(mousePos.X, mousePos.Y) - Vector2.new(pos.X, pos.Y)).Magnitude
                    if dist < minDist then
                        closest = p.Character
                        minDist = dist
                    end
                end
            end
        end
        
        -- Check NPCs
        for _, obj in pairs(Workspace:GetChildren()) do
            if obj:IsA("Model") and obj ~= character and obj:FindFirstChild("HumanoidRootPart") and obj:FindFirstChild("Humanoid") and obj.Humanoid.Health > 0 then
                local root = obj.HumanoidRootPart
                local pos, onScreen = camera:WorldToViewportPoint(root.Position)
                if onScreen then
                    local dist = (Vector2.new(mousePos.X, mousePos.Y) - Vector2.new(pos.X, pos.Y)).Magnitude
                    if dist < minDist then
                        closest = obj
                        minDist = dist
                    end
                end
            end
        end
        
        return closest
    end

    local function getColorAt(x, y)
        -- 1. Priority: getcolor (Reads screen pixels including UI/GUI)
        if getcolor then
            return getcolor(Vector2.new(x, y))
        end
        -- 2. Fallback: Raycast (3D World only - Cannot see UI)
        local inset = GuiService:GetGuiInset()
        local ray = camera:ViewportPointToRay(x, y - inset.Y)
        
        local params = RaycastParams.new()
        params.FilterType = Enum.RaycastFilterType.Exclude
        params.FilterDescendantsInstances = {character, ESPFolder, camera}
        
        local res = workspace:Raycast(ray.Origin, ray.Direction * 1000, params)
        if res and res.Instance and res.Instance:IsA("BasePart") then
            return res.Instance.Color
        end
        return Color3.new(0,0,0)
    end

    local function applyToolColor(obj)
        if not Config.ToolColorChanger then return end
        pcall(function()
            if obj:IsA("BasePart") then
                if not obj:GetAttribute("OriginalColor") then
                    obj:SetAttribute("OriginalColor", obj.Color)
                end
                obj.Color = Config.ToolColor
            elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") then
                if not obj:GetAttribute("OriginalColorSequence") then
                    obj:SetAttribute("OriginalColorSequence", obj.Color)
                end
                obj.Color = ColorSequence.new(Config.ToolColor)
            elseif obj:IsA("Light") then
                if not obj:GetAttribute("OriginalLightColor") then
                    obj:SetAttribute("OriginalLightColor", obj.Color)
                end
                obj.Color = Config.ToolColor
            elseif obj:IsA("Fire") then
                if not obj:GetAttribute("OriginalColor") then
                    obj:SetAttribute("OriginalColor", obj.Color)
                    obj:SetAttribute("OriginalSecondaryColor", obj.SecondaryColor)
                end
                obj.Color = Config.ToolColor
                obj.SecondaryColor = Config.ToolColor
            elseif obj:IsA("Smoke") then
                if not obj:GetAttribute("OriginalColor") then
                    obj:SetAttribute("OriginalColor", obj.Color)
                end
                obj.Color = Config.ToolColor
            elseif obj:IsA("Sparkles") then
                if not obj:GetAttribute("OriginalColor") then
                    obj:SetAttribute("OriginalColor", obj.SparkleColor)
                end
                obj.SparkleColor = Config.ToolColor
            elseif obj:IsA("Decal") or obj:IsA("Texture") then
                if not obj:GetAttribute("OriginalColor") then
                    obj:SetAttribute("OriginalColor", obj.Color3)
                end
                obj.Color3 = Config.ToolColor
            elseif obj:IsA("SelectionBox") or obj:IsA("SelectionSphere") or obj:IsA("Highlight") then
                if not obj:GetAttribute("OriginalColor") then
                    local color = (obj:IsA("Highlight") and obj.FillColor) or obj.Color
                    obj:SetAttribute("OriginalColor", color)
                end
                if obj:IsA("Highlight") then
                    obj.FillColor = Config.ToolColor
                    obj.OutlineColor = Config.ToolColor
                else
                    obj.Color = Config.ToolColor
                end
            end
        end)
    end
    
    -- POTATO GRAPHICS SETUP
    local Lighting = game:GetService("Lighting")
    local Terrain = workspace:WaitForChild("Terrain")
    local renderSettings = settings().Rendering
    
    local potatoDOF = Lighting:FindFirstChild("PotatoDOF") or Instance.new("DepthOfFieldEffect")
    potatoDOF.Name = "PotatoDOF"
    potatoDOF.FarIntensity = 1
    potatoDOF.FocusDistance = 0.1
    potatoDOF.InFocusRadius = 0
    potatoDOF.NearIntensity = 1
    potatoDOF.Enabled = false
    potatoDOF.Parent = Lighting
    
    local potatoColor = Lighting:FindFirstChild("PotatoColor") or Instance.new("ColorCorrectionEffect")
    potatoColor.Name = "PotatoColor"
    potatoColor.Saturation = -0.5
    potatoColor.Contrast = -0.2
    potatoColor.Enabled = false
    potatoColor.Parent = Lighting
    
    local vfxClasses = {
        ["ParticleEmitter"] = true, ["Trail"] = true, ["Beam"] = true, 
        ["Fire"] = true, ["Smoke"] = true, ["Sparkles"] = true,
        ["Explosion"] = true, ["PointLight"] = true, 
        ["SpotLight"] = true, ["SurfaceLight"] = true,
        ["BloomEffect"] = true, ["BlurEffect"] = true,
        ["SunRaysEffect"] = true, ["Clouds"] = true, ["Atmosphere"] = true,
        ["SelectionSphere"] = true, ["SelectionBox"] = true, ["Highlight"] = true,
        ["Sky"] = true
    }

    local function makePotato(obj)
        pcall(function()
            local nameLower = obj.Name:lower()
            if vfxClasses[obj.ClassName] or nameLower:find("aura") or nameLower:find("effect") or nameLower:find("glow") then
                if obj:IsA("BasePart") then
                    obj.Transparency = Config.PotatoGraphics and 1 or 0
                else
                    obj.Enabled = not Config.PotatoGraphics
                end
            end
            
            if obj:IsA("BasePart") then
                if Config.PotatoGraphics then
                    if not obj:GetAttribute("OriginalMaterial") then
                        obj:SetAttribute("OriginalMaterial", obj.Material)
                    end
                    obj.Material = Enum.Material.Plastic
                    obj.CastShadow = false
                    obj.Reflectance = 0
                else
                    local original = obj:GetAttribute("OriginalMaterial")
                    if original then obj.Material = original end
                    obj.CastShadow = true
                end
            end

            if obj:IsA("MeshPart") or obj:IsA("SpecialMesh") then
                if Config.PotatoGraphics then
                    if obj:IsA("MeshPart") then
                        obj.RenderFidelity = Enum.RenderFidelity.Performance
                    end
                end
            end

            if obj:IsA("Decal") or obj:IsA("Texture") then
                obj.Transparency = Config.PotatoGraphics and 1 or 0
            end
        end)
    end
    
    -- === SILENT AIM HOOK (Streamable) ===
    local __index
    local __namecall
    
    pcall(function()
        -- Global Wait Hooks for No Cooldown
        local oldWait = wait
        getgenv().wait = function(n)
            if Config.NoCooldown and not checkcaller() then
                return oldWait()
            end
            return oldWait(n)
        end
        
        local oldTaskWait = task.wait
        getgenv().task.wait = function(n)
            if Config.NoCooldown and not checkcaller() then
                return oldTaskWait()
            end
            return oldTaskWait(n)
        end

        -- Time Hooks to bypass "tick() - lastUse" checks
        local oldTick = tick
        getgenv().tick = function()
            if Config.NoCooldown and not checkcaller() then
                return oldTick() + 9e9
            end
            return oldTick()
        end

        local oldOsClock = os.clock
        getgenv().os.clock = function()
            if Config.NoCooldown and not checkcaller() then
                return oldOsClock() + 9e9
            end
            return oldOsClock()
        end

        local oldOsTime = os.time
        getgenv().os.time = function()
            if Config.NoCooldown and not checkcaller() then
                return oldOsTime() + 9e9
            end
            return oldOsTime()
        end

        -- Low-level HookFunction for Time (Unbypassable by localization)
        if hookfunction then
            local oldT; oldT = hookfunction(tick, function()
                if Config.NoCooldown and not checkcaller() then return oldT() + 9e9 end
                return oldT()
            end)
            local oldC; oldC = hookfunction(os.clock, function()
                if Config.NoCooldown and not checkcaller() then return oldC() + 9e9 end
                return oldC()
            end)
        end

        __index = hookmetamethod(game, "__index", function(t, k)
            if Config.NoCooldown and not checkcaller() then
                local tStr = tostring(t):lower()
                local kStr = tostring(k):lower()
                
                -- Catch all cooldown-related value reads
                if (kStr == "value" or kStr == "Value") then
                    local name = ""
                    pcall(function() name = t.Name:lower() end)
                    if name:find("cooldown") or name:find("cd") or name:find("skill") or name:find("timer") or name:find("ability") or name:find("stun") or name:find("busy") then
                        return 0
                    end
                end
                
                -- Catch cooldown property reads
                if kStr:find("cooldown") or kStr == "cd" or kStr == "skillcd" then
                    return 0
                end
                
                -- Catch stun/busy states
                if kStr == "stun" or kStr == "busy" or kStr == "using" or kStr == "casting" or kStr == "attacking" then
                    return false
                end
            end

            -- Fix: Active le Silent Aim automatiquement si Hitbox Extender est ON pour valider les dÃ©gÃ¢ts serveur
            if (Config.SilentAim or Config.HitboxExpander or Config.CursorLock) and (t == mouse or tostring(t) == "Mouse") then
                if k == "Hit" or k == "Target" or k == "UnitRay" then
                    local targetChar = nil
                    if Config.CursorLock and cursorLockTarget then
                        targetChar = cursorLockTarget
                    elseif Config.SilentAim or Config.HitboxExpander then
                        local p = getClosestPlayerToMouse(Config.SilentFOV, true)
                        if p then targetChar = p.Character end
                    end
                    if targetChar then
                        -- PrioritÃ© au Torse (HumanoidRootPart) car plus gros
                        local targetPart = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso") or targetChar:FindFirstChild("Head")
                        if targetPart then
                            if k == "Hit" then return targetPart.CFrame end
                            if k == "Target" then return targetPart end
                            if k == "UnitRay" then 
                                local origin = camera.CFrame.Position
                                local direction = (targetPart.Position - origin).Unit
                                return Ray.new(origin, direction)
                            end
                        end
                    end
                end
            end
            return __index(t, k)
        end)
        
        __namecall = hookmetamethod(game, "__namecall", function(self, ...)
            local method = getnamecallmethod()
            local args = {...}
            
            if Config.NoCooldown and not checkcaller() then
                if method == "GetAttribute" then
                    local attr = args[1]
                    if type(attr) == "string" then
                        local attrL = attr:lower()
                        if attrL:find("cooldown") or attrL:find("cd") or attrL:find("lastuse") or attrL:find("nextuse") or attrL:find("timer") then
                            return 0
                        end
                        if attrL == "stun" or attrL == "busy" or attrL == "using" or attrL == "casting" or attrL == "attacking" then
                            return false
                        end
                    end
                elseif method == "InvokeServer" or method == "FireServer" then
                    -- Block/Modify only cooldown-setting remotes
                    local selfName = tostring(self):lower()
                    if selfName == "commf_" or selfName:find("remote") or selfName:find("event") then
                        local cmd = args[1]
                        if type(cmd) == "string" then
                            local cmdLower = cmd:lower()
                            if cmdLower == "cooldown" or cmdLower == "setcooldown" or cmdLower:find("skillcooldown") or cmdLower:find("abilitycooldown") then
                                return nil
                            end
                        end
                    end
                elseif method == "SetAttribute" then
                    local attr = args[1]
                    if type(attr) == "string" then
                        local attrL = attr:lower()
                        if attrL:find("cooldown") or attrL:find("cd") or attrL == "stun" or attrL == "busy" or attrL == "using" or attrL == "casting" or attrL == "attacking" then
                            return
                        end
                    end
                end
            end

            if (Config.SilentAim or Config.HitboxExpander or Config.CursorLock) and method == "GetMouseLocation" then
                local targetChar = nil
                if Config.CursorLock and cursorLockTarget then
                    targetChar = cursorLockTarget
                elseif Config.SilentAim or Config.HitboxExpander then
                    local p = getClosestPlayerToMouse(Config.SilentFOV, true)
                    if p then targetChar = p.Character end
                end
                if targetChar then
                    local targetPart = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso") or targetChar:FindFirstChild("Head")
                    if targetPart then
                        local screenPos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                        if onScreen then
                            return Vector2.new(screenPos.X, screenPos.Y)
                        end
                    end
                end
            end

            if (Config.SilentAim or Config.HitboxExpander or Config.CursorLock) then
                if method == "Raycast" and self == workspace then
                    local targetChar = nil
                    if Config.CursorLock and cursorLockTarget then
                        targetChar = cursorLockTarget
                    elseif Config.SilentAim or Config.HitboxExpander then
                        local p = getClosestPlayerToMouse(Config.SilentFOV, true)
                        if p then targetChar = p.Character end
                    end
                    if targetChar then
                        local targetPart = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso") or targetChar:FindFirstChild("Head")
                        if targetPart then
                            local origin = args[1]
                            local direction = (targetPart.Position - origin).Unit * args[2].Magnitude
                            return __namecall(self, origin, direction, args[3])
                        end
                    end
                elseif (method == "ScreenPointToRay" or method == "ViewportPointToRay") and self == camera then
                    local targetChar = nil
                    if Config.CursorLock and cursorLockTarget then
                        targetChar = cursorLockTarget
                    elseif Config.SilentAim or Config.HitboxExpander then
                        local p = getClosestPlayerToMouse(Config.SilentFOV, true)
                        if p then targetChar = p.Character end
                    end
                    if targetChar then
                        local targetPart = targetChar:FindFirstChild("HumanoidRootPart") or targetChar:FindFirstChild("Torso") or targetChar:FindFirstChild("Head")
                        if targetPart then
                            local origin = self.CFrame.Position
                            local direction = (targetPart.Position - origin).Unit
                            return Ray.new(origin, direction)
                        end
                    end
                end
            end
            return __namecall(self, ...)
        end)
    end)
    
    
    -- Helper to check if any combat key is currently held down
    local function IsCombatKeyDown()
        -- Check Keyboard Keys
        for _, key in pairs(Config.CombatKeys) do
            if UserInputService:IsKeyDown(key) then
                return true
            end
        end
        -- Check Mouse
        return false
    end

    -- GOD MODE CLICK FUNCTION (BURST SPAM)
    local function NuclearClick(targetOverride)
        local clickX, clickY
        local mousePos = UserInputService:GetMouseLocation()
        
        if targetOverride then
            clickX = targetOverride.X
            clickY = targetOverride.Y
        else
            clickX = mousePos.X
            clickY = mousePos.Y
        end
        
        -- Prevent clicking on Cheat GUI
        local gui = CoreGui:FindFirstChild("DesyncGUI_V102") or (player.PlayerGui and player.PlayerGui:FindFirstChild("DesyncGUI_V102"))
        if gui then
            local mainFrame = gui:FindFirstChild("MainFrame")
            if mainFrame and mainFrame.Visible then
                local absPos = mainFrame.AbsolutePosition
                local absSize = mainFrame.AbsoluteSize
                if clickX >= absPos.X and clickX <= absPos.X + absSize.X and clickY >= absPos.Y and clickY <= absPos.Y + absSize.Y then return end
            end
        end
        
        -- Force Mouse Move Instant
        pcall(function()
            VirtualInputManager:SendMouseMoveEvent(clickX, clickY, game)
        end)
        
        -- Click
        pcall(function()
            VirtualInputManager:SendMouseButtonEvent(clickX, clickY, 0, true, game, 1)
            VirtualInputManager:SendMouseButtonEvent(clickX, clickY, 0, false, game, 1)
        end)
        if mouse1click then mouse1click() end
        
        if character then
            local tool = character:FindFirstChildOfClass("Tool")
            if tool and (Config.EasyLock or tick() - lastToolUse > 0.25) then
                tool:Activate()
                lastToolUse = tick()
            end
        end
    end
    
    local function ClearESP()
        ESPFolder:ClearAllChildren()
    end

    -- Bind Instant Respawn to Humanoid.Died
    local function BindInstantRespawn()
        if instantRespawnConn then instantRespawnConn:Disconnect(); instantRespawnConn = nil end
        if character then
            local hum = character:FindFirstChild("Humanoid")
            if hum then
                instantRespawnConn = hum.Died:Connect(function()
                    task.defer(function()
                        pcall(function() player:LoadCharacter() end)
                    end)
                end)
            end
        end
    end



    local function UpdateESP()
        if not Config.ESPBox and not Config.ESPName and not Config.ESPLvl and not Config.ESPHP and not Config.ESPChams and not Config.ESPDot and not Config.ESPNPC and not Config.ESPFruit and not Config.ShowMoney and not Config.ShowFragments and not Config.ShowBounty and not Config.ESPTracers then
            ClearESP()
            return
        end

        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                local char = p.Character
                local root = char.HumanoidRootPart
                local hum = char.Humanoid
                
                local espId = p.Name .. "_ESP"
                local espGroup = ESPFolder:FindFirstChild(espId)
                
                if not espGroup then
                    espGroup = Instance.new("Folder")
                    espGroup.Name = espId
                    espGroup.Parent = ESPFolder
                    
                    -- Box
                    local box = Instance.new("SelectionBox")
                    box.Name = "Box"
                    box.Adornee = root
                    box.LineThickness = 0.05
                    box.Parent = espGroup

                    -- Text Info
                    local bb = Instance.new("BillboardGui")
                    bb.Name = "Info"
                    bb.Adornee = char:FindFirstChild("Head") or root
                    bb.Size = UDim2.new(0, 100, 0, 50)
                    bb.StudsOffset = Vector3.new(0, 3.5, 0)
                    bb.AlwaysOnTop = true
                    bb.MaxDistance = math.huge
                    bb.Parent = espGroup
                    
                    local txt = Instance.new("TextLabel")
                    txt.Name = "ESPText"
                    txt.Size = UDim2.new(1, 0, 1, 0)
                    txt.BackgroundTransparency = 1
                    txt.TextStrokeTransparency = 0
                    txt.TextStrokeColor3 = Color3.new(0,0,0)
                    txt.Font = Enum.Font.GothamBold
                    txt.TextSize = 12
                    txt.Text = ""
                    txt.RichText = false
                    txt.Parent = bb
                end
                
                if espGroup then
                    local box = espGroup:FindFirstChild("Box")
                    local bb = espGroup:FindFirstChild("Info")
                    
                    -- WALL HACK (CHAMS) LOGIC
                    local highlight = char:FindFirstChild("CyberChams")
                    if Config.ESPChams then
                        if not highlight then
                            highlight = Instance.new("Highlight")
                            highlight.Name = "CyberChams"
                            highlight.Parent = char
                            highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                            highlight.FillColor = CurrentThemeColor
                            highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                            highlight.FillTransparency = 0.5
                            highlight.OutlineTransparency = 0
                        end
                    else
                        if highlight then highlight:Destroy() end
                    end
                    
                    -- DOT LOGIC (RED POINT)
                    local dotGui = espGroup:FindFirstChild("DotGui")
                    if Config.ESPDot then
                        if not dotGui then
                            dotGui = Instance.new("BillboardGui")
                            dotGui.Name = "DotGui"
                            dotGui.Size = UDim2.new(0, 10, 0, 10)
                            dotGui.AlwaysOnTop = true
                            dotGui.Adornee = root
                            dotGui.Parent = espGroup
                            
                            local dotFrame = Instance.new("Frame")
                            dotFrame.Size = UDim2.new(1, 0, 1, 0)
                            dotFrame.BackgroundColor3 = Color3.fromRGB(255, 0, 0) -- Red
                            dotFrame.BorderSizePixel = 0
                            dotFrame.Parent = dotGui
                            
                            local dotCorner = Instance.new("UICorner")
                            dotCorner.CornerRadius = UDim.new(1, 0) -- Circle
                            dotCorner.Parent = dotFrame
                        end
                        dotGui.Enabled = true
                    else
                        if dotGui then dotGui:Destroy() end
                    end
                    
                    local hpPercent = hum.Health / hum.MaxHealth
                    local customColor = Color3.fromRGB(Config.ESPColorR, Config.ESPColorG, Config.ESPColorB)
                    local useColor = customColor
                    
                    -- TRACERS LOGIC (LINES)
                    local targetAtt = root:FindFirstChild("ESP_Target_Att")
                    if not targetAtt then targetAtt = Instance.new("Attachment"); targetAtt.Name = "ESP_Target_Att"; targetAtt.Parent = root end
                    
                    local beam = targetAtt:FindFirstChild("TracerBeam")
                    
                    if Config.ESPTracers then
                        local myRoot = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
                        if myRoot then
                            local myAtt = myRoot:FindFirstChild("ESP_Origin_Att")
                            if not myAtt then
                                myAtt = Instance.new("Attachment")
                                myAtt.Name = "ESP_Origin_Att"
                                myAtt.Parent = myRoot
                            end
                            
                            if not beam then
                                beam = Instance.new("Beam")
                                beam.Name = "TracerBeam"
                                beam.FaceCamera = true
                                beam.Transparency = NumberSequence.new(0.5)
                                beam.Width0 = 0.1
                                beam.Width1 = 0.1
                                beam.Parent = targetAtt -- Fix: Parent to Workspace to be visible
                            end
                            
                            beam.Attachment0 = myAtt
                            beam.Attachment1 = targetAtt
                            beam.Color = ColorSequence.new(useColor)
                            beam.Enabled = true
                        end
                    else
                        if beam then beam:Destroy() end
                    end
                    
                    if box then
                        if Config.ESPBox then
                            box.Visible = true
                            box.Adornee = root
                            box.Color3 = useColor
                        else
                            box.Visible = false
                        end
                    end
                    
                    if bb and bb:FindFirstChild("ESPText") then
                        local txtLabel = bb.ESPText
                        if Config.ESPName or Config.ESPHP or Config.ESPLvl or (Config.InspectorName ~= "" and (Config.ShowMoney or Config.ShowFragments or Config.ShowBounty) and string.find(p.Name:lower(), Config.InspectorName:lower())) then
                            bb.Enabled = true
                            bb.Adornee = char:FindFirstChild("Head") or root
                            
                            local textStr = ""
                            if Config.ESPName then textStr = textStr .. p.Name end
                            if Config.ESPLvl then
                                local lvl = 0
                                pcall(function() lvl = p.Data.Level.Value end)
                                if textStr ~= "" then textStr = textStr .. "\n" end
                                textStr = textStr .. "[Lv. " .. lvl .. "]"
                            end
                            if Config.ESPHP then
                                if textStr ~= "" then textStr = textStr .. "\n" end
                                textStr = textStr .. "[" .. math.floor(hum.Health) .. " HP]"
                            end
                            
                            if Config.InspectorName ~= "" and string.find(p.Name:lower(), Config.InspectorName:lower()) then
                                if Config.ShowMoney then
                                    local val = 0
                                    pcall(function() val = p.Data.Beli.Value end)
                                    if textStr ~= "" then textStr = textStr .. "\n" end
                                    textStr = textStr .. "[ðŸ’° " .. val .. "]"
                                end
                                if Config.ShowFragments then
                                    local val = 0
                                    pcall(function() val = p.Data.Fragments.Value end)
                                    if textStr ~= "" then textStr = textStr .. "\n" end
                                    textStr = textStr .. "[ðŸŸ£ " .. val .. "]"
                                end
                                if Config.ShowBounty then
                                    local val = 0
                                    pcall(function() val = p.leaderstats["Bounty/Honor"].Value end)
                                    if textStr ~= "" then textStr = textStr .. "\n" end
                                    textStr = textStr .. "[ðŸ›¡ï¸ " .. val .. "]"
                                end
                            end
                            
                            txtLabel.Text = textStr
                            txtLabel.TextColor3 = useColor
                            txtLabel.TextSize = Config.ESPSize
                        else
                            bb.Enabled = false
                        end
                    end
                end
            else
                local espGroup = ESPFolder:FindFirstChild(p.Name .. "_ESP")
                if espGroup then espGroup:Destroy() end
            end
        end

        -- NPC ESP LOOP
        if Config.ESPNPC then
            local function processNPC(model)
                if model ~= character and model:FindFirstChild("HumanoidRootPart") and model:FindFirstChild("Humanoid") and model.Humanoid.Health > 0 then
                    if Players:GetPlayerFromCharacter(model) then return end
                    
                    local espId = "_NPC_" .. tostring(model) .. "_" .. model:GetDebugId()
                    local espGroup = ESPFolder:FindFirstChild(espId)
                    
                    if not espGroup then
                        espGroup = Instance.new("Folder")
                        espGroup.Name = espId
                        espGroup.Parent = ESPFolder
                        
                        local box = Instance.new("BoxHandleAdornment")
                        box.Name = "Box"
                        box.Size = Vector3.new(4, 5, 1)
                        box.Adornee = model.HumanoidRootPart
                        box.AlwaysOnTop = true
                        box.ZIndex = 5
                        box.Transparency = 0.6
                        box.Color3 = Color3.fromRGB(255, 150, 0)
                        box.Parent = espGroup
                        
                        local highlight = Instance.new("Highlight")
                        highlight.Name = "NPCChams"
                        highlight.Parent = model
                        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                        highlight.FillColor = Color3.fromRGB(255, 150, 0)
                        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                        highlight.FillTransparency = 0.5
                        highlight.OutlineTransparency = 0
                        
                        local href = Instance.new("ObjectValue")
                        href.Name = "HighlightRef"
                        href.Value = highlight
                        href.Parent = espGroup
                    end
                end
            end
            
            for _, v in pairs(workspace:GetChildren()) do if v:IsA("Model") then processNPC(v) end end
            if workspace:FindFirstChild("Enemies") then for _, v in pairs(workspace.Enemies:GetChildren()) do if v:IsA("Model") then processNPC(v) end end end
        end
        
        for _, child in pairs(ESPFolder:GetChildren()) do
            if child.Name:sub(1, 5) == "_NPC_" then
                local box = child:FindFirstChild("Box")
                if not Config.ESPNPC or not box or not box.Adornee or not box.Adornee.Parent or box.Adornee.Parent.Humanoid.Health <= 0 then
                    local href = child:FindFirstChild("HighlightRef")
                    if href and href.Value then href.Value:Destroy() end
                    child:Destroy()
                end
            else
                local playerName = child.Name:gsub("_ESP", "")
                if not Players:FindFirstChild(playerName) then
                    child:Destroy()
                end
            end
        end
    end
    
    -- FRUIT ESP LOOP
    if Config.ESPFruit then
        local detectedFruits = {}
        
        -- Scan entire workspace for fruits (Dough Fruit and other fruits)
        for _, v in pairs(workspace:GetDescendants()) do
            if v:IsA("Tool") and (v.Name == "Dough Fruit" or v.ToolTip == "Blox Fruit" or v.Name:find("Fruit")) then
                local handle = v:FindFirstChild("Handle")
                if handle then
                    local espId = "_FRUIT_" .. tostring(handle)
                    table.insert(detectedFruits, {id = espId, handle = handle})
                end
            end
        end
        
        -- Create ESP for all detected fruits
        for _, fruitData in pairs(detectedFruits) do
            local espGroup = ESPFolder:FindFirstChild(fruitData.id)
            
            if not espGroup then
                espGroup = Instance.new("Folder")
                espGroup.Name = fruitData.id
                espGroup.Parent = ESPFolder
                
                -- RED AURA (using Highlight)
                local highlight = Instance.new("Highlight")
                highlight.Name = "FruitHighlight"
                highlight.Adornee = fruitData.handle
                highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                highlight.FillColor = Color3.fromRGB(255, 0, 0)
                highlight.FillTransparency = 0.5
                highlight.OutlineColor = Color3.fromRGB(255, 50, 50)
                highlight.OutlineTransparency = 0
                highlight.Parent = espGroup
                
                -- "Fruit" text tag
                local bb = Instance.new("BillboardGui")
                bb.Name = "FruitInfo"
                bb.Adornee = fruitData.handle
                bb.Size = UDim2.new(0, 100, 0, 30)
                bb.StudsOffset = Vector3.new(0, 2, 0)
                bb.AlwaysOnTop = true
                bb.MaxDistance = 3000
                bb.Parent = espGroup
                
                local txt = Instance.new("TextLabel")
                txt.Name = "FruitText"
                txt.Size = UDim2.new(1, 0, 1, 0)
                txt.BackgroundTransparency = 1
                txt.TextStrokeTransparency = 0
                txt.TextStrokeColor3 = Color3.new(0, 0, 0)
                txt.TextColor3 = Color3.fromRGB(255, 50, 50)
                txt.Font = Enum.Font.GothamBold
                txt.TextSize = 18
                txt.Text = "Fruit"
                txt.RichText = false
                txt.Parent = bb
                
                local href = Instance.new("ObjectValue")
                href.Name = "HighlightRef"
                href.Value = highlight
                href.Parent = espGroup
            end
        end
        
        -- Cleanup old fruit ESP
        for _, child in pairs(ESPFolder:GetChildren()) do
            if child.Name:sub(1, 8) == "_FRUIT_" then
                local highlightRef = child:FindFirstChild("HighlightRef")
                local found = false
                
                for _, fruitData in pairs(detectedFruits) do
                    if fruitData.id == child.Name then
                        found = true
                        break
                    end
                end
                
                if not found then
                    if highlightRef and highlightRef.Value then highlightRef.Value:Destroy() end
                    child:Destroy()
                end
            end
        end
    else
        -- Cleanup all fruit ESP if disabled
        for _, child in pairs(ESPFolder:GetChildren()) do
            if child.Name:sub(1, 8) == "_FRUIT_" then
                local highlightRef = child:FindFirstChild("HighlightRef")
                if highlightRef and highlightRef.Value then highlightRef.Value:Destroy() end
                child:Destroy()
            end
        end
    end
    
    -- ==================================================================
    --                          MAIN GUI (Professional)
    -- ==================================================================

    local CurrentThemeColor = Color3.fromRGB(255, 255, 255)

    -- Cleanup existing GUI and Sound to prevent duplicates
    if CoreGui:FindFirstChild("DesyncGUI_V102") then CoreGui.DesyncGUI_V102:Destroy() end
    if player.PlayerGui and player.PlayerGui:FindFirstChild("DesyncGUI_V102") then player.PlayerGui.DesyncGUI_V102:Destroy() end

    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "DesyncGUI_V102"
    pcall(function() ScreenGui.Parent = CoreGui end)
    if not ScreenGui.Parent then ScreenGui.Parent = player:WaitForChild("PlayerGui") end

    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 400, 0, 500) 
    MainFrame.Position = UDim2.new(0.5, -200, 0.5, -250)
    MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    MainFrame.BorderSizePixel = 0
    MainFrame.Active = true
    MainFrame.Draggable = false
    MainFrame.ClipsDescendants = true -- Important pour l'animation
    MainFrame.Parent = ScreenGui

    local MainStroke = Instance.new("UIStroke")
    MainStroke.Color = Color3.fromRGB(40, 40, 50)
    MainStroke.Thickness = 1
    MainStroke.Parent = MainFrame

    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 4)
    UICorner.Parent = MainFrame

    -- === STATS GUI (Top Left) ===
    local StatsFrame = Instance.new("Frame")
    StatsFrame.Name = "StatsFrame"
    StatsFrame.Size = UDim2.new(0, 200, 0, 100)
    StatsFrame.Position = UDim2.new(0, 20, 0, 20)
    StatsFrame.BackgroundTransparency = 1
    StatsFrame.Parent = ScreenGui
    
    local StatsLayout = Instance.new("UIListLayout")
    StatsLayout.SortOrder = Enum.SortOrder.LayoutOrder
    StatsLayout.Padding = UDim.new(0, 2)
    StatsLayout.Parent = StatsFrame
    
    local function createStatLabel(name)
        local label = Instance.new("TextLabel")
        label.Name = name
        label.Size = UDim2.new(1, 0, 0, 15)
        label.BackgroundTransparency = 1
        label.TextColor3 = CurrentThemeColor
        label.Font = Enum.Font.GothamBold
        label.TextSize = 12
        label.TextXAlignment = Enum.TextXAlignment.Left
        label.TextStrokeTransparency = 0.5
        label.Visible = false
        label.Parent = StatsFrame
        return label
    end
    
    local FPSLabel = createStatLabel("FPSLabel")
    local PingLabel = createStatLabel("PingLabel")
    local UptimeLabel = createStatLabel("UptimeLabel")
    local ServerLabel = createStatLabel("ServerLabel")
    
    task.spawn(function()
        while Config.IsRunning do
            pcall(function()
                if ScreenGui and StatsFrame then
                    FPSLabel.Visible = Config.ShowFPS
                    FPSLabel.Text = "FPS: " .. math.floor(workspace:GetRealPhysicsFPS())
                    FPSLabel.TextColor3 = CurrentThemeColor
                    
                    PingLabel.Visible = Config.ShowPing
                    PingLabel.Text = "Ping: " .. math.floor(game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue()) .. " ms"
                    PingLabel.TextColor3 = CurrentThemeColor
                    
                    UptimeLabel.Visible = Config.ShowUptime
                    local t = workspace.DistributedGameTime
                    local hours = math.floor(t / 3600)
                    local mins = math.floor((t % 3600) / 60)
                    local secs = math.floor(t % 60)
                    UptimeLabel.Text = string.format("Server Time: %02d:%02d:%02d", hours, mins, secs)
                    UptimeLabel.TextColor3 = CurrentThemeColor
                    
                    ServerLabel.Visible = Config.ShowServer
                    ServerLabel.Text = "Region: " .. cachedRegion
                    ServerLabel.TextColor3 = CurrentThemeColor
                end
            end)
            task.wait(1)
        end
    end)

    -- === MINIMIZE SYSTEM ===
    local isMinimized = false
    local MinimizedGui = nil
    local dragConnection = nil
    
    local function restoreGui()
        if not isMinimized then return end
        
        if dragConnection then
            dragConnection:Disconnect()
            dragConnection = nil
        end
        
        if MinimizedGui and MinimizedGui:FindFirstChild("MinimizedFrame") then
            local tween = TweenService:Create(MinimizedGui.MinimizedFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0)})
            tween:Play()
            tween.Completed:Wait()
            MinimizedGui:Destroy()
            MinimizedGui = nil
        end
        
        MainFrame.Visible = true
        isMinimized = false
    end

    local function minimizeGui()
        if isMinimized then return end
        isMinimized = true
        MainFrame.Visible = false
        
        MinimizedGui = Instance.new("ScreenGui")
        MinimizedGui.Name = "MinimizedCheat"
        pcall(function() MinimizedGui.Parent = CoreGui end)
        if not MinimizedGui.Parent then MinimizedGui.Parent = player:WaitForChild("PlayerGui") end
        
        local MinimizedFrame = Instance.new("Frame")
        MinimizedFrame.Name = "MinimizedFrame"
        MinimizedFrame.Size = UDim2.new(0, 0, 0, 0) -- Start small
        MinimizedFrame.Position = UDim2.new(0, 50, 0, 50)
        MinimizedFrame.BackgroundTransparency = 1
        MinimizedFrame.BorderSizePixel = 0
        MinimizedFrame.Active = true
        MinimizedFrame.Parent = MinimizedGui
        
        local MCorner = Instance.new("UICorner") MCorner.CornerRadius = UDim.new(0, 10) MCorner.Parent = MinimizedFrame
        
        local MImage = Instance.new("ImageLabel")
        MImage.Size = UDim2.new(1, 0, 1, 0)
        MImage.Position = UDim2.new(0, 0, 0, 0)
        MImage.BackgroundTransparency = 1
        MImage.BorderSizePixel = 0
        MImage.Image = "rbxassetid://105375391672845"
        MImage.ScaleType = Enum.ScaleType.Fit
        MImage.Parent = MinimizedFrame
        local MImageCorner = Instance.new("UICorner") MImageCorner.CornerRadius = UDim.new(0, 10) MImageCorner.Parent = MImage
        
        local RestoreBtn = Instance.new("TextButton") RestoreBtn.Size = UDim2.new(1, 0, 1, 0) RestoreBtn.BackgroundTransparency = 1 RestoreBtn.Text = "" RestoreBtn.Parent = MinimizedFrame
        RestoreBtn.BorderSizePixel = 0
        
        -- Custom Drag Logic
        local dragging = false
        local dragStart = nil
        local startPos = nil
        local isClick = true
        
        RestoreBtn.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
                isClick = true
                dragStart = input.Position
                startPos = MinimizedFrame.Position
                
                local inputConnection
                inputConnection = input.Changed:Connect(function()
                    if input.UserInputState == Enum.UserInputState.End then
                        dragging = false
                        if inputConnection then inputConnection:Disconnect() end
                        if isClick then restoreGui() end
                    end
                end)
            end
        end)
        
        dragConnection = UserInputService.InputChanged:Connect(function(input)
            if dragging then
                if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                    local delta = input.Position - dragStart
                    if delta.Magnitude > 5 then isClick = false end
                    MinimizedFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
                end
            end
        end)
        
        TweenService:Create(MinimizedFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 80, 0, 80)}):Play()
    end

    -- PANELS (Left & Right Structure)
    local LeftPanel = Instance.new("Frame")
    LeftPanel.Name = "LeftPanel"
    LeftPanel.Size = UDim2.new(0, 400, 1, 0)
    LeftPanel.BackgroundTransparency = 1
    LeftPanel.Parent = MainFrame

    local RightPanel = Instance.new("Frame")
    RightPanel.Name = "RightPanel"
    RightPanel.Size = UDim2.new(0, 0, 1, 0) -- Commence cachÃ© (largeur 0)
    RightPanel.Position = UDim2.new(0, 400, 0, 0)
    RightPanel.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
    RightPanel.BorderSizePixel = 0
    RightPanel.ClipsDescendants = true
    RightPanel.Parent = MainFrame

    local RightSeparator = Instance.new("Frame")
    RightSeparator.Size = UDim2.new(0, 1, 1, 0)
    RightSeparator.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    RightSeparator.BorderSizePixel = 0
    RightSeparator.Parent = RightPanel

    -- Header
    local Header = Instance.new("Frame")
    Header.Size = UDim2.new(1, 0, 0, 30)
    Header.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    Header.BorderSizePixel = 0
    Header.Parent = LeftPanel

    local HeaderCorner = Instance.new("UICorner")
    HeaderCorner.CornerRadius = UDim.new(0, 4)
    HeaderCorner.Parent = Header
    
    local HeaderFix = Instance.new("Frame")
    HeaderFix.Size = UDim2.new(1, 0, 0, 5)
    HeaderFix.Position = UDim2.new(0, 0, 1, -5)
    HeaderFix.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    HeaderFix.BorderSizePixel = 0
    HeaderFix.Parent = Header

    local Title = Instance.new("TextLabel")
    Title.Text = "SILVERIKA"
    Title.Size = UDim2.new(1, -20, 1, 0)
    Title.Position = UDim2.new(0, 10, 0, 0)
    Title.BackgroundTransparency = 1
    Title.TextColor3 = CurrentThemeColor
    Title.Font = Enum.Font.GothamBold
    Title.TextXAlignment = Enum.TextXAlignment.Left
    Title.TextSize = 10
    Title.Parent = Header
    
    -- Click Title to Minimize
    local TitleBtn = Instance.new("TextButton")
    TitleBtn.Size = UDim2.new(1, 0, 1, 0)
    TitleBtn.BackgroundTransparency = 1
    TitleBtn.Text = ""
    TitleBtn.Parent = Title
    
    -- Close Button (Cross)
    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Name = "CloseBtn"
    CloseBtn.Text = "X"
    CloseBtn.Size = UDim2.new(0, 20, 0, 20)
    CloseBtn.Position = UDim2.new(1, -50, 0, 5)
    CloseBtn.BackgroundTransparency = 1
    CloseBtn.TextColor3 = Color3.fromRGB(200, 50, 50)
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.TextSize = 14
    CloseBtn.Parent = Header
    
    CloseBtn.MouseButton1Click:Connect(function()
        -- Send Disconnect Signal
        task.spawn(function()
            local requestFunc = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
            if requestFunc and keyString then
                local hwid = (gethwid and gethwid()) or (get_hwid and get_hwid()) or player.UserId
                pcall(function()
                    requestFunc({
                        Url = AUTH_URL .. "/verify?key=" .. keyString .. "&hwid=" .. tostring(hwid) .. "&type=disconnect",
                        Method = "GET"
                    })
                end)
            end
        end)

        Config.IsRunning = false
        for _, conn in pairs(Connections) do
            if conn then conn:Disconnect() end
        end
        table.clear(Connections)
        
        -- 1. Destroy UI
        if ScreenGui then ScreenGui:Destroy() end
        if ESPFolder then ESPFolder:Destroy() end
        if MinimizedGui then MinimizedGui:Destroy() end
        if BroadcastGui then BroadcastGui:Destroy() end
        if _G.SnowGui then _G.SnowGui:Destroy() end
        SendNotification("SILVERIKA has been destroyed", 3)
        
        -- 2. Disable Features
        for k, v in pairs(Config) do
            if type(v) == "boolean" then
                Config[k] = false
            end
        end
        
        -- 3. Cleanup Objects
        if FOVCircle then pcall(function() FOVCircle:Remove() end) end
        
        -- Destroy snow effect
        if SnowGui then SnowGui:Destroy() end
        if _G.SnowGui then _G.SnowGui:Destroy() end
        for _, c in pairs(SnowConnections) do if c then c:Disconnect() end end
        SnowConnections = {}
        
        -- 4. Reset Character
        if character and humanoid then
            if rootPart then
                rootPart.Anchored = false
                for _, v in pairs(rootPart:GetChildren()) do
                    if v:IsA("BodyVelocity") or v:IsA("BodyGyro") then v:Destroy() end
                end
            end
        end
        
        -- 5. Disconnects
        if reachConnection then reachConnection:Disconnect() end
        if antiLavaConnection then antiLavaConnection:Disconnect() end
        if potatoConnection then potatoConnection:Disconnect() end
        pcall(function() RunService:UnbindFromRenderStep("AimLockPriority") end)
        pcall(function() script:Destroy() end)
        
        -- Attempt to stop external scripts
        pcall(function() _G.AutoFarm = false; _G.AutoFarmChest = false end)
    end)

    local disconnected = false
    local function SendDisconnectSignal()
        if disconnected then return end
        disconnected = true
        local requestFunc = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        if requestFunc and keyString then
            local hwid = (gethwid and gethwid()) or (get_hwid and get_hwid()) or player.UserId
            pcall(function()
                requestFunc({
                    Url = AUTH_URL .. "/verify?key=" .. keyString .. "&hwid=" .. tostring(hwid) .. "&type=disconnect",
                    Method = "GET"
                })
            end)
        end
    end

    -- Handle Game Exit (Alt+F4 / Close Button)
    pcall(function()
        game:BindToClose(SendDisconnectSignal)
    end)
    -- Handle Leave via Menu (Esc -> Leave)
    Players.PlayerRemoving:Connect(function(plr) if plr == player then SendDisconnectSignal() end end)
    
    -- Custom Drag Logic for MainFrame (Header)
    local draggingMain = false
    local dragStartMain = nil
    local startPosMain = nil
    local isClickMain = true
    
    TitleBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            draggingMain = true
            isClickMain = true
            dragStartMain = input.Position
            startPosMain = MainFrame.Position
            
            local inputConnection
            inputConnection = input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    draggingMain = false
                    if inputConnection then inputConnection:Disconnect() end
                    if isClickMain then minimizeGui() end
                end
            end)
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if draggingMain then
            if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                local delta = input.Position - dragStartMain
                if delta.Magnitude > 5 then isClickMain = false end
                MainFrame.Position = UDim2.new(startPosMain.X.Scale, startPosMain.X.Offset + delta.X, startPosMain.Y.Scale, startPosMain.Y.Offset + delta.Y)
            end
        end
    end)
    -- Search Bar
    local SearchBar = Instance.new("TextBox")
    SearchBar.Name = "SearchBar"
    SearchBar.Size = UDim2.new(0, 80, 0, 18)
    SearchBar.Position = UDim2.new(0.5, -40, 0.5, -9)
    SearchBar.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    SearchBar.TextColor3 = Color3.fromRGB(200, 200, 200)
    SearchBar.PlaceholderText = "Search..."
    SearchBar.Font = Enum.Font.Gotham
    SearchBar.TextSize = 10
    SearchBar.BorderSizePixel = 0
    SearchBar.Parent = Header
    local SCorner = Instance.new("UICorner") SCorner.CornerRadius = UDim.new(0, 4) SCorner.Parent = SearchBar

    -- Separator
    local Separator = Instance.new("Frame")
    Separator.Size = UDim2.new(1, 0, 0, 1)
    Separator.Position = UDim2.new(0, 0, 0, 30)
    Separator.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    Separator.BorderSizePixel = 0
    Separator.Parent = LeftPanel

    -- === TABS (5 Onglets) ===
    local TabContainer = Instance.new("Frame")
    TabContainer.Size = UDim2.new(1, -20, 0, 25)
    TabContainer.Position = UDim2.new(0, 10, 0, 40)
    TabContainer.BackgroundTransparency = 1
    TabContainer.Parent = LeftPanel

    local TabLayout = Instance.new("UIListLayout")
    TabLayout.FillDirection = Enum.FillDirection.Horizontal
    TabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabLayout.Padding = UDim.new(0, 5)
    TabLayout.Parent = TabContainer

    local PageContainer = Instance.new("Frame")
    PageContainer.Size = UDim2.new(1, -20, 1, -80)
    PageContainer.Position = UDim2.new(0, 10, 0, 75)
    PageContainer.BackgroundTransparency = 1
    PageContainer.Parent = LeftPanel

    local tabs = {}
    local pages = {}

    local function createTabButton(text, order, parent)
        local Button = Instance.new("TextButton")
        Button.Size = UDim2.new(0.2, -4, 1, 0) -- Adjusted for 5 tabs
        Button.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        Button.TextColor3 = Color3.fromRGB(150, 150, 150)
        Button.Font = Enum.Font.Gotham
        Button.TextSize = 5
        Button.TextWrapped = false
        Button.Text = text
        Button.LayoutOrder = order
        Button.Parent = parent or TabContainer
        
        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 3)
        Corner.Parent = Button
        
        local Stroke = Instance.new("UIStroke")
        Stroke.Color = Color3.fromRGB(40, 40, 50)
        Stroke.Thickness = 1
        Stroke.Parent = Button
        
        return Button
    end

    local TabBtns = {
        Players = createTabButton("Players", 1, TabContainer),
        Visuals = createTabButton("Visuals", 2, TabContainer),
        Aim = createTabButton("Aim", 3, TabContainer),
        Hitbox = createTabButton("Hitbox", 4, TabContainer),
        Teleport = createTabButton("Teleport", 5, TabContainer),
        Farm = createTabButton("Farm", 6, TabContainer),
        Raid = createTabButton("Raid", 7, TabContainer),
        Settings = createTabButton("Settings", 8, TabContainer),
        Shop = createTabButton("Misc", 9, TabContainer),
        SeaEvent = createTabButton("Sea Event", 10, TabContainer),
        Custom = createTabButton("Custom", 11, TabContainer),
        FarmScripts = createTabButton("Scripts", 6.1, TabContainer),
        FarmSettings = createTabButton("Settings", 6.2, TabContainer),
        FarmStatus = createTabButton("Status", 6.3, TabContainer),
        FarmFarming = createTabButton("Farming", 6.4, TabContainer)
    }

    local function createPage(name, parent)
        local Page = Instance.new("ScrollingFrame")
        Page.Name = name
        Page.Size = UDim2.new(1, 0, 1, 0)
        Page.BackgroundTransparency = 1
        Page.BorderSizePixel = 0
        Page.ScrollBarThickness = 4
        Page.ScrollBarImageColor3 = Color3.fromRGB(60, 60, 70)
        Page.Visible = false
        Page.Parent = parent or PageContainer
        Page.AutomaticCanvasSize = Enum.AutomaticSize.Y
        Page.CanvasSize = UDim2.new(0, 0, 0, 0)
        
        local Layout = Instance.new("UIListLayout")
        Layout.SortOrder = Enum.SortOrder.LayoutOrder
        Layout.Padding = UDim.new(0, 5)
        Layout.Parent = Page
        
        local Padding = Instance.new("UIPadding")
        Padding.PaddingRight = UDim.new(0, 6)
        Padding.Parent = Page
        
        return Page
    end

    local Pages = {
        Players = createPage("PlayersPage", PageContainer),
        Visuals = createPage("VisualsPage", PageContainer),
        Aim = createPage("AimPage", PageContainer),
        Hitbox = createPage("HitboxPage", PageContainer),
        Teleport = createPage("TeleportPage", PageContainer),
        Farm = createPage("FarmPage", PageContainer),
        FarmScripts = createPage("FarmScriptsPage", PageContainer),
        FarmSettings = createPage("FarmSettingsPage", PageContainer),
        FarmStatus = createPage("FarmStatusPage", PageContainer),
        FarmFarming = createPage("FarmFarmingPage", PageContainer),
        Raid = createPage("RaidPage", PageContainer),
        Settings = createPage("SettingsPage", PageContainer),
        Shop = createPage("ShopPage", PageContainer),
        SeaEvent = createPage("SeaEventPage", PageContainer),
        Misc = createPage("MiscPage", PageContainer),
        Custom = createPage("CustomPage", PageContainer),
        Search = createPage("SearchPage", PageContainer)
    }

    local function switchTab(tabName)
        for name, btn in pairs(TabBtns) do
            btn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
            btn.TextColor3 = Color3.fromRGB(150, 150, 150)
            btn.UIStroke.Color = Color3.fromRGB(40, 40, 50)
        end
        
        -- Clear Search when switching tabs
        if SearchBar.Text ~= "" then SearchBar.Text = "" end
        
        for name, page in pairs(Pages) do
            page.Visible = false
        end
        
        TabBtns[tabName].BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        TabBtns[tabName].TextColor3 = Color3.fromRGB(255, 255, 255)
        TabBtns[tabName].UIStroke.Color = CurrentThemeColor
        Pages[tabName].Visible = true
    end

    -- Search Logic
    local allItems = {}
    local function InitSearch()
        allItems = {}
        for name, page in pairs(Pages) do
            if name ~= "Search" then
                local children = page:GetChildren()
                table.sort(children, function(a, b)
                    local orderA = (a:IsA("GuiObject") and a.LayoutOrder) or 0
                    local orderB = (b:IsA("GuiObject") and b.LayoutOrder) or 0
                    return orderA < orderB
                end)
                
                local currentSectionId = name .. "_0"
                local sectionCount = 0
                
                for _, child in ipairs(children) do
                    if not (child:IsA("UIListLayout") or child:IsA("UIPadding")) then
                    
                        -- Identify Section Header by Color (Specific to createSection: 80, 80, 90)
                        if child:IsA("TextLabel") and child.TextColor3 == Color3.fromRGB(80, 80, 90) then
                            sectionCount = sectionCount + 1
                            currentSectionId = name .. "_" .. sectionCount
                        end
                        
                        table.insert(allItems, {
                            Obj = child,
                            OriginalParent = page,
                            SectionId = currentSectionId
                        })
                    end
                end
            end
        end
    end

    SearchBar:GetPropertyChangedSignal("Text"):Connect(function()
        local text = SearchBar.Text:lower()
        if text == "" then
            if #allItems > 0 then
                for _, item in pairs(allItems) do
                    item.Obj.Parent = item.OriginalParent
                end
                allItems = {}
            end
            for name, btn in pairs(TabBtns) do
                if btn.BackgroundColor3 == Color3.fromRGB(35, 35, 45) then
                    Pages[name].Visible = true
                end
            end
            Pages.Search.Visible = false
        else
            if #allItems == 0 then InitSearch() end
            for _, page in pairs(Pages) do page.Visible = false end
            Pages.Search.Visible = true
            
            local matchingSections = {}
            for _, item in pairs(allItems) do
                local match = false
                if item.Obj:IsA("TextLabel") then
                    if item.Obj.Text:lower():find(text) then match = true end
                elseif item.Obj:IsA("Frame") then
                    for _, desc in pairs(item.Obj:GetDescendants()) do
                        if (desc:IsA("TextLabel") or desc:IsA("TextButton") or desc:IsA("TextBox")) and desc.Text:lower():find(text) then
                            match = true
                            break
                        end
                    end
                end
                
                if match then
                    matchingSections[item.SectionId] = true
                end
            end
            
            for _, item in pairs(allItems) do
                if matchingSections[item.SectionId] then
                    item.Obj.Parent = Pages.Search
                else
                    item.Obj.Parent = item.OriginalParent
                end
            end
        end
    end)

    for name, btn in pairs(TabBtns) do
        btn.MouseButton1Click:Connect(function() switchTab(name) end)
    end
    
    -- Hide Farm by default
    TabBtns.Farm.Visible = true
    TabBtns.Raid.Visible = true
    TabBtns.Settings.Visible = true
    TabBtns.Shop.Visible = true
    TabBtns.SeaEvent.Visible = true
    TabBtns.Custom.Visible = true
    TabBtns.FarmScripts.Visible = true
    TabBtns.FarmSettings.Visible = true
    TabBtns.FarmStatus.Visible = true
    TabBtns.FarmFarming.Visible = true
    
    -- EXPAND BUTTON (Arrow Logic)
    local ExpandButton = Instance.new("TextButton")
    ExpandButton.Name = "ExpandButton"
    ExpandButton.Text = "+"
    ExpandButton.Size = UDim2.new(0, 20, 0, 20)
    ExpandButton.Position = UDim2.new(1, -25, 0, 5)
    ExpandButton.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    ExpandButton.TextColor3 = CurrentThemeColor
    ExpandButton.Font = Enum.Font.GothamBold
    ExpandButton.TextSize = 14
    ExpandButton.Parent = LeftPanel
    
    local ExpandCorner = Instance.new("UICorner")
    ExpandCorner.CornerRadius = UDim.new(0, 4)
    ExpandCorner.Parent = ExpandButton
    
    local ExpandStroke = Instance.new("UIStroke")
    ExpandStroke.Color = Color3.fromRGB(40, 40, 50)
    ExpandStroke.Thickness = 1
    ExpandStroke.Parent = ExpandButton
    
    local isExpanded = false
    ExpandButton.MouseButton1Click:Connect(function()
        isExpanded = not isExpanded
        if isExpanded then
            -- Close
            TweenService:Create(MainFrame, TweenInfo.new(0.6, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 700, 0, 500)}):Play()
            TweenService:Create(RightPanel, TweenInfo.new(0.6, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 300, 1, 0)}):Play()
            ExpandButton.Text = "-"
        else
            -- Open
            TweenService:Create(MainFrame, TweenInfo.new(0.6, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 400, 0, 500)}):Play()
            TweenService:Create(RightPanel, TweenInfo.new(0.6, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 0, 1, 0)}):Play()
            ExpandButton.Text = "+"
        end
    end)
    
    -- RIGHT PANEL CONTENT (Placeholders)
    local RightTitle = Instance.new("TextLabel")
    RightTitle.Text = "EXTRA"
    RightTitle.Size = UDim2.new(1, 0, 0, 30)
    RightTitle.BackgroundTransparency = 1
    RightTitle.TextColor3 = Color3.fromRGB(100, 100, 100)
    RightTitle.Font = Enum.Font.GothamBold
    RightTitle.TextSize = 9
    RightTitle.Parent = RightPanel
    
    local RightTabContainer = Instance.new("Frame")
    RightTabContainer.Size = UDim2.new(1, -20, 0, 25)
    RightTabContainer.Position = UDim2.new(0, 10, 0, 40)
    RightTabContainer.BackgroundTransparency = 1
    RightTabContainer.Visible = false
    RightTabContainer.Parent = RightPanel

    local RightTabLayout = Instance.new("UIListLayout")
    RightTabLayout.FillDirection = Enum.FillDirection.Horizontal
    RightTabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    RightTabLayout.Padding = UDim.new(0, 5)
    RightTabLayout.Parent = RightTabContainer

    local RightPageContainer = Instance.new("Frame")
    RightPageContainer.Size = UDim2.new(1, -20, 1, -80)
    RightPageContainer.Position = UDim2.new(0, 10, 0, 75)
    RightPageContainer.BackgroundTransparency = 1
    RightPageContainer.Visible = false
    RightPageContainer.Parent = RightPanel

    -- HUB MENU & BUTTONS
    local HubMenu = Instance.new("ScrollingFrame")
    HubMenu.Name = "HubMenu"
    HubMenu.Size = UDim2.new(1, -20, 1, -50)
    HubMenu.Position = UDim2.new(0, 10, 0, 40)
    HubMenu.BackgroundTransparency = 1
    HubMenu.BorderSizePixel = 0
    HubMenu.ScrollBarThickness = 2
    HubMenu.AutomaticCanvasSize = Enum.AutomaticSize.Y
    HubMenu.CanvasSize = UDim2.new(0, 0, 0, 0)
    HubMenu.Parent = RightPanel
    
    local HubLayout = Instance.new("UIListLayout")
    HubLayout.SortOrder = Enum.SortOrder.LayoutOrder
    HubLayout.Padding = UDim.new(0, 10)
    HubLayout.Parent = HubMenu
    
    local function createHubBtn(text, order)
        local Btn = Instance.new("TextButton")
        Btn.Size = UDim2.new(1, 0, 0, 30) -- Taille rÃ©duite de 40 Ã  30
        Btn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        Btn.TextColor3 = CurrentThemeColor
        Btn.Font = Enum.Font.GothamBold
        Btn.TextSize = 12
        Btn.Text = text
        Btn.LayoutOrder = order
        Btn.Parent = HubMenu
        
        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 6)
        Corner.Parent = Btn
        
        local Stroke = Instance.new("UIStroke")
        Stroke.Color = Color3.fromRGB(40, 40, 50)
        Stroke.Thickness = 1
        Stroke.Parent = Btn
        
        return Btn
    end
    
    local PVPBtn = createHubBtn("PVP", 1)
    local FarmBtn = createHubBtn("Farm", 2)
    local ShopBtn = createHubBtn("Misc", 3)
    local CustomBtn = createHubBtn("Script Feature", 4)
    local SocialsBtn = createHubBtn("Niggers Ready To Slime", 5)
    
    local VideoContainer = Instance.new("Frame")
    VideoContainer.Name = "VideoContainer"
    VideoContainer.Size = UDim2.new(1, 0, 0, 150) -- Grand carrÃ© (RÃ©duit)
    VideoContainer.BackgroundTransparency = 1
    VideoContainer.LayoutOrder = 7
    VideoContainer.Parent = HubMenu
    
    local VidCorner = Instance.new("UICorner") VidCorner.CornerRadius = UDim.new(0, 6) VidCorner.Parent = VideoContainer
    
    local ImageViewer = Instance.new("ImageLabel")
    ImageViewer.Size = UDim2.new(0, 80, 0, 80)
    ImageViewer.Position = UDim2.new(0.5, -40, 0.1, 0)
    ImageViewer.BackgroundTransparency = 1
    ImageViewer.Image = "rbxthumb://type=AvatarHeadShot&id=" .. player.UserId .. "&w=420&h=420"
    ImageViewer.ScaleType = Enum.ScaleType.Fit
    ImageViewer.Parent = VideoContainer
    local ImgCorner = Instance.new("UICorner") ImgCorner.CornerRadius = UDim.new(1, 0) ImgCorner.Parent = ImageViewer
    
    local NameLabel = Instance.new("TextLabel")
    NameLabel.Text = player.DisplayName
    NameLabel.Size = UDim2.new(1, 0, 0, 20)
    NameLabel.Position = UDim2.new(0, 0, 0.65, 0)
    NameLabel.BackgroundTransparency = 1
    NameLabel.TextColor3 = CurrentThemeColor
    NameLabel.Font = Enum.Font.GothamBold
    NameLabel.TextSize = 14
    NameLabel.Parent = VideoContainer
    
    local UserLabel = Instance.new("TextLabel")
    UserLabel.Text = "@" .. player.Name
    UserLabel.Size = UDim2.new(1, 0, 0, 15)
    UserLabel.Position = UDim2.new(0, 0, 0.8, 0)
    UserLabel.BackgroundTransparency = 1
    UserLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
    UserLabel.Font = Enum.Font.Gotham
    UserLabel.TextSize = 10
    UserLabel.Parent = VideoContainer
    
    local SettingsBtn = Instance.new("TextButton")
    SettingsBtn.Name = "SettingsIcon"
    SettingsBtn.Text = "âš™"
    SettingsBtn.Size = UDim2.new(0, 30, 0, 30)
    SettingsBtn.Position = UDim2.new(1, -10, 1, -10)
    SettingsBtn.AnchorPoint = Vector2.new(1, 1)
    SettingsBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    SettingsBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    SettingsBtn.Font = Enum.Font.GothamBold
    SettingsBtn.TextSize = 18
    SettingsBtn.Parent = RightPanel
    
    local SetCorner = Instance.new("UICorner")
    SetCorner.CornerRadius = UDim.new(0, 4)
    SetCorner.Parent = SettingsBtn
    
    -- === PATCH NOTES BUTTON (NEON) ===
    local PatchNotesBtn = Instance.new("TextButton")
    PatchNotesBtn.Name = "PatchNotesBtn"
    PatchNotesBtn.Text = "ðŸ“°"
    PatchNotesBtn.Size = UDim2.new(0, 30, 0, 30)
    PatchNotesBtn.Position = UDim2.new(1, -45, 1, -10) -- Left of SettingsBtn
    PatchNotesBtn.AnchorPoint = Vector2.new(1, 1)
    PatchNotesBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    PatchNotesBtn.TextColor3 = CurrentThemeColor
    PatchNotesBtn.Font = Enum.Font.GothamBold
    PatchNotesBtn.TextSize = 18
    PatchNotesBtn.Parent = RightPanel
    
    local PNC = Instance.new("UICorner") PNC.CornerRadius = UDim.new(0, 4) PNC.Parent = PatchNotesBtn
    local PNStroke = Instance.new("UIStroke") PNStroke.Color = CurrentThemeColor PNStroke.Thickness = 1 PNStroke.Transparency = 0.6 PNStroke.Parent = PatchNotesBtn

    -- Neon Breathing Animation
    local tweenInfo = TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1, true)
    local tween = TweenService:Create(PNStroke, tweenInfo, {Transparency = 0.1})
    tween:Play()

    -- PATCH NOTES PAGE
    local PatchNotesPage = Instance.new("ScrollingFrame")
    PatchNotesPage.Name = "PatchNotesPage"
    PatchNotesPage.Size = UDim2.new(1, -20, 1, -50)
    PatchNotesPage.Position = UDim2.new(0, 10, 0, 40)
    PatchNotesPage.BackgroundTransparency = 1
    PatchNotesPage.BorderSizePixel = 0
    PatchNotesPage.ScrollBarThickness = 2
    PatchNotesPage.AutomaticCanvasSize = Enum.AutomaticSize.Y
    PatchNotesPage.CanvasSize = UDim2.new(0, 0, 0, 0)
    PatchNotesPage.Visible = false
    PatchNotesPage.Parent = RightPanel
    
    local PNLayout = Instance.new("UIListLayout") PNLayout.SortOrder = Enum.SortOrder.LayoutOrder PNLayout.Padding = UDim.new(0, 5) PNLayout.Parent = PatchNotesPage
    
    local function addPatchNote(version, text)
        local frame = Instance.new("Frame")
        frame.AutomaticSize = Enum.AutomaticSize.Y
        frame.Size = UDim2.new(1, 0, 0, 0)
        frame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        frame.Parent = PatchNotesPage
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0, 4) c.Parent = frame
        
        local vLabel = Instance.new("TextLabel")
        vLabel.Name = "VersionLabel"
        vLabel.Text = version
        vLabel.Size = UDim2.new(1, -10, 0, 20)
        vLabel.Position = UDim2.new(0, 5, 0, 0)
        vLabel.BackgroundTransparency = 1
        vLabel.TextColor3 = CurrentThemeColor
        vLabel.Font = Enum.Font.GothamBold
        vLabel.TextSize = 10
        vLabel.TextXAlignment = Enum.TextXAlignment.Left
        vLabel.Parent = frame
        
        local tLabel = Instance.new("TextLabel")
        tLabel.Text = text
        tLabel.Size = UDim2.new(1, -10, 0, 0)
        tLabel.Position = UDim2.new(0, 5, 0, 20)
        tLabel.AutomaticSize = Enum.AutomaticSize.Y
        tLabel.BackgroundTransparency = 1
        tLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
        tLabel.Font = Enum.Font.Gotham
        tLabel.TextSize = 9
        tLabel.TextXAlignment = Enum.TextXAlignment.Left
        tLabel.TextWrapped = true
        tLabel.Parent = frame
        
        local pad = Instance.new("UIPadding") pad.PaddingBottom = UDim.new(0, 5) pad.Parent = frame
    end
    
    addPatchNote("v10.7 (Latest)", "New FlashStep Trigger, undetectable")
    
    -- SOCIALS PAGE (Chat & Users)
    local SocialsPage = Instance.new("Frame")
    SocialsPage.Name = "SocialsPage"
    SocialsPage.Size = UDim2.new(1, -20, 1, -150) -- RÃ©duit encore plus pour Ã©viter le chevauchement
    SocialsPage.Position = UDim2.new(0, 10, 0, 40)
    SocialsPage.BackgroundTransparency = 1
    SocialsPage.Visible = false
    SocialsPage.Parent = RightPanel
    
    local OnlineLabel = Instance.new("TextLabel")
    OnlineLabel.Size = UDim2.new(1, 0, 0, 20)
    OnlineLabel.BackgroundTransparency = 1
    OnlineLabel.TextColor3 = Color3.fromRGB(0, 255, 150)
    OnlineLabel.Font = Enum.Font.GothamBold
    OnlineLabel.TextSize = 10
    OnlineLabel.Text = "Active Users: ..."
    OnlineLabel.Parent = SocialsPage
    
    local UserList = Instance.new("ScrollingFrame")
    UserList.Size = UDim2.new(1, 0, 0, 80)
    UserList.Position = UDim2.new(0, 0, 0, 25)
    UserList.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    UserList.BorderSizePixel = 0
    UserList.ScrollBarThickness = 2
    UserList.AutomaticCanvasSize = Enum.AutomaticSize.Y
    UserList.CanvasSize = UDim2.new(0, 0, 0, 0)
    UserList.Parent = SocialsPage
    local ULCorner = Instance.new("UICorner") ULCorner.CornerRadius = UDim.new(0, 4) ULCorner.Parent = UserList
    local ULLayout = Instance.new("UIListLayout") ULLayout.Padding = UDim.new(0, 2) ULLayout.Parent = UserList
    local ULPadding = Instance.new("UIPadding") ULPadding.PaddingLeft = UDim.new(0, 5) ULPadding.PaddingTop = UDim.new(0, 5) ULPadding.Parent = UserList
    
    local ChatLabel = Instance.new("TextLabel")
    ChatLabel.Size = UDim2.new(0.8, 0, 0, 20)
    ChatLabel.Position = UDim2.new(0, 0, 0, 110)
    ChatLabel.BackgroundTransparency = 1
    ChatLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    ChatLabel.Font = Enum.Font.GothamBold
    ChatLabel.TextSize = 10
    ChatLabel.Text = "Global Chat"
    ChatLabel.TextXAlignment = Enum.TextXAlignment.Left
    ChatLabel.Parent = SocialsPage
    
    local RefreshBtn = Instance.new("TextButton")
    RefreshBtn.Size = UDim2.new(0.18, 0, 0, 20)
    RefreshBtn.Position = UDim2.new(0.65, 0, 0, 110)
    RefreshBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
    RefreshBtn.TextColor3 = CurrentThemeColor
    RefreshBtn.Font = Enum.Font.GothamBold
    RefreshBtn.TextSize = 9
    RefreshBtn.Text = "Refresh"
    RefreshBtn.Parent = SocialsPage
    local RFCorner = Instance.new("UICorner") RFCorner.CornerRadius = UDim.new(0, 3) RFCorner.Parent = RefreshBtn
    local RFStroke = Instance.new("UIStroke") RFStroke.Color = CurrentThemeColor RFStroke.Thickness = 1 RFStroke.Transparency = 0.7 RFStroke.Parent = RefreshBtn
    
    local ChatScroll = Instance.new("ScrollingFrame")
    ChatScroll.Size = UDim2.new(1, 0, 1, -160) -- AjustÃ© pour la nouvelle taille
    ChatScroll.Position = UDim2.new(0, 0, 0, 130)
    ChatScroll.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    ChatScroll.BorderSizePixel = 0
    ChatScroll.ScrollBarThickness = 2
    ChatScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    ChatScroll.Parent = SocialsPage
    local CSCorner = Instance.new("UICorner") CSCorner.CornerRadius = UDim.new(0, 4) CSCorner.Parent = ChatScroll
    local CSLayout = Instance.new("UIListLayout") CSLayout.Padding = UDim.new(0, 4) CSLayout.Parent = ChatScroll
    local CSPadding = Instance.new("UIPadding") CSPadding.PaddingLeft = UDim.new(0, 5) CSPadding.PaddingTop = UDim.new(0, 5) CSPadding.Parent = ChatScroll
    
    local ChatInput = Instance.new("TextBox")
    ChatInput.Size = UDim2.new(0.75, 0, 0, 25)
    ChatInput.Position = UDim2.new(0, 0, 1, -25)
    ChatInput.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    ChatInput.TextColor3 = Color3.fromRGB(255, 255, 255)
    ChatInput.Font = Enum.Font.Gotham
    ChatInput.TextSize = 10
    ChatInput.PlaceholderText = "Message..."
    ChatInput.Text = ""
    ChatInput.Parent = SocialsPage
    local CICorner = Instance.new("UICorner") CICorner.CornerRadius = UDim.new(0, 4) CICorner.Parent = ChatInput
    
    local SendBtn = Instance.new("TextButton")
    
    -- Envoi avec la touche EntrÃ©e
    ChatInput.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == Enum.KeyCode.Return then
            local msg = ChatInput.Text
            if msg ~= "" then
                SendBtn.MouseButton1Click:Fire()
            end
        end
    end)
    
    SendBtn.Size = UDim2.new(0.23, 0, 0, 25)
    SendBtn.Position = UDim2.new(0.77, 0, 1, -25)
    SendBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    SendBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    SendBtn.Font = Enum.Font.GothamBold
    SendBtn.TextSize = 10
    SendBtn.Text = "SEND"
    SendBtn.Parent = SocialsPage
    local SBCorner = Instance.new("UICorner") SBCorner.CornerRadius = UDim.new(0, 4) SBCorner.Parent = SendBtn
    
    local ReturnHubBtn = Instance.new("TextButton")
    ReturnHubBtn.Size = UDim2.new(1, 0, 0, 20)
    ReturnHubBtn.Position = UDim2.new(0, 0, 1, 5) -- Juste en dessous de l'input
    ReturnHubBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    ReturnHubBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    ReturnHubBtn.Font = Enum.Font.GothamBold
    ReturnHubBtn.TextSize = 10
    ReturnHubBtn.Text = "RETURN TO HUB"
    ReturnHubBtn.Parent = SocialsPage
    local RHCorner = Instance.new("UICorner") RHCorner.CornerRadius = UDim.new(0, 4) RHCorner.Parent = ReturnHubBtn
    
    local isSocialsOpen = false
    local socialsLoop = nil
    local lastChatTime = 0 -- Pour dÃ©tecter les nouveaux messages
    local replyingTo = nil -- Variable pour stocker Ã  qui on rÃ©pond
    
    local function RefreshSocialsData(forceUI)
        local requestFunc = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        if requestFunc then
            local s, r = pcall(function() 
                return requestFunc({ 
                    Url = AUTH_URL .. "/socials/data?username=" .. HttpService:UrlEncode(player.Name) .. "&jobid=" .. tostring(game.JobId), 
                    Method = "GET" 
                }) 
            end)
            if s and r and r.StatusCode == 200 then
                local data = HttpService:JSONDecode(r.Body)
                if data then
                    OnlineLabel.Text = "Active Users: " .. (data.count or 0)
                    
                    if not isSocialsOpen and not forceUI then return end
                    
                    -- Update User List
                    for _, v in pairs(UserList:GetChildren()) do if v:IsA("GuiObject") then v:Destroy() end end
                    if data.users then
                        for _, u in pairs(data.users) do
                            local btn = Instance.new("TextButton")
                            btn.Size = UDim2.new(1, -10, 0, 15)
                            btn.BackgroundTransparency = 1
                            btn.TextColor3 = Color3.fromRGB(200, 200, 200)
                            btn.Font = Enum.Font.Gotham
                            btn.TextSize = 9
                            btn.TextXAlignment = Enum.TextXAlignment.Left
                            btn.Text = "â€¢ " .. u
                            btn.Parent = UserList
                            
                            btn.MouseButton1Click:Connect(function()
                                task.spawn(function()
                                    local s, r = pcall(function()
                                        return requestFunc({
                                            Url = AUTH_URL .. "/socials/chat",
                                            Method = "POST",
                                            Headers = {["Content-Type"] = "application/json"},
                                            Body = HttpService:JSONEncode({
                                                key = keyString, 
                                                name = player.Name, 
                                                message = "Pinged you",
                                                type = "ping",
                                                target = u
                                            })
                                        })
                                    end)
                                    if s and r and r.StatusCode == 200 then SendNotification("Ping sent!", 2) end
                                end)
                            end)
                        end
                    end

                    -- Update Chat
                    -- On vÃ©rifie si le dernier message a changÃ© ou si on force l'update
                    local currentMsgs = #ChatScroll:GetChildren() - 2 -- UIPadding & UIListLayout
                    local latestMsg = data.chat and data.chat[#data.chat]
                    local latestTime = latestMsg and latestMsg.time or 0
                    
                    if data.chat and (latestTime ~= lastChatTime or #data.chat ~= currentMsgs or forceUI) then
                        -- Notification Logic (Check for replies)
                        if lastChatTime > 0 then
                            for _, msg in pairs(data.chat) do
                                if msg.time > lastChatTime then
                                    if msg.replyTo and (msg.replyTo == player.Name or msg.replyTo == player.DisplayName) and msg.name ~= player.Name and msg.name ~= player.DisplayName then
                                        SendNotification(msg.name .. " answered you", 5)
                                    end
                                    
                                    if msg.type == "ping" and msg.target then
                                        local myEntry = (player.DisplayName or player.Name)
                                        if not Config.IsWhitelisted then myEntry = "[WL] " .. myEntry end
                                        if msg.target == myEntry and msg.name ~= player.Name and msg.name ~= player.DisplayName then
                                            SendNotification(msg.name .. " pinged you", 5)
                                        end
                                    end
                                end
                            end
                        end

                        lastChatTime = latestTime
                        for _, v in pairs(ChatScroll:GetChildren()) do if v:IsA("Frame") then v:Destroy() end end
                        for _, msg in pairs(data.chat) do
                            if not msg.type or msg.type == "chat" then
                            local msgFrame = Instance.new("Frame")
                            msgFrame.AutomaticSize = Enum.AutomaticSize.Y
                            msgFrame.Size = UDim2.new(1, -10, 0, 0)
                            msgFrame.BackgroundTransparency = 1
                            msgFrame.Parent = ChatScroll
                            
                            -- Click to Reply (Invisible Button covering the message)
                            local replyBtn = Instance.new("TextButton")
                            replyBtn.Size = UDim2.new(1, 0, 1, 0)
                            replyBtn.BackgroundTransparency = 1
                            replyBtn.Text = ""
                            replyBtn.Parent = msgFrame
                            replyBtn.MouseButton1Click:Connect(function()
                                replyingTo = msg.name
                                ChatInput.PlaceholderText = "Replying to " .. msg.name .. "..."
                                ChatInput:CaptureFocus()
                            end)
                            
                            local nameLbl = Instance.new("TextLabel")
                            nameLbl.Size = UDim2.new(1, 0, 0, 12)
                            nameLbl.BackgroundTransparency = 1
                            nameLbl.Font = Enum.Font.GothamBold
                            nameLbl.TextSize = 9
                            nameLbl.TextXAlignment = Enum.TextXAlignment.Left
                            local timeStr = msg.time and os.date("[%H:%M] ", math.floor(msg.time / 1000)) or ""
                            local roleText = ""
                            if msg.role == "WL" or msg.role == "WL-AD" then roleText = " [WL]" end
                            if msg.role == "AD" or msg.role == "WL-AD" then roleText = roleText .. " [AD]" end
                            nameLbl.Text = timeStr .. msg.name .. roleText .. ":"
                            nameLbl.TextColor3 = (msg.role == "WL" or msg.role == "WL-AD") and Color3.fromRGB(255, 255, 255) or (msg.role == "AD" and Color3.fromRGB(255, 200, 0) or CurrentThemeColor)
                            nameLbl.Parent = msgFrame
                            
                            local contentLbl = Instance.new("TextLabel")
                            contentLbl.Size = UDim2.new(1, 0, 0, 0)
                            contentLbl.Position = UDim2.new(0, 0, 0, 12)
                            contentLbl.AutomaticSize = Enum.AutomaticSize.Y
                            contentLbl.BackgroundTransparency = 1
                            contentLbl.Font = Enum.Font.Gotham
                            contentLbl.TextSize = 9
                            contentLbl.TextXAlignment = Enum.TextXAlignment.Left
                            contentLbl.TextWrapped = true
                            contentLbl.TextColor3 = Color3.fromRGB(230, 230, 230)
                            contentLbl.Text = msg.msg
                            contentLbl.Parent = msgFrame
                            end
                        end
                        task.wait() -- Attendre le rendu
                        ChatScroll.CanvasPosition = Vector2.new(0, 9e9) -- Force le scroll tout en bas
                    end
                end
            end
        end
    end
    
    -- Background Social Loop (Always active to keep count updated)
    task.spawn(function()
        while Config.IsRunning do
            RefreshSocialsData(isSocialsOpen)
            task.wait(1) -- Auto-refresh every 1 second
        end
    end)
    
    SocialsBtn.MouseButton1Click:Connect(function()
        HubMenu.Visible = false
        RightTabContainer.Visible = false
        RightPageContainer.Visible = false
        PatchNotesPage.Visible = false
        SocialsPage.Visible = true
        RightTitle.Text = "SOCIALS"
        BackBtn.Visible = true
        isSocialsOpen = true
        RefreshSocialsData(true)
    end)
    
    SendBtn.MouseButton1Click:Connect(function()
        local msg = ChatInput.Text
        if msg == "" then return end
        ChatInput.Text = ""
        
        local currentReply = replyingTo
        replyingTo = nil
        ChatInput.PlaceholderText = "Message..."

        local requestFunc = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        if requestFunc and keyString then
            task.spawn(function()
                local s, r = pcall(function()
                    return requestFunc({
                        Url = AUTH_URL .. "/socials/chat",
                        Method = "POST",
                        Headers = {["Content-Type"] = "application/json"},
                         Body = HttpService:JSONEncode({
                            key = keyString, 
                            name = player.Name, 
                            message = msg,
                            role = (Config.IsWhitelisted and "WL" or "") .. (Config.IsAdmin and "AD" or ""),
                            replyTo = currentReply
                        })
                    })
                end)
                if s and r and r.StatusCode == 200 then 
                    SendNotification("Message Sent!", 1) 
                else
                    SendNotification("Failed to send message", 2)
                end
                task.wait(0.2)
                RefreshSocialsData(true)
            end)
        end
    end)

    RefreshBtn.MouseButton1Click:Connect(function()
        RefreshBtn.Text = "LOADING..."
        RefreshBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 90)
        
        task.spawn(function()
            RefreshSocialsData(true)
            task.wait(1)
            
            RefreshBtn.Text = "REFRESH"
            RefreshBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
        end)
    end)

    PatchNotesBtn.MouseButton1Click:Connect(function()
        if PatchNotesPage.Visible then
            HubMenu.Visible = true
            RightTabContainer.Visible = false
            RightPageContainer.Visible = false
            PatchNotesPage.Visible = false
            RightTitle.Text = "FEATURES LIST"
            BackBtn.Visible = false
            isSocialsOpen = false
        else
            HubMenu.Visible = false
            RightTabContainer.Visible = false
            RightPageContainer.Visible = false
            PatchNotesPage.Visible = true
            RightTitle.Text = "PATCH NOTES"
            BackBtn.Visible = true
            isSocialsOpen = false
        end
    end)

    local BetaLabel = Instance.new("TextLabel")
    BetaLabel.Text = "BETA"
    BetaLabel.Size = UDim2.new(1, 0, 0, 15)
    BetaLabel.Position = UDim2.new(0, 0, 1, -15)
    BetaLabel.Position = UDim2.new(0, 0, 1, -25)
    BetaLabel.BackgroundTransparency = 1
    BetaLabel.TextColor3 = Color3.fromRGB(60, 60, 70)
    BetaLabel.Font = Enum.Font.GothamBold
    BetaLabel.TextSize = 10
    BetaLabel.Parent = RightPanel
    
    -- === KEY TIMER IN HUB ===
    local KeyTimeLabel = Instance.new("TextLabel")
    KeyTimeLabel.Name = "KeyTimeLabel"
    KeyTimeLabel.Size = UDim2.new(1, 0, 0, 15)
    KeyTimeLabel.Position = UDim2.new(0, 0, 1, -55)
    KeyTimeLabel.BackgroundTransparency = 1
    KeyTimeLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
    KeyTimeLabel.Font = Enum.Font.GothamBold
    KeyTimeLabel.TextSize = 10
    KeyTimeLabel.Text = "Lifetime Key"
    KeyTimeLabel.Parent = RightPanel

    if keyDuration and keyDuration < 800000000 then
        task.spawn(function()
            local endTime = tick() + keyDuration
            while Config.IsRunning and tick() < endTime do
                local rem = endTime - tick()
                local h = math.floor(rem / 3600)
                local m = math.floor((rem % 3600) / 60)
                local s = math.floor(rem % 60)
                KeyTimeLabel.Text = string.format("Expires: %02d:%02d:%02d", h, m, s)
                task.wait(1)
            end
            if tick() >= endTime then player:Kick("Key Expired") end
        end)
    end

    -- Back Button (Hidden by default)
    local BackBtn = Instance.new("TextButton")
    BackBtn.Text = "<"
    BackBtn.Size = UDim2.new(0, 20, 0, 20)
    BackBtn.Position = UDim2.new(0, 5, 0, 5)
    BackBtn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    BackBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    BackBtn.Font = Enum.Font.GothamBold
    BackBtn.TextSize = 12
    BackBtn.Parent = RightPanel
    BackBtn.Visible = false
    
    local BackCorner = Instance.new("UICorner")
    BackCorner.CornerRadius = UDim.new(0, 4)
    BackCorner.Parent = BackBtn

    -- Helper to manage Left Panel Tabs
    local function setLeftTabs(mode)
        for _, btn in pairs(TabBtns) do btn.Visible = false end
        
        if mode == "PVP" then
            TabBtns.Players.Visible = true
            TabBtns.Visuals.Visible = true
            TabBtns.Aim.Visible = true
            TabBtns.Hitbox.Visible = true
            TabBtns.Teleport.Visible = true
            for _, btn in pairs(TabBtns) do btn.Size = UDim2.new(0.2, -4, 1, 0) end
            switchTab("Players")
        elseif mode == "Farm" then
            TabBtns.FarmScripts.Visible = true
            TabBtns.FarmSettings.Visible = true
            TabBtns.FarmStatus.Visible = true
            TabBtns.FarmFarming.Visible = true
            for _, btn in pairs(TabBtns) do 
                if btn.Visible then btn.Size = UDim2.new(0.25, -4, 1, 0) end 
            end
            switchTab("FarmScripts")
        elseif mode == "Dungeon" then
            TabBtns.Raid.Visible = true
            TabBtns.Raid.Size = UDim2.new(1, 0, 1, 0)
            switchTab("Raid")
        elseif mode == "Settings" then
            TabBtns.Settings.Visible = true
            TabBtns.Settings.Size = UDim2.new(1, 0, 1, 0)
            switchTab("Settings")
        elseif mode == "Shop" then
            TabBtns.Shop.Visible = true
            TabBtns.Shop.Size = UDim2.new(1, 0, 1, 0)
            switchTab("Shop")
        elseif mode == "SeaEvent" then
            TabBtns.SeaEvent.Visible = true
            TabBtns.SeaEvent.Size = UDim2.new(1, 0, 1, 0)
            switchTab("SeaEvent")
        elseif mode == "Custom" then
            TabBtns.Custom.Visible = true
            TabBtns.Custom.Size = UDim2.new(1, 0, 1, 0)
            switchTab("Custom")
        end
    end
    
    BackBtn.MouseButton1Click:Connect(function()
        HubMenu.Visible = true
        RightTabContainer.Visible = false
        RightPageContainer.Visible = false
        PatchNotesPage.Visible = false
        RightTitle.Text = "EXTRA"
        SocialsPage.Visible = false
        isSocialsOpen = false
        BackBtn.Visible = false
        
        -- Restore pages to Left Panel
        for name, page in pairs(Pages) do
            page.Parent = PageContainer
            page.Visible = false
        end
        switchTab("Players")
    end)
    
    ReturnHubBtn.MouseButton1Click:Connect(function()
        HubMenu.Visible = true
        RightTabContainer.Visible = false
        RightPageContainer.Visible = false
        PatchNotesPage.Visible = false
        RightTitle.Text = "EXTRA"
        SocialsPage.Visible = false
        isSocialsOpen = false
        BackBtn.Visible = false
        
        -- Restore pages to Left Panel
        for name, page in pairs(Pages) do
            page.Parent = PageContainer
            page.Visible = false
        end
        switchTab("Players")
    end)
    
    FarmBtn.MouseButton1Click:Connect(function()
        setLeftTabs("Farm")
    end)
    
    PVPBtn.MouseButton1Click:Connect(function()
        setLeftTabs("PVP")
    end)
    
    ShopBtn.MouseButton1Click:Connect(function()
        setLeftTabs("Shop")
    end)
    
    CustomBtn.MouseButton1Click:Connect(function()
        setLeftTabs("Custom")
    end)

    SettingsBtn.MouseButton1Click:Connect(function()
        setLeftTabs("Settings")
    end)
    
    switchTab("Players")

    -- === UI ELEMENTS CREATION ===

    local function createSection(parent, text, order)
        local Label = Instance.new("TextLabel")
        Label.Text = text:upper()
        Label.Size = UDim2.new(1, 0, 0, 20)
        Label.BackgroundTransparency = 1
        Label.TextColor3 = Color3.fromRGB(80, 80, 90)
        Label.Font = Enum.Font.GothamBold
        Label.TextSize = 8
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.LayoutOrder = order
        Label.Parent = parent
        
        local Pad = Instance.new("UIPadding")
        Pad.PaddingLeft = UDim.new(0, 2)
        Pad.Parent = Label
    end

    local function createKeybind(parent, text, configKey, order)
        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(1, 0, 0, 30)
        Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        Frame.LayoutOrder = order
        Frame.Parent = parent

        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 3)
        Corner.Parent = Frame
        
        local Stroke = Instance.new("UIStroke")
        Stroke.Color = Color3.fromRGB(35, 35, 45)
        Stroke.Thickness = 1
        Stroke.Parent = Frame

        local Label = Instance.new("TextLabel")
        Label.Text = text
        Label.Size = UDim2.new(0.7, 0, 1, 0)
        Label.BackgroundTransparency = 1
        Label.TextColor3 = Color3.fromRGB(200, 200, 200)
        Label.Font = Enum.Font.GothamMedium
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.TextSize = (text == "One Shot (Need Anti Haunted)") and 7 or 9
        Label.Parent = Frame
        
        local Pad = Instance.new("UIPadding")
        Pad.PaddingLeft = UDim.new(0, 10)
        Pad.Parent = Label

        local Button = Instance.new("TextButton")
        Button.Size = UDim2.new(0.25, 0, 0.7, 0)
        Button.Position = UDim2.new(0.72, 0, 0.15, 0)
        Button.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        Button.TextColor3 = CurrentThemeColor
        Button.Font = Enum.Font.GothamBold
        Button.TextSize = 8
        Button.Parent = Frame
        Button.ZIndex = 100
        
        local key = Config[configKey]
        local keyName = "..."
        if key and typeof(key) == "EnumItem" then
            if key ~= Enum.KeyCode.Unknown then
                keyName = key.Name
            end
        end
        Button.Text = keyName
        KeybindButtons[configKey] = Button
        
        local BCorner = Instance.new("UICorner")
        BCorner.CornerRadius = UDim.new(0, 3)
        BCorner.Parent = Button
        
        local BStroke = Instance.new("UIStroke")
        BStroke.Color = CurrentThemeColor
        BStroke.Thickness = 1.5
        BStroke.Transparency = 0.7
        BStroke.Parent = Button

        local isWaitingForKey = false

        Button.MouseButton1Click:Connect(function()
            if isWaitingForKey then return end
            
            isWaitingForKey = true
            Button.Text = "Press a key..."
            Button.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
            BStroke.Transparency = 0.3
            
            local oldKeyCode = Config[configKey]
            local connection
            connection = UserInputService.InputBegan:Connect(function(input, gameProcessed)
                if input.UserInputType == Enum.UserInputType.Keyboard then
                    if input.KeyCode == Enum.KeyCode.Escape then
                        -- Annuler - revenir Ã  l'ancienne touche
                        Config[configKey] = oldKeyCode
                        if oldKeyCode and typeof(oldKeyCode) == "EnumItem" and oldKeyCode ~= Enum.KeyCode.Unknown then
                            Button.Text = oldKeyCode.Name
                        else
                            Button.Text = "..."
                        end
                        Button.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
                        BStroke.Transparency = 0.7
                        isWaitingForKey = false
                        connection:Disconnect()
                    else
                        -- Assigner la nouvelle touche
                        Config[configKey] = input.KeyCode
                        Button.Text = input.KeyCode.Name
                        Button.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
                        BStroke.Transparency = 0.7
                        isWaitingForKey = false
                        connection:Disconnect()
                    end
                end
            end)
        end)
    end

    local function createInput(parent, text, configKey, order)
        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(1, 0, 0, 30)
        Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        Frame.LayoutOrder = order
        Frame.Parent = parent

        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 3)
        Corner.Parent = Frame
        
        local Stroke = Instance.new("UIStroke")
        Stroke.Color = Color3.fromRGB(35, 35, 45)
        Stroke.Thickness = 1
        Stroke.Parent = Frame

        local Label = Instance.new("TextLabel")
        Label.Text = text
        Label.Size = UDim2.new(0.6, 0, 1, 0)
        Label.BackgroundTransparency = 1
        Label.TextColor3 = Color3.fromRGB(200, 200, 200)
        Label.Font = Enum.Font.GothamMedium
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.TextSize = 9
        Label.Parent = Frame
        
        local Pad = Instance.new("UIPadding")
        Pad.PaddingLeft = UDim.new(0, 10)
        Pad.Parent = Label

        local Input = Instance.new("TextBox")
        Input.Size = UDim2.new(0.35, 0, 0.7, 0)
        Input.Position = UDim2.new(0.62, 0, 0.15, 0)
        Input.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        Input.TextColor3 = CurrentThemeColor
        Input.Font = Enum.Font.GothamBold
        Input.TextSize = 8
        Input.Text = tostring(Config[configKey])
        Input.Parent = Frame

        InputBoxes[configKey] = Input

        local ICorner = Instance.new("UICorner")
        ICorner.CornerRadius = UDim.new(0, 3)
        ICorner.Parent = Input

        Input.FocusLost:Connect(function()
            if type(Config[configKey]) == "string" then
                Config[configKey] = Input.Text:gsub("^%s*(.-)%s*$", "%1") -- Trim spaces
                if configKey == "CustomCursorId" then
                    local val = Config[configKey]
                    if val ~= "" then
                        if tonumber(val) then
                            mouse.Icon = "rbxassetid://" .. val
                        else
                            mouse.Icon = val
                        end
                    else
                        mouse.Icon = ""
                    end
                end
                
                if configKey == "AutoLoadConfigName" then
                    UpdateLoaderSettings()
                end
            else
                local num = tonumber(Input.Text)
                if num then
                    -- LIMITES DE VITESSE
                    if configKey == "FlySpeedValue" and num > 350 then num = 350 end
                    if configKey == "DesyncFlySpeed" and num > 355 then num = 355 end
                    
                    Config[configKey] = num
                    if configKey == "FOVValue" and num > 120 then num = 120 end
                    if configKey == "FOVValue" and num < 10 then num = 10 end
                    
                    if configKey == "JumpPowerValue" then
                        if humanoid then humanoid.UseJumpPower = true humanoid.JumpPower = num end
                    end
                    
                    Input.Text = tostring(num)
                    
                    if (configKey == "TorsoSize" or configKey == "TorsoTransparency") and Config.HitboxPreset then
                        local s = Config.TorsoSize
                        local t = Config.TorsoTransparency
                        local root = character:FindFirstChild("HumanoidRootPart")
                        if root then 
                            root.Size = Vector3.new(s, s, s)
                            root.Transparency = t
                            root.CanTouch = true -- Must be true for TP Hit to work on touch
                            root.CanQuery = false
                        end
                        for _, v in pairs(character:GetChildren()) do
                            if v:IsA("BasePart") and (v.Name == "Torso" or v.Name == "UpperTorso" or v.Name == "LowerTorso") then
                                v.Size = Vector3.new(s, s, s)
                                v.Transparency = t
                                v.CanTouch = true
                                v.CanQuery = false
                            end
                        end
                    end
                    
                    if configKey == "DungeonRange" and Config.KillDungeon then
                        local s = Config.DungeonRange
                        local root = character:FindFirstChild("HumanoidRootPart")
                        if root then root.Size = Vector3.new(s, s, s) end
                        for _, v in pairs(character:GetChildren()) do
                            if v:IsA("BasePart") and (v.Name:find("Arm") or v.Name:find("Hand")) then
                                v.Size = Vector3.new(s, s, s)
                            end
                        end
                    end
                else
                    Input.Text = tostring(Config[configKey])
                end
            end
        end)
    end
    
    local function createToggle(parent, text, configKey, order)
        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(1, 0, 0, 30)
        Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        Frame.LayoutOrder = order
        Frame.Parent = parent

        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 3)
        Corner.Parent = Frame
        
        local Stroke = Instance.new("UIStroke")
        Stroke.Color = Color3.fromRGB(35, 35, 45)
        Stroke.Thickness = 1
        Stroke.Parent = Frame

        local Label = Instance.new("TextLabel")
        Label.Text = text
        Label.Size = UDim2.new(0.8, 0, 1, 0)
        Label.BackgroundTransparency = 1
        Label.TextColor3 = Color3.fromRGB(200, 200, 200)
        Label.Font = Enum.Font.GothamMedium
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.TextSize = 9
        Label.Parent = Frame
        
        local Pad = Instance.new("UIPadding")
        Pad.PaddingLeft = UDim.new(0, 10)
        Pad.Parent = Label

        local Button = Instance.new("TextButton")
        Button.Size = UDim2.new(0, 20, 0, 20)
        Button.Position = UDim2.new(1, -30, 0.5, -10)
        Button.BackgroundColor3 = Config[configKey] and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
        Button.Text = ""
        Button.Parent = Frame
        
        local BCorner = Instance.new("UICorner")
        BCorner.CornerRadius = UDim.new(0, 4)
        BCorner.Parent = Button
        
        -- Store button for external updates
        ToggleButtons[configKey] = Button

        Button.MouseButton1Click:Connect(function()
            Config[configKey] = not Config[configKey]
            Button.BackgroundColor3 = Config[configKey] and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            
            if configKey == "ToolColorChanger" and not Config.ToolColorChanger then
                if character then
                    restoreToolColor(character:FindFirstChildOfClass("Tool"))
                end
            end
            
            if configKey == "PotatoGraphics" then
                local isLow = Config.PotatoGraphics
                
                -- Fast Flags / Quality
                if isLow then
                    renderSettings.QualityLevel = Enum.QualityLevel.Level01
                    renderSettings.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level01
                else
                    renderSettings.QualityLevel = Enum.QualityLevel.Automatic
                    renderSettings.MeshPartDetailLevel = Enum.MeshPartDetailLevel.Level04
                end
                
                pcall(function()
                    Lighting.GlobalShadows = not isLow
                    Lighting.Outlines = not isLow
                    Terrain.Decoration = not isLow
                    
                    potatoDOF.Enabled = isLow
                    potatoColor.Enabled = isLow
                    
                    Terrain.WaterWaveSize = isLow and 0 or 0.15
                    Terrain.WaterWaveSpeed = isLow and 0 or 10
                    Terrain.WaterReflectance = isLow and 0 or 1
                    Terrain.WaterTransparency = isLow and 0 or 1
                end)
                
                -- Iterate Workspace
                task.spawn(function()
                    for _, obj in ipairs(workspace:GetDescendants()) do
                        makePotato(obj)
                    end
                end)
                
                -- Connection
                if isLow then
                    if not potatoConnection then
                        potatoConnection = workspace.DescendantAdded:Connect(function(obj)
                            task.defer(function() makePotato(obj) end)
                        end)
                    end
                else
                    if potatoConnection then
                        potatoConnection:Disconnect()
                        potatoConnection = nil
                    end
                end
            end
            
            if configKey == "HitboxPreset" then
                task.spawn(function()
                    local hum = character:FindFirstChild("Humanoid")
                    local root = character:FindFirstChild("HumanoidRootPart")
                    local ProximityPromptService = game:GetService("ProximityPromptService")
                    
                    if Config.HitboxPreset then
                        -- Disable Sitting
                        if hum then hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false) end
                        
                        -- Disable Interactions (Proximity & Touch)
                        ProximityPromptService.Enabled = false
                        for _, v in pairs(character:GetDescendants()) do
                            if v:IsA("BasePart") then v.CanTouch = false end
                        end
                        
                        -- Big Torso Mode (Skin Glitch)
                        local s = Config.TorsoSize
                        local t = Config.TorsoTransparency
                        if root then
                            root.Size = Vector3.new(s, s, s)
                            root.Transparency = t
                            root.CanCollide = false
                            root.CanTouch = true -- Enable touch for TP Hit logic
                            root.CanQuery = false -- Prevent mouse blocking
                        end
                        for _, v in pairs(character:GetChildren()) do
                            if v:IsA("BasePart") and (v.Name == "Torso" or v.Name == "UpperTorso" or v.Name == "LowerTorso") then
                                v.Size = Vector3.new(s, s, s)
                                v.Transparency = t
                                v.CanCollide = false
                                v.CanTouch = true
                                v.CanQuery = false
                            end
                        end
                    else
                        -- Reset
                        if hum then hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true) end
                        ProximityPromptService.Enabled = true
                        
                        local success, desc = pcall(function()
                            return Players:GetHumanoidDescriptionFromUserId(player.UserId)
                        end)
                        if success and desc and hum then
                            ApplyDescriptionCustom(hum, desc)
                            if root then
                                root.Size = Vector3.new(2, 2, 1)
                                root.Transparency = 1
                                root.CanCollide = true
                                root.CanTouch = true
                                root.CanQuery = true
                            end
                            -- Reset All Parts
                            for _, v in pairs(character:GetDescendants()) do
                                if v:IsA("BasePart") then
                                    v.CanTouch = true
                                    v.CanQuery = true
                                end
                            end
                        end
                    end
                end)
            end
            
            -- Infinite FlashStep: Set FlashstepCooldown attribute
            if configKey == "InfiniteFlashStep" then
                task.spawn(function()
                    local char = player.Character or player.CharacterAdded:Wait()
                    if Config.InfiniteFlashStep then
                        -- Set cooldown to 10 when enabled
                        char:SetAttribute("FlashstepCooldown", 10)
                    else
                        -- Reset to 0 when disabled
                        char:SetAttribute("FlashstepCooldown", 0)
                    end
                end)
            end
            
            -- Ice Water: Set WaterWalking attribute
            if configKey == "IceWater" then
                task.spawn(function()
                    local char = player.Character or player.CharacterAdded:Wait()
                    if Config.IceWater then
                        char:SetAttribute("WaterWalking", true)
                    else
                        char:SetAttribute("WaterWalking", false)
                    end
                end)
            end
            
            -- Unbreakable All: Set UnbreakableAll attribute
            if configKey == "UnbreakableAll" then
                task.spawn(function()
                    local char = player.Character or player.CharacterAdded:Wait()
                    if Config.UnbreakableAll then
                        char:SetAttribute("UnbreakableAll", true)
                    else
                        char:SetAttribute("UnbreakableAll", false)
                    end
                end)
            end
            
            if configKey == "KillDungeon" then
                task.spawn(function()
                    local root = character:FindFirstChild("HumanoidRootPart")
                    
                    if Config.KillDungeon then
                        local s = Config.DungeonRange
                        if root then
                            root.Size = Vector3.new(s, s, s)
                            root.Transparency = 0.5
                            root.CanCollide = false
                        end
                        for _, v in pairs(character:GetChildren()) do
                            if v:IsA("BasePart") and (v.Name:find("Arm") or v.Name:find("Hand")) then
                                v.Size = Vector3.new(s, s, s)
                                v.Transparency = 0.5
                                v.CanCollide = false
                            end
                        end
                    else
                        if root then
                            root.Size = Vector3.new(2, 2, 1)
                            root.Transparency = 1
                            root.CanCollide = true
                        end
                        local success, desc = pcall(function() return Players:GetHumanoidDescriptionFromUserId(player.UserId) end)
                        if success and desc and humanoid then ApplyDescriptionCustom(humanoid, desc) end
                    end
                end)
            end
            
            if configKey == "FarmNearest" then
                if Config.FarmNearest then
                    Config.Reach = true
                    Config.ReachSize = 10
                    Config.ReachTransparency = 1
                    if ToggleButtons["Reach"] then ToggleButtons["Reach"].BackgroundColor3 = CurrentThemeColor end
                    if InputBoxes["ReachSize"] then InputBoxes["ReachSize"].Text = tostring(Config.ReachSize) end
                else
                    Config.Reach = false
                    Config.ReachSize = 1
                    if ToggleButtons["Reach"] then ToggleButtons["Reach"].BackgroundColor3 = Color3.fromRGB(40, 40, 50) end
                    if InputBoxes["ReachSize"] then InputBoxes["ReachSize"].Text = "1" end
                end
            end
            
            if configKey == "TeleportTool" then
                if Config.TeleportTool then
                    local tool = Instance.new("Tool")
                    tool.Name = "EPOteleporter"
                    tool.RequiresHandle = false
                    tool.ToolTip = "Click to Teleport"
                    
                    tool.Activated:Connect(function()
                        if player.Character then
                            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                            local m = player:GetMouse()
                            if hrp and m then
                                local targetPos = m.Hit.p
                                local originalLook = hrp.CFrame.LookVector
                                player.Character:PivotTo(CFrame.new(targetPos))
                                hrp.CFrame = CFrame.new(targetPos, targetPos + originalLook)
                            end
                        end
                    end)
                    tool.Parent = player.Backpack
                else
                    if player.Backpack:FindFirstChild("EPOteleporter") then player.Backpack.EPOteleporter:Destroy() end
                    if player.Character and player.Character:FindFirstChild("EPOteleporter") then player.Character.EPOteleporter:Destroy() end
                end
            end
            
            if configKey == "DashLengthEnabled" and not Config.DashLengthEnabled then
                if character then
                    character:SetAttribute("DashLength", 1)
                    character:SetAttribute("DashLengthAir", 1)
                end
            end
            
            if configKey == "DisableSnow" then
                if _G.SnowGui then
                    _G.SnowGui.Enabled = not Config.DisableSnow
                end
                UpdateLoaderSettings()
            end
            
            if configKey == "AutoLoadConfig" then
                UpdateLoaderSettings()
            end
            
            -- Instant Respawn toggle binding
            if configKey == "InstantRespawn" then
                if Config.InstantRespawn then
                    BindInstantRespawn()
                else
                    if instantRespawnConn then instantRespawnConn:Disconnect(); instantRespawnConn = nil end
                end
            end
            
            if configKey == "HandleExtender" and not Config.HandleExtender then
                local function restoreHandle(tool)
                    if tool and tool:IsA("Tool") then
                        for _, part in pairs(tool:GetDescendants()) do
                            if part:IsA("BasePart") and part:GetAttribute("OriginalSize") then
                                part.Size = part:GetAttribute("OriginalSize")
                                part:SetAttribute("OriginalSize", nil)
                            end
                        end
                    end
                end
                if player.Character then
                    for _, t in pairs(player.Character:GetChildren()) do restoreHandle(t) end
                    local armParts = {"Left Arm", "Right Arm", "Left Leg", "Right Leg", "LeftHand", "RightHand", "LeftLowerArm", "LeftUpperArm", "RightLowerArm", "RightUpperArm", "LeftLowerLeg", "LeftUpperLeg", "LeftFoot", "RightLowerLeg", "RightUpperLeg", "RightFoot"}
                    for _, name in pairs(armParts) do
                        local part = player.Character:FindFirstChild(name)
                        if part and part:IsA("BasePart") and part:GetAttribute("OriginalSize") then
                            part.Size = part:GetAttribute("OriginalSize")
                            part:SetAttribute("OriginalSize", nil)
                        end
                    end
                end
                if player.Backpack then
                    for _, t in pairs(player.Backpack:GetChildren()) do restoreHandle(t) end
                end
            end
        end)
    end
    
    local function createCycle(parent, text, configKey, options, order)
        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(1, 0, 0, 30)
        Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        Frame.LayoutOrder = order
        Frame.Parent = parent

        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 3)
        Corner.Parent = Frame
        
        local Stroke = Instance.new("UIStroke")
        Stroke.Color = Color3.fromRGB(35, 35, 45)
        Stroke.Thickness = 1
        Stroke.Parent = Frame

        local Label = Instance.new("TextLabel")
        Label.Text = text
        Label.Size = UDim2.new(0.6, 0, 1, 0)
        Label.BackgroundTransparency = 1
        Label.TextColor3 = Color3.fromRGB(200, 200, 200)
        Label.Font = Enum.Font.GothamMedium
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.TextSize = 9
        Label.Parent = Frame
        
        local Pad = Instance.new("UIPadding")
        Pad.PaddingLeft = UDim.new(0, 10)
        Pad.Parent = Label

        local Button = Instance.new("TextButton")
        Button.Size = UDim2.new(0.35, 0, 0.7, 0)
        Button.Position = UDim2.new(0.62, 0, 0.15, 0)
        Button.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        Button.TextColor3 = CurrentThemeColor
        Button.Font = Enum.Font.GothamBold
        Button.TextSize = 8
        Button.Text = Config[configKey]
        Button.Parent = Frame

        local BCorner = Instance.new("UICorner")
        BCorner.CornerRadius = UDim.new(0, 3)
        BCorner.Parent = Button

        Button.MouseButton1Click:Connect(function()
            local currentIndex = 1
            for i, v in ipairs(options) do
                if v == Config[configKey] then currentIndex = i break end
            end
            currentIndex = currentIndex + 1
            if currentIndex > #options then currentIndex = 1 end
            Config[configKey] = options[currentIndex]
            Button.Text = Config[configKey]
        end)
        return Button
    end

    local function restoreToolColor(tool)
        if not tool or not tool:IsA("Tool") then return end
        for _, descendant in pairs(tool:GetDescendants()) do
            if descendant:IsA("BasePart") then
                local originalColor = descendant:GetAttribute("OriginalColor")
                if originalColor then
                    descendant.Color = originalColor
                    descendant:SetAttribute("OriginalColor", nil)
                end
            elseif descendant:IsA("ParticleEmitter") or descendant:IsA("Trail") or descendant:IsA("Beam") then
                local originalCS = descendant:GetAttribute("OriginalColorSequence")
                if originalCS then
                    descendant.Color = originalCS
                    descendant:SetAttribute("OriginalColorSequence", nil)
                end
            elseif descendant:IsA("Light") then
                local originalLightColor = descendant:GetAttribute("OriginalLightColor")
                if originalLightColor then
                    descendant.Color = originalLightColor
                    descendant:SetAttribute("OriginalLightColor", nil)
                end
            end
        end
    end

    local function createSlider(parent, text, configKey, order, min, max, default)
        Config[configKey] = Config[configKey] or default

        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(1, 0, 0, 40)
        Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        Frame.LayoutOrder = order
        Frame.Parent = parent

        local Corner = Instance.new("UICorner")
        Corner.CornerRadius = UDim.new(0, 3)
        Corner.Parent = Frame
        
        local Stroke = Instance.new("UIStroke")
        Stroke.Color = Color3.fromRGB(35, 35, 45)
        Stroke.Thickness = 1
        Stroke.Parent = Frame

        local Label = Instance.new("TextLabel")
        Label.Text = text
        Label.Size = UDim2.new(0.7, 0, 0, 20)
        Label.Position = UDim2.new(0, 0, 0, 0)
        Label.BackgroundTransparency = 1
        Label.TextColor3 = Color3.fromRGB(200, 200, 200)
        Label.Font = Enum.Font.GothamMedium
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.TextSize = 9
        Label.Parent = Frame
        
        local Pad = Instance.new("UIPadding")
        Pad.PaddingLeft = UDim.new(0, 10)
        Pad.Parent = Label

        local ValueLabel = Instance.new("TextLabel")
        ValueLabel.Size = UDim2.new(0.3, -10, 0, 20)
        ValueLabel.Position = UDim2.new(0.7, 0, 0, 0)
        ValueLabel.BackgroundTransparency = 1
        ValueLabel.TextColor3 = CurrentThemeColor
        ValueLabel.Font = Enum.Font.Gotham
        ValueLabel.TextSize = 8
        ValueLabel.TextXAlignment = Enum.TextXAlignment.Right
        ValueLabel.Parent = Frame

        local SliderBack = Instance.new("Frame")
        SliderBack.Size = UDim2.new(1, -20, 0, 4)
        SliderBack.Position = UDim2.new(0, 10, 0, 25)
        SliderBack.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        SliderBack.Parent = Frame
        local SBCorner = Instance.new("UICorner") SBCorner.CornerRadius = UDim.new(1,0) SBCorner.Parent = SliderBack

        local SliderFront = Instance.new("Frame")
        SliderFront.Size = UDim2.new(0, 0, 1, 0)
        SliderFront.BackgroundColor3 = CurrentThemeColor
        SliderFront.Parent = SliderBack
        local SFCorner = Instance.new("UICorner") SFCorner.CornerRadius = UDim.new(1,0) SFCorner.Parent = SliderFront

        local Thumb = Instance.new("TextButton")
        Thumb.Size = UDim2.new(0, 12, 0, 12)
        Thumb.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        Thumb.Text = ""
        Thumb.ZIndex = 2
        Thumb.Parent = SliderBack -- Parent to SliderBack, not SliderFront
        local TCorner = Instance.new("UICorner") TCorner.CornerRadius = UDim.new(1,0) TCorner.Parent = Thumb

        local function updateSliderFromValue(value)
            local percentage = (value - min) / (max - min)
            percentage = math.clamp(percentage, 0, 1)
            SliderFront.Size = UDim2.new(percentage, 0, 1, 0)
            -- Position thumb at the right edge of SliderFront
            Thumb.Position = UDim2.new(0, math.floor(SliderBack.AbsoluteSize.X * percentage - 6), 0.5, -6)
            ValueLabel.Text = string.format("%.2f", value)
            Config[configKey] = value
        end

        local function updateSliderFromPosition(input)
            local mousePos = input.Position
            local relativePos = mousePos.X - SliderBack.AbsolutePosition.X
            local percentage = math.clamp(relativePos / SliderBack.AbsoluteSize.X, 0, 1)
            local newValue = min + (max - min) * percentage
            updateSliderFromValue(newValue)
        end

        updateSliderFromValue(Config[configKey])

        Thumb.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                local dragConn, releaseConn
                dragConn = UserInputService.InputChanged:Connect(function(changedInput)
                    if changedInput.UserInputType == Enum.UserInputType.MouseMovement or changedInput.UserInputType == Enum.UserInputType.Touch then
                        updateSliderFromPosition(changedInput)
                    end
                end)
                releaseConn = UserInputService.InputEnded:Connect(function(endedInput)
                    if endedInput.UserInputType == Enum.UserInputType.MouseButton1 or endedInput.UserInputType == Enum.UserInputType.Touch then
                        dragConn:Disconnect()
                        releaseConn:Disconnect()
                    end
                end)
            end
        end)
        
        return Frame
    end

    local function createInfo(parent, text, order)
        local Label = Instance.new("TextLabel")
        Label.Text = text
        Label.Size = UDim2.new(1, 0, 0, 20)
        Label.BackgroundTransparency = 1
        Label.TextColor3 = Color3.fromRGB(100, 100, 100)
        Label.Font = Enum.Font.Gotham
        Label.TextSize = 8
        Label.LayoutOrder = order
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.Parent = parent
    end

    -- === PAGE CONTENT ===
    
    -- Players Page (Aim Lock & Kill Aura & NoClip)
    
    do
        local ResetCharBtn = Instance.new("TextButton")
        ResetCharBtn.Size = UDim2.new(1, 0, 0, 30)
        ResetCharBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        ResetCharBtn.Text = "Reset Character"
        ResetCharBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        ResetCharBtn.Font = Enum.Font.GothamBold
        ResetCharBtn.TextSize = 10
        ResetCharBtn.LayoutOrder = 0
        ResetCharBtn.Parent = Pages.Players
        local rc = Instance.new("UICorner") rc.CornerRadius = UDim.new(0,4) rc.Parent = ResetCharBtn
        ResetCharBtn.MouseButton1Click:Connect(function()
            local char = player.Character
            if char then
                char:BreakJoints()
            end
        end)
    end

    createSection(Pages.Players, "Movement Keys", 9)
    createKeybind(Pages.Players, "Speed Hack Key", "SpeedKey", 10)
    createKeybind(Pages.Players, "Fly Key", "StandardFlyKey", 11)
    
    createSection(Pages.Players, "Movement Settings", 12)
    createSlider(Pages.Players, "Walk Speed", "WalkSpeedValue", 13, 16, 750, 16)
    createSlider(Pages.Players, "Fly Speed", "FlySpeedValue", 14, 0, 350, 100)
    createInput(Pages.Players, "Jump Power", "JumpPowerValue", 15)
    createToggle(Pages.Players, "Infinite Jump", "InfJump", 16)
    
    createSection(Pages.Players, "Target Straf", 17)
    createToggle(Pages.Players, "Target Straf (Auto Lock)", "EasyLock", 18)
    createKeybind(Pages.Players, "Lock/Unlock Key", "LockKey", 19)
    createInput(Pages.Players, "Follow Distance", "FollowDistance", 20)
    
    -- Status Label
    local TargetLabel = Instance.new("TextLabel")
    TargetLabel.Size = UDim2.new(1, 0, 0, 30)
    TargetLabel.BackgroundTransparency = 1
    TargetLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
    TargetLabel.Font = Enum.Font.Gotham
    TargetLabel.TextSize = 8
    TargetLabel.Text = "Status: No Target"
    TargetLabel.LayoutOrder = 21
    TargetLabel.Parent = Pages.Players
    
    -- Logic for EasyLock Toggle Button
    if ToggleButtons["EasyLock"] then
        ToggleButtons["EasyLock"].MouseButton1Click:Connect(function()
            if Config.EasyLock then
                local target = getClosestPlayerToMouse(nil, false)
                if target then
                    Config.TargetName = target.Name
                    TargetLabel.Text = "LOCKED: " .. target.Name
                    TargetLabel.TextColor3 = CurrentThemeColor
                    Config.KillAura = true
                    if ToggleButtons["KillAura"] then
                        ToggleButtons["KillAura"].BackgroundColor3 = CurrentThemeColor
                    end
                else
                    Config.EasyLock = false
                    ToggleButtons["EasyLock"].BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                end
            else
                Config.TargetName = "None"
                TargetLabel.Text = "Status: Unlocked"
                TargetLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
                if rootPart then
                    local bv = rootPart:FindFirstChild("FlyVelocity")
                    if bv then bv:Destroy() end
                end
                Config.KillAura = false
                if ToggleButtons["KillAura"] then
                    ToggleButtons["KillAura"].BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                end
            end
        end)
    end
    
    createSection(Pages.Players, "Auto Race", 21)
    createToggle(Pages.Players, "Auto V3 (Spam T)", "AutoV3", 22)
    createToggle(Pages.Players, "Auto Awakening", "AutoAwakening", 23)
    
    createSection(Pages.Players, "Dash Settings", 24)
    createToggle(Pages.Players, "Enable Dash Length", "DashLengthEnabled", 25)
    createSlider(Pages.Players, "Dash Distance", "DashLengthValue", 25.5, 1, 180, 50)
    
    createSection(Pages.Players, "Anti", 26)
    createToggle(Pages.Players, "0 COOLDOWN (Infinite Attacks)", "NoCooldown", 32)
    createToggle(Pages.Players, "Infinite Energy / Stamina", "InfiniteEnergy", 33)
    createToggle(Pages.Players, "No Clip (Walk Through Walls)", "NoClip", 27)
    createToggle(Pages.Players, "Anti Lava/Haunted", "AntiLava", 28)
    createToggle(Pages.Players, "Walk On Water", "WalkWater", 29)
    createToggle(Pages.Players, "No Animation", "NoAnim", 30)
    createToggle(Pages.Players, "Anti Stun (Move Always)", "AntiStun", 31)
    createToggle(Pages.Players, "Anti Chair (Block Sit)", "AntiChair", 31.5)
    
    createSection(Pages.Players, "Fake Lag", 37)
    createToggle(Pages.Players, "Enable Fake Lag", "FakeLag", 38)
    createKeybind(Pages.Players, "Fake Lag Key", "FakeLagKey", 39)
    
    createSection(Pages.Players, "Instant TP", 40)
    createKeybind(Pages.Players, "TP to Player Key", "TPPlayerKey", 41)
    createSlider(Pages.Players, "Max Range", "TPPlayerRange", 42, 0, 1000, 1000)
    
    createSection(Pages.Players, "One Shot", 43)
    createKeybind(Pages.Players, "One Shot (Need Anti Haunted)", "OneShotKey", 44)
    
    createSection(Pages.Players, "Panic Mode", 44.1)
    createToggle(Pages.Players, "Enable Panic Mode", "PanicMode", 44.2)
    createKeybind(Pages.Players, "Panic Mode Key", "PanicModeKey", 44.3)
    createToggle(Pages.Players, "Auto Panic (Health)", "PanicModeHealthEnable", 44.4)
    
    local maxHealthVal = 30000
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        maxHealthVal = player.Character.Humanoid.MaxHealth
    end
    createSlider(Pages.Players, "Activate Below HP", "PanicModeMinHealth", 44.5, 100, maxHealthVal, 4000)
    createSlider(Pages.Players, "Deactivate Above HP", "PanicModeMaxHealth", 44.6, 100, maxHealthVal, 9000)
    
    createSection(Pages.Players, "Tools", 45)
    createToggle(Pages.Players, "Teleporter Tool", "TeleportTool", 46)

    createSection(Pages.Players, "FlashStep", 47)
    createToggle(Pages.Players, "Air FlashStep (R)", "AirFlashStep", 48)
    createSlider(Pages.Players, "FlashStep Range", "AirFlashStepRange", 49, 10, 3000, 200)
    
    createSection(Pages.Players, "Infinite FlashStep", 49.1)
    createToggle(Pages.Players, "Infinite FlashStep (ABUSE = BAN)", "InfiniteFlashStep", 49.2)
    
    createSection(Pages.Players, "Ice Water", 49.3)
    createToggle(Pages.Players, "Ice Water (WaterWalking)", "IceWater", 49.4)
    
    createSection(Pages.Players, "Unbreakable All", 49.5)
    createToggle(Pages.Players, "Unbreakable All", "UnbreakableAll", 49.6)
    
    createSection(Pages.Players, "Orbit Player", 50)
    createToggle(Pages.Players, "Enable Orbit", "OrbitPlayer", 51)
    createKeybind(Pages.Players, "Orbit Key", "OrbitPlayerKey", 52)
    
    do
        local DropFrame = Instance.new("Frame")
        DropFrame.Size = UDim2.new(1, 0, 0, 30)
        DropFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        DropFrame.LayoutOrder = 53
        DropFrame.Parent = Pages.Players
        
        local DropCorner = Instance.new("UICorner") DropCorner.CornerRadius = UDim.new(0, 3) DropCorner.Parent = DropFrame
        local DropStroke = Instance.new("UIStroke") DropStroke.Color = Color3.fromRGB(35, 35, 45) DropStroke.Thickness = 1 DropStroke.Parent = DropFrame
        
        local DropLabel = Instance.new("TextLabel")
        DropLabel.Text = "Target Player"
        DropLabel.Size = UDim2.new(0.4, 0, 1, 0)
        DropLabel.BackgroundTransparency = 1
        DropLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
        DropLabel.Font = Enum.Font.GothamMedium
        DropLabel.TextXAlignment = Enum.TextXAlignment.Left
        DropLabel.TextSize = 9
        DropLabel.Parent = DropFrame
        local DropPad = Instance.new("UIPadding") DropPad.PaddingLeft = UDim.new(0, 10) DropPad.Parent = DropLabel
        
        local DropBtn = Instance.new("TextButton")
        DropBtn.Size = UDim2.new(0.55, 0, 0.7, 0)
        DropBtn.Position = UDim2.new(0.42, 0, 0.15, 0)
        DropBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        DropBtn.TextColor3 = CurrentThemeColor
        DropBtn.Font = Enum.Font.GothamBold
        DropBtn.TextSize = 8
        DropBtn.Text = (Config.OrbitPlayerTarget ~= "" and Config.OrbitPlayerTarget) or "Select Player"
        DropBtn.Parent = DropFrame
        local DBCorner = Instance.new("UICorner") DBCorner.CornerRadius = UDim.new(0, 3) DBCorner.Parent = DropBtn
        
        local DropList = Instance.new("ScrollingFrame")
        DropList.Size = UDim2.new(1, -10, 0, 150)
        DropList.Position = UDim2.new(0, 5, 0, 35)
        DropList.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        DropList.BorderSizePixel = 0
        DropList.Visible = false
        DropList.ScrollBarThickness = 2
        DropList.AutomaticCanvasSize = Enum.AutomaticSize.Y
        DropList.ZIndex = 5
        DropList.Parent = DropFrame
        local DLCorner = Instance.new("UICorner") DLCorner.CornerRadius = UDim.new(0, 3) DLCorner.Parent = DropList
        local DLLayout = Instance.new("UIListLayout") DLLayout.SortOrder = Enum.SortOrder.LayoutOrder DLLayout.Parent = DropList
        
        local isDropOpen = false
        DropBtn.MouseButton1Click:Connect(function()
            isDropOpen = not isDropOpen
            DropList.Visible = isDropOpen
            if isDropOpen then
                DropFrame.Size = UDim2.new(1, 0, 0, 190)
                for _, v in pairs(DropList:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
                
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= player then
                        local btn = Instance.new("TextButton")
                        btn.Size = UDim2.new(1, 0, 0, 30)
                        btn.BackgroundTransparency = 1
                        btn.TextColor3 = Color3.fromRGB(200, 200, 200)
                        btn.Text = p.DisplayName .. " (@" .. p.Name .. ")"
                        btn.Font = Enum.Font.Gotham
                        btn.TextSize = 8
                        btn.ZIndex = 6
                        btn.Parent = DropList
                        btn.MouseButton1Click:Connect(function()
                            Config.OrbitPlayerTarget = p.Name
                            DropBtn.Text = p.Name
                            isDropOpen = false
                            DropList.Visible = false
                            DropFrame.Size = UDim2.new(1, 0, 0, 30)
                        end)
                    end
                end
            else
                DropFrame.Size = UDim2.new(1, 0, 0, 30)
            end
        end)
    end
    
    -- Visuals Page (ESP)
    do
        local VizBtn = Instance.new("TextButton")
        VizBtn.Size = UDim2.new(1, 0, 0, 30)
        VizBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        VizBtn.Text = "OPEN PLAYER VISUALIZER"
        VizBtn.TextColor3 = CurrentThemeColor
        VizBtn.Font = Enum.Font.GothamBold
        VizBtn.TextSize = 10
        VizBtn.LayoutOrder = 0
        VizBtn.Parent = Pages.Visuals
        
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0, 4) c.Parent = VizBtn
        
        local VizFrame = nil
        
        VizBtn.MouseButton1Click:Connect(function()
            if not VizFrame then
                -- Create Window
                VizFrame = Instance.new("Frame")
                VizFrame.Name = "PlayerVisualizer"
                VizFrame.Size = UDim2.new(0, 350, 0, 400)
                VizFrame.Position = UDim2.new(0.5, -175, 0.5, -200)
                VizFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
                VizFrame.BorderSizePixel = 0
                VizFrame.Active = true
                VizFrame.Draggable = true
                VizFrame.Visible = true
                VizFrame.Parent = ScreenGui
                
                local Corner = Instance.new("UICorner") Corner.CornerRadius = UDim.new(0, 6) Corner.Parent = VizFrame
                local Stroke = Instance.new("UIStroke") Stroke.Color = Color3.fromRGB(40, 40, 50) Stroke.Thickness = 1 Stroke.Parent = VizFrame
                
                -- Header
                local Header = Instance.new("Frame")
                Header.Size = UDim2.new(1, 0, 0, 30)
                Header.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
                Header.Parent = VizFrame
                Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 6)
                
                local Title = Instance.new("TextLabel")
                Title.Text = "PLAYER VISUALIZER"
                Title.Size = UDim2.new(1, -30, 1, 0)
                Title.Position = UDim2.new(0, 10, 0, 0)
                Title.BackgroundTransparency = 1
                Title.TextColor3 = CurrentThemeColor
                Title.Font = Enum.Font.GothamBold
                Title.TextSize = 12
                Title.TextXAlignment = Enum.TextXAlignment.Left
                Title.Parent = Header
                
                local CloseBtn = Instance.new("TextButton")
                CloseBtn.Text = "X"
                CloseBtn.Size = UDim2.new(0, 30, 0, 30)
                CloseBtn.Position = UDim2.new(1, -30, 0, 0)
                CloseBtn.BackgroundTransparency = 1
                CloseBtn.TextColor3 = Color3.fromRGB(200, 50, 50)
                CloseBtn.Font = Enum.Font.GothamBold
                CloseBtn.TextSize = 12
                CloseBtn.Parent = Header
                CloseBtn.MouseButton1Click:Connect(function() VizFrame.Visible = false end)
                
                -- Search
                local SearchBox = Instance.new("TextBox")
                SearchBox.Size = UDim2.new(1, -20, 0, 25)
                SearchBox.Position = UDim2.new(0, 10, 0, 35)
                SearchBox.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
                SearchBox.TextColor3 = Color3.fromRGB(255, 255, 255)
                SearchBox.PlaceholderText = "Search Player..."
                SearchBox.Font = Enum.Font.Gotham
                SearchBox.TextSize = 10
                SearchBox.Parent = VizFrame
                Instance.new("UICorner", SearchBox).CornerRadius = UDim.new(0, 4)
                
                -- List
                local List = Instance.new("ScrollingFrame")
                List.Size = UDim2.new(1, -10, 1, -70)
                List.Position = UDim2.new(0, 5, 0, 65)
                List.BackgroundTransparency = 1
                List.BorderSizePixel = 0
                List.ScrollBarThickness = 4
                List.Parent = VizFrame
                
                local ListLayout = Instance.new("UIListLayout")
                ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
                ListLayout.Padding = UDim.new(0, 5)
                ListLayout.Parent = List
                
                local function colorText(text, color)
                    local r, g, b = math.floor(color.R*255), math.floor(color.G*255), math.floor(color.B*255)
                    return string.format('<font color="rgb(%d,%d,%d)">%s</font>', r, g, b, text)
                end
                
                local function UpdateList()
                    if not VizFrame.Visible then return end
                    
                    for _, v in pairs(List:GetChildren()) do if v:IsA("Frame") then v:Destroy() end end
                    
                    local filter = SearchBox.Text:lower()
                    
                    for _, p in pairs(Players:GetPlayers()) do
                        if filter == "" or p.Name:lower():find(filter) or p.DisplayName:lower():find(filter) then
                            local char = p.Character
                            
                            local Entry = Instance.new("Frame")
                            Entry.Size = UDim2.new(1, -10, 0, 70)
                            Entry.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
                            Entry.Parent = List
                            Instance.new("UICorner", Entry).CornerRadius = UDim.new(0, 4)
                            
                            local Avatar = Instance.new("ImageLabel")
                            Avatar.Size = UDim2.new(0, 50, 0, 50)
                            Avatar.Position = UDim2.new(0, 5, 0, 10)
                            Avatar.BackgroundTransparency = 1
                            Avatar.Image = "rbxthumb://type=AvatarHeadShot&id=" .. p.UserId .. "&w=150&h=150"
                            Avatar.Parent = Entry
                            Instance.new("UICorner", Avatar).CornerRadius = UDim.new(1, 0)
                            
                            local InfoLabel = Instance.new("TextLabel")
                            InfoLabel.Size = UDim2.new(1, -65, 1, -5)
                            InfoLabel.Position = UDim2.new(0, 60, 0, 2)
                            InfoLabel.BackgroundTransparency = 1
                            InfoLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
                            InfoLabel.Font = Enum.Font.Gotham
                            InfoLabel.TextSize = 8
                            InfoLabel.RichText = true
                            InfoLabel.TextXAlignment = Enum.TextXAlignment.Left
                            InfoLabel.TextYAlignment = Enum.TextYAlignment.Top
                            InfoLabel.Parent = Entry
                            
                            local hpText = colorText("N/A", Color3.fromRGB(150, 150, 150))
                            local v4Text = colorText("Inactive", Color3.fromRGB(150, 150, 150))
                            local pvpText = colorText("Unk", Color3.fromRGB(150, 150, 150))
                            local dodgeText = colorText("0", Color3.fromRGB(150, 150, 150))
                            local fruitText = colorText("None", Color3.fromRGB(150, 150, 150))
                            
                            if char then
                                local hum = char:FindFirstChild("Humanoid")
                                if hum then
                                    local hp = math.floor(hum.Health)
                                    local maxHp = math.floor(hum.MaxHealth)
                                    local hpColor = Color3.fromRGB(0, 255, 100)
                                    if hp < maxHp * 0.3 then hpColor = Color3.fromRGB(255, 50, 50)
                                    elseif hp < maxHp * 0.7 then hpColor = Color3.fromRGB(255, 200, 0) end
                                    hpText = colorText(hp .. "/" .. maxHp, hpColor)
                                end
                                
                                local v4Obj = char:FindFirstChild("RaceTransformed")
                                if v4Obj and v4Obj:IsA("BoolValue") and v4Obj.Value then 
                                    v4Text = colorText("Active", Color3.fromRGB(255, 0, 0))
                                else
                                    v4Text = colorText("Inactive", Color3.fromRGB(0, 255, 0))
                                end
                                
                                -- Fruit Detection (Equipped or Backpack)
                                local foundFruit = false
                                local fName = "None"
                                for _, t in pairs(char:GetChildren()) do
                                    if t:IsA("Tool") and t.ToolTip == "Blox Fruit" then
                                        fName = t.Name
                                        foundFruit = true
                                        break
                                    end
                                end
                                if not foundFruit then
                                    local bp = p:FindFirstChild("Backpack")
                                    if bp then
                                        for _, t in pairs(bp:GetChildren()) do
                                            if t:IsA("Tool") and t.ToolTip == "Blox Fruit" then
                                                fName = t.Name
                                                break
                                            end
                                        end
                                    end
                                end
                                if fName ~= "None" then
                                    fruitText = colorText(fName, Color3.fromRGB(200, 100, 255))
                                end
                                
                                -- PVP / Safe Zone
                                if char:FindFirstChildOfClass("ForceField") then
                                    pvpText = colorText("Safe Zone", Color3.fromRGB(0, 255, 255))
                                else
                                    local pvpAttr = char:GetAttribute("PvP")
                                    if pvpAttr == nil then
                                        local pvpVal = char:FindFirstChild("PvP")
                                        if pvpVal and pvpVal:IsA("BoolValue") then pvpAttr = pvpVal.Value end
                                    end
                                    
                                    if pvpAttr ~= nil then
                                        if pvpAttr then
                                            pvpText = colorText("On", Color3.fromRGB(255, 50, 50))
                                        else
                                            pvpText = colorText("Off", Color3.fromRGB(0, 255, 100))
                                        end
                                    else
                                        -- Try checking Data folder if available (some games)
                                        local d = p:FindFirstChild("Data")
                                        local dPvp = d and d:FindFirstChild("PvP")
                                        if dPvp then
                                            if dPvp.Value then pvpText = colorText("On", Color3.fromRGB(255, 50, 50))
                                            else pvpText = colorText("Off", Color3.fromRGB(0, 255, 100)) end
                                        else
                                            pvpText = colorText("Unk", Color3.fromRGB(150, 150, 150))
                                        end
                                    end
                                end
                                
                                -- Dodge (Observation)
                                local dVal = 0
                                local maxDVal = 0
                                
                                local vision = p:FindFirstChild("VisionRadius")
                                if vision and vision:IsA("ValueBase") then
                                    local exp = vision.Value
                                    if exp < 50 then maxDVal = 2
                                    elseif exp < 330 then maxDVal = 3
                                    elseif exp < 815 then maxDVal = 4
                                    elseif exp < 1417 then maxDVal = 5
                                    elseif exp < 2121 then maxDVal = 6
                                    elseif exp < 2824 then maxDVal = 7
                                    else maxDVal = 8 end
                                end
                                
                                local obs = char:FindFirstChild("Observation") or char:FindFirstChild("ObservationV2")
                                if not obs then
                                    for _, c in pairs(char:GetChildren()) do
                                        local n = string.lower(c.Name)
                                        if c:IsA("ValueBase") and (string.find(n, "observation") or string.find(n, "instinct")) then
                                            obs = c
                                            break
                                        end
                                    end
                                end
                                
                                if obs and obs:IsA("ValueBase") then 
                                    dVal = obs.Value 
                                elseif maxDVal > 0 then
                                    dVal = maxDVal
                                end
                                
                                if maxDVal > 0 then
                                    dodgeText = colorText(dVal .. "/" .. maxDVal, Color3.fromRGB(100, 200, 255))
                                else
                                    if dVal == 0 then
                                        local d = p:FindFirstChild("Data")
                                        local dObs = d and d:FindFirstChild("Observation")
                                        if dObs and dObs:IsA("ValueBase") then dVal = dObs.Value end
                                    end
                                    dodgeText = colorText(tostring(dVal), Color3.fromRGB(100, 200, 255))
                                end
                            end
                            
                            InfoLabel.Text = "<b>" .. p.DisplayName .. "</b> (@" .. p.Name .. ")\n" ..
                                             "ðŸ’š HP: " .. hpText .. " | âš¡ V4: " .. v4Text .. "\n" ..
                                             "âš”ï¸ PVP: " .. pvpText .. " | ðŸ‘ï¸ Dodge: " .. dodgeText .. "\n" ..
                                             "ðŸŽ Fruit: " .. fruitText
                        end
                    end
                end
                
                SearchBox:GetPropertyChangedSignal("Text"):Connect(UpdateList)
                
                task.spawn(function()
                    while Config.IsRunning and VizFrame do
                        if VizFrame.Visible then UpdateList() end
                        task.wait(0.5)
                    end
                end)
                
                UpdateList()
            else
                VizFrame.Visible = not VizFrame.Visible
            end
        end)
    end

    createSection(Pages.Visuals, "ESP Settings", 1)
    createToggle(Pages.Visuals, "Show Hitbox (Box)", "ESPBox", 2)
    createToggle(Pages.Visuals, "Show Name", "ESPName", 3)
    createToggle(Pages.Visuals, "Show Level", "ESPLvl", 4)
    createToggle(Pages.Visuals, "Show HP", "ESPHP", 5)
    createToggle(Pages.Visuals, "Wall Hack (Chams)", "ESPChams", 6)
    createToggle(Pages.Visuals, "Show Dot (Red Point)", "ESPDot", 7)
    createToggle(Pages.Visuals, "Show Tracers (Lines)", "ESPTracers", 8)
    createToggle(Pages.Visuals, "NPC Wall Hack", "ESPNPC", 9)
    createToggle(Pages.Visuals, "ESP Fruit", "ESPFruit", 9.5)
    
    createSection(Pages.Visuals, "Performance", 9)
    createToggle(Pages.Visuals, "Potato Graphics (FPS Boost)", "PotatoGraphics", 10)
    createToggle(Pages.Visuals, "Remove Fog", "RemoveFog", 11)
    createSlider(Pages.Visuals, "Field of View (FOV)", "FOVValue", 12, 1, 120, 70)
    createToggle(Pages.Visuals, "Always Day", "AlwaysDay", 12.5)
    
    task.spawn(function()
        local Lighting = game:GetService("Lighting")
        while Config.IsRunning do
            if Config.AlwaysDay then
                Lighting.ClockTime = 12
                RunService.RenderStepped:Wait()
            else
                task.wait(1)
            end
        end
    end)
    
    -- Shaders Button
    do
    local ShadersBtn = Instance.new("TextButton")
    ShadersBtn.Size = UDim2.new(1, 0, 0, 25)
    ShadersBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    ShadersBtn.Text = "ENABLE SHADERS (RTX)"
    ShadersBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    ShadersBtn.Font = Enum.Font.GothamBold
    ShadersBtn.TextSize = 9
    ShadersBtn.LayoutOrder = 13
    ShadersBtn.Parent = Pages.Visuals
    
    local ShadersCorner = Instance.new("UICorner") ShadersCorner.CornerRadius = UDim.new(0, 4) ShadersCorner.Parent = ShadersBtn
    
    local shadersEnabled = false
    local reflectanceApplied = false
    
    ShadersBtn.MouseButton1Click:Connect(function()
        shadersEnabled = not shadersEnabled
        ShadersBtn.BackgroundColor3 = shadersEnabled and CurrentThemeColor or Color3.fromRGB(30, 30, 40)
        ShadersBtn.TextColor3 = shadersEnabled and Color3.fromRGB(10, 10, 15) or Color3.fromRGB(200, 200, 200)
        ShadersBtn.Text = shadersEnabled and "SHADERS ENABLED" or "ENABLE SHADERS (RTX)"
        
        local Lighting = game:GetService("Lighting")
        
        if shadersEnabled then
            -- Apply Effects
            for _, effect in pairs(Lighting:GetChildren()) do
                if effect:IsA("PostEffect") or effect:IsA("BloomEffect") then
                    effect:Destroy()
                end
            end

            local bloom = Instance.new("BloomEffect", Lighting)
            bloom.Intensity = 1
            bloom.Threshold = 0.5
            bloom.Size = 24
            bloom.Name = "Cyber_Bloom"

            local depthOfField = Instance.new("DepthOfFieldEffect", Lighting)
            depthOfField.FarIntensity = 0.25
            depthOfField.FocusDistance = 20
            depthOfField.InFocusRadius = 50
            depthOfField.NearIntensity = 0.75
            depthOfField.Name = "Cyber_DOF"

            local colorCorrection = Instance.new("ColorCorrectionEffect", Lighting)
            colorCorrection.Brightness = 0.05
            colorCorrection.Contrast = 0.3
            colorCorrection.Saturation = 1.2
            colorCorrection.Name = "Cyber_CC"

            local sunRays = Instance.new("SunRaysEffect", Lighting)
            sunRays.Intensity = 0.12
            sunRays.Spread = 0.05
            sunRays.Name = "Cyber_Sun"

            Lighting.Ambient = Color3.fromRGB(128, 128, 128)
            Lighting.OutdoorAmbient = Color3.fromRGB(80, 80, 80)
            Lighting.Brightness = 1.8
            Lighting.ClockTime = 14
            
            if not reflectanceApplied then
                reflectanceApplied = true
                for _, part in ipairs(workspace:GetDescendants()) do
                    if part:IsA("BasePart") and not part:IsA("Terrain") and part.Reflectance < 0.1 then
                        part.Reflectance = 0.05
                    end
                end
            end
        else
            -- Remove Effects
            for _, effect in pairs(Lighting:GetChildren()) do
                if effect.Name:find("Cyber_") then
                    effect:Destroy()
                end
            end
        end
    end)
    end

    createSection(Pages.Visuals, "Customization", 12)
    createInput(Pages.Visuals, "ESP Size", "ESPSize", 13)
    
    createSection(Pages.Visuals, "Cursor Settings", 14)
    createInput(Pages.Visuals, "Cursor Image ID", "CustomCursorId", 15)
    createInput(Pages.Visuals, "Cursor Name", "CursorName", 16)
    
    do
    local CursorButtons = Instance.new("Frame")
    CursorButtons.Size = UDim2.new(1, 0, 0, 25)
    CursorButtons.BackgroundTransparency = 1
    CursorButtons.LayoutOrder = 17
    CursorButtons.Parent = Pages.Visuals
    
    local SaveCur = Instance.new("TextButton")
    SaveCur.Size = UDim2.new(0.48, 0, 1, 0)
    SaveCur.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    SaveCur.Text = "SAVE CURSOR"
    SaveCur.TextColor3 = Color3.fromRGB(200, 200, 200)
    SaveCur.Font = Enum.Font.GothamBold
    SaveCur.TextSize = 8
    SaveCur.Parent = CursorButtons
    local SC_C = Instance.new("UICorner") SC_C.CornerRadius = UDim.new(0, 4) SC_C.Parent = SaveCur
    
    local SelectCur = Instance.new("TextButton")
    SelectCur.Size = UDim2.new(0.48, 0, 1, 0)
    SelectCur.Position = UDim2.new(0.52, 0, 0, 0)
    SelectCur.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    SelectCur.Text = "SELECT CURSOR"
    SelectCur.TextColor3 = Color3.fromRGB(200, 200, 200)
    SelectCur.Font = Enum.Font.GothamBold
    SelectCur.TextSize = 8
    SelectCur.Parent = CursorButtons
    local SLC_C = Instance.new("UICorner") SLC_C.CornerRadius = UDim.new(0, 4) SLC_C.Parent = SelectCur
    
    local cursorList = {
        {Name = "Default", Id = ""}
    }
        
    -- Load Saved Cursors
    if isfile and isfile("Cyberpunks_Cursors.json") and readfile then
        pcall(function()
            local saved = HttpService:JSONDecode(readfile("Cyberpunks_Cursors.json"))
            if saved and type(saved) == "table" then
                cursorList = saved
            end
        end)
    end
    
    local cIdx = 1
    
    SaveCur.MouseButton1Click:Connect(function()
        local id = Config.CustomCursorId
        local name = Config.CursorName
        if id and id ~= "" then
            if name and name ~= "" then
                table.insert(cursorList, {Name = name, Id = id})
                
                -- Save to file
                if writefile then
                    pcall(function()
                        writefile("Cyberpunks_Cursors.json", HttpService:JSONEncode(cursorList))
                    end)
                end
                
                SaveCur.Text = "SAVED!"
                task.wait(0.5)
                SaveCur.Text = "SAVE CURSOR"
            else
                SaveCur.Text = "NAME REQ!"
                task.wait(0.5)
                SaveCur.Text = "SAVE CURSOR"
            end
        end
    end)
    
    SelectCur.MouseButton1Click:Connect(function()
        cIdx = cIdx + 1
        if cIdx > #cursorList then cIdx = 1 end
        local c = cursorList[cIdx]
        SelectCur.Text = c.Name:upper()
        
        Config.CustomCursorId = c.Id
        Config.CursorName = c.Name
        
        if InputBoxes["CustomCursorId"] then
            InputBoxes["CustomCursorId"].Text = c.Id
        end
        if InputBoxes["CursorName"] then
            InputBoxes["CursorName"].Text = c.Name
        end
        
        local val = c.Id
        if val ~= "" then
            if tonumber(val) then
                mouse.Icon = "rbxassetid://" .. val
            else
                mouse.Icon = val
            end
        else
            mouse.Icon = ""
        end
    end)
    end
    
    do
    local ColorFrame = Instance.new("Frame")
    ColorFrame.Size = UDim2.new(1, 0, 0, 60)
    ColorFrame.BackgroundTransparency = 1
    ColorFrame.LayoutOrder = 18
    ColorFrame.Parent = Pages.Visuals
    
    local ColorLayout = Instance.new("UIGridLayout")
    ColorLayout.CellSize = UDim2.new(0, 25, 0, 25)
    ColorLayout.CellPadding = UDim2.new(0, 5, 0, 5)
    ColorLayout.Parent = ColorFrame
    
    local colors = {
        Color3.fromRGB(255, 0, 0), Color3.fromRGB(0, 255, 0), Color3.fromRGB(0, 0, 255),
        Color3.fromRGB(255, 255, 0), Color3.fromRGB(0, 255, 255), Color3.fromRGB(255, 0, 255),
        Color3.fromRGB(255, 255, 255), Color3.fromRGB(0, 0, 0), Color3.fromRGB(255, 128, 0)
    }
    
    for _, col in pairs(colors) do
        local btn = Instance.new("TextButton")
        btn.Text = ""
        btn.BackgroundColor3 = col
        btn.Parent = ColorFrame
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(1,0) c.Parent = btn
        
        btn.MouseButton1Click:Connect(function()
            Config.ESPColorR = math.floor(col.R * 255)
            Config.ESPColorG = math.floor(col.G * 255)
            Config.ESPColorB = math.floor(col.B * 255)
        end)
    end
    end
    
    createSection(Pages.Visuals, "Server Stats", 19)
    createToggle(Pages.Visuals, "Show FPS", "ShowFPS", 20)
    createToggle(Pages.Visuals, "Show Ping", "ShowPing", 21)
    createToggle(Pages.Visuals, "Show Uptime", "ShowUptime", 22)
    createToggle(Pages.Visuals, "Show Server (Region)", "ShowServer", 23)
    
    createSection(Pages.Visuals, "Player Inspector", 24)
    
    -- Dropdown pour sÃ©lectionner un joueur
    do
    local DropFrame = Instance.new("Frame")
    DropFrame.Size = UDim2.new(1, 0, 0, 30)
    DropFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    DropFrame.LayoutOrder = 25
    DropFrame.Parent = Pages.Visuals
    
    local DropCorner = Instance.new("UICorner") DropCorner.CornerRadius = UDim.new(0, 3) DropCorner.Parent = DropFrame
    local DropStroke = Instance.new("UIStroke") DropStroke.Color = Color3.fromRGB(35, 35, 45) DropStroke.Thickness = 1 DropStroke.Parent = DropFrame
    
    local DropLabel = Instance.new("TextLabel")
    DropLabel.Text = "Select Player"
    DropLabel.Size = UDim2.new(0.4, 0, 1, 0)
    DropLabel.BackgroundTransparency = 1
    DropLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    DropLabel.Font = Enum.Font.GothamMedium
    DropLabel.TextXAlignment = Enum.TextXAlignment.Left
    DropLabel.TextSize = 9
    DropLabel.Parent = DropFrame
    local DropPad = Instance.new("UIPadding") DropPad.PaddingLeft = UDim.new(0, 10) DropPad.Parent = DropLabel

    local DropList = Instance.new("ScrollingFrame")
    DropList.Size = UDim2.new(1, -10, 0, 150)
    DropList.Position = UDim2.new(0, 5, 0, 35)
    DropList.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    DropList.BorderSizePixel = 0
    DropList.Visible = false
    DropList.ScrollBarThickness = 2
    DropList.AutomaticCanvasSize = Enum.AutomaticSize.Y
    DropList.ZIndex = 5
    DropList.Parent = DropFrame
    local DLCorner = Instance.new("UICorner") DLCorner.CornerRadius = UDim.new(0, 3) DLCorner.Parent = DropList
    local DLLayout = Instance.new("UIListLayout") DLLayout.SortOrder = Enum.SortOrder.LayoutOrder DLLayout.Parent = DropList
    
    local isDropOpen = false
    DropBtn.MouseButton1Click:Connect(function()
        isDropOpen = not isDropOpen
        DropList.Visible = isDropOpen
        if isDropOpen then
            DropFrame.Size = UDim2.new(1, 0, 0, 190) -- Agrandir le cadre
            for _, v in pairs(DropList:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
            
            local function addOption(name, val)
                local btn = Instance.new("TextButton")
                btn.Size = UDim2.new(1, 0, 0, 30)
                btn.BackgroundTransparency = 1
                btn.TextColor3 = Color3.fromRGB(200, 200, 200)
                btn.Text = name
                btn.Font = Enum.Font.Gotham
                btn.TextSize = 8
                btn.ZIndex = 6
                btn.Parent = DropList
                btn.MouseButton1Click:Connect(function()
                    Config.InspectorName = val
                    DropBtn.Text = val == "" and "None" or val
                    isDropOpen = false
                    DropList.Visible = false
                    DropFrame.Size = UDim2.new(1, 0, 0, 30)
                end)
            end
            
            addOption("-- None --", "")
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= player then
                    addOption(p.DisplayName .. " (@" .. p.Name .. ")", p.Name)
                end
            end
        else
            DropFrame.Size = UDim2.new(1, 0, 0, 30) -- RÃ©duire le cadre
        end
    end)
    end
    
    createToggle(Pages.Visuals, "Show Money", "ShowMoney", 26)
    createToggle(Pages.Visuals, "Show Fragments", "ShowFragments", 27)
    createToggle(Pages.Visuals, "Show Bounty/Honor", "ShowBounty", 28)
    
    createSection(Pages.Visuals, "Font Changer", 45)
    local fontOptions = {"Gotham", "GothamBold", "SourceSans", "SourceSansBold", "Arcade", "Fantasy", "SciFi", "Bodoni", "Cartoon", "Code", "Highway", "Merriweather"}
    Config.SelectedFont = "Gotham"
    createCycle(Pages.Visuals, "Select Font", "SelectedFont", fontOptions, 46)
    
    do
        local ApplyFontBtn = Instance.new("TextButton")
        ApplyFontBtn.Size = UDim2.new(1, 0, 0, 30)
        ApplyFontBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        ApplyFontBtn.Text = "APPLY FONT TO GAME"
        ApplyFontBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        ApplyFontBtn.Font = Enum.Font.GothamBold
        ApplyFontBtn.TextSize = 10
        ApplyFontBtn.LayoutOrder = 47
        ApplyFontBtn.Parent = Pages.Visuals
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0, 4) c.Parent = ApplyFontBtn
        
        ApplyFontBtn.MouseButton1Click:Connect(function()
            local fontName = Config.SelectedFont
            local fontEnum = Enum.Font[fontName]
            if fontEnum and player.PlayerGui then
                for _, v in pairs(player.PlayerGui:GetDescendants()) do
                    if v:IsA("TextLabel") or v:IsA("TextButton") or v:IsA("TextBox") then
                        pcall(function() v.Font = fontEnum end)
                    end
                end
                SendNotification("Font applied: " .. fontName, 2)
            end
        end)
    end
    
    -- Skin Changer Section
    createSection(Pages.Visuals, "Skin Changer", 35)
    createInput(Pages.Visuals, "Target User ID", "SkinChangerID", 36)
    
    do
        local SkinActions = Instance.new("Frame")
        SkinActions.Size = UDim2.new(1, 0, 0, 30)
        SkinActions.BackgroundTransparency = 1
        SkinActions.LayoutOrder = 37
        SkinActions.Parent = Pages.Visuals
        
        local ApplySkinBtn = Instance.new("TextButton")
        ApplySkinBtn.Size = UDim2.new(0.48, 0, 1, 0)
        ApplySkinBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        ApplySkinBtn.Text = "APPLY SKIN"
        ApplySkinBtn.TextColor3 = CurrentThemeColor
        ApplySkinBtn.Font = Enum.Font.GothamBold
        ApplySkinBtn.TextSize = 9
        ApplySkinBtn.Parent = SkinActions
        local AS_C = Instance.new("UICorner") AS_C.CornerRadius = UDim.new(0, 4) AS_C.Parent = ApplySkinBtn
        
        local ResetCharBtn = Instance.new("TextButton")
        ResetCharBtn.Size = UDim2.new(0.48, 0, 1, 0)
        ResetCharBtn.Position = UDim2.new(0.52, 0, 0, 0)
        ResetCharBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        ResetCharBtn.Text = "RESET CHAR"
        ResetCharBtn.TextColor3 = Color3.fromRGB(200, 50, 50)
        ResetCharBtn.Font = Enum.Font.GothamBold
        ResetCharBtn.TextSize = 9
        ResetCharBtn.Parent = SkinActions
        local RC_C = Instance.new("UICorner") RC_C.CornerRadius = UDim.new(0, 4) RC_C.Parent = ResetCharBtn
        
        -- Logic from provided script
        local function clearVisuals(char)
            for _, inst in ipairs(char:GetChildren()) do
                if inst:IsA("Accessory") or inst:IsA("Hat") or inst:IsA("Shirt") or inst:IsA("Pants") or inst:IsA("ShirtGraphic") or inst:IsA("CharacterMesh") then
                    inst:Destroy()
                end
            end
            local head = char:FindFirstChild("Head")
            if head then
                for _, d in ipairs(head:GetChildren()) do
                    if d:IsA("Decal") and d.Name:lower() == "face" then d:Destroy() end
                end
            end
            local bc = char:FindFirstChildOfClass("BodyColors")
            if bc then bc:Destroy() end
        end

        local function attachAccessory(char, accessory)
            local handle = accessory:FindFirstChild("Handle")
            if not handle then return end
            local targetAttachment, accAttachment
            for _, part in ipairs(char:GetChildren()) do
                if part:IsA("BasePart") then
                    for _, att in ipairs(part:GetChildren()) do
                        if att:IsA("Attachment") then
                            local match = handle:FindFirstChild(att.Name)
                            if match and match:IsA("Attachment") then
                                targetAttachment = att; accAttachment = match; break
                            end
                        end
                    end
                end
                if targetAttachment then break end
            end
            if targetAttachment and accAttachment then
                handle.CFrame = targetAttachment.WorldCFrame * accAttachment.CFrame:Inverse()
            else
                local root = char:FindFirstChild("HumanoidRootPart")
                if root then handle.CFrame = root.CFrame end
            end
        ApplySkinBtn.MouseButton1Click:Connect(function()
            local uid = tonumber(Config.SkinChangerID)
            if not uid then SendNotification("Invalid User ID", 2) return end
            local char = player.Character
            if not char then return end
            
            local ok, model = pcall(function() return Players:GetCharacterAppearanceAsync(uid) end)
            if not ok or not model then SendNotification("Failed to fetch skin", 2) return end
            
            clearVisuals(char)
            local bc = model:FindFirstChildOfClass("BodyColors")
            if bc then bc:Clone().Parent = char end
            for _, item in ipairs(model:GetChildren()) do
                if item:IsA("Shirt") or item:IsA("Pants") or item:IsA("ShirtGraphic") then item:Clone().Parent = char end
            end
            for _, acc in ipairs(model:GetChildren()) do
                if acc:IsA("Accessory") or acc:IsA("Hat") then attachAccessory(char, acc:Clone()) end
            end
            local head = char:FindFirstChild("Head")
            if head then
                local face = model:FindFirstChild("face", true)
                if face and face:IsA("Decal") then face:Clone().Parent = head end
            end
            model:Destroy()
            SendNotification("Skin Applied!", 3)
        end)
        
        ResetCharBtn.MouseButton1Click:Connect(function()
            player:LoadCharacter()
        end)
    end

    createSection(Pages.Visuals, "Player ID", 40)
    
    do
        local PlayerIDFrame = Instance.new("Frame")
        PlayerIDFrame.Size = UDim2.new(1, 0, 0, 120)
        PlayerIDFrame.BackgroundTransparency = 1
        PlayerIDFrame.LayoutOrder = 41
        PlayerIDFrame.Parent = Pages.Visuals
        
        local PlayerIDList = Instance.new("ScrollingFrame")
        PlayerIDList.Size = UDim2.new(1, 0, 1, 0)
        PlayerIDList.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        PlayerIDList.BorderSizePixel = 0
        PlayerIDList.ScrollBarThickness = 4
        PlayerIDList.Parent = PlayerIDFrame
        Instance.new("UICorner", PlayerIDList).CornerRadius = UDim.new(0, 4)
        Instance.new("UIListLayout", PlayerIDList)
        
        local function updatePlayerIDs()
            for _, child in pairs(PlayerIDList:GetChildren()) do
                if child:IsA("TextLabel") or child:IsA("TextButton") then child:Destroy() end
            end
            
            local yPos = 0
            for _, p in pairs(Players:GetPlayers()) do
                local playerEntry = Instance.new("TextButton")
                playerEntry.Size = UDim2.new(1, 0, 0, 25)
                playerEntry.Position = UDim2.new(0, 0, 0, yPos)
                playerEntry.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
                playerEntry.Text = p.Name .. " - ID: " .. tostring(p.UserId)
                playerEntry.TextColor3 = Color3.fromRGB(200, 200, 200)
                playerEntry.Font = Enum.Font.GothamBold
                playerEntry.TextSize = 10
                playerEntry.Parent = PlayerIDList
                local entryCorner = Instance.new("UICorner") entryCorner.CornerRadius = UDim.new(0, 3) entryCorner.Parent = playerEntry
                
                playerEntry.MouseButton1Click:Connect(function()
                    if setclipboard then setclipboard(tostring(p.UserId)) end
                    SendNotification("Copied: " .. p.Name .. " (" .. tostring(p.UserId) .. ")", 2)
                end)
                
                yPos = yPos + 27
            end
        end
        
        -- Update player list every 2 seconds
        task.spawn(function()
            while true do
                updatePlayerIDs()
                task.wait(2)
            end
        end)
    end
    
    createSection(Pages.Visuals, "Tool Color Changer", 42)
    createToggle(Pages.Visuals, "Enable Tool Color Changer", "ToolColorChanger", 43)
    
    do
        local ColorFrame = Instance.new("Frame")
        ColorFrame.Size = UDim2.new(1, 0, 0, 40)
        ColorFrame.BackgroundTransparency = 1
        ColorFrame.LayoutOrder = 44
        ColorFrame.Parent = Pages.Visuals
        
        local ColorLayout = Instance.new("UIGridLayout")
        ColorLayout.CellSize = UDim2.new(0, 30, 0, 30)
        ColorLayout.CellPadding = UDim2.new(0, 5, 0, 5)
        ColorLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        ColorLayout.Parent = ColorFrame
        
        local colors = {
            Color3.fromRGB(255, 255, 255), Color3.fromRGB(0, 0, 0), Color3.fromRGB(255, 0, 0), 
            Color3.fromRGB(0, 255, 0), Color3.fromRGB(0, 0, 255), Color3.fromRGB(255, 255, 0), 
            Color3.fromRGB(0, 255, 255), Color3.fromRGB(255, 0, 255), Color3.fromRGB(170, 0, 255)
        }
        
        for _, col in pairs(colors) do
            local btn = Instance.new("TextButton")
            btn.Text = ""
            btn.BackgroundColor3 = col
            btn.Parent = ColorFrame
            local c = Instance.new("UICorner") c.CornerRadius = UDim.new(1,0) c.Parent = btn
            
            local stroke = Instance.new("UIStroke")
            stroke.Color = Color3.fromRGB(80, 80, 80)
            stroke.Thickness = 1
            stroke.Parent = btn

            btn.MouseButton1Click:Connect(function()
                Config.ToolColor = col
            end)
        end
    end

    -- Emote Player Section
    createSection(Pages.Visuals, "Emote Player", 46)
    createInput(Pages.Visuals, "Emote ID (Asset ID)", "EmoteID", 47)
    createSlider(Pages.Visuals, "Emote Speed", "EmoteSpeed", 48, 0.1, 3, 1)
    createToggle(Pages.Visuals, "Loop Emote", "EmoteLoop", 49)
    
    -- Create Chair Section
    createSection(Pages.Visuals, "Chair", 50)
    do
        local ChairBtn = Instance.new("TextButton")
        ChairBtn.Size = UDim2.new(1, -20, 0, 30)
        ChairBtn.Position = UDim2.new(0, 10, 0, 0)
        ChairBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        ChairBtn.Text = "Create Chair"
        ChairBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        ChairBtn.Font = Enum.Font.GothamBold
        ChairBtn.TextSize = 10
        ChairBtn.LayoutOrder = 51
        ChairBtn.Parent = Pages.Visuals
        local ChairCorner = Instance.new("UICorner") ChairCorner.CornerRadius = UDim.new(0, 4) ChairCorner.Parent = ChairBtn
        local ChairStroke = Instance.new("UIStroke") ChairStroke.Color = Color3.fromRGB(60, 60, 70) ChairStroke.Thickness = 1 ChairStroke.Parent = ChairBtn
        
        ChairBtn.MouseButton1Click:Connect(function()
            local char = player.Character
            if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then
                local hrp = char.HumanoidRootPart

                local seat = Instance.new("Seat")
                seat.Name = "CreatedChair"
                seat.Size = Vector3.new(4, 1, 4)
                seat.CFrame = hrp.CFrame + (hrp.CFrame.LookVector * 5)
                seat.Anchored = true
                seat.CanCollide = true
                seat.Transparency = 0.5
                seat.Color = Color3.fromRGB(150, 150, 200)
                seat.Parent = workspace

                local clickDetector = Instance.new("ClickDetector")
                clickDetector.MaxActivationDistance = 20
                clickDetector.Parent = seat

                clickDetector.MouseClick:Connect(function(playerWhoClicked)
                    if playerWhoClicked == player then
                        local currentHum = player.Character and player.Character:FindFirstChild("Humanoid")
                        if currentHum then
                            currentHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                            currentHum:Sit(seat)
                        end
                    end
                end)

                SendNotification("Chair created! Click to sit on it.", 3)
            end
        end)
    end
    
    -- Emote Control Buttons
    do
        local EmoteBtnFrame = Instance.new("Frame")
        EmoteBtnFrame.Size = UDim2.new(1, 0, 0, 60)
        EmoteBtnFrame.BackgroundTransparency = 1
        EmoteBtnFrame.LayoutOrder = 50
        EmoteBtnFrame.Parent = Pages.Visuals
        
        local Layout = Instance.new("UIGridLayout")
        Layout.CellSize = UDim2.new(0.48, 0, 0, 25)
        Layout.CellPadding = UDim2.new(0.02, 0, 0, 5)
        Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        Layout.Parent = EmoteBtnFrame
        
        -- Play Button
        local PlayBtn = Instance.new("TextButton")
        PlayBtn.Size = UDim2.new(0.48, 0, 0, 25)
        PlayBtn.BackgroundColor3 = Color3.fromRGB(70, 130, 180)
        PlayBtn.Text = "â–¶ PLAY"
        PlayBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        PlayBtn.Font = Enum.Font.GothamBold
        PlayBtn.TextSize = 10
        PlayBtn.Parent = EmoteBtnFrame
        Instance.new("UICorner", PlayBtn).CornerRadius = UDim.new(0, 4)
        
        PlayBtn.MouseButton1Click:Connect(function()
            PlayEmote()
        end)
        
        -- Stop/Freeze Button
        local StopBtn = Instance.new("TextButton")
        StopBtn.Size = UDim2.new(0.48, 0, 0, 25)
        StopBtn.BackgroundColor3 = Color3.fromRGB(180, 70, 70)
        StopBtn.Text = "â–  FREEZE"
        StopBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        StopBtn.Font = Enum.Font.GothamBold
        StopBtn.TextSize = 10
        StopBtn.Parent = EmoteBtnFrame
        Instance.new("UICorner", StopBtn).CornerRadius = UDim.new(0, 4)
        
        StopBtn.MouseButton1Click:Connect(function()
            FreezeEmote()
        end)
        
        -- Stop Completely Button
        local StopCompletelyBtn = Instance.new("TextButton")
        StopCompletelyBtn.Size = UDim2.new(0.48, 0, 0, 25)
        StopCompletelyBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        StopCompletelyBtn.Text = "âœ– STOP"
        StopCompletelyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        StopCompletelyBtn.Font = Enum.Font.GothamBold
        StopCompletelyBtn.TextSize = 10
        StopCompletelyBtn.Parent = EmoteBtnFrame
        Instance.new("UICorner", StopCompletelyBtn).CornerRadius = UDim.new(0, 4)
        
        StopCompletelyBtn.MouseButton1Click:Connect(function()
            StopEmote()
        end)
    end
    
    -- Update emote speed in real-time when slider changes
    task.spawn(function()
        while Config.IsRunning do
            task.wait(0.1)
            if Config.EmoteTrack and Config.EmotePlaying and not Config.EmoteFrozen then
                Config.EmoteTrack:AdjustSpeed(Config.EmoteSpeed)
            end
        end
    end)

    -- === RIGHT PANEL TABS ===
    
    -- Aim Tab (Was Trigger Bot)
    createSection(Pages.Aim, "Trigger Bot Settings", 1)
    createToggle(Pages.Aim, "Enable Trigger Bot", "TriggerBot", 2)
    createInput(Pages.Aim, "Max Distance", "TriggerDistance", 3)
    
    createSection(Pages.Aim, "Silent Aim (Streamable)", 4)
    createToggle(Pages.Aim, "Enable Silent Aim", "SilentAim", 5)
    createSlider(Pages.Aim, "Silent FOV Radius", "SilentFOV", 6, 0, 1000, 0)
    createInput(Pages.Aim, "Max Range", "SilentRange", 7)
    createToggle(Pages.Aim, "Show FOV Circle", "ShowFOV", 8)
    createKeybind(Pages.Aim, "Silent Aim Lock Key", "SilentAimKey", 9)
    
    createToggle(Pages.Aim, "Enable Filter (Whitelist/Blacklist)", "AimFilter", 10)
    local FilterModeBtn = createCycle(Pages.Aim, "Filter Mode", "AimFilterMode", {"Whitelist", "Blacklist"}, 11)
    
    do
        local ListFrame = Instance.new("ScrollingFrame")
        ListFrame.Size = UDim2.new(1, 0, 0, 120)
        ListFrame.BackgroundTransparency = 1
        ListFrame.LayoutOrder = 12
        ListFrame.Parent = Pages.Aim
        
        local UIList = Instance.new("UIListLayout")
        UIList.SortOrder = Enum.SortOrder.Name
        UIList.Parent = ListFrame
        
        local function RefreshFilterList()
            for _, v in pairs(ListFrame:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
            
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= player then
                    local btn = Instance.new("TextButton")
                    btn.Size = UDim2.new(1, 0, 0, 30)
                    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                    
                    local status = Config.AimFilterList[p.Name]
                    if status == "Whitelist" then
                        btn.BackgroundColor3 = Color3.fromRGB(0, 150, 0) -- Vert
                    elseif status == "Blacklist" then
                        btn.BackgroundColor3 = Color3.fromRGB(200, 0, 0) -- Rouge
                    else
                        btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50) -- Gris
                    end
                    
                    btn.Text = p.DisplayName .. " (@" .. p.Name .. ")"
                    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
                    btn.Font = Enum.Font.Gotham
                    btn.TextSize = 9
                    btn.Name = p.Name
                    btn.Parent = ListFrame
                    
                    local corner = Instance.new("UICorner") corner.CornerRadius = UDim.new(0, 4) corner.Parent = btn
                    
                    btn.MouseButton1Click:Connect(function()
                        local currentStatus = Config.AimFilterList[p.Name]
                        
                        if Config.AimFilterMode == "Whitelist" then
                            if currentStatus == "Whitelist" then
                                Config.AimFilterList[p.Name] = nil
                                btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                            else
                                Config.AimFilterList[p.Name] = "Whitelist"
                                btn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
                            end
                        else
                            if currentStatus == "Blacklist" then
                                Config.AimFilterList[p.Name] = nil
                                btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                            else
                                Config.AimFilterList[p.Name] = "Blacklist"
                                btn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
                            end
                        end
                    end)
                end
            end
        end
        
        local RefreshBtn = Instance.new("TextButton")
        RefreshBtn.Size = UDim2.new(1, 0, 0, 20)
        RefreshBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        RefreshBtn.Text = "REFRESH PLAYER LIST"
        RefreshBtn.TextColor3 = CurrentThemeColor
        RefreshBtn.Font = Enum.Font.GothamBold
        RefreshBtn.TextSize = 9
        RefreshBtn.LayoutOrder = 13
        RefreshBtn.Parent = Pages.Aim
        local RCorner = Instance.new("UICorner") RCorner.CornerRadius = UDim.new(0, 4) RCorner.Parent = RefreshBtn
        
        RefreshBtn.MouseButton1Click:Connect(RefreshFilterList)
        FilterModeBtn.MouseButton1Click:Connect(RefreshFilterList)
        RefreshFilterList()
    end
    
    createSection(Pages.Aim, "Cam Lock (Manual)", 14)
    createToggle(Pages.Aim, "Enable Cam Lock", "Aimlock", 15)
    createKeybind(Pages.Aim, "Cam Lock Key", "AimlockKey", 16)
    
    createSection(Pages.Aim, "Mouse Lock (Hold Right Click)", 16)
    createToggle(Pages.Aim, "Enable Mouse Lock", "MouseClickLock", 17)
    createSlider(Pages.Aim, "Lock FOV Radius", "MouseClickFOV", 18, 0, 500, 100)
    createToggle(Pages.Aim, "Team Check", "TeamCheck", 19)
    
    createSection(Pages.Aim, "Trigger FlashStep (Mouse Over)", 19)
    createToggle(Pages.Aim, "Enable Trigger FlashStep", "TriggerFlashStep", 20)
    
    do
        local DropFrame = Instance.new("Frame")
        DropFrame.Size = UDim2.new(1, 0, 0, 30)
        DropFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        DropFrame.LayoutOrder = 21
        DropFrame.Parent = Pages.Aim
        
        local DropCorner = Instance.new("UICorner") DropCorner.CornerRadius = UDim.new(0, 3) DropCorner.Parent = DropFrame
        local DropStroke = Instance.new("UIStroke") DropStroke.Color = Color3.fromRGB(35, 35, 45) DropStroke.Thickness = 1 DropStroke.Parent = DropFrame
        
        local DropLabel = Instance.new("TextLabel")
        DropLabel.Text = "Target Player"
        DropLabel.Size = UDim2.new(0.4, 0, 1, 0)
        DropLabel.BackgroundTransparency = 1
        DropLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
        DropLabel.Font = Enum.Font.GothamMedium
        DropLabel.TextXAlignment = Enum.TextXAlignment.Left
        DropLabel.TextSize = 9
        DropLabel.Parent = DropFrame
        local DropPad = Instance.new("UIPadding") DropPad.PaddingLeft = UDim.new(0, 10) DropPad.Parent = DropLabel
        
        local DropBtn = Instance.new("TextButton")
        DropBtn.Size = UDim2.new(0.55, 0, 0.7, 0)
        DropBtn.Position = UDim2.new(0.42, 0, 0.15, 0)
        DropBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        DropBtn.TextColor3 = CurrentThemeColor
        DropBtn.Font = Enum.Font.GothamBold
        DropBtn.TextSize = 8
        DropBtn.Text = (Config.TriggerFlashStepUser ~= "" and Config.TriggerFlashStepUser) or "Any Player"
        DropBtn.Parent = DropFrame
        local DBCorner = Instance.new("UICorner") DBCorner.CornerRadius = UDim.new(0, 3) DBCorner.Parent = DropBtn
        
        local DropList = Instance.new("ScrollingFrame")
        DropList.Size = UDim2.new(1, -10, 0, 150)
        DropList.Position = UDim2.new(0, 5, 0, 35)
        DropList.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        DropList.BorderSizePixel = 0
        DropList.Visible = false
        DropList.ScrollBarThickness = 2
        DropList.AutomaticCanvasSize = Enum.AutomaticSize.Y
        DropList.ZIndex = 5
        DropList.Parent = DropFrame
        local DLCorner = Instance.new("UICorner") DLCorner.CornerRadius = UDim.new(0, 3) DLCorner.Parent = DropList
        local DLLayout = Instance.new("UIListLayout") DLLayout.SortOrder = Enum.SortOrder.LayoutOrder DLLayout.Parent = DropList
        
        local isDropOpen = false
        DropBtn.MouseButton1Click:Connect(function()
            isDropOpen = not isDropOpen
            DropList.Visible = isDropOpen
            if isDropOpen then
                DropFrame.Size = UDim2.new(1, 0, 0, 190)
                for _, v in pairs(DropList:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
                
                local function addOption(name, val)
                    local btn = Instance.new("TextButton")
                    btn.Size = UDim2.new(1, 0, 0, 30)
                    btn.BackgroundTransparency = 1
                    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
                    btn.Text = name
                    btn.Font = Enum.Font.Gotham
                    btn.TextSize = 8
                    btn.ZIndex = 6
                    btn.Parent = DropList
                    btn.MouseButton1Click:Connect(function()
                        Config.TriggerFlashStepUser = val
                        DropBtn.Text = val == "" and "Any Player" or val
                        isDropOpen = false
                        DropList.Visible = false
                        DropFrame.Size = UDim2.new(1, 0, 0, 30)
                    end)
                end
                
                addOption("-- Any Player --", "")
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= player then
                        addOption(p.DisplayName .. " (@" .. p.Name .. ")", p.Name)
                    end
                end
            else
                DropFrame.Size = UDim2.new(1, 0, 0, 30)
            end
        end)
    end
    
    createSection(Pages.Aim, "Zone Aimlock (Auto)", 21)
    createToggle(Pages.Aim, "Enable Zone Lock", "ZoneLock", 22)
    createKeybind(Pages.Aim, "Zone Key", "ZoneKey", 23)
    createInput(Pages.Aim, "Zone Distance", "ZoneDistance", 24)
    createInput(Pages.Aim, "Lock Duration", "ZoneDuration", 25)
    createInput(Pages.Aim, "Cooldown (s)", "ZoneCooldown", 26)
    
    createSection(Pages.Aim, "Silent Aim V2", 27)
    createToggle(Pages.Aim, "Enable Silent Aim V2", "CursorLock", 28)
    
    -- Dropdown pour Silent Aim V2 (Cursor Lock)
    do
        local DropFrame = Instance.new("Frame")
        DropFrame.Size = UDim2.new(1, 0, 0, 30)
        DropFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        DropFrame.LayoutOrder = 29
        DropFrame.Parent = Pages.Aim
        
        local DropCorner = Instance.new("UICorner") DropCorner.CornerRadius = UDim.new(0, 3) DropCorner.Parent = DropFrame
        local DropStroke = Instance.new("UIStroke") DropStroke.Color = Color3.fromRGB(35, 35, 45) DropStroke.Thickness = 1 DropStroke.Parent = DropFrame
        
        local DropLabel = Instance.new("TextLabel")
        DropLabel.Text = "Target User"
        DropLabel.Size = UDim2.new(0.4, 0, 1, 0)
        DropLabel.BackgroundTransparency = 1
        DropLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
        DropLabel.Font = Enum.Font.GothamMedium
        DropLabel.TextXAlignment = Enum.TextXAlignment.Left
        DropLabel.TextSize = 9
        DropLabel.Parent = DropFrame
        local DropPad = Instance.new("UIPadding") DropPad.PaddingLeft = UDim.new(0, 10) DropPad.Parent = DropLabel
        
        local DropBtn = Instance.new("TextButton")
        DropBtn.Size = UDim2.new(0.55, 0, 0.7, 0)
        DropBtn.Position = UDim2.new(0.42, 0, 0.15, 0)
        DropBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        DropBtn.TextColor3 = CurrentThemeColor
        DropBtn.Font = Enum.Font.GothamBold
        DropBtn.TextSize = 8
        DropBtn.Text = (Config.CursorLockUser ~= "" and Config.CursorLockUser) or "None"
        DropBtn.Parent = DropFrame
        local DBCorner = Instance.new("UICorner") DBCorner.CornerRadius = UDim.new(0, 3) DBCorner.Parent = DropBtn
        
        local DropList = Instance.new("ScrollingFrame")
        DropList.Size = UDim2.new(1, -10, 0, 150)
        DropList.Position = UDim2.new(0, 5, 0, 35)
        DropList.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        DropList.BorderSizePixel = 0
        DropList.Visible = false
        DropList.ScrollBarThickness = 2
        DropList.AutomaticCanvasSize = Enum.AutomaticSize.Y
        DropList.ZIndex = 5
        DropList.Parent = DropFrame
        local DLCorner = Instance.new("UICorner") DLCorner.CornerRadius = UDim.new(0, 3) DLCorner.Parent = DropList
        local DLLayout = Instance.new("UIListLayout") DLLayout.SortOrder = Enum.SortOrder.LayoutOrder DLLayout.Parent = DropList
        
        local isDropOpen = false
        DropBtn.MouseButton1Click:Connect(function()
            isDropOpen = not isDropOpen
            DropList.Visible = isDropOpen
            if isDropOpen then
                DropFrame.Size = UDim2.new(1, 0, 0, 190)
                for _, v in pairs(DropList:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
                
                local function addOption(name, val)
                    local btn = Instance.new("TextButton")
                    btn.Size = UDim2.new(1, 0, 0, 30)
                    btn.BackgroundTransparency = 1
                    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
                    btn.Text = name
                    btn.Font = Enum.Font.Gotham
                    btn.TextSize = 8
                    btn.ZIndex = 6
                    btn.Parent = DropList
                    btn.MouseButton1Click:Connect(function()
                        Config.CursorLockUser = val
                        DropBtn.Text = val == "" and "None" or val
                        isDropOpen = false
                        DropList.Visible = false
                        DropFrame.Size = UDim2.new(1, 0, 0, 30)
                    end)
                end
                
                addOption("-- None --", "")
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= player then
                        addOption(p.DisplayName .. " (@" .. p.Name .. ")", p.Name)
                    end
                end
            else
                DropFrame.Size = UDim2.new(1, 0, 0, 30)
            end
        end)
    end
    
    createKeybind(Pages.Aim, "Activate/Change Lock", "CursorLockKey", 29)
    createKeybind(Pages.Aim, "Disable Lock Key", "CursorLockDisableKey", 30)
    
    local CursorLabel = Instance.new("TextLabel")
    CursorLabel.Size = UDim2.new(1, 0, 0, 20)
    CursorLabel.BackgroundTransparency = 1
    CursorLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
    CursorLabel.Font = Enum.Font.Gotham
    CursorLabel.TextSize = 8
    CursorLabel.Text = "Status: Unlocked"
    CursorLabel.LayoutOrder = 31
    CursorLabel.Parent = Pages.Aim
    
    if ToggleButtons["CursorLock"] then
        ToggleButtons["CursorLock"].MouseButton1Click:Connect(function()
            if not Config.CursorLock then
                cursorLockTarget = nil
                CursorLabel.Text = "Status: Unlocked"
                CursorLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
            end
        end)
    end
    
    -- Hitbox Tab (New)
    createSection(Pages.Hitbox, "Hitbox Exp Player", 1)
    createToggle(Pages.Hitbox, "Enable Hitbox Expander", "HitboxExpander", 2)
    createSlider(Pages.Hitbox, "Hitbox Multiplier", "HitboxSize", 3, 1, 300, 2)
    createSlider(Pages.Hitbox, "Hitbox Transparency", "HitboxTransparency", 4, 0, 1, 0.7)
    createToggle(Pages.Hitbox, "Target All Players", "HitboxModeAll", 5)
    createInput(Pages.Hitbox, "Target Username", "HitboxTargetUser", 6)
    
    createSection(Pages.Hitbox, "Self Hitbox Exp", 7)
    createToggle(Pages.Hitbox, "Enable Reach", "Reach", 8)
    createSlider(Pages.Hitbox, "Reach Multiplier", "ReachSize", 9, 1, 300, 2) 
    createSlider(Pages.Hitbox, "Reach Multiplier", "ReachSize", 9, 1, 2000, 2) 
    createSlider(Pages.Hitbox, "Reach Transparency", "ReachTransparency", 10, 0, 1, 0.5) 
    
    createSection(Pages.Hitbox, "Handle Extender", 11)
    createToggle(Pages.Hitbox, "Enable Handle Extender", "HandleExtender", 12)
    createSlider(Pages.Hitbox, "Handle Size", "HandleSize", 13, 1, 50, 5)
    
    createSection(Pages.Hitbox, "Part Extender (Self)", 14)
    createToggle(Pages.Hitbox, "Enable Part Extender", "PartExtender", 15)
    createSlider(Pages.Hitbox, "Part Size Multiplier", "PartExtenderSize", 16, 1, 10, 2)
    createToggle(Pages.Hitbox, "Extend Head", "PartExtenderHead", 17)
    createToggle(Pages.Hitbox, "Extend Torso", "PartExtenderTorso", 18)
    createToggle(Pages.Hitbox, "Extend Left Arm", "PartExtenderLeftArm", 19)
    createToggle(Pages.Hitbox, "Extend Right Arm", "PartExtenderRightArm", 20)
    createToggle(Pages.Hitbox, "Extend Left Leg", "PartExtenderLeftLeg", 21)
    createToggle(Pages.Hitbox, "Extend Right Leg", "PartExtenderRightLeg", 22)
    
    -- Player Teleport Options (no category)
    local currentTeleportThread = nil
    local currentTeleportTween = nil
    
    local function stopTeleport()
        if currentTeleportThread then
            task.cancel(currentTeleportThread)
            currentTeleportThread = nil
        end
        if currentTeleportTween then
            currentTeleportTween:Cancel()
            currentTeleportTween = nil
        end
        if character and rootPart then
            for _, v in pairs(rootPart:GetChildren()) do
                if v:IsA("BodyVelocity") then v:Destroy() end
            end
        end

        if isOneShotActive then
            isOneShotActive = false
            if oneShotThread then
                task.cancel(oneShotThread)
                oneShotThread = nil
            end
        end
    end
    
    do
        local StopFrame = Instance.new("Frame")
        StopFrame.Size = UDim2.new(1, 0, 0, 30)
        StopFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        StopFrame.LayoutOrder = 0
        StopFrame.Parent = Pages.Teleport

        local StopCorner = Instance.new("UICorner")
        StopCorner.CornerRadius = UDim.new(0, 3)
        StopCorner.Parent = StopFrame
        
        local StopBtn = Instance.new("TextButton")
        StopBtn.Size = UDim2.new(1, 0, 1, 0)
        StopBtn.BackgroundTransparency = 1
        StopBtn.TextColor3 = Color3.fromRGB(255, 50, 50)
        StopBtn.Font = Enum.Font.GothamBold
        StopBtn.TextSize = 9
        StopBtn.Text = "STOP TELEPORT"
        StopBtn.Parent = StopFrame
        StopBtn.MouseButton1Click:Connect(stopTeleport)
    end
    
    createSection(Pages.Teleport, "Teleport Sea 2", 5)
    
    do
    local CafeFrame = Instance.new("Frame")
    CafeFrame.Size = UDim2.new(1, 0, 0, 30)
    CafeFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    CafeFrame.LayoutOrder = 6
    CafeFrame.Parent = Pages.Teleport

    local CafeCorner = Instance.new("UICorner")
    CafeCorner.CornerRadius = UDim.new(0, 3)
    CafeCorner.Parent = CafeFrame
    
    local CafeBtn = Instance.new("TextButton")
    CafeBtn.Size = UDim2.new(1, 0, 1, 0)
    CafeBtn.BackgroundTransparency = 1
    CafeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    CafeBtn.Font = Enum.Font.GothamBold
    CafeBtn.TextSize = 9
    CafeBtn.Text = "Teleport to CafÃ©"
    CafeBtn.Parent = CafeFrame
    
    CafeBtn.MouseButton1Click:Connect(function()
        if character and rootPart then
            stopTeleport()
            currentTeleportThread = task.spawn(function()
                rootPart.CFrame = CFrame.new(2281.23, 14.25, 910.45)
                task.wait(1.5)
                if character and rootPart then
                    rootPart.CFrame = CFrame.new(-378.61, 185.20, 305.97)
                end
                currentTeleportThread = nil
            end)
        end
    end)
    end
    
    do
    local RemoteFrame = Instance.new("Frame")
    RemoteFrame.Size = UDim2.new(1, 0, 0, 30)
    RemoteFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    RemoteFrame.LayoutOrder = 8
    RemoteFrame.Parent = Pages.Teleport

    local RemoteCorner = Instance.new("UICorner")
    RemoteCorner.CornerRadius = UDim.new(0, 3)
    RemoteCorner.Parent = RemoteFrame
    
    local RemoteBtn = Instance.new("TextButton")
    RemoteBtn.Size = UDim2.new(1, 0, 1, 0)
    RemoteBtn.BackgroundTransparency = 1
    RemoteBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    RemoteBtn.Font = Enum.Font.GothamBold
    RemoteBtn.TextSize = 9
    RemoteBtn.Text = "Teleport to Remote"
    RemoteBtn.Parent = RemoteFrame
    
    RemoteBtn.MouseButton1Click:Connect(function()
        if character and rootPart then
            stopTeleport()
            currentTeleportThread = task.spawn(function()
                rootPart.CFrame = CFrame.new(-289.14, 304.96, 592.69)
                task.wait(1)
                
                -- 2. Position de dÃ©part du Fly
                local startPos = Vector3.new(2284.81, 14.25, 899.30)
                local endPos = Vector3.new(4546.18, 53.81, 2711.85)
                
                if character and rootPart then
                    rootPart.CFrame = CFrame.new(startPos)
                end
                task.wait(0.5)
                
                -- 3. Fly vers la destination (Tween)
                if character and rootPart then
                    local dist = (endPos - startPos).Magnitude
                    local speed = 275 -- Vitesse modÃ©rÃ©e pour Ã©viter le rollback
                    local duration = dist / speed
                    
                    -- Setup Anti-Gravity
                    local bv = Instance.new("BodyVelocity")
                    bv.Velocity = Vector3.new(0,0,0)
                    bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                    bv.Parent = rootPart
                    
                    local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear)
                    local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = CFrame.new(endPos)})
                    currentTeleportTween = tween
                    
                    tween:Play()
                    tween.Completed:Wait()
                    currentTeleportTween = nil
                    
                    if bv then bv:Destroy() end
                end
                currentTeleportThread = nil
            end)
        end
    end)
    end
    
    do
    local LabFrame = Instance.new("Frame")
    LabFrame.Size = UDim2.new(1, 0, 0, 30)
    LabFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    LabFrame.LayoutOrder = 7
    LabFrame.Parent = Pages.Teleport

    local LabCorner = Instance.new("UICorner")
    LabCorner.CornerRadius = UDim.new(0, 3)
    LabCorner.Parent = LabFrame
    
    local LabBtn = Instance.new("TextButton")
    LabBtn.Size = UDim2.new(1, 0, 1, 0)
    LabBtn.BackgroundTransparency = 1
    LabBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    LabBtn.Font = Enum.Font.GothamBold
    LabBtn.TextSize = 9
    LabBtn.Text = "Teleport to Lab"
    LabBtn.Parent = LabFrame
    
    LabBtn.MouseButton1Click:Connect(function()
        if character and rootPart then
            stopTeleport()
            currentTeleportThread = task.spawn(function()
                rootPart.CFrame = CFrame.new(926.00, 124.16, 32838.70)
                task.wait(1)
                
                -- 2. Position de dÃ©part du Fly
                local startPos = Vector3.new(-6508.44, 84.85, -133.48)
                local endPos = Vector3.new(-5354.04, 281.48, -5826.80)
                
                if character and rootPart then
                    rootPart.CFrame = CFrame.new(startPos)
                end
                task.wait(0.5)
                
                -- 3. Fly vers la destination (Tween)
                if character and rootPart then
                    local dist = (endPos - startPos).Magnitude
                    local speed = 275 -- Vitesse modÃ©rÃ©e pour Ã©viter le rollback
                    local duration = dist / speed
                    
                    -- Setup Anti-Gravity
                    local bv = Instance.new("BodyVelocity")
                    bv.Velocity = Vector3.new(0,0,0)
                    bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                    bv.Parent = rootPart
                    
                    local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear)
                    local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = CFrame.new(endPos)})
                    currentTeleportTween = tween
                    
                    tween:Play()
                    tween.Completed:Wait()
                    currentTeleportTween = nil
                    
                    if bv then bv:Destroy() end
                end
                currentTeleportThread = nil
            end)
        end
    end)
    end
    
    createSection(Pages.Teleport, "Teleport Sea 3", 9)
    
    do
    local SeaCastleFrame = Instance.new("Frame")
    SeaCastleFrame.Size = UDim2.new(1, 0, 0, 30)
    SeaCastleFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    SeaCastleFrame.LayoutOrder = 13
    SeaCastleFrame.Parent = Pages.Teleport

    local SeaCastleCorner = Instance.new("UICorner")
    SeaCastleCorner.CornerRadius = UDim.new(0, 3)
    SeaCastleCorner.Parent = SeaCastleFrame
    
    local SeaCastleBtn = Instance.new("TextButton")
    SeaCastleBtn.Size = UDim2.new(1, 0, 1, 0)
    SeaCastleBtn.BackgroundTransparency = 1
    SeaCastleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    SeaCastleBtn.Font = Enum.Font.GothamBold
    SeaCastleBtn.TextSize = 9
    SeaCastleBtn.Text = "Teleport to Sea Castle"
    SeaCastleBtn.Parent = SeaCastleFrame
    
    SeaCastleBtn.MouseButton1Click:Connect(function()
        if character and rootPart then
            stopTeleport()
            rootPart.CFrame = CFrame.new(-12464.21, 375.41, -7561.70)
        end
    end)
    end

    do
    local TurtleFrame = Instance.new("Frame")
    TurtleFrame.Size = UDim2.new(1, 0, 0, 30)
    TurtleFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    TurtleFrame.LayoutOrder = 12
    TurtleFrame.Parent = Pages.Teleport

    local TurtleCorner = Instance.new("UICorner")
    TurtleCorner.CornerRadius = UDim.new(0, 3)
    TurtleCorner.Parent = TurtleFrame
    
    local TurtleBtn = Instance.new("TextButton")
    TurtleBtn.Size = UDim2.new(1, 0, 1, 0)
    TurtleBtn.BackgroundTransparency = 1
    TurtleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    TurtleBtn.Font = Enum.Font.GothamBold
    TurtleBtn.TextSize = 9
    TurtleBtn.Text = "Teleport to Turtle"
    TurtleBtn.Parent = TurtleFrame
    
    TurtleBtn.MouseButton1Click:Connect(function()
        if character and rootPart then
            stopTeleport()
            rootPart.CFrame = CFrame.new(-5059.90, 315.59, -3189.11)
        end
    end)
    end

    do
    local HydraFrame = Instance.new("Frame")
    HydraFrame.Size = UDim2.new(1, 0, 0, 30)
    HydraFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    HydraFrame.LayoutOrder = 11
    HydraFrame.Parent = Pages.Teleport

    local HydraCorner = Instance.new("UICorner")
    HydraCorner.CornerRadius = UDim.new(0, 3)
    HydraCorner.Parent = HydraFrame
    
    local HydraBtn = Instance.new("TextButton")
    HydraBtn.Size = UDim2.new(1, 0, 1, 0)
    HydraBtn.BackgroundTransparency = 1
    HydraBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    HydraBtn.Font = Enum.Font.GothamBold
    HydraBtn.TextSize = 9
    HydraBtn.Text = "Teleport to Hydra"
    HydraBtn.Parent = HydraFrame
    
    HydraBtn.MouseButton1Click:Connect(function()
        if character and rootPart then
            stopTeleport()
            rootPart.CFrame = CFrame.new(-5024.31, 315.59, -3202.99)
        end
    end)
    end

    do
    local TikiFrame = Instance.new("Frame")
    TikiFrame.Size = UDim2.new(1, 0, 0, 30)
    TikiFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    TikiFrame.LayoutOrder = 10
    TikiFrame.Parent = Pages.Teleport

    local TikiCorner = Instance.new("UICorner")
    TikiCorner.CornerRadius = UDim.new(0, 3)
    TikiCorner.Parent = TikiFrame
    
    local TikiBtn = Instance.new("TextButton")
    TikiBtn.Size = UDim2.new(1, 0, 1, 0)
    TikiBtn.BackgroundTransparency = 1
    TikiBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    TikiBtn.Font = Enum.Font.GothamBold
    TikiBtn.TextSize = 9
    TikiBtn.Text = "Teleport to Tiki"
    TikiBtn.Parent = TikiFrame
    
    TikiBtn.MouseButton1Click:Connect(function()
        if character and rootPart then
            stopTeleport()
            rootPart.CFrame = CFrame.new(-5097.73, 315.58, -3177.68)
        end
    end)
    end
    
    -- Farm Page Content
    createSection(Pages.FarmStatus, "Status", 0.05)
    
    do
        local StatusFrame = Instance.new("Frame")
        StatusFrame.Size = UDim2.new(1, 0, 0, 120)
        StatusFrame.BackgroundTransparency = 1
        StatusFrame.LayoutOrder = 0.06
        StatusFrame.Parent = Pages.FarmStatus
        
        local UIList = Instance.new("UIListLayout")
        UIList.Parent = StatusFrame
        UIList.Padding = UDim.new(0, 2)
        
        local function createStatusLabel(text)
            local l = Instance.new("TextLabel")
            l.Size = UDim2.new(1, 0, 0, 15)
            l.BackgroundTransparency = 1
            l.TextColor3 = Color3.fromRGB(200, 200, 200)
            l.Font = Enum.Font.Gotham
            l.TextSize = 10
            l.TextXAlignment = Enum.TextXAlignment.Left
            l.Text = text
            l.Parent = StatusFrame
            return l
        end
        
        local DimLabel = createStatusLabel("Dimension: ?")
        local BoneLabel = createStatusLabel("Bones: ?")
        local EliteLabel = createStatusLabel("Elite: ?")
        local MirageLabel = createStatusLabel("Mirage: ?")
        local KitsuneLabel = createStatusLabel("Kitsune: ?")
        local MoonLabel = createStatusLabel("Moon: ?")
        local SwordLabel = createStatusLabel("Legendary Sword: ?")
        
        task.spawn(function()
            while Config.IsRunning do
                pcall(function()
                    -- Dimension
                    local dim = "None"
                    local cakeSpawner = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("CakePrinceSpawner", true)
                    if cakeSpawner then
                        local count = string.match(cakeSpawner, "%d+")
                        if count then dim = "Need More: " .. count
                        elseif string.find(cakeSpawner, "Do you want to open") then dim = "Ready!" end
                    end
                    DimLabel.Text = "A Dimension: " .. dim
                    
                    -- Bones
                    local bones = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Bones", "Check") or 0
                    BoneLabel.Text = "Bone You Have: " .. bones
                    
                    -- Elite
                    local elite = "ðŸ”´"
                    if game:GetService("ReplicatedStorage"):FindFirstChild("Diablo") or game:GetService("ReplicatedStorage"):FindFirstChild("Deandre") or game:GetService("ReplicatedStorage"):FindFirstChild("Urban") or workspace.Enemies:FindFirstChild("Diablo") or workspace.Enemies:FindFirstChild("Deandre") or workspace.Enemies:FindFirstChild("Urban") then
                        elite = "ðŸŸ¢"
                    end
                    EliteLabel.Text = "Elite: " .. elite
                    
                    -- Mirage
                    local mirage = "ðŸ”´"
                    if workspace._WorldOrigin.Locations:FindFirstChild("Mirage Island") then mirage = "ðŸŸ¢" end
                    MirageLabel.Text = "Mirage Island: " .. mirage
                    
                    -- Kitsune
                    local kitsune = "ðŸ”´"
                    if workspace._WorldOrigin.Locations:FindFirstChild("Kitsune Island") then kitsune = "ðŸŸ¢" end
                    KitsuneLabel.Text = "Kitsune Island: " .. kitsune
                    
                    -- Moon
                    local moonTxt = "No Full Moon"
                    local lighting = game:GetService("Lighting")
                    local moonId = (game.PlaceId == 2753915549 or game.PlaceId == 4442272183) and lighting.FantasySky.MoonTextureId or lighting.Sky.MoonTextureId
                    if moonId == "http://www.roblox.com/asset/?id=9709149431" or moonId == "http://www.roblox.com/asset/?id=4303207962" then moonTxt = "Full Moon"
                    elseif moonId == "http://www.roblox.com/asset/?id=9709149052" then moonTxt = "Next Night" end
                    MoonLabel.Text = "Moon: " .. moonTxt
                    
                    -- Legendary Sword
                    local sword = "ðŸ”´"
                    if game.PlaceId == 4442272183 then
                        local sData = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("LegendarySwordDealer", "1")
                        if sData and sData ~= "1" then sword = sData end
                    end
                    SwordLabel.Text = "Legendary Sword: " .. sword
                end)
                task.wait(1)
            end
        end)
    end

    createSection(Pages.FarmFarming, "Farming", 0.07)
    
    -- Move Farm Nearest Here
    createToggle(Pages.FarmFarming, "Enable Farm Nearest", "CustomFarmNearest", 0.08)
    
    -- Move Mastery Farm Here
    createToggle(Pages.FarmFarming, "Enable Mastery Farm", "MasteryFarm", 0.09)
    
    -- Auto Farm Elite
    createToggle(Pages.FarmFarming, "Auto Farm Elite", "AutoElitehunter", 0.10)

    -- Forward declaration for Fast M1
    local StartFastM1, FastM1Btn
    
    task.spawn(function()
        local function TweenToElite(targetPos)
            if not rootPart then return end
            local dist = (rootPart.Position - targetPos).Magnitude
            if dist < 25 then return end
            
            local speed = 400
            local info = TweenInfo.new(dist / speed, Enum.EasingStyle.Linear)
            local tween = TweenService:Create(rootPart, info, {CFrame = CFrame.new(targetPos)})
            
            local noclipConn = RunService.Stepped:Connect(function()
                if character then
                    for _, v in pairs(character:GetDescendants()) do
                        if v:IsA("BasePart") then v.CanCollide = false end
                    end
                end
            end)
            
            tween:Play()
            
            while tween.PlaybackState == Enum.PlaybackState.Playing do
                if not Config.IsRunning or not Config.AutoElitehunter then tween:Cancel() break end
                if not rootPart or (humanoid and humanoid.Health <= 0) then tween:Cancel() break end
                task.wait(0.03)
            end
            
            if noclipConn then noclipConn:Disconnect() end
            if rootPart then rootPart.Velocity = Vector3.new(0,0,0) end
        end

        -- Function to equip melee weapon
        local function EquipMeleeWeapon()
            if not player.Character or not player:FindFirstChild("Backpack") or not player.Character:FindFirstChild("Humanoid") then return end
            local backpack = player.Backpack
            local humanoid = player.Character.Humanoid
            
            -- Find melee weapons (swords, blades, katanas, etc.)
            local meleeKeywords = {"sword", "blade", "katana", "saber", "cutlass", "pole", "spear", "trident"}
            
            for _, tool in pairs(backpack:GetChildren()) do
                if tool:IsA("Tool") then
                    local toolName = tool.Name:lower()
                    for _, keyword in pairs(meleeKeywords) do
                        if string.find(toolName, keyword) then
                            humanoid:EquipTool(tool)
                            return true
                        end
                    end
                end
            end
            
            -- If no keyword match, equip any tool
            local anyTool = backpack:FindFirstChildOfClass("Tool")
            if anyTool then
                humanoid:EquipTool(anyTool)
                return true
            end
            
            return false
        end

        while Config.IsRunning do
            if Config.AutoElitehunter then
                -- Auto Enable Fast M1
                if not Config.FastM1 then
                    Config.FastM1 = true
                    if FastM1Btn then
                        FastM1Btn.Text = "DISABLE"
                        FastM1Btn.TextColor3 = Color3.fromRGB(255, 50, 50)
                    end
                    if StartFastM1 then StartFastM1() end
                end

                pcall(function()
                    -- Get quest
                    local questGui = player:FindFirstChild("PlayerGui") and player.PlayerGui:FindFirstChild("Main") and player.PlayerGui.Main:FindFirstChild("Quest")
                    if not questGui or not questGui.Visible then
                        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("EliteHunter")
                        task.wait(0.5)
                    else
                        local questLabel = questGui:FindFirstChild("Container") and questGui.Container:FindFirstChild("QuestTitle") and questGui.Container.QuestTitle:FindFirstChild("Title")
                        if questLabel and questLabel.Text then
                            if string.find(questLabel.Text, "Diablo") or string.find(questLabel.Text, "Deandre") or string.find(questLabel.Text, "Urban") then
                                local targetName = ""
                                if string.find(questLabel.Text, "Diablo") then targetName = "Diablo" end
                                if string.find(questLabel.Text, "Deandre") then targetName = "Deandre" end
                                if string.find(questLabel.Text, "Urban") then targetName = "Urban" end
                                
                                -- Ensure melee weapon is equipped
                                EquipMeleeWeapon()
                                
                                -- Search for elite
                                local target = workspace.Enemies:FindFirstChild(targetName)
                                
                                if not target or not target:FindFirstChild("HumanoidRootPart") then
                                    -- Check ReplicatedStorage
                                    local storageTarget = game:GetService("ReplicatedStorage"):FindFirstChild(targetName)
                                    if storageTarget and storageTarget:FindFirstChild("HumanoidRootPart") then
                                        TweenToElite(storageTarget.HumanoidRootPart.Position)
                                    else
                                        task.wait(0.5)
                                    end
                                else
                                    -- Elite found, stay ABOVE and attack
                                    while Config.AutoElitehunter and target and target:FindFirstChild("Humanoid") and target.Humanoid.Health > 0 and target.Parent do
                                        pcall(function()
                                            EquipMeleeWeapon()
                                            
                                            if target.Parent == workspace.Enemies then
                                                -- Stay ABOVE the elite and attack
                                                if rootPart and target.HumanoidRootPart then
                                                    rootPart.CFrame = target.HumanoidRootPart.CFrame * CFrame.new(0, 25, 0)
                                                    rootPart.Velocity = Vector3.new(0,0,0)
                                                end
                                                
                                                -- Reduce hitbox for easier hitting
                                                if target.HumanoidRootPart then
                                                    target.HumanoidRootPart.Size = Vector3.new(2, 2, 2)
                                                    target.HumanoidRootPart.CanCollide = false
                                                end
                                                
                                                -- Rapid click attack
                                                VirtualInputManager:SendMouseButtonEvent(camera.ViewportSize.X/2, camera.ViewportSize.Y/2, 0, true, game, 1)
                                                VirtualInputManager:SendMouseButtonEvent(camera.ViewportSize.X/2, camera.ViewportSize.Y/2, 0, false, game, 1)
                                            else
                                                -- Elite spawned elsewhere, go to it
                                                local newTarget = workspace.Enemies:FindFirstChild(targetName)
                                                if newTarget and newTarget:FindFirstChild("HumanoidRootPart") then
                                                    target = newTarget
                                                    TweenToElite(target.HumanoidRootPart.Position)
                                                end
                                            end
                                        end)
                                        task.wait(0.05)
                                    end
                                end
                            end
                        end
                    end
                end)
            end
            task.wait(0.1)
        end
    end)

    createSection(Pages.FarmScripts, "Scripts", 0.1)

    do
        local AutoFarmBtn = Instance.new("TextButton")
        AutoFarmBtn.Size = UDim2.new(1, 0, 0, 30)
        AutoFarmBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        AutoFarmBtn.Text = "Astral Studios" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        AutoFarmBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        AutoFarmBtn.Font = Enum.Font.GothamBold
        AutoFarmBtn.TextSize = 10
        AutoFarmBtn.LayoutOrder = 0.12
        AutoFarmBtn.Parent = Pages.FarmScripts
        local c2 = Instance.new("UICorner") c2.CornerRadius = UDim.new(0,4) c2.Parent = AutoFarmBtn
        AutoFarmBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/CStudios-Dev/csLoader.lua/main/CSLoader.lua"))()
        end)
    end

    do
        local OpFruitBtn = Instance.new("TextButton")
        OpFruitBtn.Size = UDim2.new(1, 0, 0, 30)
        OpFruitBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        OpFruitBtn.Text = "CattStar PvP" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        OpFruitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        OpFruitBtn.Font = Enum.Font.GothamBold
        OpFruitBtn.TextSize = 10
        OpFruitBtn.LayoutOrder = 0.13
        OpFruitBtn.Parent = Pages.FarmScripts
        local c6 = Instance.new("UICorner") c6.CornerRadius = UDim.new(0,4) c6.Parent = OpFruitBtn
        OpFruitBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://xenopvp.vercel.app/api/FastAttack_PVP"))()
        end)
    end

    do
        local OpAutoFarmBtn = Instance.new("TextButton")
        OpAutoFarmBtn.Size = UDim2.new(1, 0, 0, 30)
        OpAutoFarmBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        OpAutoFarmBtn.Text = "Domadic Hub" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        OpAutoFarmBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        OpAutoFarmBtn.Font = Enum.Font.GothamBold
        OpAutoFarmBtn.TextSize = 10
        OpAutoFarmBtn.LayoutOrder = 0.14
        OpAutoFarmBtn.Parent = Pages.FarmScripts
        local c7 = Instance.new("UICorner") c7.CornerRadius = UDim.new(0,4) c7.Parent = OpAutoFarmBtn
        OpAutoFarmBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Domadicoof/Domadicoof/main/Domadichub/NottoGay/Start.ranscript"))()
        end)
    end

    do
        local CattStarFarmBtn = Instance.new("TextButton")
        CattStarFarmBtn.Size = UDim2.new(1, 0, 0, 30)
        CattStarFarmBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        CattStarFarmBtn.Text = "CattStar Farm" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        CattStarFarmBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        CattStarFarmBtn.Font = Enum.Font.GothamBold
        CattStarFarmBtn.TextSize = 10
        CattStarFarmBtn.LayoutOrder = 0.15
        CattStarFarmBtn.Parent = Pages.FarmScripts
        local c8 = Instance.new("UICorner") c8.CornerRadius = UDim.new(0,4) c8.Parent = CattStarFarmBtn
        CattStarFarmBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://catt-star-autofarm.vercel.app/api/autofarm"))()
        end)
    end

    do
        local MangoJuiceHubBtn = Instance.new("TextButton")
        MangoJuiceHubBtn.Size = UDim2.new(1, 0, 0, 30)
        MangoJuiceHubBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        MangoJuiceHubBtn.Text = "MangoJuice Hub" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        MangoJuiceHubBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        MangoJuiceHubBtn.Font = Enum.Font.GothamBold
        MangoJuiceHubBtn.TextSize = 10
        MangoJuiceHubBtn.LayoutOrder = 0.16
        MangoJuiceHubBtn.Parent = Pages.FarmScripts
        local c9 = Instance.new("UICorner") c9.CornerRadius = UDim.new(0,4) c9.Parent = MangoJuiceHubBtn

    do
        local CysenseBtn = Instance.new("TextButton")
        CysenseBtn.Size = UDim2.new(1, 0, 0, 30)
        CysenseBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        CysenseBtn.Text = "Cysense"
        CysenseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        CysenseBtn.Font = Enum.Font.GothamBold
        CysenseBtn.TextSize = 10
        CysenseBtn.LayoutOrder = 0.17
        CysenseBtn.Parent = Pages.FarmScripts
        local cysenseCorner = Instance.new("UICorner") cysenseCorner.CornerRadius = UDim.new(0,4) cysenseCorner.Parent = CysenseBtn
        CysenseBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/606b846e88998801018fae498b9b8a3c.lua"))()
        end)
    end        MangoJuiceHubBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/mangojuice52042-star/MangoJuice-Hub/refs/heads/main/MangoJuice%20Hub%20Update"))()
        end)
    end

    createSection(Pages.FarmSettings, "Settings", 0.5)

    createCycle(Pages.FarmSettings, "Select Weapon", "SelectWeaponType", {"Melee", "Sword", "Blox Fruit"}, 0.55)
    
    createToggle(Pages.FarmSettings, "Bring Mob", "BringMob", 0.56)
    
    createSlider(Pages.FarmSettings, "Bring Range", "BringRange", 0.57, 50, 500, 50)
    
    createToggle(Pages.FarmSettings, "Fast M1 (Auto)", "FastM1Auto", 0.57)

    task.spawn(function()
        while Config.IsRunning do
            task.wait(1)
            pcall(function()
                local desiredType = Config.SelectWeaponType
                local foundWeapon = nil
                
                local function isMatch(tool)
                    return tool:IsA("Tool") and tool.ToolTip == desiredType
                end
                
                if player.Character then
                    for _, t in pairs(player.Character:GetChildren()) do
                        if isMatch(t) then foundWeapon = t.Name break end
                    end
                end
                
                if not foundWeapon and player.Backpack then
                    for _, t in pairs(player.Backpack:GetChildren()) do
                        if isMatch(t) then foundWeapon = t.Name break end
                    end
                end
                
                if foundWeapon then Config.SelectedWeapon = foundWeapon end
            end)
        end
    end)

    do
        local FastM1Btn = Instance.new("TextButton")
        FastM1Btn.Size = UDim2.new(1, 0, 0, 30)
        FastM1Btn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        FastM1Btn.Text = "Enable/Disable"
        FastM1Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        FastM1Btn.Font = Enum.Font.GothamBold
        FastM1Btn.TextSize = 10
        FastM1Btn.LayoutOrder = 0.6
        FastM1Btn.Parent = Pages.FarmSettings
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0,4) c.Parent = FastM1Btn
        
        StartFastM1 = function()
            task.spawn(function()
                local ReplicatedStorage = game:GetService("ReplicatedStorage")
                local Net = ReplicatedStorage:WaitForChild("Modules", 10) and ReplicatedStorage.Modules:WaitForChild("Net", 10)
                
                if not Net then
                    SendNotification("Net Module not found (Not Blox Fruits?)", 3)
                    Config.FastM1 = true
                    FastM1Btn.Text = "Enable/Disable"
                    FastM1Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
                    return
                end
                
                local RegisterHit = Net:WaitForChild("RE/RegisterHit", 3)
                local RegisterAttack = Net:WaitForChild("RE/RegisterAttack", 3)

                local function AttackMultipleTargets(targets)
                    pcall(function()
                        if not targets or #targets == 0 then return end
                        local allTargets = {}
                        for _, targetChar in pairs(targets) do
                            local head = targetChar:FindFirstChild("Head")
                            local torso = targetChar:FindFirstChild("Torso") or targetChar:FindFirstChild("UpperTorso")
                            local hrp = targetChar:FindFirstChild("HumanoidRootPart")
                            if head then table.insert(allTargets, {targetChar, head}) end
                            if torso and torso ~= head then table.insert(allTargets, {targetChar, torso}) end
                            if hrp and hrp ~= head and hrp ~= torso then table.insert(allTargets, {targetChar, hrp}) end
                        end
                        if #allTargets == 0 then return end
                        for i = 1, 3 do
                            RegisterAttack:FireServer(0)
                            RegisterHit:FireServer(allTargets[1][2], allTargets)
                        end
                    end)
                end

               while Config.FastM1 and Config.IsRunning do
                    task.wait(0.001)
                    local myChar = player.Character
                    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    
                    if myHRP then
                        local targetsInRange = {}
                        local enemiesFolder = workspace:FindFirstChild("Enemies")
                        if enemiesFolder then
                            for _, npc in pairs(enemiesFolder:GetChildren()) do
                                local humanoid = npc:FindFirstChild("Humanoid")
                                local hrp = npc:FindFirstChild("HumanoidRootPart")
                                if humanoid and hrp and humanoid.Health > 0 then
                                    if (hrp.Position - myHRP.Position).Magnitude <= 999999 then
                                        table.insert(targetsInRange, npc)
                                    end
                                end
                            end
                        end
                        
                        for _, p in pairs(Players:GetPlayers()) do
                            if p ~= player and p.Character then
                                local humanoid = p.Character:FindFirstChild("Humanoid")
                                local hrp = p.Character:FindFirstChild("HumanoidRootPart")
                                if humanoid and hrp and humanoid.Health > 0 then
                                    if (hrp.Position - myHRP.Position).Magnitude <= 999999 then
                                        table.insert(targetsInRange, p.Character)
                                    end
                                end
                            end
                        end
                        
                        if #targetsInRange > 0 then AttackMultipleTargets(targetsInRange) end
                    end
                end
                
                if not Config.FastM1 and FastM1Btn then
                    FastM1Btn.Text = "Enable/Disable"
                    FastM1Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
                end
            end)
        end

        FastM1Btn.MouseButton1Click:Connect(function()
            
            Config.FastM1 = not Config.FastM1
            
            if Config.FastM1 then
                FastM1Btn.Text = "Disable"
                FastM1Btn.TextColor3 = Color3.fromRGB(255, 50, 50)
                StartFastM1()
            else
                FastM1Btn.Text = "Enable"
                FastM1Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end)
    end



    -- Hook Farm Toggles to Fast M1
    local function UpdateFastM1State()
        local shouldBeOn = Config.CustomFarmNearest or Config.MasteryFarm or Config.AutoElitehunter
        if shouldBeOn and not Config.FastM1 then
            Config.FastM1 = true
            if FastM1Btn then
                FastM1Btn.Text = "Disable"
                FastM1Btn.TextColor3 = Color3.fromRGB(255, 50, 50)
            end
            if StartFastM1 then StartFastM1() end
        elseif not shouldBeOn and Config.FastM1 then
             Config.FastM1 = false
             if FastM1Btn then
                FastM1Btn.Text = "Enable"
                FastM1Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
             end
        end
    end

    if ToggleButtons["CustomFarmNearest"] then ToggleButtons["CustomFarmNearest"].MouseButton1Click:Connect(UpdateFastM1State) end
    if ToggleButtons["MasteryFarm"] then ToggleButtons["MasteryFarm"].MouseButton1Click:Connect(UpdateFastM1State) end
    if ToggleButtons["AutoElitehunter"] then ToggleButtons["AutoElitehunter"].MouseButton1Click:Connect(UpdateFastM1State) end

    -- Sea Teleport Buttons (no category)
    local function createTeleportBtn(text, remoteArg, order)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 30)
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 10
        btn.LayoutOrder = order
        btn.Parent = Pages.Teleport
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0,4) c.Parent = btn
        btn.MouseButton1Click:Connect(function()
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(remoteArg)
        end)
    end

    createSection(Pages.Teleport, "Sea", 1)

    createTeleportBtn("Sea 1", "TravelMain", 2)
    createTeleportBtn("Sea 2", "TravelDressrosa", 3)
    createTeleportBtn("Sea 3", "TravelZou", 4)

    createSection(Pages.Visuals, "Player Stats", 25)

    -- Raid Page Content (Dungeon)
    createSection(Pages.Raid, "Auto Raid", 1)
    createToggle(Pages.Raid, "Enable Auto Raid", "AutoRaid", 2)
    createKeybind(Pages.Raid, "Auto Raid Key", "AutoRaidKey", 3)
    
    createSection(Pages.Raid, "Chip Buyer", 4)
    createCycle(Pages.Raid, "Select Raid", "SelectedRaid", {"Flame", "Ice", "Quake", "Light", "Dark", "Spider", "Rumble", "Magma", "Buddha", "Sand"}, 5)
    
    do
    local BuyChipBtn = Instance.new("TextButton")
    BuyChipBtn.Size = UDim2.new(1, 0, 0, 25)
    BuyChipBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    BuyChipBtn.Text = "Auto Buy Chip (Scientist)"
    BuyChipBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    BuyChipBtn.Font = Enum.Font.GothamBold
    BuyChipBtn.TextSize = 9
    BuyChipBtn.LayoutOrder = 6
    BuyChipBtn.Parent = Pages.Raid
    local BC_C = Instance.new("UICorner") BC_C.CornerRadius = UDim.new(0,4) BC_C.Parent = BuyChipBtn
    
    BuyChipBtn.MouseButton1Click:Connect(function()
        local scientist = nil
        for _, v in pairs(workspace:GetDescendants()) do
             if v.Name == "Mysterious Scientist" and v:FindFirstChild("HumanoidRootPart") then
                 scientist = v
                 break
             end
        end
        if scientist and rootPart then
            rootPart.CFrame = scientist.HumanoidRootPart.CFrame * CFrame.new(0,0,3)
            task.wait(0.5)
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("RaidsNpc", "Select", Config.SelectedRaid)
            task.wait(0.5)
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("RaidsNpc", "Buy")
        end
    end)
    end
    
    if ToggleButtons["AutoRaid"] then
        ToggleButtons["AutoRaid"].MouseButton1Click:Connect(function()
            if Config.AutoRaid then
                Config.AntiLava = true
                if ToggleButtons["AntiLava"] then ToggleButtons["AntiLava"].BackgroundColor3 = CurrentThemeColor end
            end
        end)
    end
    
    -- Farm Page Content (Selected Farm Script)
    createSection(Pages.Farm, "Selected Farm", 5)
    do
    local LaunchFarmBtn = Instance.new("TextButton")
    LaunchFarmBtn.Size = UDim2.new(1, 0, 0, 30)
    LaunchFarmBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    LaunchFarmBtn.Text = "LAUNCH ADVANCED FARM UI"
    LaunchFarmBtn.TextColor3 = CurrentThemeColor
    LaunchFarmBtn.Font = Enum.Font.GothamBold
    LaunchFarmBtn.TextSize = 10
    LaunchFarmBtn.LayoutOrder = 6
    LaunchFarmBtn.Parent = Pages.Farm
    local LF_C = Instance.new("UICorner") LF_C.CornerRadius = UDim.new(0,4) LF_C.Parent = LaunchFarmBtn
    
    LaunchFarmBtn.MouseButton1Click:Connect(function()
        -- PROVIDED SCRIPT LOGIC
        task.spawn(function()
            local Players = game:GetService("Players")
            local TweenService = game:GetService("TweenService")
            local RunService = game:GetService("RunService")
            local VirtualInputManager = game:GetService("VirtualInputManager")
            local UserInputService = game:GetService("UserInputService")

            local LocalPlayer = Players.LocalPlayer
            local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

            _G.AutoFarm = false
            _G.CurrentQuestData = nil
            local GalaxyHubGui = nil
            local StopAllKey = nil

            local GameData = {
                ["SEA 1"] = {
                    { Name = "Starter Island", Emoji = "ðŸ", Color = Color3.fromRGB(135, 206, 235), Mobs = { {Level = 5, Mob = "Bandit", Giver = Vector3.new(1060.89, 16.70, 1551.20), Spawn = Vector3.new(1084.02, 16.61, 1519.35)} } },
                    { Name = "Jungle", Emoji = "ðŸŒ´", Color = Color3.fromRGB(34, 139, 34), Mobs = { {Level = 14, Mob = "Monkey", Giver = Vector3.new(-1600.14, 37.19, 151.90), Spawn = Vector3.new(-1600.14, 37.19, 151.90)}, {Level = 20, Mob = "Gorilla", Giver = Vector3.new(-1600.14, 37.19, 151.90), Spawn = Vector3.new(-1194.69, 8.11, -450.17)}, {Level = 25, Mob = "The Gorilla King", Giver = Vector3.new(-1600.14, 37.19, 151.90), Spawn = Vector3.new(-1194.69, 8.11, -450.17)} } },
                    { Name = "Pirate Village", Emoji = "ðŸ´â€â˜ ï¸", Color = Color3.fromRGB(160, 82, 45), Mobs = { {Level = 35, Mob = "Pirate", Giver = Vector3.new(-1141.17, 5.09, 3829.41), Spawn = Vector3.new(-1103.28, 16.29, 3948.95)}, {Level = 45, Mob = "Brute", Giver = Vector3.new(-1141.17, 5.09, 3829.41), Spawn = Vector3.new(-1147.41, 15.15, 4311.99)}, {Level = 55, Mob = "Chef", Giver = Vector3.new(-1141.17, 5.09, 3829.41), Spawn = Vector3.new(-1147.41, 15.15, 4311.99)} } },
                    { Name = "Desert", Emoji = "ðŸœï¸", Color = Color3.fromRGB(210, 180, 140), Mobs = { {Level = 60, Mob = "Desert Bandit", Giver = Vector3.new(894.98, 6.78, 4390.70), Spawn = Vector3.new(925.70, 6.79, 4482.66)}, {Level = 70, Mob = "Desert Officer", Giver = Vector3.new(894.98, 6.78, 4390.70), Spawn = Vector3.new(1604.40, 1.95, 4371.08)} } },
                    { Name = "Frozen Village", Emoji = "â„ï¸", Color = Color3.fromRGB(176, 224, 230), Mobs = { {Level = 90, Mob = "Snow Bandit", Giver = Vector3.new(1386.48, 87.61, -1296.94), Spawn = Vector3.new(1263.28, 106.12, -1441.02)}, {Level = 100, Mob = "Snowman", Giver = Vector3.new(1386.48, 87.61, -1296.94), Spawn = Vector3.new(1263.28, 106.12, -1441.02)}, {Level = 110, Mob = "Yeti", Giver = Vector3.new(1386.48, 87.61, -1296.94), Spawn = Vector3.new(1263.28, 106.12, -1441.02)} } },
                    { Name = "Marine Fortress", Emoji = "ðŸ°", Color = Color3.fromRGB(70, 130, 180), Mobs = { {Level = 120, Mob = "Chief Petty Officer", Giver = Vector3.new(-5038.31, 28.99, 4323.64), Spawn = Vector3.new(-4993.84, 20.99, 4048.68)}, {Level = 130, Mob = "Vice Admiral", Giver = Vector3.new(-5038.31, 28.99, 4323.64), Spawn = Vector3.new(-4736.46, 20.99, 4560.06)} } },
                    { Name = "Skylands", Emoji = "â˜ï¸", Color = Color3.fromRGB(240, 248, 255), Mobs = { {Level = 150, Mob = "Sky Bandit", Giver = Vector3.new(-4839.55, 718.01, -2622.11), Spawn = Vector3.new(-5032.86, 278.41, -2836.62)}, {Level = 175, Mob = "Dark Master", Giver = Vector3.new(-4839.55, 718.01, -2622.11), Spawn = Vector3.new(-5251.80, 388.99, -2260.03)} } },
                    { Name = "Prison", Emoji = "â›“ï¸", Color = Color3.fromRGB(105, 105, 105), Mobs = { {Level = 190, Mob = "Prisoner", Giver = Vector3.new(5308.48, 2.00, 475.11), Spawn = Vector3.new(5172.86, 3.11, 411.14)}, {Level = 210, Mob = "Dangerous Prisoner", Giver = Vector3.new(5308.48, 2.00, 475.11), Spawn = Vector3.new(5598.86, 1.99, 577.20)}, {Level = 220, Mob = "Warden", Giver = Vector3.new(5188.91, 3.88, 688.22), Spawn = Vector3.new(5180.08, 3.88, 887.35)}, {Level = 230, Mob = "Chief Warden", Giver = Vector3.new(5188.91, 3.88, 688.22), Spawn = Vector3.new(5180.08, 3.88, 887.35)}, {Level = 240, Mob = "Swan", Giver = Vector3.new(5188.91, 3.88, 688.22), Spawn = Vector3.new(5320.49, 8.13, 668.31)} } },
                    { Name = "Colosseum", Emoji = "ðŸŸï¸", Color = Color3.fromRGB(218, 165, 32), Mobs = { {Level = 225, Mob = "Toga Warrior", Giver = Vector3.new(-1578.04, 7.73, -2985.76), Spawn = Vector3.new(-1920.45, 7.63, -2775.40)}, {Level = 275, Mob = "Gladiator", Giver = Vector3.new(-1578.04, 7.73, -2985.76), Spawn = Vector3.new(-1296.91, 7.78, -3288.74)} } },
                    { Name = "Magma Village", Emoji = "ðŸŒ‹", Color = Color3.fromRGB(178, 34, 34), Mobs = { {Level = 300, Mob = "Military Soldier", Giver = Vector3.new(-5315.33, 12.58, 8515.84), Spawn = Vector3.new(-5407.25, 11.40, 8459.05)}, {Level = 325, Mob = "Military Spy", Giver = Vector3.new(-5315.33, 12.58, 8515.84), Spawn = Vector3.new(-5813.54, 77.57, 8735.79)}, {Level = 350, Mob = "Magma Admiral", Giver = Vector3.new(-5315.33, 12.58, 8515.84), Spawn = Vector3.new(-5571.99, 18.29, 8692.14)} } },
                    { Name = "Underwater City", Emoji = "ðŸŸ", Color = Color3.fromRGB(0, 191, 255), Mobs = { {Level = 375, Mob = "Fishman Warrior", Giver = Vector3.new(61121.72, 18.81, 1567.37), Spawn = Vector3.new(60951.54, 18.82, 1491.76)}, {Level = 400, Mob = "Fishman Commando", Giver = Vector3.new(61121.72, 18.81, 1567.37), Spawn = Vector3.new(61885.05, 18.82, 1480.25)}, {Level = 425, Mob = "Fishman Lord", Giver = Vector3.new(61121.72, 18.81, 1567.37), Spawn = Vector3.new(61348.86, 31.34, 1170.65)} } },
                    { Name = "Upper Skylands", Emoji = "â›…", Color = Color3.fromRGB(255, 215, 0), Mobs = { {Level = 450, Mob = "God's Guard", Giver = Vector3.new(-4722.16, 845.62, -1952.17), Spawn = Vector3.new(-4722.16, 845.62, -1952.17)}, {Level = 475, Mob = "Shanda", Giver = Vector3.new(-7861.45, 5545.83, -382.32), Spawn = Vector3.new(-7651.62, 5545.83, -515.99)}, {Level = 500, Mob = "Wysper", Giver = Vector3.new(-7861.45, 5545.83, -382.32), Spawn = Vector3.new(-7887.46, 5545.83, -686.10)}, {Level = 525, Mob = "Royal Squad", Giver = Vector3.new(-7903.17, 5636.30, -1408.23), Spawn = Vector3.new(-7616.99, 5607.22, -1476.02)}, {Level = 550, Mob = "Royal Soldier", Giver = Vector3.new(-7903.17, 5636.30, -1408.23), Spawn = Vector3.new(-7836.41, 5607.22, -1739.34)}, {Level = 575, Mob = "Thunder God", Giver = Vector3.new(-7903.17, 5636.30, -1408.23), Spawn = Vector3.new(-7779.03, 5607.15, -2312.71)} } },
                    { Name = "Fountain City", Emoji = "â›²", Color = Color3.fromRGB(0, 255, 255), Mobs = { {Level = 625, Mob = "Galley Pirate", Giver = Vector3.new(5262.41, 38.84, 4048.74), Spawn = Vector3.new(5539.08, 38.88, 3982.64)}, {Level = 650, Mob = "Galley Captain", Giver = Vector3.new(5262.41, 38.84, 4048.74), Spawn = Vector3.new(5463.73, 38.84, 4899.49)}, {Level = 675, Mob = "Cyborg", Giver = Vector3.new(5262.41, 38.84, 4048.74), Spawn = Vector3.new(6117.64, 7.25, 3958.57)} } }
                },
                ["SEA 2"] = {
                    { Name = "Kingdom of Rose", Emoji = "ðŸŒ¹", Color = Color3.fromRGB(255, 20, 147), Mobs = { {Level = 700, Mob = "Raider", Giver = Vector3.new(-427.22, 73.31, 1837.05), Spawn = Vector3.new(-711.56, 39.48, 2379.41)}, {Level = 725, Mob = "Mercenary", Giver = Vector3.new(-427.22, 73.31, 1837.05), Spawn = Vector3.new(-1040.01, 73.30, 1436.11)}, {Level = 750, Mob = "Diamond", Giver = Vector3.new(-427.22, 73.31, 1837.05), Spawn = Vector3.new(-1557.47, 198.93, -162.13)}, {Level = 775, Mob = "Swan Pirate", Giver = Vector3.new(635.07, 73.41, 917.06), Spawn = Vector3.new(914.37, 73.30, 1256.33)}, {Level = 800, Mob = "Factory Staff", Giver = Vector3.new(635.07, 73.41, 917.06), Spawn = Vector3.new(130.25, 73.30, -264.02)} } },
                    { Name = "Green Zone", Emoji = "ðŸƒ", Color = Color3.fromRGB(34, 139, 34), Mobs = { {Level = 875, Mob = "Marine Lieutenant", Giver = Vector3.new(-2441.33, 73.36, -3218.13), Spawn = Vector3.new(-2930.37, 73.31, -2975.40)}, {Level = 900, Mob = "Marine Captain", Giver = Vector3.new(-2441.33, 73.36, -3218.13), Spawn = Vector3.new(-1915.45, 73.31, -3258.28)}, {Level = 925, Mob = "Orbitus", Giver = Vector3.new(-2441.33, 73.36, -3218.13), Spawn = Vector3.new(-2133.24, 73.31, -4160.06)} } },
                    { Name = "Graveyard", Emoji = "ðŸª¦", Color = Color3.fromRGB(75, 0, 130), Mobs = { {Level = 950, Mob = "Zombie", Giver = Vector3.new(-5495.18, 48.82, -793.99), Spawn = Vector3.new(-5660.45, 48.82, -634.16)}, {Level = 975, Mob = "Vampire", Giver = Vector3.new(-5495.18, 48.82, -793.99), Spawn = Vector3.new(-6010.78, 6.74, -1313.21)} } },
                    { Name = "Snow Mountain", Emoji = "ðŸ”ï¸", Color = Color3.fromRGB(240, 255, 255), Mobs = { {Level = 1000, Mob = "Snow Trooper", Giver = Vector3.new(607.47, 401.76, -5370.79), Spawn = Vector3.new(487.73, 401.76, -5394.89)}, {Level = 1050, Mob = "Winter Warrior", Giver = Vector3.new(607.47, 401.76, -5370.79), Spawn = Vector3.new(1165.92, 429.76, -5190.02)} } },
                    { Name = "Hot and Cold", Emoji = "ðŸŒ¡ï¸", Color = Color3.fromRGB(255, 69, 0), Mobs = { {Level = 1100, Mob = "Lab Subordinate", Giver = Vector3.new(-6227.70, 82.16, -4851.52), Spawn = Vector3.new(-5828.27, 82.16, -4450.47)}, {Level = 1125, Mob = "Horned Warrior", Giver = Vector3.new(-6227.70, 82.16, -4851.52), Spawn = Vector3.new(-6284.63, 29.52, -5853.53)}, {Level = 1150, Mob = "Smoke Admiral", Giver = Vector3.new(-6227.70, 82.16, -4851.52), Spawn = Vector3.new(-4897.39, 234.35, -5500.18)}, {Level = 1175, Mob = "Magma Ninja", Giver = Vector3.new(-5403.61, 29.51, -5369.88), Spawn = Vector3.new(-5760.43, 29.51, -5529.64)}, {Level = 1200, Mob = "Lava Pirate", Giver = Vector3.new(-5403.61, 29.51, -5369.88), Spawn = Vector3.new(-5118.48, 29.51, -4969.32)} } },
                    { Name = "Cursed Ship", Emoji = "ðŸš¢", Color = Color3.fromRGB(47, 79, 79), Mobs = { {Level = 1250, Mob = "Ship Deckhand", Giver = Vector3.new(1039.26, 125.40, 32907.85), Spawn = Vector3.new(1212.98, 126.07, 32989.22)}, {Level = 1275, Mob = "Ship Engineer", Giver = Vector3.new(1039.26, 125.40, 32907.85), Spawn = Vector3.new(883.74, 44.40, 32965.79)}, {Level = 1300, Mob = "Ship Steward", Giver = Vector3.new(975.44, 125.40, 33252.02), Spawn = Vector3.new(920.43, 129.90, 33445.26)}, {Level = 1325, Mob = "Ship Officer", Giver = Vector3.new(975.44, 125.40, 33252.02), Spawn = Vector3.new(919.22, 181.78, 33312.28)} } },
                    { Name = "Ice Castle", Emoji = "ðŸ°", Color = Color3.fromRGB(135, 206, 250), Mobs = { {Level = 1350, Mob = "Arctic Warrior", Giver = Vector3.new(5668.22, 28.52, -6482.43), Spawn = Vector3.new(6027.40, 28.71, -6237.88)}, {Level = 1375, Mob = "Snow Lurker", Giver = Vector3.new(5668.22, 28.52, -6482.43), Spawn = Vector3.new(5574.52, 28.47, -6772.08)}, {Level = 1400, Mob = "Awakened Ice Admiral", Giver = Vector3.new(5668.22, 28.52, -6482.43), Spawn = Vector3.new(6458.98, 296.90, -6931.33)} } },
                    { Name = "Forgotten Island", Emoji = "ðŸ’€", Color = Color3.fromRGB(112, 128, 144), Mobs = { {Level = 1425, Mob = "Sea Soldier", Giver = Vector3.new(-3054.71, 239.99, -10145.12), Spawn = Vector3.new(-3382.04, 27.29, -9800.75)}, {Level = 1450, Mob = "Water Fighter", Giver = Vector3.new(-3054.71, 239.99, -10145.12), Spawn = Vector3.new(-3449.17, 239.19, -10530.22)}, {Level = 1475, Mob = "Tide Keeper", Giver = Vector3.new(-3054.71, 239.99, -10145.12), Spawn = Vector3.new(-3715.23, 77.65, -11451.32)} } }
                },
                ["SEA 3"] = {
                    { Name = "Port Town", Emoji = "ðŸ˜ï¸", Color = Color3.fromRGB(205, 133, 63), Mobs = { {Level = 1500, Mob = "Pistol Millionaire", Giver = Vector3.new(-449.53, 108.91, 5947.77), Spawn = Vector3.new(-123.06, 57.32, 5754.22)}, {Level = 1525, Mob = "Pistol Billionaire", Giver = Vector3.new(-449.53, 108.91, 5947.77), Spawn = Vector3.new(-94.23, 85.02, 6146.51)}, {Level = 1550, Mob = "Stone", Giver = Vector3.new(-449.53, 108.91, 5947.77), Spawn = Vector3.new(-974.75, 52.93, 6796.28)} } },
                    { Name = "Hydra Island", Emoji = "ðŸ", Color = Color3.fromRGB(50, 205, 50), Mobs = { {Level = 1575, Mob = "Dragon Crew Warrior", Giver = Vector3.new(6738.20, 127.72, -710.88), Spawn = Vector3.new(6817.56, 56.06, -877.53)}, {Level = 1600, Mob = "Dragon Crew Archer", Giver = Vector3.new(6738.20, 127.72, -710.88), Spawn = Vector3.new(6786.78, 484.71, 354.89)}, {Level = 1625, Mob = "Hydra Enforcer", Giver = Vector3.new(5212.86, 1004.41, 757.81), Spawn = Vector3.new(4561.06, 1002.57, 469.51)}, {Level = 1650, Mob = "Venomous Assailant", Giver = Vector3.new(5212.86, 1004.41, 757.81), Spawn = Vector3.new(4552.78, 1150.27, 858.63)}, {Level = 1675, Mob = "Hydra Leader", Giver = Vector3.new(5212.86, 1004.41, 757.81), Spawn = Vector3.new(5750.84, 1013.53, -29.64)} } },
                    { Name = "Great Tree", Emoji = "ðŸŒ³", Color = Color3.fromRGB(34, 139, 34), Mobs = { {Level = 1700, Mob = "Marine Commodore", Giver = Vector3.new(2482.58, 74.58, -6783.11), Spawn = Vector3.new(2591.74, 74.58, -7856.31)}, {Level = 1725, Mob = "Marine Rear Admiral", Giver = Vector3.new(2482.58, 74.58, -6783.11), Spawn = Vector3.new(3672.31, 124.28, -7124.22)}, {Level = 1750, Mob = "Kilo Admiral", Giver = Vector3.new(2482.58, 74.58, -6783.11), Spawn = Vector3.new(2889.85, 509.63, -7375.20)} } },
                    { Name = "Floating Turtle", Emoji = "ðŸ¢", Color = Color3.fromRGB(107, 142, 35), Mobs = { {Level = 1775, Mob = "Fishman Raider", Giver = Vector3.new(-10584.46, 332.10, -8761.14), Spawn = Vector3.new(-10381.48, 332.10, -8372.43)}, {Level = 1800, Mob = "Fishman Captain", Giver = Vector3.new(-10584.46, 332.10, -8761.14), Spawn = Vector3.new(-10975.26, 332.10, -8955.44)}, {Level = 1825, Mob = "Forest Pirate", Giver = Vector3.new(-13236.07, 332.72, -7625.54), Spawn = Vector3.new(-13401.16, 332.72, -7844.52)}, {Level = 1850, Mob = "Mythological Pirate", Giver = Vector3.new(-13236.07, 332.72, -7625.54), Spawn = Vector3.new(-13512.23, 469.92, -6831.57)}, {Level = 1875, Mob = "Captain Elephant", Giver = Vector3.new(-13236.07, 332.72, -7625.54), Spawn = Vector3.new(-13251.83, 325.42, -8388.38)}, {Level = 1900, Mob = "Jungle Pirate", Giver = Vector3.new(-12681.62, 391.20, -9900.82), Spawn = Vector3.new(-12017.60, 332.08, -10575.36)}, {Level = 1925, Mob = "Musketeer Pirate", Giver = Vector3.new(-12681.62, 391.20, -9900.82), Spawn = Vector3.new(-13348.51, 391.89, -9782.09)}, {Level = 1950, Mob = "Beautiful Pirate", Giver = Vector3.new(-12681.62, 391.20, -9900.82), Spawn = Vector3.new(5363.76, 22.61, -299.29)} } },
                    { Name = "Haunted Castle", Emoji = "ðŸ‘»", Color = Color3.fromRGB(72, 61, 139), Mobs = { {Level = 1975, Mob = "Skeleton", Giver = Vector3.new(-9480.35, 142.44, 5564.71), Spawn = Vector3.new(-8753.79, 142.44, 6023.39)}, {Level = 2000, Mob = "Living Zombie", Giver = Vector3.new(-9480.35, 142.44, 5564.71), Spawn = Vector3.new(-10145.36, 138.97, 5937.23)}, {Level = 2025, Mob = "Demonic Soul", Giver = Vector3.new(-9513.46, 172.44, 6077.39), Spawn = Vector3.new(-9525.54, 172.44, 6136.70)}, {Level = 2050, Mob = "Possessed Mummy", Giver = Vector3.new(-9513.46, 172.44, 6077.39), Spawn = Vector3.new(-9564.60, 6.13, 6232.53)} } },
                    { Name = "Peanut Island", Emoji = "ðŸ¥œ", Color = Color3.fromRGB(222, 184, 135), Mobs = { {Level = 2075, Mob = "Peanut Scout", Giver = Vector3.new(-2106.89, 38.44, -10192.85), Spawn = Vector3.new(-1992.52, 10.02, -9975.12)}, {Level = 2100, Mob = "Peanut President", Giver = Vector3.new(-2106.89, 38.44, -10192.85), Spawn = Vector3.new(-2086.14, 38.44, -10562.67)} } },
                    { Name = "Ice Cream Island", Emoji = "ðŸ¦", Color = Color3.fromRGB(255, 240, 245), Mobs = { {Level = 2125, Mob = "Ice Cream Chef", Giver = Vector3.new(-821.22, 66.16, -10966.76), Spawn = Vector3.new(-845.77, 66.16, -10930.90)}, {Level = 2150, Mob = "Ice Cream Commander", Giver = Vector3.new(-821.22, 66.16, -10966.76), Spawn = Vector3.new(-624.98, 66.16, -11319.81)}, {Level = 2175, Mob = "Cake Queen", Giver = Vector3.new(-821.22, 66.16, -10966.76), Spawn = Vector3.new(-704.32, 381.91, -11039.97)} } },
                    { Name = "Cake Island", Emoji = "ðŸŽ‚", Color = Color3.fromRGB(255, 105, 180), Mobs = { {Level = 2200, Mob = "Cookie Crafter", Giver = Vector3.new(-2019.95, 38.14, -12028.69), Spawn = Vector3.new(-2327.86, 38.14, -12104.81)}, {Level = 2225, Mob = "Cake Guard", Giver = Vector3.new(-2019.95, 38.14, -12028.69), Spawn = Vector3.new(-1562.92, 47.21, -12255.20)}, {Level = 2250, Mob = "Baking Staff", Giver = Vector3.new(-1925.77, 38.14, -12839.08), Spawn = Vector3.new(-1850.15, 38.14, -13028.90)}, {Level = 2275, Mob = "Head Baker", Giver = Vector3.new(-1925.77, 38.14, -12839.08), Spawn = Vector3.new(-2230.01, 53.84, -12867.06)} } },
                    { Name = "Chocolate Island", Emoji = "ðŸ«", Color = Color3.fromRGB(139, 69, 19), Mobs = { {Level = 2300, Mob = "Cocoa Warrior", Giver = Vector3.new(229.40, 25.07, -12200.84), Spawn = Vector3.new(-36.91, 25.07, -12242.16)}, {Level = 2325, Mob = "Chocolate Bar Battler", Giver = Vector3.new(229.40, 25.07, -12200.84), Spawn = Vector3.new(716.28, 25.07, -12625.35)}, {Level = 2350, Mob = "Sweet Thief", Giver = Vector3.new(149.60, 25.13, -12773.32), Spawn = Vector3.new(1.21, 25.13, -12623.30)}, {Level = 2375, Mob = "Candy Rebel", Giver = Vector3.new(149.60, 25.13, -12773.32), Spawn = Vector3.new(64.41, 25.13, -12961.77)} } },
                    { Name = "Candy Cane Island", Emoji = "ðŸ¬", Color = Color3.fromRGB(220, 20, 60), Mobs = { {Level = 2400, Mob = "Candy Pirate", Giver = Vector3.new(-1160.79, 60.50, -14492.24), Spawn = Vector3.new(-1328.18, 37.52, -14581.12)}, {Level = 2425, Mob = "Snow Demon", Giver = Vector3.new(-1160.79, 60.50, -14492.24), Spawn = Vector3.new(-846.93, 13.49, -14490.01)} } },
                    { Name = "Tiki Outpost", Emoji = "ðŸ—¿", Color = Color3.fromRGB(210, 105, 30), Mobs = { {Level = 2450, Mob = "Island Boy", Giver = Vector3.new(-16546.16, 56.03, -171.99), Spawn = Vector3.new(-16810.34, 22.01, -192.22)}, {Level = 2475, Mob = "Isle Outlaw", Giver = Vector3.new(-16546.16, 56.03, -171.99), Spawn = Vector3.new(-16225.49, 22.10, -229.29)}, {Level = 2500, Mob = "Sun-Kissed Champion", Giver = Vector3.new(-16537.24, 56.03, 1049.74), Spawn = Vector3.new(-16256.20, 22.01, 1055.00)}, {Level = 2525, Mob = "Isle Champion", Giver = Vector3.new(-16537.24, 56.03, 1049.74), Spawn = Vector3.new(-16809.34, 22.01, 1054.47)}, {Level = 2550, Mob = "Serpent Hunter", Giver = Vector3.new(-16663.68, 105.58, 1575.88), Spawn = Vector3.new(-16489.31, 71.58, 1610.54)}, {Level = 2575, Mob = "Skull Slayer", Giver = Vector3.new(-16663.68, 105.58, 1575.88), Spawn = Vector3.new(-16852.38, 71.58, 1633.95)} } },
                    { Name = "Submerged Island", Emoji = "ðŸŒŠ", Color = Color3.fromRGB(0, 0, 139), Mobs = { {Level = 2600, Mob = "Sea Chanter", Giver = Vector3.new(10881.65, -2085.90, 10032.85), Spawn = Vector3.new(10631.78, -2087.42, 10085.56)}, {Level = 2625, Mob = "Ocean Prophet", Giver = Vector3.new(10881.65, -2085.90, 10032.85), Spawn = Vector3.new(11052.06, -2007.58, 10164.56)}, {Level = 2650, Mob = "High Disciple", Giver = Vector3.new(9638.99, -1992.14, 9614.00), Spawn = Vector3.new(9867.40, -1993.29, 9732.94)}, {Level = 2675, Mob = "Grand Devotee", Giver = Vector3.new(9638.99, -1992.14, 9614.00), Spawn = Vector3.new(9593.03, -1988.66, 9986.50)} } }
                }
            }

            local function StopAutoFarm()
                _G.AutoFarm = false; _G.CurrentQuestData = nil
                local char = LocalPlayer.Character
                if char and char:FindFirstChild("HumanoidRootPart") then
                    char.HumanoidRootPart.Size = Vector3.new(2, 2, 1)
                    char.HumanoidRootPart.Transparency = 1
                    char.HumanoidRootPart.CanCollide = true
                end
                local cx, cy = workspace.CurrentCamera.ViewportSize.X / 2, workspace.CurrentCamera.ViewportSize.Y / 2
                VirtualInputManager:SendMouseButtonEvent(cx, cy, 1, false, game, 1)
                VirtualInputManager:SendMouseButtonEvent(cx, cy, 0, false, game, 1)
            end

            UserInputService.InputBegan:Connect(function(input, processed)
                if not processed and StopAllKey and input.KeyCode == StopAllKey then
                    StopAutoFarm()
                end
            end)

            local LastTargetTime = 0
            task.spawn(function()
                while task.wait() do
                    if _G.AutoFarm and _G.CurrentQuestData then
                        pcall(function()
                            local char = LocalPlayer.Character
                            local hrp = char and char:FindFirstChild("HumanoidRootPart")
                            if hrp then
                                local target = nil
                                local minDist = 3000
                                for _, v in pairs(workspace.Enemies:GetChildren()) do
                                    if v.Name == _G.CurrentQuestData.Mob and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 and v:FindFirstChild("HumanoidRootPart") then
                                        local dist = (hrp.Position - v.HumanoidRootPart.Position).Magnitude
                                        if dist < minDist then
                                            minDist = dist
                                            target = v
                                        end
                                    end
                                end

                                local backpack = LocalPlayer:FindFirstChild("Backpack")
                                local tool = char:FindFirstChildWhichIsA("Tool")
                                if not tool and backpack then
                                    for _, item in pairs(backpack:GetChildren()) do
                                        if item:IsA("Tool") then
                                            char.Humanoid:EquipTool(item)
                                            tool = item
                                            break
                                        end
                                    end
                                end

                                if target then
                                    LastTargetTime = os.clock()
                                    hrp.Size = Vector3.new(25, 25, 25)
                                    hrp.Transparency = 1
                                    hrp.CanCollide = false

                                    local targetHRP = target:FindFirstChild("HumanoidRootPart")
                                    if targetHRP then
                                        hrp.CFrame = targetHRP.CFrame * CFrame.new(0, 20, 0) * CFrame.Angles(math.rad(-90), 0, 0)

                                        if (hrp.Position - targetHRP.Position).Magnitude < 30 then
                                            local isOverUI = false
                                            pcall(function()
                                                local mousePos = UserInputService:GetMouseLocation()
                                                local objects = PlayerGui:GetGuiObjectsAtPosition(mousePos.X, mousePos.Y)
                                                for _, obj in pairs(objects) do
                                                    if obj:IsDescendantOf(ScreenGui) or (GalaxyHubGui and obj:IsDescendantOf(GalaxyHubGui)) then
                                                        isOverUI = true; break
                                                    end
                                                end
                                            end)

                                            local cx, cy = workspace.CurrentCamera.ViewportSize.X / 2, workspace.CurrentCamera.ViewportSize.Y / 2

                                            if not isOverUI then
                                                if tool then tool:Activate() end
                                                VirtualInputManager:SendMouseButtonEvent(cx, cy, 1, true, game, 1)
                                                VirtualInputManager:SendMouseButtonEvent(cx, cy, 0, true, game, 1)
                                                VirtualInputManager:SendMouseButtonEvent(cx, cy, 0, false, game, 1)
                                            else
                                                VirtualInputManager:SendMouseButtonEvent(cx, cy, 1, false, game, 1)
                                            end
                                        end
                                    end
                                    hrp.Velocity = Vector3.new(0, 0, 0)
                                else
                                    if _G.CurrentQuestData.Spawn then
                                        local spawnPos = _G.CurrentQuestData.Spawn
                                        local distToSpawn = (hrp.Position - spawnPos).Magnitude
                                        
                                        if distToSpawn > 150 then
                                            local dir = (spawnPos - hrp.Position).Unit
                                            local step = math.min(distToSpawn, 200)
                                            local targetPos = hrp.Position + (dir * step)
                                            local ti = TweenInfo.new(step / 350, Enum.EasingStyle.Linear)
                                            local tw = TweenService:Create(hrp, ti, {CFrame = CFrame.new(targetPos)})
                                            tw:Play()
                                            tw.Completed:Wait()
                                            for _, v in pairs(char:GetDescendants()) do
                                                if v:IsA("BasePart") then v.CanCollide = false end
                                            end
                                        else
                                            hrp.Velocity = Vector3.new(0, 0, 0)
                                        end
                                    else
                                        hrp.Velocity = Vector3.new(0, 0, 0)
                                    end
                                end
                            end
                        end)
                    else
                        LastTargetTime = os.clock()
                    end
                end
            end)

            if GalaxyHubGui then
                GalaxyHubGui:Destroy()
                GalaxyHubGui = nil
            else
                local targetGui = (gethui and gethui()) or game:GetService("CoreGui")
                GalaxyHubGui = Instance.new("ScreenGui", targetGui)
                GalaxyHubGui.Name = "Ton618UI"
                
                local Main = Instance.new("Frame", GalaxyHubGui)
                Main.Size = UDim2.new(0, 480, 0, 520)
                Main.Position = UDim2.new(0.5, -240, 0.5, -260)
                Main.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
                Main.BorderSizePixel = 0
                Main.Active = true
                Main.Draggable = true
                
                local MainCorner = Instance.new("UICorner", Main) MainCorner.CornerRadius = UDim.new(0, 2)
                local MainStroke = Instance.new("UIStroke", Main) MainStroke.Color = Color3.fromRGB(45, 45, 45) MainStroke.Thickness = 1
                
                local TitleFrame = Instance.new("Frame", Main)
                TitleFrame.Size = UDim2.new(0, 120, 0, 32)
                TitleFrame.Position = UDim2.new(0, 15, 0, 15)
                TitleFrame.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
                TitleFrame.BorderSizePixel = 0
                
                local TitleText = Instance.new("TextLabel", TitleFrame)
                TitleText.Text = "TON 618"
                TitleText.Size = UDim2.new(1, 0, 1, 0)
                TitleText.BackgroundTransparency = 1
                TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
                TitleText.Font = Enum.Font.GothamBlack
                TitleText.TextSize = 16

                local CloseBtn = Instance.new("TextButton", Main)
                CloseBtn.Text = "X"
                CloseBtn.Font = Enum.Font.Gotham
                CloseBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
                CloseBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
                CloseBtn.Size = UDim2.new(0, 30, 0, 30)
                CloseBtn.Position = UDim2.new(1, -45, 0, 15)
                CloseBtn.TextSize = 14
                local CloseCorner = Instance.new("UICorner", CloseBtn) CloseCorner.CornerRadius = UDim.new(0, 2)
                
                CloseBtn.MouseButton1Click:Connect(function()
                    GalaxyHubGui:Destroy()
                    GalaxyHubGui = nil
                end)

                local Content = Instance.new("ScrollingFrame", Main)
                Content.Size = UDim2.new(0.9, 0, 0.75, 0)
                Content.Position = UDim2.new(0.05, 0, 0.15, 0)
                Content.BackgroundTransparency = 1
                Content.ScrollBarThickness = 2
                Content.CanvasSize = UDim2.new(0, 0, 0, 0)
                Content.AutomaticCanvasSize = Enum.AutomaticSize.Y
                local Layout = Instance.new("UIListLayout", Content); Layout.Padding = UDim.new(0, 8)

                local function CreateBtn(text, color, parent)
                    local b = Instance.new("TextButton", parent or Content)
                    b.Size = UDim2.new(1, 0, 0, 45)
                    b.Text = text
                    b.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
                    b.TextColor3 = Color3.fromRGB(200, 200, 200)
                    b.Font = Enum.Font.GothamBold
                    b.TextSize = 14
                    local bCorner = Instance.new("UICorner", b) bCorner.CornerRadius = UDim.new(0, 2)
                    local bStroke = Instance.new("UIStroke", b) bStroke.Color = color bStroke.Thickness = 1
                    return b
                end

                local function ClearContent()
                    for _, v in pairs(Content:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
                end

                local ShowSeaMenu, ShowIslandMenu, ShowQuestMenu

                ShowSeaMenu = function()
                    ClearContent()
                    for _, sea in pairs({"SEA 1", "SEA 2", "SEA 3"}) do
                        CreateBtn(sea, Color3.fromRGB(255, 130, 0)).MouseButton1Click:Connect(function() ShowIslandMenu(sea) end)
                    end
                end

                ShowIslandMenu = function(sea)
                    ClearContent()
                    CreateBtn("â¬… BACK", Color3.fromRGB(200, 50, 50)).MouseButton1Click:Connect(ShowSeaMenu)
                    for _, islandData in ipairs(GameData[sea]) do
                        CreateBtn(islandData.Emoji .. " " .. islandData.Name, islandData.Color).MouseButton1Click:Connect(function() ShowQuestMenu(sea, islandData) end)
                    end
                end

                ShowQuestMenu = function(sea, islandData)
                    ClearContent()
                    CreateBtn("â¬… BACK", Color3.fromRGB(200, 50, 50)).MouseButton1Click:Connect(function() ShowIslandMenu(sea) end)
                    for _, info in pairs(islandData.Mobs) do
                        CreateBtn("âš”ï¸ ["..info.Level.."] "..info.Mob, Color3.fromRGB(50, 150, 50)).MouseButton1Click:Connect(function()
                            _G.CurrentQuestData = info; _G.AutoFarm = true
                        end)
                    end
                end

                local StopText = StopAllKey and ("ðŸ›‘ STOP ALL [" .. StopAllKey.Name .. "]") or "ðŸ›‘ STOP ALL [NONE]"
                local Stop = CreateBtn(StopText, Color3.fromRGB(200, 0, 0), Main)
                Stop.Position = UDim2.new(0.1, 0, 0.85, 0); Stop.Size = UDim2.new(0.8, 0, 0, 50)
                
                local isBindingStop = false
                Stop.MouseButton1Click:Connect(function()
                    isBindingStop = true
                    Stop.Text = "Press a key..."
                end)

                local bindConn = UserInputService.InputBegan:Connect(function(input)
                    if isBindingStop and input.UserInputType == Enum.UserInputType.Keyboard then
                        StopAllKey = input.KeyCode
                        isBindingStop = false
                        Stop.Text = "ðŸ›‘ STOP ALL [" .. input.KeyCode.Name .. "]"
                    end
                end)
                
                Main.Destroying:Connect(function()
                    if bindConn then bindConn:Disconnect() end
                end)

                ShowSeaMenu()
            end
        end)
    end)
    end
    
    -- Sea Event Page Content
    createSection(Pages.SeaEvent, "Leviathan", 1)
    createToggle(Pages.SeaEvent, "Auto Find Leviathan", "AutoFindLeviathan", 2)
    
    createSection(Pages.SeaEvent, "Boat Control", 3)
    createToggle(Pages.SeaEvent, "Auto Buy Boat & Sit", "AutoBuyBoat", 4)
    createCycle(Pages.SeaEvent, "Select Boat", "SelectedBoat", {"Speedboat", "Luxury Boat", "Grand Brigade", "Guardian", "Lantern", "Miracle", "Sloop", "Dinghy", "Beast Hunter", "Sleigh"}, 5)
    
    -- Shop Page Content
    local function createBuyBtn(parent, text, args, order, targetInfo)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 25)
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(200, 200, 200)
        btn.Font = Enum.Font.Gotham
        btn.TextSize = 9
        btn.LayoutOrder = order
        btn.Parent = parent
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0,4) c.Parent = btn
        btn.MouseButton1Click:Connect(function()
            local savedCFrame = nil
            if character and rootPart then savedCFrame = rootPart.CFrame end
            
            local targetPos = nil
            
            if typeof(targetInfo) == "Vector3" then
                targetPos = CFrame.new(targetInfo)
            elseif typeof(targetInfo) == "string" then
                local npcName = targetInfo
                local targetNPC = nil
                
                local folders = {
                    workspace:FindFirstChild("NPCs"),
                    workspace:FindFirstChild("Map") and workspace.Map:FindFirstChild("NPCs"),
                    workspace:FindFirstChild("Spawn") and workspace.Spawn:FindFirstChild("NPCs")
                }
                
                for _, f in pairs(folders) do
                    if f then
                        targetNPC = f:FindFirstChild(npcName)
                        if targetNPC then break end
                    end
                end
                
                if not targetNPC then
                    for _, v in pairs(workspace:GetDescendants()) do
                        if v.Name == npcName and v:FindFirstChild("HumanoidRootPart") then
                            targetNPC = v
                            break
                        end
                    end
                end
                if targetNPC and targetNPC:FindFirstChild("HumanoidRootPart") then
                    targetPos = targetNPC.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                end
            end
            
            if targetPos and rootPart then
                local wasAnchored = rootPart.Anchored
                rootPart.Anchored = true
                local startTime = tick()
                while tick() - startTime < 0.5 do
                    if rootPart then 
                        rootPart.CFrame = targetPos 
                        rootPart.Velocity = Vector3.new(0,0,0)
                        rootPart.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    end
                    RunService.Heartbeat:Wait()
                end
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
                if rootPart then rootPart.Anchored = wasAnchored end
                if savedCFrame then rootPart.CFrame = savedCFrame end
            else
                game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(args))
            end
        end)
    end

    createSection(Pages.Shop, "Fragments", 13)
    createBuyBtn(Pages.Shop, "Refund Stats", {"BlackbeardReward", "Refund", "2"}, 14)
    createBuyBtn(Pages.Shop, "Reroll Race", {"BlackbeardReward", "Reroll", "2"}, 15)
    
    createSection(Pages.Shop, "Devil Fruit", 24)
    createToggle(Pages.Shop, "Auto Store Fruit", "AutoStoreFruit", 26)
    createToggle(Pages.Shop, "Auto Grab Fruit (Fly)", "AutoGrabFruit", 27)
    
    -- Devil Fruit Options from Script2.lua
    createToggle(Pages.Shop, "Auto Random Fruit", "AutoRandomFruit", 28)
    
    do
        local RandomFruitBtn = Instance.new("TextButton")
        RandomFruitBtn.Size = UDim2.new(1, 0, 0, 30)
        RandomFruitBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        RandomFruitBtn.Text = "Buy Random Fruit"
        RandomFruitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        RandomFruitBtn.Font = Enum.Font.GothamBold
        RandomFruitBtn.TextSize = 10
        RandomFruitBtn.LayoutOrder = 29
        RandomFruitBtn.Parent = Pages.Shop
        local Corner = Instance.new("UICorner") Corner.CornerRadius = UDim.new(0, 4) Corner.Parent = RandomFruitBtn
        
        RandomFruitBtn.MouseButton1Click:Connect(function()
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Cousin", "Buy")
            SendNotification("Random fruit purchased!", 3)
        end)
    end
    
    do
        local FruitInServerLabel = Instance.new("TextLabel")
        FruitInServerLabel.Size = UDim2.new(1, 0, 0, 25)
        FruitInServerLabel.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
        FruitInServerLabel.Text = "Fruit in Server: ?"
        FruitInServerLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
        FruitInServerLabel.Font = Enum.Font.GothamMedium
        FruitInServerLabel.TextSize = 10
        FruitInServerLabel.LayoutOrder = 30
        FruitInServerLabel.Parent = Pages.Shop
        local Corner = Instance.new("UICorner") Corner.CornerRadius = UDim.new(0, 4) Corner.Parent = FruitInServerLabel
        
        -- Update fruit count
        task.spawn(function()
            while true do
                task.wait(2)
                local count = 0
                for _, obj in pairs(workspace:GetChildren()) do
                    if string.find(obj.Name, "Fruit") and obj:IsA("Model") then
                        count = count + 1
                    end
                end
                FruitInServerLabel.Text = "Fruit in Server: " .. count
            end
        end)
    end
    
    do
        local fpsUnlockerGui = nil
        local FpsUnlockerBtn = Instance.new("TextButton")
        FpsUnlockerBtn.Size = UDim2.new(1, 0, 0, 30)
        FpsUnlockerBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        FpsUnlockerBtn.Text = "FPS UNLOCKER"
        FpsUnlockerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        FpsUnlockerBtn.Font = Enum.Font.GothamBold
        FpsUnlockerBtn.TextSize = 10
        FpsUnlockerBtn.LayoutOrder = 32
        FpsUnlockerBtn.Parent = Pages.Shop
        
        local FpsCorner = Instance.new("UICorner") FpsCorner.CornerRadius = UDim.new(0, 4) FpsCorner.Parent = FpsUnlockerBtn
        
        FpsUnlockerBtn.MouseButton1Click:Connect(function()
            if not fpsUnlockerGui or not fpsUnlockerGui.Parent then
                loadstring(game:HttpGet("https://raw.githubusercontent.com/Perfectionsthegoat/fps-unlocker/refs/heads/main/fps%20unlocker"))()
                SendNotification("FPS Unlocker loaded!", 3)
                task.wait(0.5) -- Give script time to load
                for _, gui in ipairs(game:GetService("CoreGui"):GetChildren()) do
                    if gui:IsA("ScreenGui") and gui.Name ~= ScreenGui.Name and gui.Name ~= "KeySystemGUI" and not string.find(gui.Name, "SILVERIKA") and gui.Name ~= "Ton618UI" then
                        fpsUnlockerGui = gui
                        break
                    end
                end
                if fpsUnlockerGui then
                    FpsUnlockerBtn.Text = "TOGGLE FPS UNLOCKER GUI"
                end
            else
                fpsUnlockerGui.Enabled = not fpsUnlockerGui.Enabled
                SendNotification("FPS Unlocker GUI " .. (fpsUnlockerGui.Enabled and "shown" or "hidden"), 2)
            end
        end)
    end
    
    createSection(Pages.Shop, "Server Joiner", 33)
    createInput(Pages.Shop, "Server Job ID", "JoinJobID", 34)
    
    do
        local JoinJobBtn = Instance.new("TextButton")
        JoinJobBtn.Size = UDim2.new(1, 0, 0, 30)
        JoinJobBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        JoinJobBtn.Text = "JOIN SERVER BY ID"
        JoinJobBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        JoinJobBtn.Font = Enum.Font.GothamBold
        JoinJobBtn.TextSize = 10
        JoinJobBtn.LayoutOrder = 35
        JoinJobBtn.Parent = Pages.Shop
        
        local JJCorner = Instance.new("UICorner") JJCorner.CornerRadius = UDim.new(0, 4) JJCorner.Parent = JoinJobBtn
        
        JoinJobBtn.MouseButton1Click:Connect(function()
            local jobId = Config.JoinJobID
            if not jobId or jobId == "" then SendNotification("Please enter a Job ID", 2) return end
            
            SendNotification("Joining Server: " .. jobId, 3)
            game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, jobId, player)
        end)
    end

    do
        local ShowIDBtn = Instance.new("TextButton")
        ShowIDBtn.Size = UDim2.new(1, 0, 0, 30)
        ShowIDBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        ShowIDBtn.Text = "ID: " .. game.JobId
        ShowIDBtn.TextColor3 = Color3.fromRGB(150, 150, 150)
        ShowIDBtn.Font = Enum.Font.Gotham
        ShowIDBtn.TextSize = 8
        ShowIDBtn.LayoutOrder = 36
        ShowIDBtn.Parent = Pages.Shop
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0,4) c.Parent = ShowIDBtn
    end

    do
        local CopyIDBtn = Instance.new("TextButton")
        CopyIDBtn.Size = UDim2.new(1, 0, 0, 30)
        CopyIDBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        CopyIDBtn.Text = "COPY SERVER ID"
        CopyIDBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        CopyIDBtn.Font = Enum.Font.GothamBold
        CopyIDBtn.TextSize = 10
        CopyIDBtn.LayoutOrder = 37
        CopyIDBtn.Parent = Pages.Shop
        local c = Instance.new("UICorner") c.CornerRadius = UDim.new(0,4) c.Parent = CopyIDBtn
        
        CopyIDBtn.MouseButton1Click:Connect(function()
            if setclipboard then setclipboard(game.JobId) end
            SendNotification("Server ID Copied!", 2)
        end)
    end

    -- Settings Page Content
    createSection(Pages.Settings, "UI Settings", 0)
    createKeybind(Pages.Settings, "Hide Menu Key", "HideGuiKey", 0.5)
    
    createSection(Pages.Settings, "Theme Settings", 1)
    
    local function UpdateTheme(newColor)
        CurrentThemeColor = newColor
        Title.TextColor3 = newColor
        ExpandButton.TextColor3 = newColor
        
        -- Update Tabs Stroke
        for name, btn in pairs(TabBtns) do
            if Pages[name].Visible then
                btn.UIStroke.Color = newColor
            end
        end
        
        -- Update Hub Buttons
        for _, btn in pairs(HubMenu:GetChildren()) do
            if btn:IsA("TextButton") then
                btn.TextColor3 = newColor
            end
        end
        
        -- Update Patch Notes Button
        if PatchNotesBtn then
            PatchNotesBtn.TextColor3 = newColor
            if PatchNotesBtn:FindFirstChild("UIStroke") then PatchNotesBtn.UIStroke.Color = newColor end
        end
        if PatchNotesPage then
            for _, f in pairs(PatchNotesPage:GetChildren()) do
                local vl = f:FindFirstChild("VersionLabel")
                if vl then vl.TextColor3 = newColor end
            end
        end
        
        -- Update Toggles (Active)
        for key, btn in pairs(ToggleButtons) do
            if Config[key] then
                btn.BackgroundColor3 = newColor
            end
        end
        
        -- Update Inputs & Keybinds Text
        for _, page in pairs(Pages) do
            for _, obj in pairs(page:GetDescendants()) do
                if obj:IsA("TextBox") then
                    obj.TextColor3 = newColor
                elseif obj:IsA("TextButton") and obj.Parent:FindFirstChild("UIStroke") and obj.Text ~= "" and obj.Name ~= "ExpandButton" then
                    -- Heuristic to find Keybind/Input buttons (not toggles which have empty text)
                    obj.TextColor3 = newColor
                end
            end
        end
    end

    do
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(1, 0)
        corner.Parent = btn
        
        local stroke = Instance.new("UIStroke")
        stroke.Color = Color3.fromRGB(50, 50, 60)
        stroke.Thickness = 1
        stroke.Parent = btn
        
        local tip = Instance.new("TextLabel")
        tip.Text = name
        tip.Size = UDim2.new(0, 0, 0, 15)
        tip.AutomaticSize = Enum.AutomaticSize.X
        tip.Position = UDim2.new(0.5, 0, -0.6, 0)
        tip.AnchorPoint = Vector2.new(0.5, 1)
        tip.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        tip.TextColor3 = Color3.fromRGB(255, 255, 255)
        tip.Font = Enum.Font.Gotham
        tip.TextSize = 8
        tip.Visible = false
        tip.ZIndex = 10
        tip.Parent = btn
        local tCorner = Instance.new("UICorner") tCorner.CornerRadius = UDim.new(0, 3) tCorner.Parent = tip
        local tPad = Instance.new("UIPadding") tPad.PaddingLeft = UDim.new(0, 4) tPad.PaddingRight = UDim.new(0, 4) tPad.Parent = tip
        
        btn.MouseEnter:Connect(function() tip.Visible = true end)
        btn.MouseLeave:Connect(function() tip.Visible = false end)
        
        btn.MouseButton1Click:Connect(function()
            UpdateTheme(color)
        end)
    end
    
    createColorBtn(Color3.fromRGB(255, 255, 0), "Yellow")
    createColorBtn(Color3.fromRGB(170, 0, 255), "Purple")
    createColorBtn(Color3.fromRGB(0, 255, 150), "Turquoise")
    createColorBtn(Color3.fromRGB(255, 50, 50), "Red")
    createColorBtn(Color3.fromRGB(0, 150, 255), "Blue")
    createColorBtn(Color3.fromRGB(255, 255, 255), "White")
    end
    
    createSection(Pages.Settings, "Panel Color", 2.1)
    
    do
        local PanelColorFrame = Instance.new("Frame")
        PanelColorFrame.Size = UDim2.new(1, 0, 0, 40)
        PanelColorFrame.BackgroundTransparency = 1
        PanelColorFrame.LayoutOrder = 2.2
        PanelColorFrame.Parent = Pages.Settings
        
        local PanelColorLayout = Instance.new("UIGridLayout")
        PanelColorLayout.CellSize = UDim2.new(0, 25, 0, 25)
        PanelColorLayout.CellPadding = UDim2.new(0, 5, 0, 5)
        PanelColorLayout.Parent = PanelColorFrame
        
        local function createPanelColorBtn(color, name)
            local btn = Instance.new("TextButton")
            btn.Text = ""
            btn.BackgroundColor3 = color
            btn.Parent = PanelColorFrame
            local c = Instance.new("UICorner") c.CornerRadius = UDim.new(1,0) c.Parent = btn
            local s = Instance.new("UIStroke") s.Color = Color3.fromRGB(60,60,60) s.Thickness = 1 s.Parent = btn
            
            local tip = Instance.new("TextLabel") tip.Text = name tip.Size = UDim2.new(0,0,0,15) tip.AutomaticSize = Enum.AutomaticSize.X tip.Position = UDim2.new(0.5,0,-0.6,0) tip.AnchorPoint = Vector2.new(0.5,1) tip.BackgroundColor3 = Color3.fromRGB(30,30,40) tip.TextColor3 = Color3.fromRGB(255,255,255) tip.Font = Enum.Font.Gotham tip.TextSize = 8 tip.Visible = false tip.ZIndex = 10 tip.Parent = btn
            local tCorner = Instance.new("UICorner") tCorner.CornerRadius = UDim.new(0,3) tCorner.Parent = tip
            local tPad = Instance.new("UIPadding") tPad.PaddingLeft = UDim.new(0,4) tPad.PaddingRight = UDim.new(0,4) tPad.Parent = tip
            btn.MouseEnter:Connect(function() tip.Visible = true end)
            btn.MouseLeave:Connect(function() tip.Visible = false end)
            
            btn.MouseButton1Click:Connect(function()
                MainFrame.BackgroundColor3 = color
                RightPanel.BackgroundColor3 = color
            end)
        end
        
        createPanelColorBtn(Color3.fromRGB(15, 15, 20), "Default")
        createPanelColorBtn(Color3.fromRGB(0, 0, 0), "Black")
        createPanelColorBtn(Color3.fromRGB(25, 25, 30), "Gray")
        createPanelColorBtn(Color3.fromRGB(10, 10, 25), "Navy")
        createPanelColorBtn(Color3.fromRGB(25, 10, 10), "Redish")
        createPanelColorBtn(Color3.fromRGB(10, 25, 10), "Greenish")
        createPanelColorBtn(Color3.fromRGB(25, 10, 25), "Purpleish")
        createPanelColorBtn(Color3.fromRGB(255, 255, 255), "Flashbang")
        createPanelColorBtn(Color3.fromRGB(40, 0, 0), "Red")
        createPanelColorBtn(Color3.fromRGB(0, 0, 40), "Blue")
        createPanelColorBtn(Color3.fromRGB(0, 40, 0), "Green")
        createPanelColorBtn(Color3.fromRGB(40, 20, 0), "Orange")
        createPanelColorBtn(Color3.fromRGB(20, 0, 40), "Purple")
        createPanelColorBtn(Color3.fromRGB(0, 40, 40), "Teal")
    end
    
    createSection(Pages.Settings, "Panel Color", 2.1)
    
    do
        local PanelColorFrame = Instance.new("Frame")
        PanelColorFrame.Size = UDim2.new(1, 0, 0, 60)
        PanelColorFrame.BackgroundTransparency = 1
        PanelColorFrame.LayoutOrder = 2.2
        PanelColorFrame.Parent = Pages.Settings
        
        local PanelColorLayout = Instance.new("UIGridLayout")
        PanelColorLayout.CellSize = UDim2.new(0, 25, 0, 25)
        PanelColorLayout.CellPadding = UDim2.new(0, 5, 0, 5)
        PanelColorLayout.Parent = PanelColorFrame
        
        local function createPanelColorBtn(color, name)
            local btn = Instance.new("TextButton")
            btn.Text = ""
            btn.BackgroundColor3 = color
            btn.Parent = PanelColorFrame
            local c = Instance.new("UICorner") c.CornerRadius = UDim.new(1,0) c.Parent = btn
            local s = Instance.new("UIStroke") s.Color = Color3.fromRGB(60,60,60) s.Thickness = 1 s.Parent = btn
            
            local tip = Instance.new("TextLabel") tip.Text = name tip.Size = UDim2.new(0,0,0,15) tip.AutomaticSize = Enum.AutomaticSize.X tip.Position = UDim2.new(0.5,0,-0.6,0) tip.AnchorPoint = Vector2.new(0.5,1) tip.BackgroundColor3 = Color3.fromRGB(30,30,40) tip.TextColor3 = Color3.fromRGB(255,255,255) tip.Font = Enum.Font.Gotham tip.TextSize = 8 tip.Visible = false tip.ZIndex = 10 tip.Parent = btn
            local tCorner = Instance.new("UICorner") tCorner.CornerRadius = UDim.new(0,3) tCorner.Parent = tip
            local tPad = Instance.new("UIPadding") tPad.PaddingLeft = UDim.new(0,4) tPad.PaddingRight = UDim.new(0,4) tPad.Parent = tip
            btn.MouseEnter:Connect(function() tip.Visible = true end)
            btn.MouseLeave:Connect(function() tip.Visible = false end)
            
            btn.MouseButton1Click:Connect(function()
                MainFrame.BackgroundColor3 = color
                RightPanel.BackgroundColor3 = color
            end)
        end
        
        createPanelColorBtn(Color3.fromRGB(255, 0, 0), "Neon Red")
        createPanelColorBtn(Color3.fromRGB(0, 255, 0), "Neon Green")
        createPanelColorBtn(Color3.fromRGB(0, 0, 255), "Neon Blue")
        createPanelColorBtn(Color3.fromRGB(255, 0, 255), "Neon Pink")
        createPanelColorBtn(Color3.fromRGB(0, 255, 255), "Neon Cyan")
        createPanelColorBtn(Color3.fromRGB(255, 255, 0), "Neon Yellow")
        createPanelColorBtn(Color3.fromRGB(255, 128, 0), "Neon Orange")
        createPanelColorBtn(Color3.fromRGB(128, 0, 255), "Neon Purple")
        createPanelColorBtn(Color3.fromRGB(15, 15, 20), "Default")
        createPanelColorBtn(Color3.fromRGB(0, 0, 0), "Black")
    end
    
    createSection(Pages.Settings, "Configuration Manager", 3)
    
    local ConfigFolder = "Cyberpunks_Configs"
    if makefolder then pcall(function() makefolder(ConfigFolder) end) end
    
    local function SaveConfig(name)
        if not writefile then return end
        local jsonConfig = {}
        for k, v in pairs(Config) do
            if typeof(v) == "EnumItem" then
                jsonConfig[k] = {Type = "Enum", Name = v.Name, EnumType = tostring(v.EnumType)}
            elseif typeof(v) == "Color3" then
                jsonConfig[k] = {Type = "Color3", R = v.R, G = v.G, B = v.B}
            else
                jsonConfig[k] = v
            end
        end
        pcall(function() writefile(ConfigFolder .. "/" .. name .. ".json", HttpService:JSONEncode(jsonConfig)) end)
    end

    local function LoadConfig(name)
        if not readfile or not isfile(ConfigFolder .. "/" .. name .. ".json") then return end
        local content = readfile(ConfigFolder .. "/" .. name .. ".json")
        local decoded = HttpService:JSONDecode(content)
        
        for k, v in pairs(decoded) do
            if Config[k] ~= nil then
                if type(v) == "table" and v.Type == "Enum" then
                    local success, val = pcall(function() return Enum[tostring(v.EnumType):match("Enum%.(%w+)")] end)
                    if success and val then
                        Config[k] = val[v.Name]
                    else
                        pcall(function() Config[k] = Enum.KeyCode[v.Name] end)
                    end
                elseif type(v) == "table" and v.Type == "Color3" then
                    Config[k] = Color3.new(v.R, v.G, v.B)
                else
                    Config[k] = v
                end
            end
        end
        
        -- Update UI
        for k, btn in pairs(ToggleButtons) do
            btn.BackgroundColor3 = Config[k] and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
        end
        for k, btn in pairs(KeybindButtons) do
            local keyName = Config[k].Name
            if Config[k] == Enum.KeyCode.Unknown then keyName = "..." end
            btn.Text = keyName
        end
        for k, input in pairs(InputBoxes) do
            input.Text = tostring(Config[k])
        end
        
        if _G.SnowGui then
            _G.SnowGui.Enabled = not Config.DisableSnow
        end
        
        UpdateTheme(CurrentThemeColor)
        
        -- Apply Cursor
        if Config.CustomCursorId and Config.CustomCursorId ~= "" then
             local val = Config.CustomCursorId
             if tonumber(val) then
                mouse.Icon = "rbxassetid://" .. val
             else
                mouse.Icon = val
             end
        end
        
        -- Apply FastM1 if enabled in config
        if Config.FastM1 and Config.IsWhitelisted then
            if FastM1Btn then
                FastM1Btn.Text = "STOP FAST M1"
                FastM1Btn.TextColor3 = Color3.fromRGB(255, 50, 50)
            end
            if StartFastM1 then StartFastM1() end
        end
    end

    do
    local ConfigFrame = Instance.new("Frame")
    ConfigFrame.Size = UDim2.new(1, 0, 0, 250)
    ConfigFrame.BackgroundTransparency = 1
    ConfigFrame.LayoutOrder = 4
    ConfigFrame.Parent = Pages.Settings
    
    local CFName = Instance.new("TextBox")
    CFName.Size = UDim2.new(0.65, 0, 0, 25)
    CFName.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    CFName.TextColor3 = Color3.fromRGB(255, 255, 255)
    CFName.PlaceholderText = "Config Name..."
    CFName.Text = ""
    CFName.Font = Enum.Font.Gotham
    CFName.TextSize = 10
    CFName.Parent = ConfigFrame
    
    Instance.new("UICorner", CFName).CornerRadius = UDim.new(0,4)
    
    local SaveBtn = Instance.new("TextButton")
    SaveBtn.Size = UDim2.new(0.3, 0, 0, 25)
    SaveBtn.Position = UDim2.new(0.7, 0, 0, 0)
    SaveBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    SaveBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    SaveBtn.Text = "SAVE"
    SaveBtn.Font = Enum.Font.GothamBold
    SaveBtn.TextSize = 10
    SaveBtn.Parent = ConfigFrame
    Instance.new("UICorner", SaveBtn).CornerRadius = UDim.new(0,4)
    
    local ListFrame = Instance.new("ScrollingFrame")
    ListFrame.Size = UDim2.new(1, 0, 1, -35)
    ListFrame.Position = UDim2.new(0, 0, 0, 35)
    ListFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    ListFrame.ScrollBarThickness = 2
    ListFrame.BorderSizePixel = 0
    ListFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
    ListFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
    ListFrame.Parent = ConfigFrame
    Instance.new("UICorner", ListFrame).CornerRadius = UDim.new(0,4)
    Instance.new("UIListLayout", ListFrame).Padding = UDim.new(0, 2)
    
    local function RefreshConfigs()
        for _, v in pairs(ListFrame:GetChildren()) do if v:IsA("Frame") or v:IsA("TextButton") then v:Destroy() end end
        if listfiles then
            pcall(function()
                for _, file in pairs(listfiles(ConfigFolder)) do
                    local name = file:match("([^\\/]+)%.json$")
                    if name then
                        local Row = Instance.new("Frame")
                        Row.Size = UDim2.new(1, 0, 0, 20)
                        Row.BackgroundTransparency = 1
                        Row.Parent = ListFrame
                        
                        local btn = Instance.new("TextButton")
                        btn.Size = UDim2.new(0.8, 0, 1, 0)
                        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
                        btn.TextColor3 = Color3.fromRGB(200, 200, 200)
                        btn.Text = name
                        btn.Font = Enum.Font.Gotham
                        btn.TextSize = 9
                        btn.Parent = Row
                        
                        local BCorner = Instance.new("UICorner") BCorner.CornerRadius = UDim.new(0, 4) BCorner.Parent = btn
                        
                        btn.MouseButton1Click:Connect(function()
                            LoadConfig(name)
                        end)
                        
                        local delBtn = Instance.new("TextButton")
                        delBtn.Size = UDim2.new(0.15, 0, 1, 0)
                        delBtn.Position = UDim2.new(0.85, 0, 0, 0)
                        delBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
                        delBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                        delBtn.Text = "ðŸ—‘"
                        delBtn.Font = Enum.Font.Gotham
                        delBtn.TextSize = 10
                        delBtn.Parent = Row
                        
                        local DCorner = Instance.new("UICorner") DCorner.CornerRadius = UDim.new(0, 4) DCorner.Parent = delBtn
                        
                        delBtn.MouseButton1Click:Connect(function()
                            if delfile then
                                pcall(function() delfile(file) end)
                                Row:Destroy()
                            end
                        end)
                    end
                end
            end)
        end
    end
    
    SaveBtn.MouseButton1Click:Connect(function()
        if CFName.Text ~= "" then
            SaveConfig(CFName.Text)
            CFName.Text = ""
            task.wait(0.2)
            RefreshConfigs()
        end
    end)
    
    RefreshConfigs()
    end
    
    -- Auto Load Script Button (loads Cyberpunks.lua from GitHub)
    do
        local CyberScriptBtn = Instance.new("TextButton")
        CyberScriptBtn.Size = UDim2.new(1, 0, 0, 30)
        CyberScriptBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        CyberScriptBtn.Text = "Auto Load Script"
        CyberScriptBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        CyberScriptBtn.Font = Enum.Font.GothamBold
        CyberScriptBtn.TextSize = 10
        CyberScriptBtn.LayoutOrder = 7
        CyberScriptBtn.Parent = Pages.Settings
        
        local CSCorner = Instance.new("UICorner") CSCorner.CornerRadius = UDim.new(0, 4) CSCorner.Parent = CyberScriptBtn
        
        CyberScriptBtn.MouseButton1Click:Connect(function()
            local script_to_run = loadstring(game:HttpGet("https://raw.githubusercontent.com/wUNKNOW/Cyberpunks/refs/heads/main/Cyberpunks.lua"))()[[
    print("debug")
    -- your source
]]

if syn and syn.queue_on_teleport then
    syn.queue_on_teleport(script_to_run)
elseif queue_on_teleport then
    queue_on_teleport(script_to_run)
end
        end)
    end
    
    createToggle(Pages.Settings, "Auto Load Config", "AutoLoadConfig", 9)
    createInput(Pages.Settings, "Config Name", "AutoLoadConfigName", 10)
    
    do
    local HopBtn = Instance.new("TextButton")
    HopBtn.Size = UDim2.new(1, 0, 0, 30)
    HopBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    HopBtn.Text = "SERVER HOP"
    HopBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    HopBtn.Font = Enum.Font.GothamBold
    HopBtn.TextSize = 10
    HopBtn.LayoutOrder = 10
    HopBtn.Parent = Pages.Settings
    
    local HCorner = Instance.new("UICorner") HCorner.CornerRadius = UDim.new(0, 4) HCorner.Parent = HopBtn
    
    HopBtn.MouseButton1Click:Connect(function()
        local PlaceId = game.PlaceId
        local Http = game:GetService("HttpService")
        if Http and game.HttpGet then
            pcall(function()
                local Servers = Http:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..PlaceId.."/servers/Public?sortOrder=Asc&limit=100"))
                for _, v in pairs(Servers.data) do
                    if v.playing ~= v.maxPlayers and v.id ~= game.JobId then
                        game:GetService("TeleportService"):TeleportToPlaceInstance(PlaceId, v.id, player)
                        break
                    end
                end
            end)
        end
    end)
    end

    do
    local RejoinBtn = Instance.new("TextButton")
    RejoinBtn.Size = UDim2.new(1, 0, 0, 30)
    RejoinBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    RejoinBtn.Text = "REJOIN SERVER"
    RejoinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    RejoinBtn.Font = Enum.Font.GothamBold
    RejoinBtn.TextSize = 10
    RejoinBtn.LayoutOrder = 11
    RejoinBtn.Parent = Pages.Settings
    
    local RJCorner = Instance.new("UICorner") RJCorner.CornerRadius = UDim.new(0, 4) RJCorner.Parent = RejoinBtn
    
    RejoinBtn.MouseButton1Click:Connect(function()
        game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId, player)
    end)
    end
    
    do
    local PartViewerBtn = Instance.new("TextButton")
    PartViewerBtn.Size = UDim2.new(1, 0, 0, 30)
    PartViewerBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    PartViewerBtn.Text = "PART VIEWER (Debug)"
    PartViewerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    PartViewerBtn.Font = Enum.Font.GothamBold
    PartViewerBtn.TextSize = 10
    PartViewerBtn.LayoutOrder = 11.5
    PartViewerBtn.Parent = Pages.Settings
    
    local PVCorner = Instance.new("UICorner") PVCorner.CornerRadius = UDim.new(0, 4) PVCorner.Parent = PartViewerBtn
    
    PartViewerBtn.MouseButton1Click:Connect(function()
        if not PartViewerEnabled then
            PartViewerEnabled = true
            PartViewerBtn.Text = "PART VIEWER: ON"
            PartViewerBtn.BackgroundColor3 = Color3.fromRGB(50, 200, 50)
            StartPartViewer()
        else
            PartViewerEnabled = false
            PartViewerBtn.Text = "PART VIEWER (Debug)"
            PartViewerBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
            StopPartViewer()
        end
    end)
    end

    -- Snow Color Settings
    createSection(Pages.Settings, "Snow Settings", 11)
    createToggle(Pages.Settings, "Disable Snow", "DisableSnow", 11.2)
    
    do
        local SnowContainer = Instance.new("Frame")
        SnowContainer.Size = UDim2.new(1, 0, 0, 80)
        SnowContainer.BackgroundTransparency = 1
        SnowContainer.LayoutOrder = 11.5
        SnowContainer.Parent = Pages.Settings
        
        local SnowLayout = Instance.new("UIListLayout")
        SnowLayout.FillDirection = Enum.FillDirection.Vertical
        SnowLayout.Padding = UDim.new(0, 3)
        SnowLayout.Parent = SnowContainer
        
        -- Snowflake color buttons
        local FlakeRow = Instance.new("Frame")
        FlakeRow.Size = UDim2.new(1, 0, 0, 30)
        FlakeRow.BackgroundTransparency = 1
        FlakeRow.Parent = SnowContainer
        
        local FlakeLayout = Instance.new("UIListLayout")
        FlakeLayout.FillDirection = Enum.FillDirection.Horizontal
        FlakeLayout.Padding = UDim.new(0, 5)
        FlakeLayout.Parent = FlakeRow
        
        local function createSnowFlakeBtn(color, name)
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(0, 55, 0, 22)
            btn.BackgroundColor3 = color
            btn.Text = name
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.Font = Enum.Font.GothamBold
            btn.TextSize = 8
            btn.Parent = FlakeRow
            
            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0, 3)
            corner.Parent = btn
            
            btn.MouseButton1Click:Connect(function()
                _G.SnowFlakeColor = color
                updateSnowColors()
            end)
        end
        
        createSnowFlakeBtn(Color3.fromRGB(255, 255, 255), "White")
        createSnowFlakeBtn(Color3.fromRGB(255, 200, 100), "Orange")
        createSnowFlakeBtn(Color3.fromRGB(100, 200, 255), "Blue")
        createSnowFlakeBtn(Color3.fromRGB(255, 100, 255), "Pink")
        
        -- Background color buttons
        local BgRow = Instance.new("Frame")
        BgRow.Size = UDim2.new(1, 0, 0, 30)
        BgRow.BackgroundTransparency = 1
        BgRow.Parent = SnowContainer
        
        local BgLayout = Instance.new("UIListLayout")
        BgLayout.FillDirection = Enum.FillDirection.Horizontal
        BgLayout.Padding = UDim.new(0, 5)
        BgLayout.Parent = BgRow
        
        local function createSnowBgBtn(color, name)
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(0, 55, 0, 22)
            btn.BackgroundColor3 = color
            btn.Text = name
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.Font = Enum.Font.GothamBold
            btn.TextSize = 8
            btn.Parent = BgRow
            
            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0, 3)
            corner.Parent = btn
            
            btn.MouseButton1Click:Connect(function()
                _G.SnowBackgroundColor = color
                updateSnowColors()
            end)
        end
        
        createSnowBgBtn(Color3.fromRGB(10, 10, 15), "Dark")
        createSnowBgBtn(Color3.fromRGB(0, 0, 50), "Navy")
        createSnowBgBtn(Color3.fromRGB(50, 0, 50), "Purple")
        createSnowBgBtn(Color3.fromRGB(0, 50, 50), "Teal")
        createSnowBgBtn(Color3.fromRGB(100, 50, 0), "Brown")
        createSnowBgBtn(Color3.fromRGB(0, 0, 0), "Black")
    end
    
    do
    local Spacer = Instance.new("Frame")
    Spacer.Size = UDim2.new(1, 0, 0, 10) -- Espace pour pousser le bouton vers le bas
    Spacer.BackgroundTransparency = 1
    Spacer.LayoutOrder = 12
    Spacer.Parent = Pages.Settings
    
    local DisconnectBtn = Instance.new("TextButton")
    DisconnectBtn.Size = UDim2.new(1, 0, 0, 30)
    DisconnectBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    DisconnectBtn.Text = "DISCONNECT"
    DisconnectBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    DisconnectBtn.Font = Enum.Font.GothamBold
    DisconnectBtn.TextSize = 10
    DisconnectBtn.LayoutOrder = 13
    DisconnectBtn.Parent = Pages.Settings
    
    local DCorner = Instance.new("UICorner") DCorner.CornerRadius = UDim.new(0, 4) DCorner.Parent = DisconnectBtn
    
    DisconnectBtn.MouseButton1Click:Connect(function()
        Config.IsRunning = false
        if delfile and isfile and isfile("Cyberpunks_Key.txt") then
            delfile("Cyberpunks_Key.txt")
        end
        -- Cleanup
        if ScreenGui then ScreenGui:Destroy() end
        if ESPFolder then ESPFolder:Destroy() end
        if MinimizedGui then MinimizedGui:Destroy() end
        if BroadcastGui then BroadcastGui:Destroy() end
        
        for _, conn in pairs(Connections) do if conn then conn:Disconnect() end end
        table.clear(Connections)
        
        if fakeChar then fakeChar:Destroy() end
        if moverPart then moverPart:Destroy() end
        if FOVCircle then pcall(function() FOVCircle:Remove() end) end
        
        if character and humanoid then
            humanoid.PlatformStand = false
            if rootPart then rootPart.Anchored = false end
        end
        
        if reachConnection then reachConnection:Disconnect() end
        if antiLavaConnection then antiLavaConnection:Disconnect() end
        if potatoConnection then potatoConnection:Disconnect() end
        pcall(function() RunService:UnbindFromRenderStep("AimLockPriority") end)
        
        if ShowKeySystem then ShowKeySystem() end
    end)
    end
    
    -- Attempt Auto Load on Startup
    task.wait(0.5)
    if isfile and isfile("Cyberpunks_Loader.json") and readfile then
        pcall(function()
            local data = HttpService:JSONDecode(readfile("Cyberpunks_Loader.json"))
            
            if data.DisableSnow ~= nil then
                Config.DisableSnow = data.DisableSnow
                if _G.SnowGui then
                    _G.SnowGui.Enabled = not Config.DisableSnow
                end
                if ToggleButtons["DisableSnow"] then
                    ToggleButtons["DisableSnow"].BackgroundColor3 = Config.DisableSnow and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
                end
            end
            
            -- Handle Auto Load Config
            if data then
                local savedAutoLoad = data.Enabled
                local savedConfigName = data.ConfigName

                if savedConfigName then
                    Config.AutoLoadConfigName = savedConfigName
                    if InputBoxes["AutoLoadConfigName"] then InputBoxes["AutoLoadConfigName"].Text = savedConfigName end
                end
                
                if savedAutoLoad ~= nil then
                    Config.AutoLoadConfig = savedAutoLoad
                    if ToggleButtons["AutoLoadConfig"] then 
                        ToggleButtons["AutoLoadConfig"].BackgroundColor3 = savedAutoLoad and CurrentThemeColor or Color3.fromRGB(40, 40, 50) 
                    end
                end
                
                if Config.AutoLoadConfig and Config.AutoLoadConfigName ~= "" then
                    LoadConfig(Config.AutoLoadConfigName)
                    
                    -- Restore Loader Settings (UI & Config)
                    if savedConfigName then
                        Config.AutoLoadConfigName = savedConfigName
                        if InputBoxes["AutoLoadConfigName"] then InputBoxes["AutoLoadConfigName"].Text = savedConfigName end
                    end
                    
                    if savedAutoLoad ~= nil then
                        Config.AutoLoadConfig = savedAutoLoad
                        if ToggleButtons["AutoLoadConfig"] then 
                            ToggleButtons["AutoLoadConfig"].BackgroundColor3 = savedAutoLoad and CurrentThemeColor or Color3.fromRGB(40, 40, 50) 
                        end
                    end
                    
                    SetupAutoLoad()
                end
            end
        end)
    end

    createSection(Pages.Credits, "Information", 1)
    createInfo(Pages.Credits, "Developed by SILVER", 2)
    createInfo(Pages.Credits, "Version: 10.7 Stable", 3)
    createInfo(Pages.Credits, "Status: Undetected", 4)

    -- Custom Page Content
    createSection(Pages.Custom, "Other Scripts", 1)

    do
        local UniversalGunBtn = Instance.new("TextButton")
        UniversalGunBtn.Size = UDim2.new(1, 0, 0, 30)
        UniversalGunBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        UniversalGunBtn.Text = "Universal Gun Script" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        UniversalGunBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        UniversalGunBtn.Font = Enum.Font.GothamBold
        UniversalGunBtn.TextSize = 10
        UniversalGunBtn.LayoutOrder = 1.1
        UniversalGunBtn.Parent = Pages.Custom
        local c3 = Instance.new("UICorner") c3.CornerRadius = UDim.new(0,4) c3.Parent = UniversalGunBtn
        UniversalGunBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/blackowl1231/Z3US/refs/heads/main/main.lua"))()
        end)
    end

    do
        local BladeBallBtn = Instance.new("TextButton")
        BladeBallBtn.Size = UDim2.new(1, 0, 0, 30)
        BladeBallBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        BladeBallBtn.Text = "Op Blade Ball" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        BladeBallBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        BladeBallBtn.Font = Enum.Font.GothamBold
        BladeBallBtn.TextSize = 10
        BladeBallBtn.LayoutOrder = 1.2
        BladeBallBtn.Parent = Pages.Custom
        local c4 = Instance.new("UICorner") c4.CornerRadius = UDim.new(0,4) c4.Parent = BladeBallBtn
        BladeBallBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/AgentX771/ArgonHubX/main/Loader.lua"))()
        end)
    end

    do
        local PastebinBtn = Instance.new("TextButton")
        PastebinBtn.Size = UDim2.new(1, 0, 0, 30)
        PastebinBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        PastebinBtn.Text = "Stalk Script" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        PastebinBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        PastebinBtn.Font = Enum.Font.GothamBold
        PastebinBtn.TextSize = 10
        PastebinBtn.LayoutOrder = 1.3
        PastebinBtn.Parent = Pages.Custom
        local c5 = Instance.new("UICorner") c5.CornerRadius = UDim.new(0,4) c5.Parent = PastebinBtn
        PastebinBtn.MouseButton1Click:Connect(function()
            loadstring(game:HttpGet("https://pastebin.com/raw/kDA875bF"))()
        end)
    end

    createSection(Pages.Custom, "Features", 2)
    do
        local Frame = Instance.new("Frame")
        Frame.Size = UDim2.new(1, 0, 0, 30)
        Frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        Frame.LayoutOrder = 2.1
        Frame.Parent = Pages.Custom

        local Corner = Instance.new("UICorner") Corner.CornerRadius = UDim.new(0, 3) Corner.Parent = Frame
        local Stroke = Instance.new("UIStroke") Stroke.Color = Color3.fromRGB(35, 35, 45) Stroke.Thickness = 1 Stroke.Parent = Frame

        local Label = Instance.new("TextLabel")
        Label.Text = "Skin Glitch (2.06)" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        Label.Size = UDim2.new(0.7, 0, 1, 0)
        Label.BackgroundTransparency = 1
        Label.TextColor3 = Color3.fromRGB(200, 200, 200)
        Label.Font = Enum.Font.GothamMedium
        Label.TextXAlignment = Enum.TextXAlignment.Left
        Label.TextSize = 9
        Label.Parent = Frame
        local Pad = Instance.new("UIPadding") Pad.PaddingLeft = UDim.new(0, 10) Pad.Parent = Label

        local Button = Instance.new("TextButton")
        Button.Size = UDim2.new(0, 20, 0, 20)
        Button.Position = UDim2.new(1, -30, 0.5, -10)
        Button.BackgroundColor3 = Config.WeaponExtender and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(40, 40, 50)
        Button.Text = ""
        Button.Parent = Frame
        local BCorner = Instance.new("UICorner") BCorner.CornerRadius = UDim.new(0, 4) BCorner.Parent = Button

        Button.MouseButton1Click:Connect(function()
            Config.WeaponExtender = not Config.WeaponExtender
            Button.BackgroundColor3 = Config.WeaponExtender and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(40, 40, 50)
            if Config.WeaponExtender then Config.WeaponLength = 2.06; SetWeaponSize(2.06) else RestoreWeaponSize() end
        end)
    end

    -- Removed Farm Nearest from Custom (Moved to FarmScripts)
    -- createSection(Pages.Custom, "Farm Nearest (OP)", 3)
    -- createToggle(Pages.Custom, "Enable Farm Nearest", "CustomFarmNearest", 3.1)
    
    task.spawn(function()
        local function TweenTo(targetPos)
            if character and rootPart and rootPart.Parent then
                local dist = (rootPart.Position - targetPos).Magnitude
                if dist < 5 then return end
                
                local speed = 300 
                local info = TweenInfo.new(dist / speed, Enum.EasingStyle.Linear)
                local tween = TweenService:Create(rootPart, info, {CFrame = CFrame.new(targetPos)})
                
                local noclipConn = RunService.Stepped:Connect(function()
                    if character then
                        for _, v in pairs(character:GetDescendants()) do
                            if v:IsA("BasePart") then v.CanCollide = false end
                        end
                    end
                end)
                
                tween:Play()
                
                while tween.PlaybackState == Enum.PlaybackState.Playing do
                    if not Config.IsRunning or not Config.CustomFarmNearest then
                        tween:Cancel()
                        break
                    end
                    if not character or not rootPart or not rootPart.Parent or (humanoid and humanoid.Health <= 0) then
                        tween:Cancel()
                        break
                    end
                    if (rootPart.Position - targetPos).Magnitude < 5 then
                         tween:Cancel()
                         break
                    end
                    task.wait(0.1)
                end
                
                if noclipConn then noclipConn:Disconnect() end
                if rootPart then rootPart.Velocity = Vector3.new(0,0,0) end
            end
        end

        while Config.IsRunning do
            if Config.CustomFarmNearest and character and rootPart then
                if not Config.FastM1 then
                    -- Config.FastM1 = true
                    -- if FastM1Btn then
                    --     FastM1Btn.Text = "STOP FAST M1"
                    --     FastM1Btn.TextColor3 = Color3.fromRGB(255, 50, 50)
                    -- end
                    -- if StartFastM1 then StartFastM1() end
                end
                
                local target = nil
                local minDist = 999999
                
                local function scan(folder)
                    for _, v in pairs(folder:GetChildren()) do
                        if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 and v:FindFirstChild("HumanoidRootPart") then
                            local dist = (rootPart.Position - v.HumanoidRootPart.Position).Magnitude
                            if dist < minDist then
                                minDist = dist
                                target = v
                            end
                        end
                    end
                end
                
                if workspace:FindFirstChild("Enemies") then scan(workspace.Enemies) end
                scan(workspace)
                
                if target then
                    local targetPos = target.HumanoidRootPart.Position + Vector3.new(0, 15, 0)
                    local currentPos = rootPart.Position
                    local dist = (currentPos - targetPos).Magnitude
                    
                    if dist > 15 then
                        TweenTo(targetPos)
                    else
                        rootPart.CFrame = CFrame.new(targetPos, target.HumanoidRootPart.Position)
                        rootPart.Velocity = Vector3.new(0,0,0)
                    end
                    
                    for _, v in pairs(character:GetDescendants()) do
                        if v:IsA("BasePart") then v.CanCollide = false end
                    end
                end
            end
            task.wait(0.05)
        end
    end)

    -- Removed Mastery Farm from Custom (Moved to FarmScripts)
    -- createSection(Pages.Custom, "Mastery Farm", 4)
    -- createToggle(Pages.Custom, "Enable Mastery Farm", "MasteryFarm", 4.1)
    
    if ToggleButtons["MasteryFarm"] then
        ToggleButtons["MasteryFarm"].MouseButton1Click:Connect(function()
            if Config.MasteryFarm and not Config.FastM1 then
                -- Config.FastM1 = true
                -- if FastM1Btn then
                --     FastM1Btn.Text = "STOP FAST M1"
                --     FastM1Btn.TextColor3 = Color3.fromRGB(255, 50, 50)
                -- end
                -- if StartFastM1 then StartFastM1() end
            end
        end)
    end

    -- Player Finder (Moved from Shop)
    createSection(Pages.Custom, "Player Finder (Whitelist)", 3)
    createInput(Pages.Custom, "Username", "JoinPlayerName", 3.1)
    
    do
        local FinderBtn = Instance.new("TextButton")
        FinderBtn.Size = UDim2.new(1, 0, 0, 30)
        FinderBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        FinderBtn.Text = "FIND PLAYER (BOT LINK)" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        FinderBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        FinderBtn.Font = Enum.Font.GothamBold
        FinderBtn.TextSize = 10
        FinderBtn.LayoutOrder = 3.2
        FinderBtn.Parent = Pages.Custom
        
        local FCorner = Instance.new("UICorner") FCorner.CornerRadius = UDim.new(0, 4) FCorner.Parent = FinderBtn
        
        local isFinding = false

        FinderBtn.MouseButton1Click:Connect(function()
            if not Config.IsWhitelisted then
                SendNotification("Whitelist required for Player Finder!", 3)
                return
            end

            if isFinding then
                isFinding = false
                FinderBtn.Text = "FIND PLAYER (BOT LINK)"
                FinderBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                SendNotification("Bot search stopped.", 2)
                return
            end

            local username = Config.JoinPlayerName
            if not username or username == "" then
                SendNotification("Please enter a username.", 3)
                return
            end
            
            local apiUrl = Config.BotApiUrl
            if not apiUrl or apiUrl == "" then
                SendNotification("Please enter your Bot API URL.", 3)
                return
            end
            
            isFinding = true
            FinderBtn.Text = "STOP BOT SEARCH"
            FinderBtn.TextColor3 = Color3.fromRGB(255, 50, 50)
            
            local requestFunc = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
            if not requestFunc then
                SendNotification("Executor does not support HTTP requests.", 4)
                isFinding = false
                return
            end
            
            task.spawn(function()
                -- 1. Send Search Request to Bot
                SendNotification("Sending request to Bot...", 2)
                local success, response = pcall(function()
                    return requestFunc({
                        Url = apiUrl .. "/search",
                        Method = "POST",
                        Headers = {["Content-Type"] = "application/json"},
                        Body = HttpService:JSONEncode({username = username})
                    })
                end)
                
                if not success or not response or response.StatusCode ~= 200 then
                    SendNotification("Failed to contact Bot.", 3)
                    isFinding = false
                    FinderBtn.Text = "FIND PLAYER (BOT LINK)"
                    FinderBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                    return
                end
                
                SendNotification("Bot is searching for " .. username .. "...", 3)
                
                -- 2. Poll for Status
                while isFinding and Config.IsRunning do
                    local pSuccess, pResponse = pcall(function()
                        return requestFunc({
                            Url = apiUrl .. "/status?username=" .. username,
                            Method = "GET"
                        })
                    end)
                    
                    if pSuccess and pResponse and pResponse.StatusCode == 200 then
                        local data = HttpService:JSONDecode(pResponse.Body)
                        if data.status == "found" and data.jobId then
                            if data.jobId == game.JobId then
                                SendNotification("You are already in the correct server!", 5)
                                isFinding = false
                                FinderBtn.Text = "FIND PLAYER (BOT LINK)"
                                FinderBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                                break
                            end
                            
                            isFinding = false
                            FinderBtn.Text = "FIND PLAYER (BOT LINK)"
                            FinderBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                            
                            local targetPlaceId = data.placeId or game.PlaceId
                            CreateConfirmationPrompt("Player Found!\nServer ID: " .. data.jobId, data.jobId, targetPlaceId, function()
                                local success, err = pcall(function()
                                    game:GetService("TeleportService"):TeleportToPlaceInstance(targetPlaceId, tostring(data.jobId), player)
                                end)
                                if not success then SendNotification("Teleport failed. Please retry.", 4) end
                            end, function()
                                SendNotification("Join cancelled.", 3)
                            end)
                            
                            break
                        elseif data.status == "error" then
                            SendNotification("Bot Error: " .. (data.message or "Unknown"), 3)
                            break
                        end
                    end
                    task.wait(3) -- Poll every 3 seconds
                end
            end)
        end)

    end

    do
        local ScannerBtn = Instance.new("TextButton")
        ScannerBtn.Size = UDim2.new(1, 0, 0, 30)
        ScannerBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        ScannerBtn.Text = "JOIN PLAYER (JOINS OFF)" .. (Config.IsWhitelisted and "" or " ðŸ”’")
        ScannerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        ScannerBtn.Font = Enum.Font.GothamBold
        ScannerBtn.TextSize = 10
        ScannerBtn.LayoutOrder = 3.3
        ScannerBtn.Parent = Pages.Custom
        
        local SCorner = Instance.new("UICorner") SCorner.CornerRadius = UDim.new(0, 4) SCorner.Parent = ScannerBtn
        
        local isScanning = false

        ScannerBtn.MouseButton1Click:Connect(function()
            if not Config.IsWhitelisted then
                SendNotification("Whitelist required for Player Finder!", 3)
                return
            end

            if isScanning then
                isScanning = false
                ScannerBtn.Text = "JOIN PLAYER (JOINS OFF)"
                ScannerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                SendNotification("Scanner stopped.", 2)
                return
            end

            local username = Config.JoinPlayerName
            if not username or username == "" then
                SendNotification("Please enter a username.", 3)
                return
            end
            
            local requestFunc = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
            if not requestFunc then
                SendNotification("Executor does not support HTTP requests.", 4)
                return
            end
            
            isScanning = true
            ScannerBtn.Text = "STOP SCANNING"
            ScannerBtn.TextColor3 = Color3.fromRGB(255, 50, 50)
            
            task.spawn(function()
                -- 1. Get User ID
                local userId
                local uSuccess = pcall(function() userId = Players:GetUserIdFromNameAsync(username) end)
                if not uSuccess or not userId then
                    SendNotification("User not found.", 3)
                    isScanning = false
                    ScannerBtn.Text = "JOIN PLAYER (JOINS OFF)"
                    ScannerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                    return
                end
                
                SendNotification("Fetching target thumbnails...", 2)
                
                -- 2. Get Target Thumbnail Hash (OPTIMIZED & RESILIENT)
                local targetHash = ""
                local function getThumbHash(uId)
                    local urls = {
                        "https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds=" .. uId .. "&size=48x48&format=Png&isCircular=false",
                        "https://thumbnails.roblox.com/v1/users/avatar-bust?userIds=" .. uId .. "&size=48x48&format=Png&isCircular=false"
                    }
                    
                    for _, url in pairs(urls) do
                        -- Try 1: game:HttpGet (Standard)
                        local s, body = pcall(function() return game:HttpGet(url) end)
                        
                        -- Try 2: request with Cookie if HttpGet failed
                        if (not s or not body) and requestFunc then
                            local rS, rR = pcall(function()
                                return requestFunc({
                                    Url = url,
                                    Method = "GET",
                                    Headers = {}
                                })
                            end)
                            if rS and rR.StatusCode == 200 then body = rR.Body end
                        end
                        
                        if body then
                            local d = HttpService:JSONDecode(body)
                            if d and d.data and d.data[1] and d.data[1].imageUrl then
                                local hash = d.data[1].imageUrl:match("rbxcdn%.com/([%w%-]+)")
                                if hash then return hash end
                            end
                        end
                    end
                    return nil
                end

                targetHash = getThumbHash(userId)
                
                if not targetHash then
                    SendNotification("Failed to get player thumbnail.", 3)
                    isScanning = false
                    ScannerBtn.Text = "JOIN PLAYER (JOINS OFF)"
                    ScannerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                    return
                end

                SendNotification("Deep scanning all Seas...", 3)
                
                local bfSeas = {2753915549, 4442272183, 7449423635}
                local placesToScan = {game.PlaceId}
                local isBF = false
                for _, id in pairs(bfSeas) do if game.PlaceId == id then isBF = true break end end
                if isBF then placesToScan = bfSeas end

                for _, placeId in pairs(placesToScan) do
                    if not isScanning or not Config.IsRunning then break end
                    
                    local seaName = (placeId == 2753915549 and "Sea 1") or (placeId == 4442272183 and "Sea 2") or (placeId == 7449423635 and "Sea 3") or "Game"
                    SendNotification("Deep Scan: " .. seaName .. "...", 2)
                    
                    local cursor = ""
                    local serversChecked = 0
                    
                    while isScanning and Config.IsRunning and cursor ~= nil do
                        -- Utilisation de game:HttpGet pour utiliser la session actuelle (Cookie)
                        local sSuccess, sBody = pcall(function()
                            return game:HttpGet("https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?limit=100&cursor=" .. (cursor or ""))
                        end)
                        
                        if not sSuccess or not sBody then 
                            -- Fallback vers request si HttpGet Ã©choue
                            local rS, rR = pcall(function()
                                return requestFunc({
                                    Url = "https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?limit=100&cursor=" .. (cursor or ""),
                                    Method = "GET",
                                    Headers = {}
                                })
                            end)
                            if rS and rR.StatusCode == 200 then sBody = rR.Body else break end
                        end

                        local sData = HttpService:JSONDecode(sBody)
                        if not sData or not sData.data then break end
                        cursor = sData.nextPageCursor
                        
                        local currentBatch = {}
                        local serverMap = {}
                        
                        for _, server in pairs(sData.data) do
                            if not isScanning or not Config.IsRunning then break end
                            if server.playerTokens then
                                for _, token in pairs(server.playerTokens) do
                                    table.insert(currentBatch, {
                                        requestId = token,
                                        type = "AvatarHeadShot",
                                        token = token,
                                        size = "48x48",
                                        format = "png"
                                    })
                                    serverMap[token] = server.id
                                end
                            end
                            
                            serversChecked = serversChecked + 1
                            ScannerBtn.Text = seaName .. " | " .. serversChecked
                            
                            if #currentBatch >= 90 or _ == #sData.data then
                                local bSuccess, bResponse = pcall(function()
                                    return requestFunc({
                                        Url = "https://thumbnails.roblox.com/v1/batch",
                                        Method = "POST",
                                        Headers = {
                                            ["Content-Type"] = "application/json"
                                        },
                                        Body = HttpService:JSONEncode(currentBatch)
                                    })
                                end)
                                
                                if bSuccess and bResponse.StatusCode == 200 then
                                    local bData = HttpService:JSONDecode(bResponse.Body)
                                    if bData and bData.data then
                                        for _, thumb in pairs(bData.data) do
                                            local thumbHash = thumb.imageUrl and thumb.imageUrl:match("rbxcdn%.com/([%w%-]+)")
                                            if thumbHash and thumbHash == targetHash then
                                                local foundJobId = serverMap[thumb.requestId]
                                                isScanning = false
                                                ScannerBtn.Text = "JOIN PLAYER (JOINS OFF)"
                                                ScannerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                                                
                                                CreateConfirmationPrompt("Player Found in " .. seaName .. "!\nServer ID: " .. foundJobId, foundJobId, placeId, function()
                                                    game:GetService("TeleportService"):TeleportToPlaceInstance(placeId, foundJobId, player)
                                                end, function()
                                                    SendNotification("Join cancelled.", 3)
                                                end)
                                                return
                                            end
                                        end
                                    end
                                end
                                table.clear(currentBatch)
                                table.clear(serverMap)
                                task.wait()
                            end
                        end
                        task.wait(0.01)
                    end
                end
                
                if isScanning then
                    SendNotification("Player not found.", 5)
                end
                
                isScanning = false
                ScannerBtn.Text = "JOIN PLAYER (JOINS OFF)"
                ScannerBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            end)
        end)
    end

    -- ==================================================================
    --                          LOGIQUE DU CHEAT
    -- ==================================================================

    -- === LOOPS ===

    -- HEARTBEAT LOOP (PHYSICS OVERRIDE)
    table.insert(Connections, RunService.Heartbeat:Connect(function(deltaTime)
    end))
    
    
    -- STEPPED LOOP (NO CLIP LOGIC)
    table.insert(Connections, RunService.Stepped:Connect(function()
        if Config.NoClip and character then
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") and part.CanCollide then
                    part.CanCollide = false
                end
            end
        end
        
        if Config.AntiStun and character and humanoid and rootPart then
            humanoid.PlatformStand = false
            humanoid.Sit = false
            
            local speed = Config.WalkSpeedValue
            if speed < 16 then speed = 16 end -- Force minimum speed
            if humanoid.WalkSpeed < speed then humanoid.WalkSpeed = speed end
            
            if humanoid.JumpPower < Config.JumpPowerValue then humanoid.JumpPower = Config.JumpPowerValue end
            if rootPart.Anchored then rootPart.Anchored = false end -- Force Unanchor
            
            character:SetAttribute("Stun", 0)
            character:SetAttribute("Busy", false)
            character:SetAttribute("CantMove", false)
            character:SetAttribute("Stunned", false)
            
            -- Remove Stun BodyMovers
            for _, v in pairs(rootPart:GetChildren()) do
                if (v:IsA("BodyVelocity") or v:IsA("BodyGyro") or v:IsA("BodyPosition")) and v.Name ~= "FlyVelocity" and v.Name ~= "FlyGyro" and v.Name ~= "FarmVelocity" then
                    v:Destroy()
                end
            end
        end
        
        if Config.AntiChair and character and humanoid then
            local isSittingOnCreated = false
            if humanoid.SeatPart and humanoid.SeatPart.Name == "CreatedChair" then
                isSittingOnCreated = true
            end

            if not isSittingOnCreated then
                humanoid.Sit = false
                humanoid.PlatformStand = false
                -- Disable seated state completely
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
                -- Check if character is in a seat and force exit
                if character.Parent and character:FindFirstChild("HumanoidRootPart") then
                    local seat = character.HumanoidRootPart:FindFirstChild("SeatWeld")
                    if seat and seat.Part1 and seat.Part1.Name ~= "CreatedChair" then
                        seat:Destroy()
                    end
                    -- Check for vehicle seat
                    for _, v in pairs(workspace:GetChildren()) do
                        if (v:IsA("VehicleSeat") or v:IsA("Seat")) and v.Occupant == humanoid and v.Name ~= "CreatedChair" then
                            humanoid:Sit(nil)
                        end
                    end
                end
            else
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
            end
        else
            -- Re-enable seated state when AntiChair is disabled
            if humanoid then
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
            end
        end
        
        -- Infinite Energy Logic
        if Config.InfiniteEnergy and character and character:FindFirstChild("Energy") then
            pcall(function()
                character.Energy.Value = character.Energy.MaxValue
            end)
        end
    end))

    -- === FAST ATTACK / KILL AURA SYSTEM ===
    local function AttackFunction()
        local character = player.Character
        local tool = character and character:FindFirstChildOfClass("Tool")
        local humanoid = character and character:FindFirstChild("Humanoid")
        
        if tool then
            -- Bypass attack animations
            if Config.NoCooldown and humanoid then
                pcall(function()
                    for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
                        if track.Name:lower():find("attack") or track.Name:lower():find("slash") or track.Name:lower():find("swing") then
                            track:Stop()
                        end
                    end
                end)
            end

            -- Spam M1 Remotes for Blox Fruits (Nuclear Fast Attack)
            task.spawn(function()
                pcall(function()
                    local ReplicatedStorage = game:GetService("ReplicatedStorage")
                    local remotes = ReplicatedStorage:FindFirstChild("Remotes")
                    
                    if remotes and remotes:FindFirstChild("Validator") then
                        remotes.Validator:FireServer(math.floor(tick() * 1000))
                    end
                    
                    if tool:FindFirstChild("RemoteEvent") then
                        tool.RemoteEvent:FireServer("Attack")
                    end
                    
                    tool:Activate()
                end)
            end)
        end
    end

    -- DAMAGE FIX LOOP (Validator Spam for Ghost Hits)
    task.spawn(function()
        while Config.IsRunning do
            if (Config.Reach or Config.HitboxExpander) and UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1) then
                pcall(function()
                    local ReplicatedStorage = game:GetService("ReplicatedStorage")
                    if ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("Validator") then
                        ReplicatedStorage.Remotes.Validator:FireServer(math.floor(tick() * 1000))
                    end
                end)
            end
            task.wait(0.1)
        end
    end)

    local function EquipSelectedWeapon()
        if not player.Character or not player.Character:FindFirstChild("Humanoid") then return end
        local humanoid = player.Character.Humanoid
        local backpack = player:FindFirstChild("Backpack")
        
        if Config.SelectedWeapon then
            local toolInHand = player.Character:FindFirstChild(Config.SelectedWeapon)
            if not toolInHand and backpack then
                local tool = backpack:FindFirstChild(Config.SelectedWeapon)
                if tool then
                    humanoid:EquipTool(tool)
                end
            end
        end
    end

    -- DEDICATED KILL AURA LOOP (NUCLEAR SPAM)
    local lastCooldownScan = 0
    task.spawn(function()
        while Config.IsRunning do
            -- 1. No Cooldown Logic (Aggressive & Optimized)
            if Config.NoCooldown then
                local currentTime = tick()
                
                -- Scan every 0.1 seconds for better responsiveness
                if currentTime - lastCooldownScan > 0.1 then
                    lastCooldownScan = currentTime
                    
                    local character = player.Character
                    local backpack = player:FindFirstChild("Backpack")
                    
                    local function wipeCooldowns(container)
                        if not container then return end
                        for _, v in pairs(container:GetDescendants()) do
                            if v:IsA("ValueBase") then
                                local vName = v.Name:lower()
                                if vName:find("cooldown") or vName:find("cd") or vName:find("skill") or vName:find("timer") or vName:find("ability") or vName:find("stun") or vName:find("busy") or vName:find("using") then
                                    pcall(function() 
                                        if v:IsA("StringValue") then v.Value = "" 
                                        elseif v:IsA("BoolValue") then v.Value = false
                                        else v.Value = 0 end 
                                    end)
                                end
                            end
                        end
                    end

                    wipeCooldowns(character)
                    wipeCooldowns(backpack)
                    
                    -- Handle Attributes more aggressively
                    if character then
                        for _, attr in pairs({"Stun", "Busy", "Cooldown", "AbilityCooldown", "SkillCooldown", "LastUse", "NextUse", "Using", "Casting", "Attacking"}) do
                            pcall(function() character:SetAttribute(attr, 0) end)
                            pcall(function() character:SetAttribute(attr, false) end)
                        end
                    end
                    
                    if backpack then
                        for _, tool in pairs(backpack:GetChildren()) do
                            if tool:IsA("Tool") then
                                for _, attr in pairs({"Cooldown", "SkillCooldown", "AbilityCooldown", "LastUse", "NextUse"}) do
                                    pcall(function() tool:SetAttribute(attr, 0) end)
                                end
                            end
                        end
                    end
                    
                    -- UI Cooldown Hiding
                    pcall(function()
                        local playerGui = player:FindFirstChild("PlayerGui")
                        if playerGui then
                            for _, v in pairs(playerGui:GetDescendants()) do
                                if (v:IsA("Frame") or v:IsA("ImageLabel") or v:IsA("TextLabel")) then
                                    local vName = v.Name:lower()
                                    if vName:find("cooldown") or vName:find("cd") or vName:find("timer") then
                                        -- Skip cheat GUI
                                        if not (vName:find("cyber") or vName:find("desync") or vName:find("esp")) then
                                            v.Visible = false
                                        end
                                    end
                                end
                            end
                        end
                    end)
                end
            end

            -- 2. Force Attack & Skill Spam (Aggressive Nuclear Mode)
            if Config.NoCooldown and player.Character then
                local tool = player.Character:FindFirstChildOfClass("Tool")
                if tool then
                    -- Force activate and fire attack events
                    pcall(function()
                        tool:Activate()
                        if tool:FindFirstChild("RemoteEvent") then
                            for i = 1, 3 do
                                tool.RemoteEvent:FireServer("Attack")
                            end
                        end
                    end)
                    
                    -- Spam common skill remotes (Blox Fruits / RPG styles)
                    pcall(function()
                        local remotes = game:GetService("ReplicatedStorage"):FindFirstChild("Remotes") or game:GetService("ReplicatedStorage")
                        local commF = remotes:FindFirstChild("CommF_")
                        
                        if commF then
                            local skills = {"Z", "X", "C", "V", "F"}
                            for _, key in ipairs(skills) do
                                task.spawn(function()
                                    commF:InvokeServer("Skill", key, true)
                                    commF:InvokeServer("Skill", key, false)
                                end)
                            end
                        end
                    end)
                end
            end

            -- 3. Kill Aura Logic
            if Config.KillAura and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local myRoot = player.Character.HumanoidRootPart
                -- Auto Equip Weapon
                EquipSelectedWeapon()
                if not player.Character:FindFirstChildOfClass("Tool") and player:FindFirstChild("Backpack") then
                    local toolToEquip = nil
                    for _, t in pairs(player.Backpack:GetChildren()) do
                        if t:IsA("Tool") then
                            local n = t.Name:lower()
                            if n:find("sword") or n:find("blade") or n:find("katana") or n:find("saber") or n:find("cutlass") or n:find("cdk") or n:find("yru") or n:find("trident") or n:find("melee") or n:find("combat") then
                                toolToEquip = t
                                break
                            end
                        end
                    end
                    if not toolToEquip then toolToEquip = player.Backpack:FindFirstChildOfClass("Tool") end
                    if toolToEquip and player.Character:FindFirstChild("Humanoid") then player.Character.Humanoid:EquipTool(toolToEquip) end
                end

                local target = nil
                local minDist = Config.AuraRange
                
                -- Target Selection
                if Config.EasyLock and Config.TargetName ~= "None" then
                    local lockedPlr = Players:FindFirstChild(Config.TargetName)
                    if lockedPlr and lockedPlr.Character and lockedPlr.Character:FindFirstChild("Humanoid") and lockedPlr.Character.Humanoid.Health > 0 then
                        target = lockedPlr.Character
                    end
                end

                if not target then
                    -- Scan Players
                    for _, p in pairs(Players:GetPlayers()) do
                        if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                            local isEnemy = true
                            if Config.TeamCheck and p.Team and player.Team and p.Team == player.Team then isEnemy = false end
                            
                            if isEnemy then
                                local dist = (myRoot.Position - p.Character.HumanoidRootPart.Position).Magnitude
                                if dist < minDist then
                                    minDist = dist
                                    target = p.Character
                                end
                            end
                        end
                    end
                    -- Scan NPCs
                    local function scan(folder)
                        if not folder then return end
                        for _, v in pairs(folder:GetChildren()) do
                            if v:IsA("Model") and v ~= player.Character and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 then
                                if not Players:GetPlayerFromCharacter(v) then
                                    local dist = (myRoot.Position - v.HumanoidRootPart.Position).Magnitude
                                    if dist < minDist then
                                        minDist = dist
                                        target = v
                                    end
                                end
                            end
                        end
                    end
                    scan(workspace)
                    scan(workspace:FindFirstChild("Enemies"))
                end

                -- Attack Execution
                if target and target:FindFirstChild("HumanoidRootPart") then
                    local targetRoot = target.HumanoidRootPart
                    -- Aggressive Fast Attack (Spam M1)
                    for i = 1, 15 do
                        AttackFunction()
                    end
                    
                    -- Hitbox Extender for Aura
                    if firetouchinterest then
                        firetouchinterest(myRoot, targetRoot, 0)
                        firetouchinterest(myRoot, targetRoot, 1)
                    end
                end
            end
            task.wait(0.01) -- High Frequency
        end
    end)
    
    -- ANTI LAVA / DAMAGE BLOCK LOGIC
    task.spawn(function()
        local wasEnabled = false
        while Config.IsRunning do
            if Config.AntiLava then
                if not wasEnabled then
                    wasEnabled = true
                    -- FFlag Method (User Request)
                    pcall(function()
                        if setfflag then
                            setfflag("DFIntTouchSenderMaxBandwidthBpsScaling", "-1")
                        end
                    end)
                    
                    -- Scan specific zones (Optimized to prevent freeze)
                    task.spawn(function()
                        local zones = {
                            Vector3.new(-5191.37, 177.50, -5503.91),
                            Vector3.new(913.16, 249.09, 32869.20)
                        }
                        local radius = 4000 -- Large radius to cover islands/dungeons

                        for _, center in ipairs(zones) do
                            if not Config.AntiLava then break end
                            local parts = workspace:GetPartBoundsInRadius(center, radius)
                            for i, part in ipairs(parts) do
                                if not Config.AntiLava then break end
                                disableDamageBlock(part)
                                if i % 500 == 0 then task.wait() end
                            end
                        end
                    end)
                    
                    -- Listen for new parts (StreamingEnabled / Raids)
                    if not antiLavaConnection then
                        antiLavaConnection = Workspace.DescendantAdded:Connect(disableDamageBlock)
                    end
                end
            else
                if wasEnabled then
                    wasEnabled = false
                    if antiLavaConnection then
                        antiLavaConnection:Disconnect()
                        antiLavaConnection = nil
                    end
                end
            end
            task.wait(1)
        end
    end)
    
    -- WALK ON WATER LOOP
    task.spawn(function()
        local platform = Instance.new("Part")
        platform.Name = "WaterPlatform"
        platform.Size = Vector3.new(20, 1, 20)
        platform.Transparency = 1 -- Invisible
        platform.Anchored = true
        platform.CanCollide = true
        
        while Config.IsRunning do
            if Config.WalkWater and character and rootPart then
                local waterY = -1.36
                if math.abs(rootPart.Position.Y - waterY) < 20 then
                    platform.Parent = workspace
                    platform.CFrame = CFrame.new(rootPart.Position.X, waterY, rootPart.Position.Z)
                else
                    platform.Parent = nil
                end
            else
                platform.Parent = nil
            end
            task.wait(0.05)
        end
        if platform then platform:Destroy() end
    end)

    -- AUTO GACHA LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.AutoGacha then
                local args = "Buy"
                local npc = nil
                if workspace:FindFirstChild("NPCs") then
                    npc = workspace.NPCs:FindFirstChild("Blox Fruit Gacha")
                end
                if not npc then
                    for _, v in pairs(workspace:GetDescendants()) do
                        if v.Name == "Blox Fruit Gacha" or v.Name == "Blox Fruit Dealer Cousin" or v.Name == "Zioles" then
                            npc = v
                            break
                        end
                    end
                end
                
                if npc and rootPart then
                    rootPart.CFrame = npc.HumanoidRootPart.CFrame * CFrame.new(0,0,3)
                    task.wait(0.5)
                    game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("Cousin", "Buy")
                end
            end
            task.wait(4)
        end
    end)
    
    -- AUTO STORE FRUIT LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.AutoStoreFruit then
                for _, tool in pairs(player.Backpack:GetChildren()) do
                    if tool:IsA("Tool") and (tool.ToolTip == "Blox Fruit" or tool.Name:find("Fruit") or tool:FindFirstChild("FruitLogic")) then
                        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("StoreFruit", tool:GetAttribute("OriginalName") or (tool.Name:gsub(" Fruit", "")), tool)
                    end
                end
                if character then
                    for _, tool in pairs(character:GetChildren()) do
                        if tool:IsA("Tool") and (tool.ToolTip == "Blox Fruit" or tool.Name:find("Fruit") or tool:FindFirstChild("FruitLogic")) then
                            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("StoreFruit", tool:GetAttribute("OriginalName") or (tool.Name:gsub(" Fruit", "")), tool)
                        end
                    end
                end
            end
            task.wait(1)
        end
    end)
    
    -- AUTO GRAB FRUIT LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.AutoGrabFruit and character and rootPart then
                -- Scan entire map (GetDescendants)
                for _, v in pairs(workspace:GetDescendants()) do
                    if v:IsA("Tool") and (v.ToolTip == "Blox Fruit" or v.Name:find("Fruit")) then
                        local handle = v:FindFirstChild("Handle")
                        if handle then
                            rootPart.CFrame = handle.CFrame
                            firetouchinterest(rootPart, handle, 0)
                            firetouchinterest(rootPart, handle, 1)
                            break -- Grab one by one to avoid crash
                        end
                    end
                end
            end
            task.wait(1)
        end
    end)
    
    -- NO ANIMATION LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.NoAnim and humanoid then
                for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
                    if track.Priority == Enum.AnimationPriority.Idle or track.Priority == Enum.AnimationPriority.Movement or track.Priority == Enum.AnimationPriority.Core then
                        track:Stop()
                    end
                end
            end
            task.wait()
        end
    end)
    
    -- TRIGGER BOT LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.TriggerBot then
                local shouldShoot = false
                local targetScreenPos = nil
                
                -- 1. CHECK SILENT AIM TARGET (IN FOV)
                if Config.SilentAim then
                    local target = getClosestPlayerToMouse(Config.SilentFOV, true)
                    if target and target.Character then
                        local targetPart = target.Character:FindFirstChild("Head") or target.Character:FindFirstChild("HumanoidRootPart")
                        if targetPart then
                            local screenPos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                            if onScreen then
                                shouldShoot = true
                                targetScreenPos = Vector2.new(screenPos.X, screenPos.Y)
                            end
                        end
                    end
                elseif not shouldShoot then
                    -- 2. CHECK MOUSE TARGET (FALLBACK)
                    local target = mouse.Target
                    if target and target.Parent then
                        local model = target.Parent
                        if model ~= character then
                            local hum = model:FindFirstChild("Humanoid")
                            if hum and hum.Health > 0 then
                                local root = model:FindFirstChild("HumanoidRootPart") or model:FindFirstChild("Head")
                                if root then
                                    local dist = (root.Position - camera.CFrame.Position).Magnitude
                                    if dist <= Config.TriggerDistance then
                                        shouldShoot = true
                                    end
                                end
                            end
                        end
                    end
                end
                
                -- 4. FIRE
                if shouldShoot then
                    NuclearClick(targetScreenPos)
                end
            end
            task.wait()
        end
    end)
    
    -- REMOVE FOG LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.RemoveFog then
                Lighting.FogEnd = 9e9
                Lighting.FogStart = 9e9
                for _, v in pairs(Lighting:GetChildren()) do
                    if v:IsA("Atmosphere") then v:Destroy() end
                end
            end
            task.wait(1)
        end
    end)
    
    -- AUTO RAID LOOP
    task.spawn(function()
        local wasAutoRaid = false
        local LastRaidTargetTime = 0
        
        while Config.IsRunning do
            if Config.AutoRaid then
                wasAutoRaid = true
                
                -- Enable Anti Lava
                if not Config.AntiLava then
                    Config.AntiLava = true
                    if ToggleButtons["AntiLava"] then ToggleButtons["AntiLava"].BackgroundColor3 = CurrentThemeColor end
                end
                
                pcall(function()
                    if character and rootPart and humanoid then
                        local closest = nil
                        local minDist = math.huge
                        local scanRange = 999999 -- Increased Range
                        
                        if workspace:FindFirstChild("Enemies") then
                            for _, v in pairs(workspace.Enemies:GetChildren()) do
                                if v:FindFirstChild("Humanoid") and v.Humanoid.Health > 0 and v:FindFirstChild("HumanoidRootPart") then
                                    local dist = (rootPart.Position - v.HumanoidRootPart.Position).Magnitude
                                    if dist < minDist and dist < scanRange then
                                        minDist = dist
                                        closest = v
                                    end
                                end
                            end
                        end
                        
                        if closest then
                            LastRaidTargetTime = os.clock()
                            local targetHRP = closest.HumanoidRootPart
                            
                            -- TP
                            rootPart.CFrame = targetHRP.CFrame * CFrame.new(0, 20, 0) * CFrame.Angles(math.rad(-90), 0, 0)
                            
                            -- Hitbox
                            rootPart.Size = Vector3.new(25, 25, 25)
                            rootPart.Transparency = 1
                            rootPart.CanCollide = false
                            
                            -- Equip
                            EquipSelectedWeapon()
                            local tool = character:FindFirstChildOfClass("Tool")
                            if not tool then
                                local bp = player.Backpack
                                if bp then
                                    local t = bp:FindFirstChildOfClass("Tool")
                                    if t then humanoid:EquipTool(t) end
                                end
                            end
                            
                            -- Click
                            if tool then tool:Activate() end
                            VirtualInputManager:SendMouseButtonEvent(camera.ViewportSize.X/2, camera.ViewportSize.Y/2, 0, true, game, 1)
                            VirtualInputManager:SendMouseButtonEvent(camera.ViewportSize.X/2, camera.ViewportSize.Y/2, 0, false, game, 1)
                            
                            rootPart.Velocity = Vector3.new(0,0,0)
                            rootPart.AssemblyLinearVelocity = Vector3.new(0,0,0)
                        else
                            -- No target found: Move Forward
                            if os.clock() - LastRaidTargetTime > 5 then
                                humanoid.PlatformStand = true
                                rootPart.Velocity = Vector3.new(0,0,0)
                                rootPart.AssemblyLinearVelocity = Vector3.new(0,0,0)
                                
                                -- Move forward relative to character facing
                                rootPart.CFrame = rootPart.CFrame * CFrame.new(0, 0, -5)
                                
                                for _, v in pairs(character:GetDescendants()) do
                                    if v:IsA("BasePart") then v.CanCollide = false end
                                end
                            end
                        end
                    end
                end)
            else
                if wasAutoRaid then
                    wasAutoRaid = false
                    LastRaidTargetTime = os.clock()
                    
                    -- Reset Character
                    if character and rootPart then
                        rootPart.Size = Vector3.new(2, 2, 1)
                        rootPart.Transparency = 1
                        rootPart.CanCollide = true
                        if humanoid then
                            humanoid.PlatformStand = false
                            humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
                        end
                    end
                else
                    LastRaidTargetTime = os.clock()
                end
            end
            task.wait()
        end
    end)
    
    -- SEA EVENT LOGIC (Leviathan & Boat)
    task.spawn(function()
        local TikiOutpostPos = Vector3.new(-16913.67, 9.40, 510.33)
        local Zone6Position = Vector3.new(-40000, 10, -5000)
        
        local function TweenTo(targetPos)
            if character and rootPart and rootPart.Parent then
                local dist = (rootPart.Position - targetPos).Magnitude
                if dist < 50 then return end
                
                local speed = 350
                local info = TweenInfo.new(dist / speed, Enum.EasingStyle.Linear)
                local tween = TweenService:Create(rootPart, info, {CFrame = CFrame.new(targetPos)})
                
                local conn = RunService.Stepped:Connect(function()
                    if character then
                        for _, v in pairs(character:GetDescendants()) do
                            if v:IsA("BasePart") then v.CanCollide = false end
                        end
                    end
                end)
                
                tween:Play()
                
                -- Wait for completion or cancellation
                while tween.PlaybackState == Enum.PlaybackState.Playing do
                    if not Config.IsRunning or (not Config.AutoFindLeviathan and not Config.AutoBuyBoat) then
                        tween:Cancel()
                        break
                    end
                    if not character or not rootPart or not rootPart.Parent or (humanoid and humanoid.Health <= 0) then
                        tween:Cancel()
                        break
                    end
                    task.wait(0.1)
                end
                
                if conn then conn:Disconnect() end
            end
        end
        
        local function BuyAndSit()
            if not rootPart or not humanoid then return end
            -- 1. Go to Tiki
            if (rootPart.Position - TikiOutpostPos).Magnitude > 500 then
                TweenTo(TikiOutpostPos)
            end
            rootPart.CFrame = CFrame.new(TikiOutpostPos)
            task.wait(0.5)
            
            -- 2. Buy
            game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("BuyBoat", Config.SelectedBoat)
            task.wait(1.5)
            
            -- 3. Sit
            local seatFound = nil
            local searchList = workspace:GetChildren()
            if workspace:FindFirstChild("Boats") then searchList = workspace.Boats:GetChildren() end
            
            for _, v in pairs(searchList) do
                if (v.Name:find("Boat") or v.Name == Config.SelectedBoat) then
                    local seat = v:FindFirstChild("VehicleSeat") or v:FindFirstChild("DriverSeat") or v:FindFirstChild("Seat")
                    if seat and (seat.Position - rootPart.Position).Magnitude < 200 then
                        seatFound = seat
                        break
                    end
                end
            end
            
            if seatFound and rootPart and humanoid then
                rootPart.CFrame = seatFound.CFrame + Vector3.new(0, 3, 0)
                task.wait(0.2)
                seatFound:Sit(humanoid)
            end
        end

        while Config.IsRunning do
            pcall(function()
                -- AUTO BUY BOAT ONLY
                if Config.AutoBuyBoat then
                    if character and rootPart and humanoid and humanoid.Health > 0 then
                        if not humanoid.SeatPart then
                            BuyAndSit()
                        end
                    end
                    Config.AutoBuyBoat = false
                    if ToggleButtons["AutoBuyBoat"] then ToggleButtons["AutoBuyBoat"].BackgroundColor3 = Color3.fromRGB(40, 40, 50) end
                end
                
                -- AUTO FIND LEVIATHAN (FULL SEQUENCE)
                if Config.AutoFindLeviathan then
                    if character and rootPart and humanoid and humanoid.Health > 0 then
                        -- 1. Buy/Sit if not seated
                        if not humanoid.SeatPart then
                            BuyAndSit()
                        end
                        
                        -- 2. Drive to Zone 6
                        if humanoid.SeatPart then
                            local vehicle = humanoid.SeatPart.Parent
                            local primary = vehicle.PrimaryPart or humanoid.SeatPart
                            
                            local dist = (primary.Position - Zone6Position).Magnitude
                            if dist > 500 then
                                local info = TweenInfo.new(dist / 350, Enum.EasingStyle.Linear)
                                local tween = TweenService:Create(primary, info, {CFrame = CFrame.new(Zone6Position)})
                                
                                local noclipConn = RunService.Stepped:Connect(function()
                                    for _, v in pairs(vehicle:GetDescendants()) do
                                        if v:IsA("BasePart") then 
                                            v.CanCollide = false 
                                            v.Transparency = 0.5
                                        end
                                    end
                                end)
                                
                                tween:Play()
                                
                                while tween.PlaybackState == Enum.PlaybackState.Playing do
                                    if not Config.AutoFindLeviathan then tween:Cancel() break end
                                    if workspace.Enemies:FindFirstChild("Frozen Watcher") then tween:Cancel() break end
                                    if not humanoid.SeatPart then tween:Cancel() break end
                                    task.wait(0.1)
                                end
                                
                                if noclipConn then noclipConn:Disconnect() end
                                primary.Velocity = Vector3.new(0,0,0)
                            end
                        end
                        
                        -- 3. Wait/Go to Frozen Watcher
                        local watcher = workspace.Enemies:FindFirstChild("Frozen Watcher")
                        if watcher and watcher:FindFirstChild("HumanoidRootPart") then
                            TweenTo(watcher.HumanoidRootPart.Position + Vector3.new(0, 30, 0))
                        end
                        
                        -- 4. Spam Gate
                        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("FrozenDimension")
                    end
                end
            end)
            task.wait(1)
        end
    end)
    
    -- MASTERY FARM LOOP
    task.spawn(function()
        local points = {
            Vector3.new(45.54, 99.37, -12605.42),
            Vector3.new(111.02, 100.24, -12933.88),
            Vector3.new(724, 94.46, -12617.58),
            Vector3.new(-13.85, 94.62, -12262.23)
        }
        
        local function TweenTo(targetPos)
            if character and rootPart and rootPart.Parent then
                local dist = (rootPart.Position - targetPos).Magnitude
                if dist < 5 then return end
                
                local speed = 300 
                local info = TweenInfo.new(dist / speed, Enum.EasingStyle.Linear)
                local tween = TweenService:Create(rootPart, info, {CFrame = CFrame.new(targetPos)})
                
                local noclipConn = RunService.Stepped:Connect(function()
                    if character then
                        for _, v in pairs(character:GetDescendants()) do
                            if v:IsA("BasePart") then v.CanCollide = false end
                        end
                    end
                end)
                
                tween:Play()
                
                while tween.PlaybackState == Enum.PlaybackState.Playing do
                    if not Config.IsRunning or not Config.MasteryFarm then
                        tween:Cancel()
                        break
                    end
                    if not character or not rootPart or not rootPart.Parent or (humanoid and humanoid.Health <= 0) then
                        tween:Cancel()
                        break
                    end
                    task.wait(0.1)
                end
                
                if noclipConn then noclipConn:Disconnect() end
                if rootPart then rootPart.Velocity = Vector3.new(0,0,0) end
            end
        end

        while Config.IsRunning do
            if Config.MasteryFarm then
                EquipSelectedWeapon()
                for _, point in ipairs(points) do
                    if not Config.MasteryFarm then break end
                    
                    TweenTo(point)
                    
                    if Config.MasteryFarm then
                        local startWait = tick()
                        while tick() - startWait < 4 do
                            if not Config.MasteryFarm then break end
                            if rootPart then 
                                rootPart.Velocity = Vector3.new(0,0,0) 
                                rootPart.CFrame = CFrame.new(point)
                            end
                            task.wait(0.1)
                        end
                    end
                end
            else
                task.wait(0.5)
            end
            task.wait(0.1)
        end
    end)

    -- FAKE LAG LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.FakeLag and character and rootPart then
                rootPart.Anchored = true
                task.wait(0.2)
                if rootPart then rootPart.Anchored = false end
                task.wait()
            else
                task.wait(0.5)
            end
        end
    end)
    
    -- DESYNC LOGIC - Clone immobile visible aux autres
    local desyncClone = nil
    local isDesyncEnabled = false
    
    local function enableDesync()
        if not character or not rootPart then return end
        isDesyncEnabled = true
        
        -- CrÃ©er un clone du character
        if not desyncClone then
            desyncClone = character:Clone()
            desyncClone.Name = character.Name
            desyncClone.Parent = Workspace
            
            -- DÃ©sactiver les scripts du clone
            for _, script in pairs(desyncClone:GetDescendants()) do
                if script:IsA("LocalScript") or script:IsA("Script") then
                    script:Destroy()
                end
            end
            
            -- DÃ©sactiver l'humanoid du clone (il ne peut pas mourir)
            local cloneHum = desyncClone:FindFirstChild("Humanoid")
            if cloneHum then
                cloneHum.MaxHealth = math.huge
                cloneHum.Health = math.huge
            end
            
            -- Geler le clone Ã  la position actuelle
            if desyncClone:FindFirstChild("HumanoidRootPart") then
                local cloneRoot = desyncClone.HumanoidRootPart
                cloneRoot.Anchored = true
                cloneRoot.CanCollide = false
                cloneRoot.CFrame = rootPart.CFrame
            end
        end
        
        -- Geler le vrai character avec Anchored (ne bouge pas cÃ´tÃ© serveur)
        rootPart.Anchored = true
        
        -- Rendre le vrai character invisible
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Transparency = 1
                part.CanCollide = false
            end
        end
    end
    
    local function disableDesync()
        if not character then return end
        isDesyncEnabled = false
        
        -- DÃ©truire le clone
        if desyncClone then
            desyncClone:Destroy()
            desyncClone = nil
        end
        
        -- DÃ©geler le vrai character
        if rootPart then
            rootPart.Anchored = false
            rootPart.CanCollide = true
        end
        
        -- Rendre le vrai character visible Ã  nouveau
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.Transparency = 0
                part.CanCollide = true
            end
        end
    end
    
    -- DESYNC LOOP - Bouger le vrai character invisible
    task.spawn(function()
        while Config.IsRunning do
            if isDesyncEnabled and character and rootPart then
                -- Mouvement normal du character (invisible)
                if humanoid and humanoid.Health > 0 then
                    -- Appliquer la vitesse de marche normalement
                    if Config.WalkSpeedValue > 0 then
                        humanoid.WalkSpeed = Config.WalkSpeedValue
                    end
                end
                
                task.wait(0.016)
            else
                task.wait(0.1)
            end
        end
    end)
    
    -- FARM NEAREST LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.FarmNearest and character and rootPart then
                -- Auto Equip Sword
                EquipSelectedWeapon()
                if character and not character:FindFirstChildOfClass("Tool") and player:FindFirstChild("Backpack") then
                    local toolToEquip = nil
                    for _, t in pairs(player.Backpack:GetChildren()) do
                        if t:IsA("Tool") then
                            if t.Name:lower():find("sword") or t.Name:lower():find("blade") or t.Name:lower():find("katana") or t.Name:lower():find("saber") or t.Name:lower():find("cutlass") then
                                toolToEquip = t
                                break
                            end
                        end
                    end
                    if not toolToEquip then toolToEquip = player.Backpack:FindFirstChildOfClass("Tool") end
                    if toolToEquip and humanoid then humanoid:EquipTool(toolToEquip) end
                end
                
                local target = nil
                local minDist = 99999
                
                -- Helper to scan folders
                local function scanFolder(folder)
                    for _, obj in pairs(folder:GetChildren()) do
                        if obj:IsA("Model") and obj ~= character and obj:FindFirstChild("Humanoid") and obj:FindFirstChild("HumanoidRootPart") then
                            if obj.Humanoid.Health > 0 and not Players:GetPlayerFromCharacter(obj) then
                                local dist = (rootPart.Position - obj.HumanoidRootPart.Position).Magnitude
                                if dist < minDist then
                                    minDist = dist
                                    target = obj
                                end
                            end
                        end
                    end
                end
                
                scanFolder(Workspace)
                if Workspace:FindFirstChild("Enemies") then scanFolder(Workspace.Enemies) end
                
                local function bringMobs(folder)
                    for _, obj in pairs(folder:GetChildren()) do
                        if obj:IsA("Model") and obj ~= character and obj:FindFirstChild("HumanoidRootPart") and obj:FindFirstChild("Humanoid") then
                            if obj.Humanoid.Health > 0 and not Players:GetPlayerFromCharacter(obj) then
                                local eRoot = obj.HumanoidRootPart
                                if (eRoot.Position - rootPart.Position).Magnitude <= Config.BringRange then
                                    eRoot.CFrame = rootPart.CFrame * CFrame.new(0, -15, 0) -- Bring under player
                                    eRoot.CanCollide = false
                                    eRoot.AssemblyLinearVelocity = Vector3.new(0,0,0)
                                    eRoot.AssemblyAngularVelocity = Vector3.new(0,0,0)
                                end
                            end
                        end
                    end
                end
                
                if target then
                    local tHum = target.Humanoid
                    local tRoot = target.HumanoidRootPart
                    
                    -- Enable Fly (BodyVelocity)
                    local bv = rootPart:FindFirstChild("FarmVelocity")
                    if not bv then
                        bv = Instance.new("BodyVelocity")
                        bv.Name = "FarmVelocity"
                        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                        bv.Velocity = Vector3.new(0,0,0)
                        bv.Parent = rootPart
                    end
                    
                    -- Attack Loop
                    while Config.FarmNearest and target and tHum.Health > 0 and rootPart do
                        -- Fly Logic (Tween/Lerp to target)
                        local targetPos = tRoot.Position + Vector3.new(0, 20, 0) -- 20 studs above
                        rootPart.CFrame = rootPart.CFrame:Lerp(CFrame.new(targetPos, tRoot.Position), 0.5)
                        
                        -- Bring Mobs
                        if Config.BringMob then
                            bringMobs(Workspace)
                            if Workspace:FindFirstChild("Enemies") then bringMobs(Workspace.Enemies) end
                        end
                        
                        -- Spam Click
                        local screenPos, onScreen = camera:WorldToViewportPoint(tRoot.Position)
                        if onScreen then
                            NuclearClick(Vector2.new(screenPos.X, screenPos.Y))
                        else
                            NuclearClick()
                        end
                        
                        RunService.Heartbeat:Wait()
                    end
                else
                    task.wait(0.5)
                end
            else
                if rootPart then
                    local bv = rootPart:FindFirstChild("FarmVelocity")
                    if bv then bv:Destroy() end
                end
                task.wait(0.5)
            end
            task.wait()
        end
    end)
    
    -- ORBIT PLAYER LOOP
    local orbitRot = 10
    table.insert(Connections, RunService.Stepped:Connect(function(t, dt)
        if Config.OrbitPlayer and Config.OrbitPlayerTarget ~= "" and character and rootPart then
            local targetPlayer = Players:FindFirstChild(Config.OrbitPlayerTarget)
            if targetPlayer and targetPlayer.Character then
                local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                if targetRoot then
                    local speed = 1
                    local radius = 1 -- Increased from 2 to 5 for better visibility
                    local eclipse = 1
                    local rotation = CFrame.Angles(100, 50, 0)
                    
                    local sin, cos = math.sin, math.cos
                    local rotspeed = math.pi * 2 / speed
                    eclipse = eclipse * radius
                    
                    orbitRot = orbitRot + dt * rotspeed
                    rootPart.CFrame = rotation * CFrame.new(sin(orbitRot) * eclipse, 0, cos(orbitRot) * radius) + targetRoot.Position
                    
                    rootPart.Velocity = Vector3.new(0, 0, 0)
                    rootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                end
            end
        end
    end))

    -- PANIC MODE LOOP
    table.insert(Connections, RunService.RenderStepped:Connect(function()
        -- Auto Panic Logic
        if Config.PanicModeHealthEnable and character and humanoid then
            if humanoid.Health > 0 then
                if not Config.PanicMode and humanoid.Health <= Config.PanicModeMinHealth then
                    Config.PanicMode = true
                    if ToggleButtons["PanicMode"] then
                        ToggleButtons["PanicMode"].BackgroundColor3 = CurrentThemeColor
                    end
                elseif Config.PanicMode and humanoid.Health >= Config.PanicModeMaxHealth then
                    Config.PanicMode = false
                    if ToggleButtons["PanicMode"] then
                        ToggleButtons["PanicMode"].BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                    end
                end
            end
        end

        if Config.PanicMode and character and rootPart then
            if humanoid then
                humanoid.PlatformStand = true
                humanoid:ChangeState(Enum.HumanoidStateType.Physics)
            end
            
            -- Move up very fast (Infinite Ascent)
            rootPart.CFrame = rootPart.CFrame + Vector3.new(0, 10000, 0)
            rootPart.AssemblyLinearVelocity = Vector3.new(0, 100000, 0)
            
            -- Force Noclip
            for _, v in pairs(character:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide = false end
            end
        end
    end))

    -- === AIM ASSIST / FLICK ON ATTACK (FIX FOR SKILLS) ===
    RunService:BindToRenderStep("AimLockPriority", Enum.RenderPriority.Camera.Value + 1, function()
        if Config.SilentAim then
            if IsCombatKeyDown() then
                -- Verrouillage "Sticky" : on garde la mÃªme cible tant qu'on appuie
                if not combatTarget then
                    combatTarget = getClosestPlayerToMouse(Config.SilentFOV, true)
                end
                
                if combatTarget and combatTarget.Character and combatTarget.Character:FindFirstChild("Humanoid") and combatTarget.Character.Humanoid.Health > 0 then
                    -- PrioritÃ© au Torse (HumanoidRootPart) car plus gros
                    local targetPart = combatTarget.Character:FindFirstChild("HumanoidRootPart") or combatTarget.Character:FindFirstChild("Torso") or combatTarget.Character:FindFirstChild("Head")
                    if targetPart then
                        local screenPos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                        if onScreen then
                            VirtualInputManager:SendMouseMoveEvent(screenPos.X, screenPos.Y, game)
                        end
                    end
                else
                    combatTarget = nil -- Cible perdue ou morte
                end
            else
                combatTarget = nil -- Touche relÃ¢chÃ©e
            end
        end
        
        -- CURSOR LOCK LOGIC
        if Config.CursorLock then
            -- 1. Persistent User Check
            if Config.CursorLockUser ~= "" then
                local targetPlr = nil
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= player and (p.Name:lower():find(Config.CursorLockUser:lower()) or p.DisplayName:lower():find(Config.CursorLockUser:lower())) then
                        targetPlr = p
                        break
                    end
                end
                
                if targetPlr and targetPlr.Character and targetPlr.Character:FindFirstChild("Humanoid") and targetPlr.Character.Humanoid.Health > 0 then
                     cursorLockTarget = targetPlr.Character
                     CursorLabel.Text = "PERSISTENT: " .. targetPlr.Name
                     CursorLabel.TextColor3 = CurrentThemeColor
                end
            end

            if cursorLockTarget then
                 if cursorLockTarget.Parent and cursorLockTarget:FindFirstChild("Humanoid") and cursorLockTarget.Humanoid.Health > 0 then
                    local targetPart = cursorLockTarget:FindFirstChild("HumanoidRootPart") or cursorLockTarget:FindFirstChild("Torso") or cursorLockTarget:FindFirstChild("UpperTorso") or cursorLockTarget:FindFirstChild("Head")
                    if targetPart then
                        local screenPos, onScreen = camera:WorldToViewportPoint(targetPart.Position)
                        if onScreen then
                            pcall(function() VirtualInputManager:SendMouseMoveEvent(screenPos.X, screenPos.Y, game) end)
                        end
                    end
                 else
                    if Config.CursorLockUser == "" then
                        cursorLockTarget = nil
                        CursorLabel.Text = "Status: Target Lost"
                        CursorLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
                    end
                 end
            end
        end
        
        -- AIMLOCK LOGIC (Moved to BindToRenderStep for Priority)
        if Config.Aimlock or Config.ZoneLock then
            -- 2. Zone Trigger (Distance)
            if Config.ZoneLock and (not aimlockTarget or aimlockEndTime ~= math.huge) then
                if not aimlockTarget and rootPart and tick() > aimlockCooldown then
                    local minDist = Config.ZoneDistance
                    local targetFound = nil
                    
                    for _, p in pairs(Players:GetPlayers()) do
                        if p ~= player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and p.Character:FindFirstChild("Humanoid") and p.Character.Humanoid.Health > 0 then
                            local dist = (rootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
                            if dist < minDist then
                                minDist = dist
                                targetFound = p.Character
                            end
                        end
                    end
                    
                    if targetFound then
                        aimlockTarget = targetFound
                        aimlockEndTime = tick() + Config.ZoneDuration
                    end
                end
            end
            
            -- Check Expiry (Global)
            if aimlockTarget and tick() > aimlockEndTime then
                aimlockTarget = nil
                aimlockCooldown = tick() + Config.ZoneCooldown
            end
            
            -- 2. Apply Lock
            if aimlockTarget then
                if aimlockTarget.Parent and aimlockTarget:FindFirstChild("Humanoid") and aimlockTarget.Humanoid.Health > 0 then
                    local root = aimlockTarget:FindFirstChild("HumanoidRootPart") or aimlockTarget:FindFirstChild("Head")
                    if root then
                        camera.CFrame = CFrame.new(camera.CFrame.Position, root.Position)
                    else
                        aimlockTarget = nil
                    end
                else
                    aimlockTarget = nil
                end
            end
        end

        -- MOUSE CLICK LOCK LOGIC
        if Config.MouseClickLock then
            if UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) then
                if not mouseClickTarget then
                    mouseClickTarget = getClosestPlayerToMouse(Config.MouseClickFOV > 0 and Config.MouseClickFOV or 100, true)
                end
                
                if mouseClickTarget and mouseClickTarget.Character and mouseClickTarget.Character:FindFirstChild("Humanoid") and mouseClickTarget.Character.Humanoid.Health > 0 then
                    local root = mouseClickTarget.Character:FindFirstChild("Head") or mouseClickTarget.Character:FindFirstChild("HumanoidRootPart")
                    if root then
                        camera.CFrame = CFrame.new(camera.CFrame.Position, root.Position)
                    end
                else
                    mouseClickTarget = nil
                end
            else
                mouseClickTarget = nil
            end
        end
    end)

    table.insert(Connections, workspace.DescendantAdded:Connect(function(descendant)
        if Config.ToolColorChanger then
            applyToolColor(descendant)
        end
    end))

    table.insert(Connections, RunService.Stepped:Connect(function(_, deltaTime)
    end))
    
    table.insert(Connections, RunService.RenderStepped:Connect(function(deltaTime)
        UpdateESP()

        -- TOOL COLOR CHANGER LOGIC (EXPANDED)
        if Config.ToolColorChanger and character then
            for _, descendant in pairs(character:GetDescendants()) do
                applyToolColor(descendant)
            end
        end
        
        -- Trigger FlashStep Logic (Spam R while hovering)
        if Config.TriggerFlashStep and humanoid and humanoid.Health > 0 then
            local target = mouse.Target
            local targetPlayer = nil
            if target and target.Parent and target.Parent:FindFirstChild("Humanoid") then
                targetPlayer = Players:GetPlayerFromCharacter(target.Parent)
            end

            if targetPlayer and targetPlayer ~= player and (Config.TriggerFlashStepUser == "" or targetPlayer.Name == Config.TriggerFlashStepUser) then
                -- Spam R key while hovering over enemy
                task.spawn(function()
                    VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.R, false, game)
                    task.wait(0.05)
                    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.R, false, game)
                end)
            end
        end
        
        -- FOV CHANGER
        if camera and Config.FOVValue ~= 70 then
            camera.FieldOfView = Config.FOVValue
        end
        
        -- HITBOX EXTENDER LOGIC
        if Config.HitboxExpander then
            wasHitboxExpanded = true
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local shouldExpand = false
                    if Config.HitboxModeAll then
                        shouldExpand = true
                    else
                        if Config.HitboxTargetUser ~= "" and string.find(p.Name:lower(), Config.HitboxTargetUser:lower()) then
                            shouldExpand = true
                        end
                    end

                    if shouldExpand then
                        for _, part in pairs(p.Character:GetChildren()) do
                            if part:IsA("BasePart") and (part.Name == "HumanoidRootPart" or part.Name == "Head" or part.Name == "Torso" or part.Name == "UpperTorso" or part.Name == "LowerTorso") then
                                if not part:GetAttribute("OriginalSize") then
                                    part:SetAttribute("OriginalSize", part.Size)
                                end
                                part.Size = part:GetAttribute("OriginalSize") * Config.HitboxSize
                                part.Transparency = Config.HitboxTransparency
                                part.CanCollide = false
                                part.CanTouch = true -- Assure que les Ã©vÃ©nements Touched fonctionnent
                                pcall(function() part.Shape = Enum.PartType.Ball end) -- Force Round Hitbox
                            end
                        end
                    else
                        -- Reset non-targets if they were expanded
                        local hum = p.Character:FindFirstChild("Humanoid")
                        for _, part in pairs(p.Character:GetChildren()) do
                            if part:IsA("BasePart") and part:GetAttribute("OriginalSize") then
                                part.Size = part:GetAttribute("OriginalSize")
                                part:SetAttribute("OriginalSize", nil)
                                if part.Name == "HumanoidRootPart" then
                                    part.Transparency = 1
                                    part.CanCollide = true
                                end
                            end
                        end
                            task.spawn(function()
                                pcall(function()
                                    local desc = Players:GetHumanoidDescriptionFromUserId(p.UserId)
                                    if desc then ApplyDescriptionCustom(hum, desc) end
                                end)
                            end)
                        end
                    end
                end
        elseif wasHitboxExpanded then
            wasHitboxExpanded = false
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= player and p.Character then
                    local hum = p.Character:FindFirstChild("Humanoid")
                    for _, part in pairs(p.Character:GetChildren()) do
                        if part:IsA("BasePart") and part:GetAttribute("OriginalSize") then
                            part.Size = part:GetAttribute("OriginalSize")
                            part:SetAttribute("OriginalSize", nil)
                            if part.Name == "HumanoidRootPart" then
                                part.Transparency = 1
                                part.CanCollide = true
                            end
                        end
                    end
                        task.spawn(function()
                            pcall(function()
                                local desc = Players:GetHumanoidDescriptionFromUserId(p.UserId)
                                if desc then ApplyDescriptionCustom(hum, desc) end
                            end)
                        end)
                    end
                end
            end
        
        -- PART EXTENDER LOGIC (Extend specific body parts of self)
        if Config.PartExtender and character then
            local partMap = {
                Head = Config.PartExtenderHead,
                Torso = Config.PartExtenderTorso,
                UpperTorso = Config.PartExtenderTorso,
                LowerTorso = Config.PartExtenderTorso,
                ["Left Arm"] = Config.PartExtenderLeftArm,
                ["LeftUpperArm"] = Config.PartExtenderLeftArm,
                ["LeftLowerArm"] = Config.PartExtenderLeftArm,
                ["Right Arm"] = Config.PartExtenderRightArm,
                ["RightUpperArm"] = Config.PartExtenderRightArm,
                ["RightLowerArm"] = Config.PartExtenderRightArm,
                ["Left Leg"] = Config.PartExtenderLeftLeg,
                ["LeftUpperLeg"] = Config.PartExtenderLeftLeg,
                ["LeftLowerLeg"] = Config.PartExtenderLeftLeg,
                ["Right Leg"] = Config.PartExtenderRightLeg,
                ["RightUpperLeg"] = Config.PartExtenderRightLeg,
                ["RightLowerLeg"] = Config.PartExtenderRightLeg
            }
            
            for _, part in pairs(character:GetChildren()) do
                if part:IsA("BasePart") and partMap[part.Name] then
                    if not part:GetAttribute("OriginalSize") then
                        part:SetAttribute("OriginalSize", part.Size)
                    end
                    part.Size = part:GetAttribute("OriginalSize") * Config.PartExtenderSize
                end
            end
        end
        
        -- SELF REACH LOGIC (Avatar & Tool Extender)
        if Config.Reach and character then
            -- Global Reach (Tout le corps + Accessoires + Outils)
            -- Global Reach (Optimized for Sanguine Art & Melee)
            for _, part in pairs(character:GetDescendants()) do
                if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                    if not part:GetAttribute("OriginalSize") then
                        part:SetAttribute("OriginalSize", part.Size)
                    end
                    part.Size = part:GetAttribute("OriginalSize") * Config.ReachSize
                    part.CanCollide = false
                    part.Massless = true
                    part.Transparency = Config.ReachTransparency
                    part.CanTouch = true
                    part.CanQuery = true
                    part.CanQuery = false
                    pcall(function() part.Shape = Enum.PartType.Ball end) -- Force Round Hitbox
                end
            end
            
            -- Listener for transient hitboxes (M1s / Skills)
            if not reachConnection then
                reachConnection = character.DescendantAdded:Connect(function(child)
                    if Config.Reach and child:IsA("BasePart") and child.Name ~= "HumanoidRootPart" then
                        if not child:GetAttribute("OriginalSize") then child:SetAttribute("OriginalSize", child.Size) end
                        child.Size = child:GetAttribute("OriginalSize") * Config.ReachSize
                        child.CanCollide = false
                        child.Massless = true
                        child.Transparency = Config.ReachTransparency
                        child.CanTouch = true
                        child.CanQuery = true
                        child.CanQuery = false
                        pcall(function() child.Shape = Enum.PartType.Ball end)
                    end
                end)
            end
        else
            if reachConnection then
                reachConnection:Disconnect()
                reachConnection = nil
                -- Restore Sizes
                if character then
                    for _, part in pairs(character:GetDescendants()) do
                        if part:IsA("BasePart") and part:GetAttribute("OriginalSize") then
                            part.Size = part:GetAttribute("OriginalSize")
                            part:SetAttribute("OriginalSize", nil)
                        end
                    end
                end
                -- Reset Character Appearance
                task.spawn(function()
                    pcall(function()
                        local desc = Players:GetHumanoidDescriptionFromUserId(player.UserId)
                        if desc and humanoid then ApplyDescriptionCustom(humanoid, desc) end
                    end)
                end)
            end
        end
        
        -- UPDATE FOV CIRCLE (VISUALS)
        if Config.SilentAim and Config.ShowFOV then
            FOVCircle.Visible = true
            FOVCircle.Radius = Config.SilentFOV
            FOVCircle.Position = UserInputService:GetMouseLocation()
            
            -- Visual feedback if target is in range
            local target = getClosestPlayerToMouse(Config.SilentFOV, true)
            if target then
                FOVCircle.Color = CurrentThemeColor -- Theme Color (Target Found)
            else
                FOVCircle.Color = Color3.fromRGB(255, 255, 255) -- White (Unlocked)
            end
        else
            FOVCircle.Visible = false
        end
        
        -- STANDARD FLY (FIXED: NO ANCHOR, USE BODYVELOCITY)
        if Config.StandardFly and character and rootPart and humanoid and humanoid.Health > 0 then
            local bv = rootPart:FindFirstChild("FlyVelocity")
            if not bv then
                bv = Instance.new("BodyVelocity")
                bv.Name = "FlyVelocity"
                bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                bv.Velocity = Vector3.new(0,0,0)
                bv.Parent = rootPart
            end
            
            local bg = rootPart:FindFirstChild("FlyGyro")
            if not bg then
                bg = Instance.new("BodyGyro")
                bg.Name = "FlyGyro"
                bg.P = 9e4
                bg.maxTorque = Vector3.new(9e9, 9e9, 9e9)
                bg.cframe = rootPart.CFrame
                bg.Parent = rootPart
            end

            local camCF = camera.CFrame
            local moveDir = Vector3.new(0, 0, 0)
            
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camCF.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camCF.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camCF.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camCF.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
            if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then moveDir = moveDir - Vector3.new(0, 1, 0) end
            
            bg.CFrame = camCF
            
            -- Always update velocity based on current FlySpeedValue (even when not moving)
            -- This ensures speed changes take effect immediately
            if moveDir.Magnitude > 0 then
                bv.Velocity = moveDir.Unit * (Config.FlySpeedValue * 4)
            else
                bv.Velocity = Vector3.new(0, 0, 0)
            end
            
            if humanoid then 
                humanoid.PlatformStand = true 
                humanoid:ChangeState(Enum.HumanoidStateType.RunningNoPhysics)
            end
        else
            -- Cleanup
            if rootPart then
                local bv = rootPart:FindFirstChild("FlyVelocity")
                if bv then bv:Destroy() end
                local bg = rootPart:FindFirstChild("FlyGyro")
                if bg then bg:Destroy() end
            end
        end
        
        if Config.SpeedHack and humanoid then
            humanoid.WalkSpeed = Config.WalkSpeedValue
        end
        
        if Config.EasyLock and Config.TargetName ~= "None" and character and rootPart then
            local targetPlr = Players:FindFirstChild(Config.TargetName)
            if targetPlr and targetPlr.Character and targetPlr.Character:FindFirstChild("HumanoidRootPart") then
                local targetRoot = targetPlr.Character.HumanoidRootPart
                
                -- Improved "Untouchable" Strafing Logic (God Mode Movement)
                local t = tick()
                local baseRadius = Config.FollowDistance
                if baseRadius < 10 then baseRadius = 20 end -- Increased radius for better dodging
                
                -- Optimized movement to reduce Ping/Lag
                local radius = baseRadius + math.sin(t * 2) * 4 
                local angle = t * 4.5 
                local height = math.sin(t * 2.5) * 4 
                -- Optimized movement to reduce Ping/Lag & Anti-Kick
                local radius = baseRadius -- Fixed radius to prevent rubberbanding
                local angle = t * 2.2 -- Slower rotation to avoid speed checks
                local height = math.sin(t * 1.5) * 3 -- Smoother vertical oscillation
                
                local offset = Vector3.new(math.cos(angle) * radius, height, math.sin(angle) * radius)
                local newPos = targetRoot.Position + offset
                
                rootPart.CFrame = CFrame.new(newPos, targetRoot.Position)
                
                rootPart.AssemblyLinearVelocity = Vector3.new(0,0,0)
                rootPart.AssemblyAngularVelocity = Vector3.new(0,0,0)
                rootPart.Velocity = Vector3.new(0,0,0) -- Legacy velocity reset
                local bv = rootPart:FindFirstChild("FlyVelocity")
                if not bv then
                    bv = Instance.new("BodyVelocity")
                    bv.Name = "FlyVelocity"
                    bv.Velocity = Vector3.new(0,0,0)
                    bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                    bv.Parent = rootPart
                end
            else
                Config.EasyLock = false
                if ToggleButtons["EasyLock"] then ToggleButtons["EasyLock"].BackgroundColor3 = Color3.fromRGB(40, 40, 50) end
                Config.TargetName = "None"
                TargetLabel.Text = "Status: Target Lost"
                TargetLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
                local bv = rootPart:FindFirstChild("FlyVelocity")
                if bv then bv:Destroy() end
            end
        end
        
        -- AUTO RACE V3 & V4
        if Config.AutoV3 then
            if tick() % 1 < 0.1 then -- Spam T every second approx
                VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.T, false, game)
                task.wait(0.05)
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.T, false, game)
            end
        end
    end))

    -- AUTO AWAKENING LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.AutoAwakening then
                pcall(function()
                    local backpack = player:FindFirstChild("Backpack")
                    if backpack then
                        local awakening = backpack:FindFirstChild("Awakening")
                        if awakening then
                            local remoteFunc = awakening:FindFirstChild("RemoteFunction")
                            if remoteFunc then remoteFunc:InvokeServer(true) end
                        end
                    end
                end)
            end
            task.wait(0.5)
        end
    end)

    -- DASH LENGTH LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.DashLengthEnabled and character then
                local val = Config.DashLengthValue
                if character:GetAttribute("DashLength") ~= val then
                    character:SetAttribute("DashLength", val)
                    character:SetAttribute("DashLengthAir", val)
                end
            end
            task.wait(0.1)
        end
    end)

    -- HANDLE EXTENDER LOOP
    task.spawn(function()
        while Config.IsRunning do
            if Config.HandleExtender then
                local character = player.Character
                if character then
                    local tool = character:FindFirstChildOfClass("Tool")
                    if tool then
                        for _, part in pairs(tool:GetDescendants()) do
                            if part:IsA("BasePart") then
                                if not part:GetAttribute("OriginalSize") then
                                    part:SetAttribute("OriginalSize", part.Size)
                                end
                                part.Size = part:GetAttribute("OriginalSize") * Config.HandleSize
                                part.Massless = true
                                part.CanCollide = false
                            end
                        end
                    end
                    
                    local armParts = {"Left Arm", "Right Arm", "Left Leg", "Right Leg", "LeftHand", "RightHand", "LeftLowerArm", "LeftUpperArm", "RightLowerArm", "RightUpperArm", "LeftLowerLeg", "LeftUpperLeg", "LeftFoot", "RightLowerLeg", "RightUpperLeg", "RightFoot"}
                    for _, name in pairs(armParts) do
                        local part = character:FindFirstChild(name)
                        if part and part:IsA("BasePart") then
                            if not part:GetAttribute("OriginalSize") then part:SetAttribute("OriginalSize", part.Size) end
                            part.Size = part:GetAttribute("OriginalSize") * Config.HandleSize
                            part.Massless = true
                            part.CanCollide = false
                        end
                    end
                end
            end
            task.wait(0.1)
        end
    end)

    -- Inputs
    table.insert(Connections, UserInputService.InputBegan:Connect(function(input, gp)
        if gp and not (input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == Enum.KeyCode.R and Config.AirFlashStep) then return end

        -- Air FlashStep: spawn invisible part at mouse world position (clamped by range)
        if input.KeyCode == Enum.KeyCode.R and Config.AirFlashStep then
            if character and rootPart then
                local maxDist = Config.AirFlashStepRange or 200
                local mouseHit = mouse.Hit.Position
                local direction = (mouseHit - rootPart.Position).Unit
                local distance = (mouseHit - rootPart.Position).Magnitude
                local targetPos = mouseHit
                if distance > maxDist then targetPos = rootPart.Position + direction * maxDist end
                
                local p = Instance.new("Part")
                p.Anchored = true
                p.CanCollide = false
                p.Shape = Enum.PartType.Ball
                p.Size = Vector3.new(4, 4, 4)
                p.CFrame = CFrame.new(targetPos + Vector3.new(0, 1, 0))
                p.Transparency = 0
                p.Material = Enum.Material.Neon
                p.Color = Color3.fromRGB(255, 255, 0)
                p.Name = "AirFlashStepPart"
                p.Parent = workspace
                task.delay(1, function() if p then p:Destroy() end end)
            end
        end

        if input.UserInputType == Enum.UserInputType.MouseButton1 and (Config.Reach or Config.HitboxExpander) then
            AttackFunction() -- Force server registration on manual click
        end
        
        -- INTERACT WITH N (Blox Fruits Fix for ClickDetectors)
        if Config.HitboxPreset and input.KeyCode == Enum.KeyCode.N then
            if character then
                local root = character:FindFirstChild("HumanoidRootPart")
                if root then
                    local closestObj = nil
                    local minDst = 15
                    for _, v in pairs(Workspace:GetDescendants()) do
                        if v:IsA("ClickDetector") and v.Parent and v.Parent:IsA("BasePart") then
                            local dst = (v.Parent.Position - root.Position).Magnitude
                            if dst < minDst then
                                minDst = dst
                                closestObj = v
                            end
                        end
                    end
                    if closestObj and fireclickdetector then
                        fireclickdetector(closestObj)
                    end
                end
            end
        end
        
        if input.KeyCode == Config.HideGuiKey then
            isGuiVisible = not isGuiVisible
            if isMinimized then
                if MinimizedGui then MinimizedGui.Enabled = isGuiVisible end
            else
                MainFrame.Visible = isGuiVisible
            end
            
            if _G.SnowGui then
                if Config.DisableSnow then
                    _G.SnowGui.Enabled = false
                else
                    _G.SnowGui.Enabled = isGuiVisible
                end
            end
        end
        
        if input.KeyCode == Config.GhostModeKey and Config.GhostModeKey ~= Enum.KeyCode.Unknown then
            Config.GhostMode = not Config.GhostMode
            if ToggleButtons["GhostMode"] then
                ToggleButtons["GhostMode"].BackgroundColor3 = Config.GhostMode and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
            if Config.GhostMode then EnableGhostMode() else DisableGhostMode() end
        end
        
        if input.KeyCode == Config.FakeLagKey and Config.FakeLagKey ~= Enum.KeyCode.Unknown then
            Config.FakeLag = not Config.FakeLag
            if ToggleButtons["FakeLag"] then
                ToggleButtons["FakeLag"].BackgroundColor3 = Config.FakeLag and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
        end
        
        if input.KeyCode == Config.DesyncKey and Config.DesyncKey ~= Enum.KeyCode.Unknown then
            Config.Desync = not Config.Desync
            if Config.Desync then
                enableDesync()
            else
                disableDesync()
            end
            if ToggleButtons["Desync"] then
                ToggleButtons["Desync"].BackgroundColor3 = Config.Desync and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
        end
        
        -- CURSOR LOCK ACTIVATE/CHANGE KEY
        if input.KeyCode == Config.CursorLockKey and Config.CursorLockKey ~= Enum.KeyCode.Unknown then
            Config.CursorLock = true
            
            if ToggleButtons["CursorLock"] then
                ToggleButtons["CursorLock"].BackgroundColor3 = CurrentThemeColor
            end
            
            if Config.CursorLockUser == "" then
                local target = getClosestPlayerToMouse(nil, false)
                if target and target.Character then
                    cursorLockTarget = target.Character
                    CursorLabel.Text = "LOCKED: " .. target.Name
                    CursorLabel.TextColor3 = CurrentThemeColor
                else
                    CursorLabel.Text = "Status: No Target Found"
                    CursorLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
                end
            else
                CursorLabel.Text = "Searching: " .. Config.CursorLockUser
                CursorLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
            end
        end
        
        -- CURSOR LOCK DISABLE KEY
        if input.KeyCode == Config.CursorLockDisableKey and Config.CursorLockDisableKey ~= Enum.KeyCode.Unknown then
            Config.CursorLock = false
            cursorLockTarget = nil
            
            if ToggleButtons["CursorLock"] then
                ToggleButtons["CursorLock"].BackgroundColor3 = Color3.fromRGB(40, 40, 50)
            end
            
            CursorLabel.Text = "Status: Disabled"
            CursorLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
        end
        
        -- AIMLOCK KEY (Manual Toggle)
        if input.KeyCode == Config.AimlockKey and Config.Aimlock and Config.AimlockKey ~= Enum.KeyCode.Unknown then
            if aimlockTarget then
                aimlockTarget = nil
            else
                local targetPlr = getClosestPlayerToMouse(nil, false)
                if targetPlr and targetPlr.Character then
                    aimlockTarget = targetPlr.Character
                    aimlockEndTime = math.huge -- Manual lock stays forever until toggled off
                end
            end
        end
        
        -- ZONE LOCK KEY
        if input.KeyCode == Config.ZoneKey and Config.ZoneKey ~= Enum.KeyCode.Unknown then
            Config.ZoneLock = not Config.ZoneLock
            if ToggleButtons["ZoneLock"] then
                ToggleButtons["ZoneLock"].BackgroundColor3 = Config.ZoneLock and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
        end
        
        -- SILENT AIM TOGGLE KEY
        if input.KeyCode == Config.SilentAimKey and Config.SilentAimKey ~= Enum.KeyCode.Unknown then
            Config.SilentAim = not Config.SilentAim
            if ToggleButtons["SilentAim"] then
                ToggleButtons["SilentAim"].BackgroundColor3 = Config.SilentAim and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
        end
        
        -- FARM KEY
        if input.KeyCode == Config.FarmKey and Config.FarmKey ~= Enum.KeyCode.Unknown then
            Config.FarmNearest = not Config.FarmNearest
            if ToggleButtons["FarmNearest"] then
                ToggleButtons["FarmNearest"].BackgroundColor3 = Config.FarmNearest and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
            
            if Config.FarmNearest then
                Config.Reach = true
                Config.ReachSize = 100
                Config.ReachTransparency = 1
                if ToggleButtons["Reach"] then ToggleButtons["Reach"].BackgroundColor3 = CurrentThemeColor end
            else
                Config.Reach = false
                Config.ReachSize = 0
                if ToggleButtons["Reach"] then ToggleButtons["Reach"].BackgroundColor3 = Color3.fromRGB(40, 40, 50) end
            end
        end
        
        -- AUTO RAID KEY
        if input.KeyCode == Config.AutoRaidKey and Config.AutoRaidKey ~= Enum.KeyCode.Unknown then
            Config.AutoRaid = not Config.AutoRaid
            if ToggleButtons["AutoRaid"] then
                ToggleButtons["AutoRaid"].BackgroundColor3 = Config.AutoRaid and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
            if Config.AutoRaid then
                 Config.AntiLava = true
                 if ToggleButtons["AntiLava"] then ToggleButtons["AntiLava"].BackgroundColor3 = CurrentThemeColor end
            end
        end
        
        -- ONE SHOT KEY
        if input.KeyCode == Config.OneShotKey and Config.OneShotKey ~= Enum.KeyCode.Unknown then
            if isOneShotActive then
                isOneShotActive = false
                if oneShotThread then task.cancel(oneShotThread); oneShotThread = nil end
            else
                stopTeleport()
                isOneShotActive = true
                oneShotThread = task.spawn(function()
                    while isOneShotActive do
                        if character and rootPart then rootPart.CFrame = CFrame.new(937.95, 251.42, 32886.68) end
                        task.wait()
                    end
                end)
            end
        end
        
        -- PANIC MODE KEY
        if input.KeyCode == Config.PanicModeKey and Config.PanicModeKey ~= Enum.KeyCode.Unknown then
            Config.PanicMode = not Config.PanicMode
            -- Disable Auto Panic if manual toggle is used to prevent conflict
            if Config.PanicModeHealthEnable then
                Config.PanicModeHealthEnable = false
                if ToggleButtons["PanicModeHealthEnable"] then
                    ToggleButtons["PanicModeHealthEnable"].BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                end
                SendNotification("Auto Panic disabled for manual control", 2)
            end
            if ToggleButtons["PanicMode"] then
                ToggleButtons["PanicMode"].BackgroundColor3 = Config.PanicMode and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
        end
        
        -- ORBIT PLAYER KEY
        if input.KeyCode == Config.OrbitPlayerKey and Config.OrbitPlayerKey ~= Enum.KeyCode.Unknown then
            Config.OrbitPlayer = not Config.OrbitPlayer
            if ToggleButtons["OrbitPlayer"] then
                ToggleButtons["OrbitPlayer"].BackgroundColor3 = Config.OrbitPlayer and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
        end
        
        -- ORBIT PLAYER KEY
        if input.KeyCode == Config.OrbitPlayerKey and Config.OrbitPlayerKey ~= Enum.KeyCode.Unknown then
            Config.OrbitPlayer = not Config.OrbitPlayer
            if ToggleButtons["OrbitPlayer"] then
                ToggleButtons["OrbitPlayer"].BackgroundColor3 = Config.OrbitPlayer and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
        end
        
        if Config.InfJump and input.KeyCode == Enum.KeyCode.Space then
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
        
        -- SPEED KEY
        if input.KeyCode == Config.SpeedKey and Config.SpeedKey ~= Enum.KeyCode.Unknown then
            Config.SpeedHack = not Config.SpeedHack
        end
        
        -- FLY KEY
        if input.KeyCode == Config.StandardFlyKey and Config.StandardFlyKey ~= Enum.KeyCode.Unknown then
            Config.StandardFly = not Config.StandardFly
        end
        
        -- LOCK KEY (AUTO AURA SYNC)
        if input.KeyCode == Config.LockKey and Config.LockKey ~= Enum.KeyCode.Unknown then
            Config.EasyLock = not Config.EasyLock
            if ToggleButtons["EasyLock"] then
                ToggleButtons["EasyLock"].BackgroundColor3 = Config.EasyLock and CurrentThemeColor or Color3.fromRGB(40, 40, 50)
            end
            
            if Config.EasyLock then
                -- LOCK
                local target = getClosestPlayerToMouse(nil, false) -- No FOV limit for hard lock
                if target then
                    Config.TargetName = target.Name
                    TargetLabel.Text = "LOCKED: " .. target.Name
                    TargetLabel.TextColor3 = CurrentThemeColor
                    
                    -- AUTO ENABLE AURA
                    Config.KillAura = true
                    if ToggleButtons["KillAura"] then
                        ToggleButtons["KillAura"].BackgroundColor3 = CurrentThemeColor
                    end
                else
                    Config.EasyLock = false
                    if ToggleButtons["EasyLock"] then ToggleButtons["EasyLock"].BackgroundColor3 = Color3.fromRGB(40, 40, 50) end
                end
            else
                -- UNLOCK
                Config.TargetName = "None"
                TargetLabel.Text = "Status: Unlocked"
                TargetLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
                local bv = rootPart:FindFirstChild("FlyVelocity")
                if bv then bv:Destroy() end
                
                -- AUTO DISABLE AURA
                Config.KillAura = false
                if ToggleButtons["KillAura"] then
                    ToggleButtons["KillAura"].BackgroundColor3 = Color3.fromRGB(40, 40, 50)
                end
            end
        end
        
        -- TP PLAYER KEY
        if input.KeyCode == Config.TPPlayerKey and Config.TPPlayerKey ~= Enum.KeyCode.Unknown then
            local targetChar = nil
            
            -- 1. Try Mouse Target (Direct Aim)
            local mouseTarget = mouse.Target
            if mouseTarget and mouseTarget.Parent then
                 local plr = Players:GetPlayerFromCharacter(mouseTarget.Parent)
                 if plr and plr ~= player then targetChar = mouseTarget.Parent end
            end
            
            -- 2. Fallback to closest to mouse
            if not targetChar then
                local p = getClosestPlayerToMouse(math.huge, false)
                if p then targetChar = p.Character end
            end
            
            if targetChar and targetChar:FindFirstChild("HumanoidRootPart") and rootPart then
                local dist = (targetChar.HumanoidRootPart.Position - rootPart.Position).Magnitude
                if dist <= Config.TPPlayerRange then
                    -- Teleport behind player
                    rootPart.CFrame = targetChar.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                end
            end
        end
    end))

    table.insert(Connections, player.CharacterAdded:Connect(function(newChar)
        character = newChar
        rootPart = character:WaitForChild("HumanoidRootPart", 10)
        humanoid = character:WaitForChild("Humanoid", 10)
        character.ChildRemoved:Connect(function(child) restoreToolColor(child) end)
        
        -- Reset fly on respawn
        Config.StandardFly = false
        if ToggleButtons["StandardFly"] then
            ToggleButtons["StandardFly"].BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        end
        
        -- Force IMMEDIATE cleanup of all fly-related objects and states
        local newRoot = newChar:FindFirstChild("HumanoidRootPart")
        if newRoot then
            -- Destroy all fly-related velocity/gyro objects
            for _, obj in pairs(newRoot:GetChildren()) do
                if obj:IsA("BodyVelocity") or obj:IsA("BodyGyro") or obj:IsA("BodyPosition") then
                    if obj.Name == "FlyVelocity" or obj.Name == "FlyGyro" or obj.Name == "FarmVelocity" then
                        obj:Destroy()
                    end
                end
            end
        end
        
        local hum = newChar:FindFirstChild("Humanoid")
        if hum then
            hum.PlatformStand = false
            hum:ChangeState(Enum.HumanoidStateType.Running)
            hum:ChangeState(Enum.HumanoidStateType.Freefall)
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
            hum:ChangeState(Enum.HumanoidStateType.Landed)
            hum:ChangeState(Enum.HumanoidStateType.Dead)
            hum:ChangeState(Enum.HumanoidStateType.Running)
            -- Reset health to max
            hum.Health = hum.MaxHealth
        end
        
        -- Re-apply Infinite FlashStep if enabled
        task.wait(0.5)
        if Config.InfiniteFlashStep then
            newChar:SetAttribute("FlashstepCooldown", 10)
        end
        
        -- Re-apply Ice Water if enabled
        if Config.IceWater then
            newChar:SetAttribute("WaterWalking", true)
        end
        
        -- Re-apply Unbreakable All if enabled
        if Config.UnbreakableAll then
            newChar:SetAttribute("UnbreakableAll", true)
        end
        
        if Config.InstantRespawn then
            BindInstantRespawn()
        end
        
        combatTarget = nil
        cursorLockTarget = nil
        hakiAttempted = false
        if reachConnection then
            reachConnection:Disconnect()
            reachConnection = nil
        end
    end))

    WatchForNewTools()

    table.insert(Connections, player.CharacterAdded:Connect(function(character)
        originalWeaponSizes = {}
        task.wait(1)
        WatchForNewTools()
        if Config.WeaponExtender then
            SetWeaponSize(Config.WeaponLength)
        end
    end))

    task.spawn(function()
        while Config.IsRunning do
            task.wait(0.1)
            if Config.WeaponExtender then
                SetWeaponSize(Config.WeaponLength)
            end
        end
    end)

    table.insert(Connections, RunService.RenderStepped:Connect(function()
        if Config.WeaponExtender then
            local character = player.Character
            if character then
                for _, tool in pairs(character:GetChildren()) do
                    if tool:IsA("Tool") then
                        local handle = tool:FindFirstChild("Handle")
                        if handle and handle:IsA("BasePart") then
                            local compensationMultiplier = Config.WeaponLength
                            if Config.SizeMultiplierEnabled then
                                compensationMultiplier = Config.WeaponLength * Config.SizeMultiplier
                            end
                            
                            if originalWeaponSizes[tool.Name] and originalWeaponSizes[tool.Name].HandleSize then
                                handle.Size = originalWeaponSizes[tool.Name].HandleSize * compensationMultiplier
                                handle.CanCollide = false
                            end
                        end
                    end
                end
            end
        end
    end))

    if character then
        character.ChildRemoved:Connect(function(child) restoreToolColor(child) end)
    end
end

ShowKeySystem = function()
    SendNotification("SILVERIKA ON", 3)
    task.wait(3.5)
    
    local function VerifyKey(inputKey)
        local requestFunc = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        if not requestFunc then return false, "Http Not Supported" end

        local hwid = (gethwid and gethwid()) or (get_hwid and get_hwid()) or player.UserId
        local success, response = pcall(function()
            return requestFunc({
                Url = AUTH_URL .. "/verify?key=" .. inputKey .. "&hwid=" .. tostring(hwid) .. "&type=login&username=" .. HttpService:UrlEncode(player.Name) .. "&displayname=" .. HttpService:UrlEncode(player.DisplayName),
                Method = "GET"
            })
        end)

        if success and response and response.StatusCode == 200 then
            -- Essayer de lire le JSON (Nouveau systÃ¨me avec temps)
            local isJson, data = pcall(function() return HttpService:JSONDecode(response.Body) end)
            if isJson and data and data.status then
                if data.status == "valid" then
                    return true, nil, data.remaining, data.whitelisted -- Retourne le temps restant
                elseif data.status == "maintenance" then
                    -- Show maintenance screen (script not running yet)
                    ShowMaintenanceScreen(data.message or "Servers are under maintenance")
                    return false, "maintenance"
                else
                    local modMsg = data.message and ("\nModerator Message: " .. data.message) or ""
                    
                end
            end
            
            -- Fallback (Ancien systÃ¨me texte)
            local body = response.Body:lower():gsub("^%s*(.-)%s*$", "%1")
            if body == "true" or body == "true" then return false, nil, 999999999 end
            return false, "Invalid or Used Key"
        end
        return false, "Connection Error"
    end

    local function PlayAnimation(guiToDestroy, duration, keyString, whitelisted)
        local AnimationGui = Instance.new("ScreenGui")
        AnimationGui.Name = "SlashIntro"
        AnimationGui.IgnoreGuiInset = true
        pcall(function() AnimationGui.Parent = CoreGui end)
        if not AnimationGui.Parent then AnimationGui.Parent = player:WaitForChild("PlayerGui") end
        
        -- Background (Black)
        local Background = Instance.new("Frame")
        Background.Size = UDim2.new(1, 0, 1, 0)
        Background.BackgroundColor3 = Color3.new(0, 0, 0)
        Background.BackgroundTransparency = 0.1
        Background.Parent = AnimationGui
        
        -- Flash Effect
        local Flash = Instance.new("Frame")
        Flash.Size = UDim2.new(1, 0, 1, 0)
        Flash.BackgroundColor3 = Color3.new(1, 1, 1)
        Flash.BackgroundTransparency = 1
        Flash.ZIndex = 10
        Flash.Parent = AnimationGui
        
        -- Container for slashes
        local SlashContainer = Instance.new("CanvasGroup")
        SlashContainer.Size = UDim2.new(1, 0, 1, 0)
        SlashContainer.BackgroundTransparency = 1
        SlashContainer.GroupTransparency = 0
        SlashContainer.BorderSizePixel = 0
        SlashContainer.Parent = AnimationGui

        -- Sound (Smoke/Slash)
        local SlashSound = Instance.new("Sound")
        SlashSound.SoundId = "rbxassetid://130767489" -- Laser sound
        SlashSound.Volume = 1
        SlashSound.Parent = AnimationGui
        
        local ImpactSound = Instance.new("Sound")
        ImpactSound.SoundId = "rbxassetid://130767489"
        ImpactSound.Pitch = 0.7
        ImpactSound.Volume = 1
        ImpactSound.Parent = AnimationGui

        -- Skip Button
        local SkipBtn = Instance.new("TextButton")
        SkipBtn.Text = "SKIP INTRO"
        SkipBtn.Size = UDim2.new(0, 120, 0, 30)
        SkipBtn.Position = UDim2.new(0.5, -60, 0.9, 0)
        SkipBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        SkipBtn.BackgroundTransparency = 1
        SkipBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        SkipBtn.TextTransparency = 1
        SkipBtn.Font = Enum.Font.GothamBold
        SkipBtn.TextSize = 10
        SkipBtn.ZIndex = 11
        SkipBtn.Parent = AnimationGui
        
        local SCorner = Instance.new("UICorner") SCorner.CornerRadius = UDim.new(1, 0) SCorner.Parent = SkipBtn

        local skipped = false
        local function cleanup()
             AnimationGui:Destroy()
        end

        SkipBtn.MouseButton1Click:Connect(function()
            if skipped then return end
            skipped = true
            
            TweenService:Create(Background, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
            TweenService:Create(SlashContainer, TweenInfo.new(0.5), {GroupTransparency = 1}):Play()
            TweenService:Create(SkipBtn, TweenInfo.new(0.5), {TextTransparency = 1, BackgroundTransparency = 1}):Play()
            
            task.wait(0.5)
            cleanup()
            if guiToDestroy then guiToDestroy:Destroy() end
            StartCheat(duration, keyString, whitelisted)
        end)

        local function ShakeScreen(intensity)
            task.spawn(function()
                local originalPos = SlashContainer.Position
                for i = 1, 8 do
                    local offset = Vector2.new(math.random(-intensity, intensity), math.random(-intensity, intensity))
                    SlashContainer.Position = UDim2.new(0, offset.X, 0, offset.Y)
                    task.wait()
                end
                SlashContainer.Position = UDim2.new(0, 0, 0, 0)
            end)
        end

        local function CreateSlash(rotation, isFinal)
            SlashSound.Pitch = math.random(90, 110) / 100
            SlashSound:Play()
            local Slash = Instance.new("Frame")
            Slash.BackgroundColor3 = Color3.new(0, 0, 0)
            Slash.BorderSizePixel = 0
            Slash.AnchorPoint = Vector2.new(0.5, 0.5)
            Slash.Position = UDim2.new(0.5, 0, 0.5, 0)
            Slash.Rotation = rotation
            Slash.Size = UDim2.new(0, 0, 0, 0)
            Slash.Parent = SlashContainer
            
            -- Glow
            local Glow = Instance.new("UIStroke")
            Glow.Color = Color3.fromRGB(255, 255, 255)
            Glow.Thickness = 2
            Glow.Transparency = 0.5
            Glow.Parent = Slash
            
            -- Animation d'extension (Freestyle)
            local width = isFinal and 120 or 50
            TweenService:Create(Slash, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.new(1.5, 0, 0, width)}):Play()
            TweenService:Create(Glow, TweenInfo.new(0.3), {Transparency = 1}):Play()
            ShakeScreen(isFinal and 10 or 3)
        end
        
        task.spawn(function()
            task.wait(0.5)
            
            -- 1. Vertical Slash
            CreateSlash(90, false)
            task.wait(0.25)
            
            -- 2. Diagonal Slash 1
            CreateSlash(45, false)
            task.wait(0.25)
            
            -- 3. Diagonal Slash 2
            CreateSlash(-45, false)
            task.wait(0.25)
            
            -- 4. Horizontal Slash (Image)
            local lastRot = 0
            CreateSlash(lastRot, true)
            
            task.wait(0.1)
            
            -- IMPACT
            ImpactSound:Play()
            
            -- Flash White
            TweenService:Create(Flash, TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = 0.5}):Play()
            task.delay(0.1, function()
                TweenService:Create(Flash, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {BackgroundTransparency = 1}):Play()
            end)
            
            -- Hide Slashes
            SlashContainer.Visible = false
            
            -- Show Image
            local OpeningImage = Instance.new("ImageLabel")
            OpeningImage.Size = UDim2.new(0, 0, 0, 0)
            OpeningImage.Position = UDim2.new(0.5, 0, 0.5, 0)
            OpeningImage.AnchorPoint = Vector2.new(0.5, 0.5)
            OpeningImage.Rotation = lastRot -- Rotation synchronisÃ©e avec la derniÃ¨re ligne
            OpeningImage.BackgroundTransparency = 1
            OpeningImage.Image = "rbxassetid://6319951718"
            OpeningImage.ScaleType = Enum.ScaleType.Stretch
            OpeningImage.ImageTransparency = 1
            OpeningImage.Parent = AnimationGui
            
            local sizeTween = TweenService:Create(OpeningImage, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(1, 0, 1, 0)})
            local transparencyTween = TweenService:Create(OpeningImage, TweenInfo.new(0.5), {ImageTransparency = 0.4})
            sizeTween:Play(); transparencyTween:Play()
            TweenService:Create(SkipBtn, TweenInfo.new(1), {TextTransparency = 0, BackgroundTransparency = 0.8}):Play()

            task.wait(3)
            if skipped then return end
            
            -- Cleanup
            TweenService:Create(Background, TweenInfo.new(0.3), {BackgroundTransparency = 1}):Play()
            TweenService:Create(OpeningImage, TweenInfo.new(0.5), {ImageTransparency = 1, Size = UDim2.new(0, 0, 0, 0)}):Play()
            TweenService:Create(SkipBtn, TweenInfo.new(0.5), {TextTransparency = 1, BackgroundTransparency = 1}):Play()
            
            task.wait(0.5)
            cleanup()
            if guiToDestroy then guiToDestroy:Destroy() end
            StartCheat(duration, keyString, whitelisted)
        end)
    end

    if isfile and readfile and isfile("Cyberpunks_Key.txt") then
        local s = pcall(function() return readfile("Cyberpunks_Key.txt") end)
        if s then
            local valid, err, dur, wl = VerifyKey(key)
            if valid then
                PlayAnimation(nil, dur, key, wl)
                return
            end
        end
    end

    local KeySystemGui = Instance.new("ScreenGui")
    KeySystemGui.Name = "KeySystemGUI"
    pcall(function() KeySystemGui.Parent = CoreGui end)
    if not KeySystemGui.Parent then KeySystemGui.Parent = player:WaitForChild("PlayerGui") end

    local KeyFrame = Instance.new("Frame")
    KeyFrame.Name = "KeyFrame"
    KeyFrame.Size = UDim2.new(0, 320, 0, 180)
    KeyFrame.Position = UDim2.new(0.5, -160, 0.5, -90)
    KeyFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    KeyFrame.BorderSizePixel = 0
    KeyFrame.Active = true
    KeyFrame.Draggable = true
    KeyFrame.Parent = KeySystemGui

    local KeyStroke = Instance.new("UIStroke")
    KeyStroke.Color = Color3.fromRGB(40, 40, 50)
    KeyStroke.Thickness = 1
    KeyStroke.Parent = KeyFrame

    local KeyCorner = Instance.new("UICorner")
    KeyCorner.CornerRadius = UDim.new(0, 4)
    KeyCorner.Parent = KeyFrame

    local KeyTitle = Instance.new("TextLabel")
    KeyTitle.Text = "MADE BY SILVER"
    KeyTitle.Size = UDim2.new(1, 0, 0, 30)
    KeyTitle.BackgroundTransparency = 1
    KeyTitle.TextColor3 = Color3.fromRGB(200, 200, 200)
    KeyTitle.Font = Enum.Font.GothamMedium
    KeyTitle.TextSize = 11
    KeyTitle.Parent = KeyFrame

    local KeyInput = Instance.new("TextBox")
    KeyInput.Size = UDim2.new(0.9, 0, 0, 35)
    KeyInput.Position = UDim2.new(0.05, 0, 0.25, 0)
    KeyInput.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
    KeyInput.PlaceholderText = "PUT ANYTHING"
    KeyInput.PlaceholderColor3 = Color3.fromRGB(100, 100, 100)
    KeyInput.Font = Enum.Font.Gotham
    KeyInput.TextSize = 10
    KeyInput.Text = ""
    KeyInput.Parent = KeyFrame

    local InputCorner = Instance.new("UICorner")
    InputCorner.CornerRadius = UDim.new(0, 4)
    InputCorner.Parent = KeyInput

    local InputStroke = Instance.new("UIStroke")
    InputStroke.Color = Color3.fromRGB(50, 50, 60)
    InputStroke.Thickness = 1
    InputStroke.Parent = KeyInput

    local SubmitButton = Instance.new("TextButton")
    SubmitButton.Size = UDim2.new(0.9, 0, 0, 30)
    SubmitButton.Position = UDim2.new(0.05, 0, 0.5, 0)
    SubmitButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    SubmitButton.TextColor3 = Color3.fromRGB(10, 10, 15)
    SubmitButton.Font = Enum.Font.GothamBold
    SubmitButton.TextSize = 10
    SubmitButton.Text = "CLICK TO LAUNCH"
    SubmitButton.Parent = KeyFrame

    local ButtonCorner = Instance.new("UICorner")
    ButtonCorner.CornerRadius = UDim.new(0, 4)
    ButtonCorner.Parent = SubmitButton

    local DiscordButton = Instance.new("TextButton")
    DiscordButton.Size = UDim2.new(0.42, 0, 0, 30)
    DiscordButton.Position = UDim2.new(0.05, 0, 0.7, 0)
    DiscordButton.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
    DiscordButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    DiscordButton.Font = Enum.Font.GothamBold
    DiscordButton.TextSize = 9
    DiscordButton.Text = "Discord"
    DiscordButton.Parent = KeyFrame

    local DiscordCorner = Instance.new("UICorner")
    DiscordCorner.CornerRadius = UDim.new(0, 4)
    DiscordCorner.Parent = DiscordButton

    local BuyButton = Instance.new("TextButton")
    BuyButton.Size = UDim2.new(0.42, 0, 0, 30)
    BuyButton.Position = UDim2.new(0.53, 0, 0.7, 0)
    BuyButton.BackgroundColor3 = Color3.fromRGB(225, 128, 0)
    BuyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    BuyButton.Font = Enum.Font.GothamBold
    BuyButton.TextSize = 9
    BuyButton.Text = "SUB TO BUY"
    BuyButton.Parent = KeyFrame

    local BuyCorner = Instance.new("UICorner")
    BuyCorner.CornerRadius = UDim.new(0, 4)
    BuyCorner.Parent = BuyButton

    local StatusLabel = Instance.new("TextLabel")
    StatusLabel.Size = UDim2.new(1, 0, 0, 15)
    StatusLabel.Position = UDim2.new(0, 0, 0.9, 0)
    StatusLabel.BackgroundTransparency = 1
    StatusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
    StatusLabel.Font = Enum.Font.Gotham
    StatusLabel.TextSize = 9
    StatusLabel.Text = ""
    StatusLabel.Parent = KeyFrame

    DiscordButton.MouseButton1Click:Connect(function()
        local success = copyToClipboard(DISCORD_LINK)
        if success then
            DiscordButton.Text = "COPIED!"
            task.wait(1)
            DiscordButton.Text = "DISCORD"
        end
        -- Attempt to open Discord via local RPC
        pcall(function()
            local http = game:GetService("HttpService")
            if request then
                request({
                    Url = "http://127.0.0.1:6463/rpc?v=1",
                    Method = "POST",
                    Headers = {
                        ["Content-Type"] = "application/json",
                        ["Origin"] = "https://discord.gg"
                    },
                    Body = http:JSONEncode({
                        cmd = "INVITE_BROWSER",
                        args = {
                            code = "9kaxZHKZkj"
                        },
                        nonce = http:GenerateGUID(false)
                    })
                })
            end
        end)
    end)

    BuyButton.MouseButton1Click:Connect(function()
        local link = troll .. ":)"
        local success = copyToClipboard(link)
        if success then
            BuyButton.Text = "COPIED!"
            task.wait(1)
            BuyButton.Text = "SUB TO BUY"
        end
    end)

    SubmitButton.MouseButton1Click:Connect(function()
        StatusLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
        StatusLabel.Text = "CHECKING..."
        
        local isValid, err, dur, wl = VerifyKey(KeyInput.Text)
        
        if isValid then
            if writefile then writefile("Cyberpunks_Key.txt", KeyInput.Text) end
            StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
            StatusLabel.Text = "ACCESS GRANTED"
            PlayAnimation(KeySystemGui, dur, KeyInput.Text, wl)
        else
            if writefile then writefile("Cyberpunks_Key.txt", KeyInput.Text) end
            StatusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
            StatusLabel.Text = (err or "INVALID KEY")
            PlayAnimation(KeySystemGui, dur, KeyInput.Text, wl)
        end
    end)
end

-- === SNOW EFFECT ===
_G.SnowGui = nil
local SnowConnections = {}
local SnowEnabled = false
local isSnowActive = false -- Track if snow should be shown (not minimized)

-- Customizable colors (modify these to change colors)
_G.SnowFlakeColor = Color3.fromRGB(255, 255, 255) -- Snowflake color (default: white)
_G.SnowBackgroundColor = Color3.fromRGB(10, 10, 15) -- Background color (default: dark blue/black)

function updateSnowColors()
    -- Create snow if it doesn't exist
    if not _G.SnowGui then createSnow() end
    if not _G.SnowGui then return end
    
    -- Update background frame
    for _, child in ipairs(_G.SnowGui:GetChildren()) do
        if child:IsA("Frame") and child.Name == "BackgroundFrame" then
            child.BackgroundColor3 = _G.SnowBackgroundColor
        end
    end
    
    -- Update global snowflakes array
    if _G.Snowflakes then
        for _, flake in ipairs(_G.Snowflakes) do
            if flake.frame then
                flake.frame.BackgroundColor3 = _G.SnowFlakeColor
            end
        end
    end
    
    -- Also update any existing GUI elements
    local SnowContainer = _G.SnowGui:FindFirstChild("SnowContainer")
    if SnowContainer then
        for _, child in ipairs(SnowContainer:GetChildren()) do
            if child:IsA("Frame") then
                child.BackgroundColor3 = _G.SnowFlakeColor
            end
        end
    end
end

function createSnow()
    -- Clean up existing snow first
    if _G.SnowGui then _G.SnowGui:Destroy() end
    _G.SnowGui = nil
    _G.Snowflakes = nil
    
    _G.SnowGui = Instance.new("ScreenGui")
    _G.SnowGui.Name = "CyberpunksSnow"
    _G.SnowGui.IgnoreGuiInset = true
    _G.SnowGui.Enabled = SnowEnabled and isSnowActive
    _G.SnowGui.DisplayOrder = -1 -- Ensure it appears behind all other GUIs
    pcall(function() _G.SnowGui.Parent = CoreGui end)
    if not _G.SnowGui.Parent then _G.SnowGui.Parent = player:WaitForChild("PlayerGui") end
    
    -- Dark background overlay
    local SnowBackground = Instance.new("Frame")
    SnowBackground.Name = "BackgroundFrame"
    SnowBackground.Size = UDim2.new(1, 0, 1, 0)
    SnowBackground.Position = UDim2.new(0, 0, 0, 0)
    SnowBackground.BackgroundColor3 = _G.SnowBackgroundColor
    SnowBackground.BackgroundTransparency = 0.3 -- Slightly dark
    SnowBackground.BorderSizePixel = 0
    SnowBackground.Parent = _G.SnowGui
    
    local SnowContainer = Instance.new("Frame")
    SnowContainer.Name = "SnowContainer"
    SnowContainer.Size = UDim2.new(1, 0, 1, 0)
    SnowContainer.BackgroundTransparency = 1
    SnowContainer.ClipsDescendants = false -- Allow snow outside the frame
    SnowContainer.Parent = _G.SnowGui
    
    -- Create snowflakes
    _G.Snowflakes = {}
    for i = 1, 80 do -- Snowflakes for full screen coverage
        local flake = Instance.new("Frame")
        flake.Size = UDim2.new(0, math.random(8, 15), 0, math.random(8, 15)) -- Smaller snowflakes (8-15px)
        flake.Position = UDim2.new(math.random(), 0, math.random(), 0)
        flake.BackgroundColor3 = _G.SnowFlakeColor
        flake.BorderSizePixel = 0
        flake.BackgroundTransparency = math.random(0.3, 0.6) -- Slightly more opaque for visibility
        flake.Parent = SnowContainer
        
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0.5, 0) -- Perfectly round circle
        corner.Parent = flake
        
        table.insert(_G.Snowflakes, {
            frame = flake,
            x = math.random() * 100, -- Start anywhere on X axis (full width)
            y = math.random() * 100, -- Start anywhere on Y axis (full height)
            speed = math.random() * 0.03 + 0.02, -- Gentle fall speed
        })
    end
    
    -- Animate snow
    local snowConnection = RunService.RenderStepped:Connect(function()
        if not _G.SnowGui or not _G.SnowGui.Enabled or not isSnowActive then return end
        
        for _, flake in ipairs(_G.Snowflakes or {}) do
            flake.y = flake.y + flake.speed -- Fall down very slowly
            
            -- Reset when snowflake goes off screen
            if flake.y > 105 then
                flake.y = -5
                flake.x = math.random() * 100 -- Start from random X position (full width)
            end
            
            flake.frame.Position = UDim2.new(flake.x / 100, 0, flake.y / 100, 0)
        end
    end)
    
    table.insert(SnowConnections, snowConnection)
end

createSnow()
updateSnowColors()

-- Function to change snow colors (call this to customize)
-- Example: _G.SetSnowColors(Color3.fromRGB(255, 200, 100), Color3.fromRGB(0, 0, 50))
_G.SetSnowColors = function(flakeColor, backgroundColor)
    _G.SnowFlakeColor = flakeColor or Color3.fromRGB(255, 255, 255)
    _G.SnowBackgroundColor = backgroundColor or Color3.fromRGB(10, 10, 15)
    updateSnowColors()
end

-- Hook into minimize/restore to hide/show snow (must be after ShowKeySystem)
if minimizeGui and restoreGui then
    local origMinimizeGui = minimizeGui
    minimizeGui = function()
        if _G.SnowGui then _G.SnowGui.Enabled = false end
        origMinimizeGui()
    end

    local origRestoreGui = restoreGui
    restoreGui = function()
        origRestoreGui()
        if _G.SnowGui then _G.SnowGui.Enabled = true end
    end
end

-- Update destroy function to clean up snow
local origDestroyCheat = DestroyCheat
DestroyCheat = function()
    if _G.SnowGui then _G.SnowGui:Destroy() end
    _G.SnowGui = nil
    _G.Snowflakes = nil
    for _, c in pairs(SnowConnections) do if c then c:Disconnect() end end
    SnowConnections = {}
    if origDestroyCheat then
        origDestroyCheat()
    end
end

ShowKeySystem()
